# Task 19.4: Overlay Lifecycle Integration

**Status:** TODO
**Priority:** MUST
**Estimated Hours:** 8h
**Depends On:** 19.3

---

## Objective

Integrate label validation with overlay storage, decay, and rollback.
Labels must be project-scoped, reversible, and tracked.

---

## Context

Relevant modules:
- `src/true_vkg/learning/overlay.py`
- `src/true_vkg/learning/decay.py`
- `src/true_vkg/learning/rollback.py`

---

## Implementation

- Add metadata fields to overlay assertions:
  - `created_by`, `labeler_version`, `source_prompt_hash`
- Add lifecycle rules:
  - Apply decay to hide stale labels
  - Rollback removes labels added after snapshot

- Add `OverlayManager`:
  - `record_validated_labels(...)`
  - `rollback_to_snapshot(snapshot_id)`

---

## Files to Create/Modify

| File | Action | Purpose |
|------|--------|---------|
| `src/true_vkg/learning/overlay.py` | MODIFY | Store lifecycle metadata |
| `src/true_vkg/learning/overlay_manager.py` | CREATE | Rollback + lifecycle |
| `tests/test_overlay_lifecycle.py` | CREATE | Lifecycle tests |

---

## Validation Criteria

- [ ] Labels decay after max age
- [ ] Rollback removes recent labels
- [ ] Overlay stats reflect active labels only

---

## Test Requirements

- Unit tests for decay
- Unit tests for rollback

Commands:
- `uv run pytest tests/test_overlay_lifecycle.py -v`

---

## Research Step

Review `R19.2` spec-mining notes for lifecycle requirements such as
upgrade boundaries or role revocations. If new lifecycle policies are
needed, create a task to extend overlay retention rules.

---

## Retrospective Trigger

If rollback failures occur, add a snapshot integrity task.
