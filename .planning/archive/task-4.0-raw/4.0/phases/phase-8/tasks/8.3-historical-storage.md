# Task 8.3: Historical Storage

**Status:** TODO
**Priority:** MUST
**Estimated Hours:** 4h
**Depends On:** Task 8.2b (Calculation Engine)

---

## Objective

Implement historical storage for metric snapshots to enable trend analysis and regression detection.

---

## Prerequisites

- Task 8.0-8.2b completed (calculator can produce MetricSnapshots)

---

## Architecture

```
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│ MetricCalculator │────►│ HistoryStore    │────►│ JSON Files      │
│  (produces)     │     │ (stores/queries) │     │ (.vrs/metrics/) │
└─────────────────┘     └─────────────────┘     └─────────────────┘
```

---

## Implementation Steps

### Step 1: Create HistoryStore

**File:** `src/true_vkg/metrics/storage.py`

```python
"""Historical storage for metric snapshots.

Task 8.3: Store and query metric history for trend analysis.
"""
from __future__ import annotations

import json
from datetime import datetime, timedelta
from pathlib import Path
from typing import Iterator

from .types import MetricSnapshot


class HistoryStore:
    """Stores and retrieves historical metric snapshots."""

    def __init__(self, storage_path: Path | str):
        """Initialize storage.

        Args:
            storage_path: Directory for storing snapshots
        """
        self.storage_path = Path(storage_path)
        self.storage_path.mkdir(parents=True, exist_ok=True)

    def save(self, snapshot: MetricSnapshot) -> Path:
        """Save a metric snapshot.

        Args:
            snapshot: MetricSnapshot to persist

        Returns:
            Path to saved file
        """
        # Use timestamp for filename (YYYY-MM-DD_HH-MM-SS.json)
        filename = snapshot.timestamp.strftime("%Y-%m-%d_%H-%M-%S.json")
        filepath = self.storage_path / filename

        with open(filepath, "w") as f:
            json.dump(snapshot.to_dict(), f, indent=2)

        return filepath

    def load(self, filepath: Path) -> MetricSnapshot:
        """Load a snapshot from file.

        Args:
            filepath: Path to snapshot file

        Returns:
            Loaded MetricSnapshot
        """
        with open(filepath) as f:
            data = json.load(f)
        return MetricSnapshot.from_dict(data)

    def get_latest(self) -> MetricSnapshot | None:
        """Get the most recent snapshot.

        Returns:
            Most recent MetricSnapshot or None if no history
        """
        snapshots = list(self._iter_snapshots())
        if not snapshots:
            return None
        return snapshots[-1]  # Sorted by timestamp, so last is latest

    def get_history(
        self,
        days: int = 30,
        limit: int | None = None,
    ) -> list[MetricSnapshot]:
        """Get snapshots from the last N days.

        Args:
            days: Number of days to look back
            limit: Maximum number of snapshots to return

        Returns:
            List of MetricSnapshots, oldest first
        """
        cutoff = datetime.now() - timedelta(days=days)
        snapshots = [
            s for s in self._iter_snapshots()
            if s.timestamp >= cutoff
        ]

        if limit:
            snapshots = snapshots[-limit:]

        return snapshots

    def get_range(
        self,
        start: datetime,
        end: datetime,
    ) -> list[MetricSnapshot]:
        """Get snapshots within a date range.

        Args:
            start: Start datetime (inclusive)
            end: End datetime (inclusive)

        Returns:
            List of MetricSnapshots in range
        """
        return [
            s for s in self._iter_snapshots()
            if start <= s.timestamp <= end
        ]

    def count(self) -> int:
        """Count total stored snapshots."""
        return sum(1 for _ in self.storage_path.glob("*.json"))

    def cleanup(self, retention_days: int = 90) -> int:
        """Remove snapshots older than retention period.

        Args:
            retention_days: Keep snapshots from last N days

        Returns:
            Number of snapshots removed
        """
        cutoff = datetime.now() - timedelta(days=retention_days)
        removed = 0

        for filepath in self.storage_path.glob("*.json"):
            try:
                snapshot = self.load(filepath)
                if snapshot.timestamp < cutoff:
                    filepath.unlink()
                    removed += 1
            except Exception:
                # Skip malformed files
                pass

        return removed

    def _iter_snapshots(self) -> Iterator[MetricSnapshot]:
        """Iterate through all snapshots in chronological order."""
        files = sorted(self.storage_path.glob("*.json"))
        for filepath in files:
            try:
                yield self.load(filepath)
            except Exception:
                # Skip malformed files
                pass


def create_history_store(storage_path: Path | str | None = None) -> HistoryStore:
    """Factory function to create history store with default path.

    Args:
        storage_path: Override default path

    Returns:
        HistoryStore instance
    """
    if storage_path is None:
        from ..config import METRICS_CONFIG
        storage_path = Path(METRICS_CONFIG.get("storage_path", ".vrs/metrics")) / "history"

    return HistoryStore(storage_path)
```

### Step 2: Update Module Exports

**File:** `src/true_vkg/metrics/__init__.py` (update)

Add to imports and exports:
- `HistoryStore`
- `create_history_store`

### Step 3: Integrate with MetricsTracker

**File:** `src/true_vkg/metrics/tracker.py` (update)

Add methods for saving/loading history.

---

## Validation Criteria

- [ ] HistoryStore can save MetricSnapshots
- [ ] HistoryStore can load saved snapshots
- [ ] get_latest returns most recent snapshot
- [ ] get_history filters by date
- [ ] cleanup removes old snapshots
- [ ] Persists across process restarts

---

## Test Requirements

**Add to:** `tests/test_metrics.py`

```python
class TestHistoryStore:
    """Test historical storage."""

    def test_save_and_load(self, tmp_path):
        """Snapshot can be saved and loaded."""
        store = HistoryStore(tmp_path)

        snapshot = MetricSnapshot(
            timestamp=datetime.now(),
            version="4.0.0",
            metrics={},
        )
        filepath = store.save(snapshot)

        loaded = store.load(filepath)
        assert loaded.version == "4.0.0"

    def test_get_latest(self, tmp_path):
        """get_latest returns most recent."""
        store = HistoryStore(tmp_path)

        for i in range(3):
            snapshot = MetricSnapshot(
                timestamp=datetime.now() - timedelta(hours=i),
                version=f"v{i}",
                metrics={},
            )
            store.save(snapshot)

        latest = store.get_latest()
        assert latest.version == "v0"  # Most recent

    def test_get_history_days(self, tmp_path):
        """get_history filters by days."""
        store = HistoryStore(tmp_path)

        # Snapshot from 10 days ago
        old = MetricSnapshot(
            timestamp=datetime.now() - timedelta(days=10),
            version="old",
            metrics={},
        )
        store.save(old)

        # Snapshot from today
        new = MetricSnapshot(
            timestamp=datetime.now(),
            version="new",
            metrics={},
        )
        store.save(new)

        history = store.get_history(days=5)
        assert len(history) == 1
        assert history[0].version == "new"

    def test_cleanup_retention(self, tmp_path):
        """cleanup removes old snapshots."""
        store = HistoryStore(tmp_path)

        # Snapshot from 100 days ago
        old = MetricSnapshot(
            timestamp=datetime.now() - timedelta(days=100),
            version="old",
            metrics={},
        )
        store.save(old)

        # Snapshot from today
        new = MetricSnapshot(
            timestamp=datetime.now(),
            version="new",
            metrics={},
        )
        store.save(new)

        removed = store.cleanup(retention_days=90)
        assert removed == 1
        assert store.count() == 1
```

---

## Files Created/Modified

| File | Action | Purpose |
|------|--------|---------|
| `src/true_vkg/metrics/storage.py` | CREATE | Historical storage |
| `src/true_vkg/metrics/__init__.py` | MODIFY | Export storage |
| `src/true_vkg/metrics/tracker.py` | MODIFY | Add history methods |
| `tests/test_metrics.py` | MODIFY | Add storage tests |

---

## Next Tasks

After completing this task:
- Task 8.4 can implement alerting using historical data
- Task 8.5 can implement CLI commands for viewing history

---

*Task 8.3 | Created 2026-01-08 | Self-contained task file*
