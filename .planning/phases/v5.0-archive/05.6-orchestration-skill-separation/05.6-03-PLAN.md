---
phase: 05.6-orchestration-skill-separation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/true_vkg/skills/shipped/agents/vkg-attacker.md
  - src/true_vkg/skills/shipped/agents/vkg-defender.md
  - src/true_vkg/skills/shipped/agents/vkg-verifier.md
  - src/true_vkg/skills/shipped/agents/vkg-supervisor.md
  - src/true_vkg/skills/shipped/agents/vkg-integrator.md
  - src/true_vkg/skills/shipped/agents/__init__.py
autonomous: true

must_haves:
  truths:
    - "Agent definitions exist in shipped package"
    - "Agents have proper role descriptions"
    - "Agents reference correct models for roles"
  artifacts:
    - path: "src/true_vkg/skills/shipped/agents/vkg-attacker.md"
      provides: "Attacker agent definition"
      contains: "claude-opus-4"
    - path: "src/true_vkg/skills/shipped/agents/vkg-defender.md"
      provides: "Defender agent definition"
      contains: "claude-sonnet-4"
  key_links:
    - from: "shipped/agents/*.md"
      to: "Agent runtime system"
      via: "Agent role definitions"
      pattern: "role:"
---

<objective>
Create shipped agent definitions for the product (attacker, defender, verifier, supervisor, integrator).

Purpose: These agent definitions ship with the product and define the roles for multi-agent verification. Development-only agents (pattern-architect, knowledge-aggregation-worker, etc.) stay in this repo's `.claude/agents/`.

Output: 5 agent definitions in `src/true_vkg/skills/shipped/agents/` directory.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.6-orchestration-skill-separation/05.6-CONTEXT.md
@.claude/agents/vkg-attacker.md
@.claude/agents/vkg-defender.md
@.claude/agents/vkg-verifier.md
@.claude/agents/vkg-supervisor.md
@.claude/agents/vkg-integrator.md
</context>

<execution_guardrails>
- Full doc alignment: align objective, must_haves, and tasks against `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, plus phase context or research docs before execution. Any scope change must be reflected in the plan and `.planning/STATE.md`.
- Workspace isolation: run any commands or experiments that mutate state in a fresh jj workspace; do not use the main workspace and do not reuse workspaces for reruns.
- Repeat-run detection markers: include `[ALPHASWARM-START] RUN_ID TIMESTAMP` and `[ALPHASWARM-END] RUN_ID EXIT_CODE DURATION_MS` markers in transcripts or logs so reruns are detectable.
- claude-code-controller subject: for interactive runs, use `claude-code-controller` and launch Claude Code as the subject in the claude-code-agent-teams session; transcripts must show the Claude Code session.
- Seed scope boundaries: treat B-seeds (baseline or regression inputs) and N-seeds (net-new exploratory inputs) as separate scopes; do not mix them in a single run and track results separately.
</execution_guardrails>

<tasks>

<task type="auto">
  <name>Task 1: Create shipped agent definitions</name>
  <files>
    src/true_vkg/skills/shipped/agents/__init__.py
    src/true_vkg/skills/shipped/agents/vkg-attacker.md
    src/true_vkg/skills/shipped/agents/vkg-defender.md
    src/true_vkg/skills/shipped/agents/vkg-verifier.md
  </files>
  <action>
    Create `src/true_vkg/skills/shipped/agents/` directory with:

    **__init__.py** - Empty package marker with docstring:
    ```python
    """Shipped agent definitions for VRS product.

    These agents are copied to target projects via `alphaswarm init`.
    Development-only agents (pattern-architect, etc.) stay in the alphaswarm repo.
    """
    ```

    **vkg-attacker.md** - Exploit path construction (adapt from .claude/agents/vkg-attacker.md):
    ```yaml
    ---
    name: BSKG Attacker
    role: attacker
    model: claude-opus-4
    description: Constructs exploit paths and attack vectors for vulnerability verification
    ---
    ```
    Body: Instructions for constructing exploit paths using BSKG graph queries.
    Key points:
    - Use BSKG semantic operations, NOT manual code reading
    - Focus on finding complete attack chains
    - Evidence must link to graph nodes
    - Output structured exploit path with confidence

    **vkg-defender.md** - Guard/mitigation search (adapt from .claude/agents/vkg-defender.md):
    ```yaml
    ---
    name: BSKG Defender
    role: defender
    model: claude-sonnet-4
    description: Searches for guards, mitigations, and protective patterns
    ---
    ```
    Body: Instructions for finding defensive code.
    Key points:
    - Search for reentrancy guards, access controls, validation
    - Check if guards are effective for the specific attack path
    - Evidence must reference specific code locations
    - Output defense assessment with coverage analysis

    **vkg-verifier.md** - Evidence cross-check (adapt from .claude/agents/vkg-verifier.md):
    ```yaml
    ---
    name: BSKG Verifier
    role: verifier
    model: claude-opus-4
    description: Cross-checks evidence from attacker and defender, renders verdict
    ---
    ```
    Body: Instructions for evidence verification.
    Key points:
    - Compare attacker claims vs defender rebuttals
    - Verify evidence is grounded in graph/code
    - Check for missing context or assumptions
    - Render final verdict with confidence
  </action>
  <verify>
    3 agent files exist in `src/true_vkg/skills/shipped/agents/` with correct model assignments
  </verify>
  <done>Core verification agents defined</done>
</task>

<task type="auto">
  <name>Task 2: Create supervisor and integrator agents</name>
  <files>
    src/true_vkg/skills/shipped/agents/vkg-supervisor.md
    src/true_vkg/skills/shipped/agents/vkg-integrator.md
  </files>
  <action>
    Create remaining agent definitions:

    **vkg-supervisor.md** - Workflow coordination (adapt from .claude/agents/vkg-supervisor.md):
    ```yaml
    ---
    name: BSKG Supervisor
    role: supervisor
    model: claude-sonnet-4
    description: Monitors work queues, enforces SLAs, handles escalation
    ---
    ```
    Body: Instructions for workflow coordination.
    Key points:
    - Monitor bead status across workers
    - Detect stuck work (> threshold time in_progress)
    - Escalate blocked beads for human review
    - Enforce concurrency limits (max 5 subagents, max 2 sub-orchestrators)
    - Log-only mode for stuck work (no auto-intervention)

    **vkg-integrator.md** - Verdict merging (adapt from .claude/agents/vkg-integrator.md):
    ```yaml
    ---
    name: BSKG Integrator
    role: integrator
    model: claude-sonnet-4
    description: Deduplicates findings, merges evidence, finalizes verdicts
    ---
    ```
    Body: Instructions for result integration.
    Key points:
    - Deduplicate findings from multiple tools/agents
    - Merge evidence chains for related findings
    - Apply confidence rules from PHILOSOPHY.md
    - Prepare final report for human review
    - All verdicts flagged for human confirmation
  </action>
  <verify>
    2 agent files exist with correct role and model assignments
  </verify>
  <done>All shipped agents defined</done>
</task>

</tasks>

<verification>
- [ ] `src/true_vkg/skills/shipped/agents/` directory exists
- [ ] 5 agent definition files present
- [ ] Attacker and Verifier use claude-opus-4
- [ ] Defender, Supervisor, Integrator use claude-sonnet-4
- [ ] All agents have role, model, description in frontmatter
- [ ] Agent instructions emphasize graph-first, evidence-based approach
</verification>

<success_criteria>
5 agent definitions are ready in the shipped package. Agents have correct role assignments and model specifications per the established architecture (Opus for critical reasoning, Sonnet for fast responses).
</success_criteria>

<output>
After completion, create `.planning/phases/05.6-orchestration-skill-separation/05.6-03-SUMMARY.md`
</output>
