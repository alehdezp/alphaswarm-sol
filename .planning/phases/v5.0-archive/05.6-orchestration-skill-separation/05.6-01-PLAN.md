---
phase: 05.6-orchestration-skill-separation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/true_vkg/skills/__init__.py
  - src/true_vkg/skills/shipped/__init__.py
  - src/true_vkg/skills/shipped/audit.md
  - src/true_vkg/skills/shipped/investigate.md
  - src/true_vkg/skills/shipped/verify.md
  - src/true_vkg/skills/shipped/debate.md
  - src/true_vkg/skills/shipped/health-check.md
  - src/true_vkg/skills/shipped/README.md
autonomous: true

must_haves:
  truths:
    - "Shipped skills exist in package data directory"
    - "Skills have correct frontmatter for Claude Code"
    - "Skills use vrs: namespace and correct context modes"
    - "Forked-context skills have context: fork in frontmatter"
    - "User-only skills have disable-model-invocation: true"
  artifacts:
    - path: "src/true_vkg/skills/shipped/audit.md"
      provides: "Main audit orchestration skill"
      contains: "slash_command: vrs:audit"
    - path: "src/true_vkg/skills/shipped/health-check.md"
      provides: "Installation validation skill"
      contains: "slash_command: vrs:health-check"
  key_links:
    - from: "src/true_vkg/skills/shipped/*.md"
      to: "Claude Code skill system"
      via: "YAML frontmatter"
      pattern: "disable-model-invocation:"
---

<objective>
Create the shipped skills package structure with all product skills (audit, investigate, verify, debate, health-check).

Purpose: Establish the foundation for skill distribution - these are the skills that get copied to target projects via `alphaswarm init`. Development skills stay in `.claude/` of this repo only.

Output: `src/true_vkg/skills/shipped/` directory with 5 core skills properly formatted for Claude Code.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.6-orchestration-skill-separation/05.6-CONTEXT.md
@.planning/phases/05.6-orchestration-skill-separation/05.6-RESEARCH.md
@.claude/skills/vkg/audit.md
@.claude/skills/vkg/investigate.md
@.claude/skills/vkg/verify.md
@.claude/skills/vkg/debate.md
</context>

<execution_guardrails>
- Full doc alignment: align objective, must_haves, and tasks against `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, plus phase context or research docs before execution. Any scope change must be reflected in the plan and `.planning/STATE.md`.
- Workspace isolation: run any commands or experiments that mutate state in a fresh jj workspace; do not use the main workspace and do not reuse workspaces for reruns.
- Repeat-run detection markers: include `[ALPHASWARM-START] RUN_ID TIMESTAMP` and `[ALPHASWARM-END] RUN_ID EXIT_CODE DURATION_MS` markers in transcripts or logs so reruns are detectable.
- claude-code-controller subject: for interactive runs, use `claude-code-controller` and launch Claude Code as the subject in the claude-code-agent-teams session; transcripts must show the Claude Code session.
- Seed scope boundaries: treat B-seeds (baseline or regression inputs) and N-seeds (net-new exploratory inputs) as separate scopes; do not mix them in a single run and track results separately.
</execution_guardrails>

<tasks>

<task type="auto">
  <name>Task 1: Create skills package structure</name>
  <files>
    src/true_vkg/skills/__init__.py
    src/true_vkg/skills/shipped/__init__.py
  </files>
  <action>
    Create `src/true_vkg/skills/` package with:
    1. `__init__.py` exposing `get_shipped_skills_path()` function
    2. `shipped/__init__.py` as empty package marker

    The `get_shipped_skills_path()` function should return the Path to the shipped skills directory using `importlib.resources` or `pathlib` relative to the module.

    Example:
    ```python
    from pathlib import Path

    def get_shipped_skills_path() -> Path:
        """Return path to shipped skills directory."""
        return Path(__file__).parent / "shipped"

    def list_shipped_skills() -> list[str]:
        """List all shipped skill files."""
        skills_dir = get_shipped_skills_path()
        return [f.name for f in skills_dir.glob("*.md") if f.name != "README.md"]
    ```
  </action>
  <verify>
    `python -c "from true_vkg.skills import get_shipped_skills_path; print(get_shipped_skills_path())"` prints valid path
  </verify>
  <done>Package structure exists and is importable</done>
</task>

<task type="auto">
  <name>Task 2: Create shipped product skills</name>
  <files>
    src/true_vkg/skills/shipped/audit.md
    src/true_vkg/skills/shipped/investigate.md
    src/true_vkg/skills/shipped/verify.md
    src/true_vkg/skills/shipped/debate.md
    src/true_vkg/skills/shipped/health-check.md
    src/true_vkg/skills/shipped/README.md
  </files>
  <action>
    Create 5 core product skills in `src/true_vkg/skills/shipped/`:

    **audit.md** - Main audit orchestration (adapt from .claude/skills/vkg/audit.md):
    ```yaml
    ---
    name: vrs-audit
    description: Run VRS security audit on Solidity contracts
    slash_command: vrs:audit
    context: fork
    disable-model-invocation: true
    allowed-tools: Read, Grep, Glob, Bash(alphaswarm:*)
    ---
    ```
    Body: Instructions to run `alphaswarm build-kg`, detect patterns, create beads, spawn verification agents.

    **investigate.md** - Deep investigation (adapt from .claude/skills/vkg/investigate.md):
    ```yaml
    ---
    name: vrs-investigate
    description: Deep-dive investigation of a specific finding
    slash_command: vrs:investigate
    context: fork
    disable-model-invocation: false
    allowed-tools: Read, Grep, Glob, Bash(alphaswarm:*)
    ---
    ```
    Body: Instructions for bead-focused investigation with BSKG queries.

    **verify.md** - Multi-agent verification (adapt from .claude/skills/vkg/verify.md):
    ```yaml
    ---
    name: vrs-verify
    description: Multi-agent verification of findings
    slash_command: vrs:verify
    context: fork
    disable-model-invocation: false
    allowed-tools: Read, Grep, Glob, Bash(alphaswarm:*)
    ---
    ```
    Body: Instructions for spawning attacker/defender/verifier roles.

    **debate.md** - Structured debate (adapt from .claude/skills/vkg/debate.md):
    ```yaml
    ---
    name: vrs-debate
    description: Run structured debate protocol on contested findings
    slash_command: vrs:debate
    context: fork
    disable-model-invocation: false
    allowed-tools: Read, Grep, Glob, Bash(alphaswarm:*)
    ---
    ```
    Body: Debate protocol instructions (CLAIM->REBUTTAL->SYNTHESIS).

    **health-check.md** - Installation validation (NEW):
    ```yaml
    ---
    name: vrs-health-check
    description: Validate VRS installation and configuration
    slash_command: vrs:health-check
    disable-model-invocation: true
    allowed-tools: Bash(alphaswarm:*), Read, Glob
    ---
    ```
    Body: Run `alphaswarm health-check --json`, report status of CLI, tools, vulndocs, skills.

    **README.md** - User documentation:
    Brief documentation explaining the VRS skills, how to use them, and what each skill does.

    Key changes from existing skills:
    - Rename namespace from `vkg:` to `vrs:`
    - Add proper `context: fork` for isolation (audit, investigate, verify, debate)
    - Add `disable-model-invocation: true` for user-controlled skills (audit, health-check)
    - Reference `alphaswarm` CLI commands (not `alphaswarm`)
    - Write boundaries: only `.claude/vrs/` and `.beads/`

    **Context mode requirements (ORCH2-03):**
    | Skill | context | disable-model-invocation | Rationale |
    |-------|---------|--------------------------|-----------|
    | audit | fork | true | User controls audit start, isolate from main context |
    | investigate | fork | false | Orchestrators can spawn, needs isolation |
    | verify | fork | false | Orchestrators can spawn, needs isolation |
    | debate | fork | false | Orchestrators can spawn, needs isolation |
    | health-check | (none) | true | Quick inline check, user-only |
  </action>
  <verify>
    All 6 files exist in `src/true_vkg/skills/shipped/` with correct frontmatter.
    Verify context modes:
    - `grep -l "context: fork" src/true_vkg/skills/shipped/*.md` returns audit.md, investigate.md, verify.md, debate.md
    - `grep -l "disable-model-invocation: true" src/true_vkg/skills/shipped/*.md` returns audit.md, health-check.md
  </verify>
  <done>5 product skills + README ready for distribution with correct context modes</done>
</task>

</tasks>

<verification>
- [ ] `src/true_vkg/skills/` is a valid Python package
- [ ] `get_shipped_skills_path()` returns correct path
- [ ] All 5 skill files have valid YAML frontmatter
- [ ] Skills use `vrs:` namespace
- [ ] audit.md and health-check.md have `disable-model-invocation: true`
- [ ] audit.md, investigate.md, verify.md, debate.md have `context: fork`
- [ ] health-check.md does NOT have `context: fork` (inline execution)
</verification>

<success_criteria>
Shipped skills package exists at `src/true_vkg/skills/shipped/` with all core product skills formatted for Claude Code consumption. The package can be imported and its path retrieved programmatically. All skills have correct context mode settings per ORCH2-03 requirements.
</success_criteria>

<output>
After completion, create `.planning/phases/05.6-orchestration-skill-separation/05.6-01-SUMMARY.md`
</output>
