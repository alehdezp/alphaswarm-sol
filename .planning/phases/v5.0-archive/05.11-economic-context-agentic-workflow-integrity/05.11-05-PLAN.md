---
phase: 05.11-economic-context-agentic-workflow-integrity
plan: 05
type: execute
wave: 3
depends_on:
  - 05.11-01
  - 05.11-02
files_modified:
  - src/alphaswarm_sol/agents/base.py
  - src/alphaswarm_sol/agents/prompts/economic_contract.py
  - .claude/skills/vrs/economic-context.md
  - .claude/skills/vrs/README.md
  - docs/reference/economic-agent-contracts.md
  - tests/test_economic_prompt_contracts.py
autonomous: true

must_haves:
  truths:
    - "Agents receive economic context and must cite assumptions with provenance dates"
    - "Prompt contracts enforce lens tags, causal chain citations, and counterfactual exploration"
    - "Skills exist for dossier, passports, policy diff, probes, refresh, and cross-protocol analysis"
    - "Agents must explore at least 2 counterfactual scenarios per finding"
  artifacts:
    - path: "src/alphaswarm_sol/agents/prompts/economic_contract.py"
      provides: "Prompt contract builder with causal and counterfactual requirements"
      exports: ["EconomicPromptContract", "PromptValidator", "LensTag"]
    - path: "docs/reference/economic-agent-contracts.md"
      provides: "Prompt contract spec with game-theoretic and causal requirements"
      exports: ["Economic Agent Contract"]
  key_links:
    - from: "src/alphaswarm_sol/agents"
      to: ".claude/skills/vrs/"
      via: "Skill-guided prompt contracts with counterfactual requirements"
    - from: "src/alphaswarm_sol/agents/prompts/economic_contract.py"
      to: "src/alphaswarm_sol/economics/payoff.py"
      via: "Include attack EV in prompt context"
---

<objective>
Update agent prompts with causal chain and counterfactual exploration requirements, add comprehensive validation tests, and create skills for advanced economic reasoning operations.

Purpose: Make agents consistently use economic context with game-theoretic awareness, causal reasoning, and cross-protocol dependency analysis.
Output: Enhanced prompt contracts + skill docs + validation tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.11-economic-context-agentic-workflow-integrity/05.11-CONTEXT.md
@.planning/phases/05.11-economic-context-agentic-workflow-integrity/05.11-RESEARCH-V2.md
@.planning/phases/05.11-economic-context-agentic-workflow-integrity/05.11-01-SUMMARY.md
@.planning/phases/05.11-economic-context-agentic-workflow-integrity/05.11-02-SUMMARY.md
@.claude/skills/vrs/README.md
@src/alphaswarm_sol/agents
</context>

<execution_guardrails>
- Full doc alignment: align objective, must_haves, and tasks against `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, plus phase context or research docs before execution. Any scope change must be reflected in the plan and `.planning/STATE.md`.
- Workspace isolation: run any commands or experiments that mutate state in a fresh jj workspace; do not use the main workspace and do not reuse workspaces for reruns.
- Repeat-run detection markers: include `[ALPHASWARM-START] RUN_ID TIMESTAMP` and `[ALPHASWARM-END] RUN_ID EXIT_CODE DURATION_MS` markers in transcripts or logs so reruns are detectable.
- claude-code-controller subject: for interactive runs, use `claude-code-controller` and launch Claude Code as the subject in the claude-code-agent-teams session; transcripts must show the Claude Code session.
- Seed scope boundaries: treat B-seeds (baseline or regression inputs) and N-seeds (net-new exploratory inputs) as separate scopes; do not mix them in a single run and track results separately.
</execution_guardrails>

<tasks>

<task type="auto">
  <name>Task 1: Add economic prompt contracts with causal and counterfactual requirements</name>
  <files>src/alphaswarm_sol/agents/base.py, src/alphaswarm_sol/agents/prompts/economic_contract.py, docs/reference/economic-agent-contracts.md</files>
  <action>
Update the shared prompt contract builder with advanced requirements:

1. EconomicPromptContract class (economic_contract.py):
   - Build prompt with: dossier summary, passport snippet, policy diff, attack EV
   - LensTag enum: VALUE, CONTROL, INCENTIVE, TRUST, TIMING, CONFIG
   - Required lens tags (at least 1 per finding)
   - Expectation provenance labels and dates required

2. Causal chain requirements (NEW):
   - Agents must cite complete causal chain (root_cause -> exploit -> loss)
   - Missing chain elements trigger validation failure
   - Chain probability must be included

3. Counterfactual exploration requirements (NEW):
   - Agents must explore at least 2 counterfactual scenarios per finding
   - "What if guard X existed?" / "What if param Y was different?"
   - Counterfactual results recorded as evidence_refs

4. Cross-protocol awareness requirements (NEW):
   - Agents must note cross-protocol dependencies when present
   - Systemic risk implications must be mentioned for high-risk findings

5. PromptValidator class:
   - validate_lens_tags(), validate_causal_chain(), validate_counterfactuals()
   - Validation hooks reject responses missing required elements

Document the enhanced contract in docs/reference/economic-agent-contracts.md.
  </action>
  <verify>
```bash
rg -n "EconomicPromptContract|LensTag|counterfactual|causal_chain" src/alphaswarm_sol/agents/prompts/economic_contract.py
```
  </verify>
  <done>
Agent prompts enforce economic context citations, lens tags, causal chains, and counterfactuals.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add skills for economic reasoning and cross-protocol analysis</name>
  <files>.claude/skills/vrs/economic-context.md, .claude/skills/vrs/README.md</files>
  <action>
Create a comprehensive Economic Context skill pack:

1. Core economic skills:
   - /vrs-protocol-dossier: build dossier from docs/audits/on-chain config
   - /vrs-passport: generate contract passports + lifecycle stages
   - /vrs-policy-diff: compare expected policy vs access-control graph
   - /vrs-econ-probes: run economic probes and record evidence refs
   - /vrs-misconfig-radar: scan for known misconfig patterns
   - /vrs-context-refresh: detect drift and expire stale facts

2. Advanced reasoning skills (NEW):
   - /vrs-attack-ev: compute game-theoretic expected value of attack
   - /vrs-causal-trace: build and validate causal exploitation chains
   - /vrs-counterfactual: explore "what if" scenarios for mitigations
   - /vrs-cascade-risk: analyze cross-protocol cascade failures
   - /vrs-loss-amplify: compute loss amplification from causal edges

3. Skill documentation:
   - Input requirements (graph, dossier, passports)
   - Output format (evidence_refs, confidence, chain)
   - Integration with prompt contracts

Update skills README with Economic Context Skills section (expanded from original).
  </action>
  <verify>
```bash
rg -n "attack-ev|causal-trace|counterfactual|cascade-risk|loss-amplify" .claude/skills/vrs
```
  </verify>
  <done>
New skills exist for game-theoretic, causal, and cross-protocol analysis.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add comprehensive validation tests for enhanced prompt contracts</name>
  <files>tests/test_economic_prompt_contracts.py</files>
  <action>
Add tests that validate all prompt contract requirements:

1. Basic economic context tests:
   - agents include economic assumptions when provided
   - lens tags are present and valid (at least 1 per finding)
   - missing provenance causes gate failure
   - expectation dates are included and not stale

2. Causal chain tests (NEW):
   - complete causal chains (root_cause -> exploit -> loss) required
   - incomplete chains trigger validation failure
   - chain probability is included and > 0.1

3. Counterfactual tests (NEW):
   - at least 2 counterfactual scenarios per finding
   - counterfactual results recorded as evidence_refs
   - missing counterfactuals trigger validation warning

4. Cross-protocol tests (NEW):
   - cross-protocol dependencies noted when present
   - systemic risk mentioned for high-risk findings
   - cascade implications recorded

5. Integration tests:
   - full prompt contract builds correctly
   - validator catches all failure modes
   - attacker/defender/verifier contracts validated
  </action>
  <verify>
```bash
pytest tests/test_economic_prompt_contracts.py -v --tb=short 2>/dev/null || rg -n "test_causal|test_counterfactual|test_cross_protocol" tests/test_economic_prompt_contracts.py
```
  </verify>
  <done>
Prompt contract validation tests pass, covering causal, counterfactual, and cross-protocol requirements.
  </done>
</task>

</tasks>

<verification>
- Agents cannot produce outputs without citations, lens tags, causal chains, and counterfactuals
- Skills exist and are discoverable in vrs README (11 economic skills total)
- Validation tests cover all prompt contract requirements
</verification>

<success_criteria>
- Economic prompt contracts and skills are active with comprehensive tests
- Agents are required to think like rational adversaries with causal and counterfactual reasoning
</success_criteria>

<output>
After completion, create `.planning/phases/05.11-economic-context-agentic-workflow-integrity/05.11-05-SUMMARY.md`.
</output>
