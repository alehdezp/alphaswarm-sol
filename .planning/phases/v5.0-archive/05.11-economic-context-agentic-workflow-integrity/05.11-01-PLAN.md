---
phase: 05.11-economic-context-agentic-workflow-integrity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/alphaswarm_sol/context/schema.py
  - src/alphaswarm_sol/context/types.py
  - src/alphaswarm_sol/context/builder.py
  - src/alphaswarm_sol/context/dossier.py
  - src/alphaswarm_sol/economics/__init__.py
  - src/alphaswarm_sol/economics/payoff.py
  - docs/reference/economic-context-overlay.md
autonomous: true

must_haves:
  truths:
    - "Protocol dossiers can be ingested with source attribution, confidence, staleness rules, and game-theoretic payoff fields"
    - "Economic context overlay supports causal edge types (CAUSES, ENABLES, AMPLIFIES, BLOCKS)"
    - "Evidence provenance is captured with declared/inferred/hypothesis labels for roles, assumptions, and invariants"
  artifacts:
    - path: "src/alphaswarm_sol/context/dossier.py"
      provides: "Protocol dossier ingestion + diff beacon + TTL support + payoff fields"
      exports: ["DossierBuilder", "DossierRecord", "PayoffField"]
    - path: "src/alphaswarm_sol/economics/payoff.py"
      provides: "Game-theoretic payoff field definitions for attack modeling"
      exports: ["AttackPayoff", "DefensePayoff", "PayoffMatrix"]
    - path: "docs/reference/economic-context-overlay.md"
      provides: "Overlay schema spec with causal edges + payoff fields + example dossier"
      exports: ["Economic Context Overlay Spec"]
  key_links:
    - from: "src/alphaswarm_sol/context/builder.py"
      to: "src/alphaswarm_sol/context/dossier.py"
      via: "Dossier ingestion during context pack build"
    - from: "src/alphaswarm_sol/context/schema.py"
      to: "src/alphaswarm_sol/economics/payoff.py"
      via: "Payoff fields embedded in context pack"
---

<objective>
Implement the economic context overlay foundations with game-theoretic payoff modeling and causal edge types, extending ProtocolContextPack with provenance/staleness handling.

Purpose: Enable economic reasoning that is evidence-first, resistant to stale assumptions, and supports game-theoretic attack viability analysis.
Output: Dossier ingestion module + payoff field definitions + causal overlay schema + reference spec.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.11-economic-context-agentic-workflow-integrity/05.11-CONTEXT.md
@.planning/phases/05.11-economic-context-agentic-workflow-integrity/05.11-RESEARCH-V2.md
@src/alphaswarm_sol/context/schema.py
@src/alphaswarm_sol/context/builder.py
</context>

<execution_guardrails>
- Full doc alignment: align objective, must_haves, and tasks against `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, plus phase context or research docs before execution. Any scope change must be reflected in the plan and `.planning/STATE.md`.
- Workspace isolation: run any commands or experiments that mutate state in a fresh jj workspace; do not use the main workspace and do not reuse workspaces for reruns.
- Repeat-run detection markers: include `[ALPHASWARM-START] RUN_ID TIMESTAMP` and `[ALPHASWARM-END] RUN_ID EXIT_CODE DURATION_MS` markers in transcripts or logs so reruns are detectable.
- claude-code-controller subject: for interactive runs, use `claude-code-controller` and launch Claude Code as the subject in the claude-code-agent-teams session; transcripts must show the Claude Code session.
- Seed scope boundaries: treat B-seeds (baseline or regression inputs) and N-seeds (net-new exploratory inputs) as separate scopes; do not mix them in a single run and track results separately.
</execution_guardrails>

<tasks>

<task type="auto">
  <name>Task 1: Extend ProtocolContextPack with provenance, TTL, and causal edge types</name>
  <files>src/alphaswarm_sol/context/schema.py, src/alphaswarm_sol/context/types.py</files>
  <action>
Add explicit provenance fields, freshness metadata, and causal edge infrastructure:

1. Provenance fields:
   - ExpectationProvenance enum: DECLARED, INFERRED, HYPOTHESIS
   - source_id, source_date, source_type for roles/assumptions/invariants
   - TTL (expires_at) field with confidence decay rules
   - per-field confidence + scope tagging

2. Causal edge types for exploitation reasoning:
   - CausalEdgeType enum: CAUSES, ENABLES, AMPLIFIES, BLOCKS
   - CausalEdge dataclass with source_node, target_node, edge_type, probability, evidence_refs
   - causal_edges field in ProtocolContextPack (List[CausalEdge])

3. Game-theoretic fields (placeholder for full implementation in payoff.py):
   - attack_payoff_model field (Optional[AttackPayoffModel])
   - defender_payoff_model field (Optional[DefenderPayoffModel])

Keep schema backwards-compatible with defaults (causal_edges=[], attack_payoff_model=None).
  </action>
  <verify>
```bash
rg -n "CausalEdgeType|ExpectationProvenance|expires_at|attack_payoff" src/alphaswarm_sol/context/schema.py src/alphaswarm_sol/context/types.py
```
  </verify>
  <done>
ProtocolContextPack includes provenance + freshness + causal edge types + payoff model placeholders.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement dossier ingestion with diff beacon and game-theoretic payoff fields</name>
  <files>src/alphaswarm_sol/context/dossier.py, src/alphaswarm_sol/context/builder.py, src/alphaswarm_sol/economics/__init__.py, src/alphaswarm_sol/economics/payoff.py</files>
  <action>
Create dossier ingestion helpers and payoff field definitions:

1. DossierBuilder class (dossier.py):
   - accept doc sources + governance proposals
   - normalize into structured DossierRecord with provenance
   - detect changes (diff beacon) and mark stale fields
   - surface unknowns when sources are stale or contradictory
   - Wire into ContextPackBuilder

2. PayoffField definitions (economics/payoff.py):
   - AttackPayoff dataclass: expected_profit, gas_cost, mev_risk, success_probability
   - DefensePayoff dataclass: detection_probability, mitigation_cost, timelock_delay
   - PayoffMatrix: 3-player game (Attacker, Protocol, MEV Searchers)
   - compute_nash_equilibrium() stub (full implementation in 05.11-06)

3. Create economics/__init__.py with exports.

The payoff fields enable game-theoretic reasoning in later plans (05.11-06).
  </action>
  <verify>
```bash
rg -n "DossierBuilder|diff_beacon|stale|AttackPayoff|PayoffMatrix" src/alphaswarm_sol/context/dossier.py src/alphaswarm_sol/economics/payoff.py
```
  </verify>
  <done>
Dossier ingestion is wired into the context build with staleness detection and payoff field support.
  </done>
</task>

<task type="auto">
  <name>Task 3: Publish economic overlay reference spec with causal edges and payoff models</name>
  <files>docs/reference/economic-context-overlay.md</files>
  <action>
Write a concise spec covering:

1. Overlay node types:
   - Role, ValueFlow, Incentive, OffchainInput, Governance, Assumption
   - NEW: CausalNode (RootCause, ExploitStep, FinancialLoss)

2. Overlay edge types:
   - ROLE_CONTROLS_FUNCTION, VALUE_FLOW_THROUGH_FUNCTION, etc.
   - NEW: CAUSES, ENABLES, AMPLIFIES, BLOCKS (causal edges)

3. Provenance rules:
   - declared vs inferred vs hypothesis expectations
   - TTL and confidence decay

4. Game-theoretic payoff fields:
   - AttackPayoff, DefensePayoff, PayoffMatrix structure
   - How they inform economic risk scoring (reference to 05.11-04)

5. Example dossier excerpt for a known protocol (Aave or MakerDAO style)

6. Usage notes for agent reasoning and evidence gates
  </action>
  <verify>
```bash
rg -n "CausalEdgeType|PayoffMatrix|CAUSES|AMPLIFIES" docs/reference/economic-context-overlay.md
```
  </verify>
  <done>
Reference spec exists, covers causal edges and payoff models, and mirrors the schema fields.
  </done>
</task>

</tasks>

<verification>
- ProtocolContextPack serializes with provenance, causal edges, and payoff model fields
- DossierBuilder returns records with source_id + source_date + provenance labels
- PayoffMatrix dataclass exists with placeholder Nash equilibrium computation
- Causal edge types (CAUSES, ENABLES, AMPLIFIES, BLOCKS) are defined and usable
</verification>

<success_criteria>
- Dossier ingestion + overlay schema + causal edges + payoff fields are implemented and documented
- Schema is backwards-compatible (existing tests pass)
</success_criteria>

<output>
After completion, create `.planning/phases/05.11-economic-context-agentic-workflow-integrity/05.11-01-SUMMARY.md`.
</output>
