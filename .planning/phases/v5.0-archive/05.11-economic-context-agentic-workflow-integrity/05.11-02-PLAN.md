---
phase: 05.11-economic-context-agentic-workflow-integrity
plan: 02
type: execute
wave: 2
depends_on:
  - 05.11-01
files_modified:
  - src/alphaswarm_sol/context/linker.py
  - src/alphaswarm_sol/context/passports.py
  - src/alphaswarm_sol/context/invariant_registry.py
  - src/alphaswarm_sol/knowledge/domain_kg.py
  - src/alphaswarm_sol/routing/router.py
  - docs/reference/contract-passport.md
autonomous: true

must_haves:
  truths:
    - "Critical functions are linked to economic context nodes with evidence and causal edges"
    - "Contract passports include cross-protocol dependency tracking and systemic risk hints"
    - "Invariant registry tracks mined vs declared invariants with confidence scores"
    - "Policy diff compares expected vs actual access control with provenance"
  artifacts:
    - path: "src/alphaswarm_sol/context/linker.py"
      provides: "Function <-> economic context linkage registry with causal chain support"
      exports: ["ContextLinker", "LinkRecord", "CausalChainLink"]
    - path: "src/alphaswarm_sol/context/passports.py"
      provides: "Contract passport generator with cross-protocol dependencies"
      exports: ["ContractPassport", "PassportBuilder", "CrossProtocolDependency"]
    - path: "src/alphaswarm_sol/context/invariant_registry.py"
      provides: "Invariant registry for mined vs declared invariants"
      exports: ["InvariantRegistry", "InvariantRecord", "InvariantSource"]
  key_links:
    - from: "src/alphaswarm_sol/routing/router.py"
      to: "src/alphaswarm_sol/context/passports.py"
      via: "Attach passports with cross-protocol deps at routing time"
    - from: "src/alphaswarm_sol/context/linker.py"
      to: "src/alphaswarm_sol/knowledge/domain_kg.py"
      via: "Overlay edges including causal chains injected into KG"
---

<objective>
Implement context linking with causal chain support, policy diff with cross-protocol dependency tracking, contract passports, and an invariant registry that distinguishes mined vs declared invariants.

Purpose: Make economic facts actionable with causal reasoning, enable cross-protocol risk analysis, and prepare for automated invariant mining.
Output: Linkage registry with causal chains, passport generator with cross-protocol deps, invariant registry, policy diff integration.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.11-economic-context-agentic-workflow-integrity/05.11-CONTEXT.md
@.planning/phases/05.11-economic-context-agentic-workflow-integrity/05.11-RESEARCH-V2.md
@.planning/phases/05.11-economic-context-agentic-workflow-integrity/05.11-01-SUMMARY.md
@src/alphaswarm_sol/context/schema.py
@src/alphaswarm_sol/routing/router.py
@src/alphaswarm_sol/knowledge/domain_kg.py
</context>

<execution_guardrails>
- Full doc alignment: align objective, must_haves, and tasks against `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, plus phase context or research docs before execution. Any scope change must be reflected in the plan and `.planning/STATE.md`.
- Workspace isolation: run any commands or experiments that mutate state in a fresh jj workspace; do not use the main workspace and do not reuse workspaces for reruns.
- Repeat-run detection markers: include `[ALPHASWARM-START] RUN_ID TIMESTAMP` and `[ALPHASWARM-END] RUN_ID EXIT_CODE DURATION_MS` markers in transcripts or logs so reruns are detectable.
- claude-code-controller subject: for interactive runs, use `claude-code-controller` and launch Claude Code as the subject in the claude-code-agent-teams session; transcripts must show the Claude Code session.
- Seed scope boundaries: treat B-seeds (baseline or regression inputs) and N-seeds (net-new exploratory inputs) as separate scopes; do not mix them in a single run and track results separately.
</execution_guardrails>

<tasks>

<task type="auto">
  <name>Task 1: Build linkage registry with causal chain support and overlay edges</name>
  <files>src/alphaswarm_sol/context/linker.py, src/alphaswarm_sol/knowledge/domain_kg.py</files>
  <action>
Create a deterministic linker that supports causal chain reasoning:

1. ContextLinker class:
   - maps functions to economic nodes using explicit mappings first
   - backfills with semantic ops (low confidence, quarantined)
   - enforces two-key evidence for surfaced links
   - writes overlay edges (including causal edges) into DomainKnowledgeGraph
   - confidence + evidence_refs on all links

2. CausalChainLink dataclass:
   - root_cause_id, exploit_steps (list), financial_loss_id
   - probability_chain (cumulative probability of chain)
   - counterfactual_blocks (list of mitigations that would break chain)

3. Causal chain building:
   - trace_causal_chain(vulnerability_id) -> CausalChainLink
   - Uses CAUSES, ENABLES, AMPLIFIES edges from schema
   - Records counterfactual queries ("if X existed, would chain break?")

4. Add causal edges to DomainKnowledgeGraph overlay.
  </action>
  <verify>
```bash
rg -n "ContextLinker|CausalChainLink|trace_causal_chain|counterfactual" src/alphaswarm_sol/context/linker.py
```
  </verify>
  <done>
Linker creates evidence-backed links with causal chains and adds overlay edges to the KG.
  </done>
</task>

<task type="auto">
  <name>Task 2: Generate contract passports with cross-protocol dependencies and invariant registry</name>
  <files>src/alphaswarm_sol/context/passports.py, src/alphaswarm_sol/context/invariant_registry.py, docs/reference/contract-passport.md</files>
  <action>
Implement contract passports and invariant tracking:

1. ContractPassport dataclass (passports.py):
   - economic purpose + assets handled
   - critical actions and allowed lifecycle stages
   - roles/controls and external dependencies
   - NEW: cross_protocol_dependencies (List[CrossProtocolDependency])
   - NEW: systemic_risk_score (derived from dependency centrality)
   - NEW: invariant_ids (references to InvariantRegistry)

2. CrossProtocolDependency dataclass:
   - protocol_id, dependency_type (oracle, liquidity, governance)
   - criticality (1-10), failure_cascade_impact
   - last_verified_date, confidence

3. InvariantRegistry class (invariant_registry.py):
   - InvariantRecord: id, expression, source (DECLARED, MINED, HYBRID), confidence
   - would_prevent_exploits: list of known exploit IDs this invariant would block
   - register_mined(), register_declared(), validate_against_traces()
   - compare_mined_vs_declared() for discrepancy detection

4. Persist passports in context storage and document schema in contract-passport.md.
  </action>
  <verify>
```bash
rg -n "ContractPassport|CrossProtocolDependency|InvariantRegistry|MINED|DECLARED" src/alphaswarm_sol/context/passports.py src/alphaswarm_sol/context/invariant_registry.py
```
  </verify>
  <done>
Passports with cross-protocol deps are generated; invariant registry tracks mined vs declared.
  </done>
</task>

<task type="auto">
  <name>Task 3: Policy diff integration with cross-protocol awareness in routing</name>
  <files>src/alphaswarm_sol/routing/router.py, src/alphaswarm_sol/context/linker.py</files>
  <action>
Add policy diff logic with cross-protocol context:

1. Policy diff computation:
   - compare expected policy from dossier against access-control graph
   - label mismatches with provenance + confidence
   - attach diffs to agent context slices

2. Cross-protocol awareness:
   - include cross-protocol dependencies in routing context
   - flag when target function depends on external protocols
   - surface systemic risk score in agent context

3. Soft gating:
   - default soft mode if policy expectations are inferred
   - optional strict mode for production audits
   - missing passports trigger context expansion request
  </action>
  <verify>
```bash
rg -n "policy_diff|cross_protocol|systemic_risk" src/alphaswarm_sol/routing/router.py
```
  </verify>
  <done>
Routing attaches policy diffs with provenance, confidence, and cross-protocol dependencies.
  </done>
</task>

</tasks>

<verification>
- Linker produces evidence-backed links with causal chain support
- Passports include cross_protocol_dependencies and invariant_ids
- InvariantRegistry distinguishes MINED vs DECLARED sources
- Policy diffs appear in routing context with cross-protocol awareness
</verification>

<success_criteria>
- Economic context is linked with causal chains, summarized with cross-protocol deps, and attached at routing time
- Invariant registry is ready for automated mining integration (05.11-09)
</success_criteria>

<output>
After completion, create `.planning/phases/05.11-economic-context-agentic-workflow-integrity/05.11-02-SUMMARY.md`.
</output>
