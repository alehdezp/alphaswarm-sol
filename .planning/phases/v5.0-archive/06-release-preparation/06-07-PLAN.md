---
phase: 06-release-preparation
plan: 07
type: execute
wave: 2
depends_on: [06-06]
files_modified:
  - docs/paper/alphaswarm-architecture.md
  - docs/paper/appendix-operations.md
  - docs/paper/appendix-patterns.md
autonomous: true

must_haves:
  truths:
    - "Paper has appendices documenting all 20 semantic operations with signature codes"
    - "Paper has pattern reference appendix with Tier A/B/C examples"
    - "Paper references 06-RESEARCH.md key papers correctly"
    - "Paper formatting is consistent throughout"
  artifacts:
    - path: "docs/paper/appendix-operations.md"
      provides: "Complete semantic operations reference"
      contains: "TRANSFERS_VALUE_OUT"
    - path: "docs/paper/appendix-patterns.md"
      provides: "Pattern tier examples"
      contains: "tier_a"
  key_links:
    - from: "docs/paper/alphaswarm-architecture.md"
      to: "docs/paper/appendix-operations.md"
      via: "Appendix reference"
      pattern: "Appendix A"
---

<objective>
Add appendices and finalize the research paper

Purpose: Complete the research paper with reference appendices that document the full semantic operations vocabulary, signature codes, and pattern examples. Ensure consistent formatting and proper references throughout.

Output: Finalized research paper with Appendix A (Semantic Operations) and Appendix B (Pattern Examples).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PROJECT.md
@.planning/phases/06-release-preparation/06-CONTEXT.md
@.planning/phases/06-release-preparation/06-RESEARCH.md
@docs/PHILOSOPHY.md
@docs/paper/alphaswarm-architecture.md
</context>

<execution_guardrails>
- Full doc alignment: align objective, must_haves, and tasks against `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, plus phase context or research docs before execution. Any scope change must be reflected in the plan and `.planning/STATE.md`.
- Workspace isolation: run any commands or experiments that mutate state in a fresh jj workspace; do not use the main workspace and do not reuse workspaces for reruns.
- Repeat-run detection markers: include `[ALPHASWARM-START] RUN_ID TIMESTAMP` and `[ALPHASWARM-END] RUN_ID EXIT_CODE DURATION_MS` markers in transcripts or logs so reruns are detectable.
- claude-code-controller subject: for interactive runs, use `claude-code-controller` and launch Claude Code as the subject in the claude-code-agent-teams session; transcripts must show the Claude Code session.
- Seed scope boundaries: treat B-seeds (baseline or regression inputs) and N-seeds (net-new exploratory inputs) as separate scopes; do not mix them in a single run and track results separately.
</execution_guardrails>

<tasks>

<task type="auto">
  <name>Task 1: Create Appendix A - Semantic Operations Reference</name>
  <files>docs/paper/appendix-operations.md</files>
  <action>
Create `docs/paper/appendix-operations.md` with the complete semantic operations reference:

**Appendix A: Semantic Operations Reference**

**A.1 Operations Table**

| Operation | Signature Code | Description | Evidence |
|-----------|----------------|-------------|----------|
| TRANSFERS_VALUE_OUT | X:out | ETH or token transfers out of contract | `transfer()`, `send()`, `call{value:}` |
| RECEIVES_VALUE_IN | X:in | Payable function or token receipt | `payable` modifier, token transfers in |
| READS_USER_BALANCE | R:bal | Reads balance mappings or balanceOf | `balances[user]`, `balanceOf()` |
| WRITES_USER_BALANCE | W:bal | Writes to balance mappings | `balances[user] = x` |
| CHECKS_PERMISSION | C:auth | require/assert or modifier-based auth | `onlyOwner`, `require(msg.sender == owner)` |
| MODIFIES_OWNER | M:own | Owner changes | `owner = newOwner` |
| MODIFIES_ROLES | M:role | Role assignments or access control updates | `grantRole()`, `revokeRole()` |
| CALLS_EXTERNAL | X:call | Any external call | `.call()`, external function calls |
| CALLS_UNTRUSTED | X:unk | External call to user-controlled address | Call to address from parameter |
| READS_EXTERNAL_VALUE | R:ext | Reads external contract values | External view calls |
| MODIFIES_CRITICAL_STATE | M:crit | Writes to privileged state | Owner, paused, fee updates |
| INITIALIZES_STATE | I:init | Initializer or constructor-like setup | `initialize()`, constructor |
| READS_ORACLE | R:orc | Oracle or price feed reads | `latestAnswer()`, Chainlink calls |
| LOOPS_OVER_ARRAY | L:arr | Unbounded or user-driven loops | `for (i=0; i < array.length; i++)` |
| USES_TIMESTAMP | U:time | block.timestamp access | `block.timestamp` |
| USES_BLOCK_DATA | U:blk | block.number, blockhash, prevrandao | `block.number`, `blockhash()` |
| PERFORMS_DIVISION | A:div | Division or precision-sensitive math | `/`, `div()` |
| PERFORMS_MULTIPLICATION | A:mul | Multiplication or overflow-sensitive math | `*`, `mul()` |
| VALIDATES_INPUT | V:in | Input validation checks | `require(amount > 0)` |
| EMITS_EVENT | E:evt | Event emission | `emit Transfer(...)` |

**A.2 Signature Code Reference**

Complete signature code vocabulary:

```
R:bal   read user balance
W:bal   write user balance
X:out   transfer value out
X:in    receive value in
X:call  external call
X:unk   call untrusted address
C:auth  permission check
M:own   modify owner
M:role  modify role
M:crit  modify critical state
R:orc   read oracle
R:ext   read external value
L:arr   loop over array
U:time  use timestamp
U:blk   use block data
A:div   division
A:mul   multiplication
V:in    validate input
E:evt   emit event
I:init  initializer pattern
```

**A.3 Signature Composition Examples**

```
R:bal → X:out → W:bal       # Reentrancy candidate
R:bal → W:bal → X:out       # Safe CEI pattern
C:auth → M:crit             # Protected critical state update
C:auth → M:own              # Protected ownership transfer
R:orc → A:div → X:out       # Oracle-driven value transfer
L:arr → X:call              # Loop with external calls (DoS risk)
U:time → C:auth             # Time-based access control
I:init → M:own → M:crit     # Initializer pattern
```

**A.4 Vocabulary Policy**

1. The 20 core operations above are the stable contract for patterns
2. Extensions are allowed but must be additive and versioned
3. New operations must define signature codes and evidence rules
4. Core operations remain stable so patterns and queries do not break
  </action>
  <verify>
- File exists at docs/paper/appendix-operations.md
- Contains all 20 semantic operations
- Each operation has signature code, description, and evidence
- Signature composition examples included
- Vocabulary policy documented
  </verify>
  <done>Appendix A complete with full semantic operations reference</done>
</task>

<task type="auto">
  <name>Task 2: Create Appendix B - Pattern Examples Reference</name>
  <files>docs/paper/appendix-patterns.md</files>
  <action>
Create `docs/paper/appendix-patterns.md` with pattern tier examples:

**Appendix B: Pattern Examples**

**B.1 Tier A Pattern: Classic Reentrancy**

```yaml
id: reentrancy-classic
name: Classic Reentrancy
severity: critical
lens: [Reentrancy]
tier: A
description: |
  Detects the classic reentrancy vulnerability where external calls
  occur before state updates. This pattern matches the behavioral
  signature R:bal → X:out → W:bal without a reentrancy guard.

match:
  tier_a:
    all:
      - property: visibility
        op: in
        value: [public, external]
      - has_all_operations:
          - TRANSFERS_VALUE_OUT
          - WRITES_USER_BALANCE
      - sequence_order:
          before: TRANSFERS_VALUE_OUT
          after: WRITES_USER_BALANCE
    none:
      - property: has_reentrancy_guard
        value: true
```

**B.2 Tier A Pattern: Permissive Access Control**

```yaml
id: access-control-permissive
name: Permissive Access Control on Critical State
severity: high
lens: [AccessControl]
tier: A
description: |
  Detects functions that modify privileged state (owner, fees, paused)
  without any access control. Matches M:crit signature without C:auth.

match:
  tier_a:
    all:
      - property: writes_privileged_state
        value: true
      - property: visibility
        op: in
        value: [public, external]
    none:
      - property: has_access_gate
        value: true
```

**B.3 Tier B Pattern: Oracle Manipulation**

```yaml
id: oracle-manipulation
name: Oracle Manipulation Vulnerability
severity: high
lens: [Oracle]
tier: B
description: |
  Exploratory pattern for oracle manipulation. Detects functions that
  read oracle values and use them in value transfers without sufficient
  validation. Requires LLM verification to assess oracle trust and
  validation adequacy.

match:
  tier_b:
    all:
      - has_operation: READS_ORACLE
      - has_operation: TRANSFERS_VALUE_OUT
    context_questions:
      - Is the oracle trusted (Chainlink, TWAP)?
      - Is there sufficient staleness checking?
      - Can the oracle be manipulated within a single block?
    verification_required: true
```

**B.4 Tier C Pattern: State Machine Violation (Label-Dependent)**

```yaml
id: state-machine-invalid-transition
name: Invalid State Machine Transition
severity: medium
lens: [Logic]
tier: C
description: |
  Label-dependent pattern that detects invalid state machine transitions.
  Requires semantic labels from the LLM labeling pass to identify
  state variables and valid transition rules.

match:
  tier_c:
    requires_labels: true
    conditions:
      - has_label: state_mutation.state_variable_update
      - missing_label: control_flow.state_transition_check
    context_questions:
      - What are the valid state transitions?
      - Is this transition allowed from the current state?
```

**B.5 Pattern Tier Summary**

| Tier | Characteristics | Verification | Confidence |
|------|-----------------|--------------|------------|
| A | Graph-only, deterministic | None required | HIGH |
| B | Exploratory, complex | LLM verification | MEDIUM |
| C | Label-dependent, semantic | Labels + LLM | MEDIUM |

**B.6 Pattern Rating Criteria**

| Status | Precision | Recall | Description |
|--------|-----------|--------|-------------|
| draft | < 70% | < 50% | Under development |
| ready | >= 70% | >= 50% | Production use |
| excellent | >= 90% | >= 85% | Validated extensively |
  </action>
  <verify>
- File exists at docs/paper/appendix-patterns.md
- Contains at least one example per tier (A, B, C)
- Pattern YAML structure is correct
- Tier summary table included
- Rating criteria documented
  </verify>
  <done>Appendix B complete with pattern examples for all tiers</done>
</task>

<task type="auto">
  <name>Task 3: Finalize paper with appendix references and formatting</name>
  <files>docs/paper/alphaswarm-architecture.md</files>
  <action>
Update the main paper to reference appendices and ensure consistent formatting:

**1. Add appendix references to main paper**

At the end of Section 4.3 (Semantic Operations), add:
> A complete reference of all semantic operations with signature codes and evidence rules is provided in Appendix A.

At the end of Section 4.5 (Pattern System), add:
> Example patterns for each tier are provided in Appendix B.

**2. Add References section before Conclusion**

Create Section 7: References with the key papers from 06-RESEARCH.md:

```
[1] Li et al. "LLM-Assisted Detection of Smart Contract Access Control
    Vulnerabilities Based on Knowledge Graphs." 2025.

[2] Ding et al. "SmartGuard: An LLM-enhanced Framework for Smart Contract
    Vulnerability Detection." 2025.

[3] Wei et al. "LLM-SmartAudit: Advanced Smart Contract Vulnerability
    Detection via LLM-Powered Multi-Agent Systems." 2025.

[4] Fan et al. "iMAD: Intelligent Multi-Agent Debate for Efficient and
    Accurate LLM Inference." 2025.

[5] Anthropic. "SCONE-bench: AI Agents Finding $4.6M in Smart Contract
    Exploits." December 2025.
```

**3. Add document metadata at the top**

Add a header block:
```
# AlphaSwarm.sol: Behavioral Vulnerability Detection Through Semantic Reasoning

**Version:** 0.5.0
**Authors:** AlphaSwarm Team
**Date:** January 2026
**Status:** Technical Report (Pre-evaluation)

---
```

**4. Verify formatting consistency**

- All section numbers are correct (1-7 + Appendices A-B)
- All code blocks use consistent syntax highlighting
- All tables are formatted consistently
- Third person throughout
- No performance claims

**5. Add Appendices section at the end**

```
## Appendices

- [Appendix A: Semantic Operations Reference](appendix-operations.md)
- [Appendix B: Pattern Examples](appendix-patterns.md)
```
  </action>
  <verify>
- Main paper references Appendix A and B appropriately
- References section contains all 5 key papers
- Document metadata header present
- Consistent formatting throughout
- Appendices section links to separate files
  </verify>
  <done>Research paper finalized with appendices, references, and consistent formatting</done>
</task>

</tasks>

<verification>
1. Appendix A exists with all 20 semantic operations
2. Appendix B exists with Tier A, B, C pattern examples
3. Main paper references appendices
4. References section contains CKG-LLM, SmartGuard, LLM-SmartAudit, iMAD, SCONE-bench
5. Document metadata header present
6. Consistent formatting throughout
7. No performance claims in any document
</verification>

<success_criteria>
- Complete research paper with all sections and appendices
- Appendix A: Full semantic operations reference (20 operations)
- Appendix B: Pattern examples for all three tiers
- References section with 5 key papers
- Ready for review and potential publication
</success_criteria>

<output>
After completion, create `.planning/phases/06-release-preparation/06-07-SUMMARY.md`
</output>
