---
phase: 01-vulndocs-completion
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .true_vkg/crawl_cache/*.md
  - knowledge/vulndocs/categories/**/*.md
  - .true_vkg/discovery/state.yaml
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Tier 7+8+9+10 sources (30 total) crawled and processed"
    - "GitHub repositories, protocol docs, FV tools, and emerging patterns extracted"
    - "VulnDocs updated with specialized and emerging knowledge"
  artifacts:
    - path: ".true_vkg/discovery/tier-7-8-9-10-complete.yaml"
      provides: "Completion manifest for Tier 7-10 crawl"
    - path: "knowledge/vulndocs/categories/**/core-pattern.md"
      provides: "Pattern documentation with specialized content"
  key_links:
    - from: "crawl4ai Docker"
      to: ".true_vkg/crawl_cache/"
      via: "HTTP POST to localhost:11235"
    - from: "knowledge-aggregation-worker"
      to: "knowledge/vulndocs/"
      via: "Decision framework with protocol-specific handling"
---

<objective>
Crawl and extract vulnerability knowledge from Tier 7+8+9+10 sources (GitHub Security Repos + Protocol Docs + Formal Verification + Emerging/Specialized, 30 sources total) using crawl4ai and knowledge-aggregation-worker subagent.

Purpose: These tiers provide practical code examples (GitHub), protocol-specific patterns (Uniswap, Aave), formal verification insights (Certora, Halmos), and emerging technology patterns (L2s, ERC-4337, restaking). Critical for comprehensive coverage.

Output: Security repositories processed, protocol-specific patterns generalized, FV insights integrated, emerging tech patterns documented.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/intel/summary.md
@.true_vkg/vulndocs_reference/sources.yaml
@.true_vkg/vulndocs_reference/extraction_guide.md
@.claude/agents/knowledge-aggregation-worker.md
@knowledge/vulndocs/TEMPLATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inventory Tier 7+8+9+10 sources with content analysis</name>
  <files>.true_vkg/discovery/tier-7-8-9-10-sources.yaml</files>
  <action>
    1. Extract all 30 sources grouped by tier:

       **Tier 7 - GitHub Security Repositories (10 sources):**
       - crytic/not-so-smart-contracts (anti-patterns)
       - crytic/building-secure-contracts (best practices)
       - Rari-Capital/solcurity (review checklist)
       - sigp/solidity-security-blog (comprehensive guide)
       - ConsenSys/smart-contract-best-practices (industry standard)
       - SunWeb3Sec/DeFiHackLabs (PoC collection)
       - ZhangZhuoSJTU/Web3Bugs (categorized bugs)
       - immunefi-team/Web3-Security-Library (comprehensive)
       - tintinweb/smart-contract-sanctuary (verified source)
       - OpenZeppelin/openzeppelin-contracts/security (advisories)

       **Tier 8 - Protocol Documentation (8 sources):**
       - Uniswap Security (AMM patterns)
       - Aave Security (lending patterns)
       - Compound Security (cToken patterns)
       - MakerDAO Security (CDP patterns)
       - Curve Security (StableSwap)
       - Balancer Security (weighted pools)
       - Chainlink Security (oracle patterns)
       - Yearn Security (vault patterns)

       **Tier 9 - Formal Verification (5 sources):**
       - Certora (CVL specs, blog)
       - Halmos (symbolic testing)
       - Echidna (fuzzing)
       - Foundry Invariants (docs)
       - Scribble (annotations)

       **Tier 10 - Emerging/Specialized (7 sources):**
       - Arbitrum Security (L2 patterns)
       - Optimism Security (L2 patterns)
       - zkSync Security (ZK rollup)
       - LayerZero Security (bridge patterns)
       - ERC-4337 (account abstraction)
       - Flashbots (MEV protection)
       - EigenLayer (restaking)

    2. Create tracking file at `.true_vkg/discovery/tier-7-8-9-10-sources.yaml`:
       ```yaml
       tiers: ["7", "8", "9", "10"]
       total_sources: 30
       breakdown:
         tier_7_github: 10
         tier_8_protocol_docs: 8
         tier_9_formal_verification: 5
         tier_10_emerging: 7
       sources:
         # Grouped by tier with content type...
       started: {timestamp}
       ```

    3. Identify extraction strategy per tier:
       - Tier 7: Clone repos, extract README + example code
       - Tier 8: Crawl security docs, extract protocol-specific patterns
       - Tier 9: Extract formal specs, invariant examples
       - Tier 10: Extract emerging patterns, tag as "emerging"
  </action>
  <verify>
    - `.true_vkg/discovery/tier-7-8-9-10-sources.yaml` exists with 30 sources
    - Sources grouped by tier and content type
    - Extraction strategies documented
  </verify>
  <done>Tier 7+8+9+10 sources inventoried with tier-specific strategies</done>
</task>

<task type="auto">
  <name>Task 2: Execute Tier 7+8+9+10 crawl and extraction via subagent</name>
  <files>.true_vkg/crawl_cache/*.md, knowledge/vulndocs/categories/**/*.md</files>
  <action>
    Spawn `knowledge-aggregation-worker` subagent with the following instructions:

    **Mission:** Process all 30 Tier 7-10 sources with tier-specific extraction strategies.

    **TIER 7 - GitHub Security Repositories:**

    **DeFiHackLabs (CRITICAL):**
    - Contains PoC code for real-world exploits
    - Extract: vulnerability pattern, exploit mechanism, affected protocol
    - Map each PoC to semantic operations
    - Note: Source code is implementation detail - extract PATTERN only

    **not-so-smart-contracts:**
    - Anti-pattern examples with explanations
    - Each directory = one vulnerability type
    - Extract: anti-pattern, correct pattern, detection signals

    **Web3Bugs:**
    - Categorized bug collection
    - Useful for coverage verification
    - Cross-reference with existing VulnDocs

    **For each GitHub repo:**
    1. Process README for overview
    2. Extract code examples (semantic-only)
    3. Map to BSKG operations
    4. MERGE into existing categories (most will merge)

    **TIER 8 - Protocol Documentation:**

    **Protocol-specific to generic pattern mapping:**

    | Protocol | Pattern Type | Generalization |
    |----------|--------------|----------------|
    | Uniswap | AMM slippage | Price manipulation via liquidity |
    | Aave | Flash loan | Atomic arbitrage/manipulation |
    | Compound | cToken | Share-based vault rounding |
    | MakerDAO | CDP | Collateral liquidation edge cases |
    | Curve | StableSwap | Amplification factor manipulation |
    | Balancer | Weighted pools | Multi-token price manipulation |
    | Chainlink | Oracle | Price feed staleness, manipulation |
    | Yearn | Vault | Share inflation, withdrawal timing |

    For each protocol doc:
    1. Extract security considerations section
    2. Identify protocol-specific risks
    3. Generalize to pattern IF similar exists in other protocols
    4. Keep protocol-specific ONLY if truly unique
    5. Tag with `protocols: [protocol_name]` in VulnDocs

    **TIER 9 - Formal Verification:**

    Extract formal properties as detection patterns:

    **Certora CVL specs:**
    - Extract invariants as detection properties
    - Map rule violations to vulnerability patterns
    - Example: `assert totalSupply == sum(balances)` -> token supply invariant

    **Echidna properties:**
    - Extract fuzzing properties
    - Map property failures to vuln classes

    **Halmos symbolic tests:**
    - Extract symbolic properties
    - Note bounded vs unbounded checks

    For each FV tool:
    1. Extract example properties/specs
    2. Map to semantic operations
    3. Add to detection.md as "formal verification signals"
    4. Note: FV content helps create Tier A patterns

    **TIER 10 - Emerging/Specialized:**

    **CRITICAL:** Tag all Tier 10 content as `emerging: true`

    **L2-specific (Arbitrum, Optimism, zkSync):**
    - Extract L2-specific risks (sequencer, forced transactions)
    - Map to cross-chain category if generalizable
    - Note L2-only limitations

    **ERC-4337 Account Abstraction:**
    - Extract AA-specific patterns
    - Map to existing categories where possible
    - Create account-abstraction subcategories as needed

    **Bridge patterns (LayerZero):**
    - Extract bridge-specific risks
    - Add to cross-chain category

    **Restaking (EigenLayer):**
    - Extract restaking-specific patterns
    - Add to restaking category (already exists)

    **Processing order:**
    1. DeFiHackLabs (real exploits)
    2. Web3Bugs (categorized coverage)
    3. Protocol docs (generalize patterns)
    4. FV tools (formal properties)
    5. Emerging tech (tag appropriately)
  </action>
  <verify>
    - All 30 sources in tracking file marked complete
    - GitHub repos processed for anti-patterns
    - Protocol-specific patterns generalized where possible
    - FV properties mapped to detection signals
    - Emerging tech properly tagged
    - Semantic-only constraint maintained
  </verify>
  <done>All 30 Tier 7-10 sources processed with specialized extraction</done>
</task>

<task type="auto">
  <name>Task 3: Generate Tier 7-10 completion manifest</name>
  <files>.true_vkg/discovery/tier-7-8-9-10-complete.yaml</files>
  <action>
    1. Generate completion manifest at `.true_vkg/discovery/tier-7-8-9-10-complete.yaml`:
       ```yaml
       tiers: ["7", "8", "9", "10"]
       completed: {timestamp}

       tier_7_github:
         sources_processed: {count}
         pocs_extracted: {count}
         antipatterns_documented: {count}

       tier_8_protocol_docs:
         sources_processed: {count}
         protocol_patterns: {count}
         generalized: {count}
         protocol_specific: {count}

       tier_9_formal_verification:
         sources_processed: {count}
         invariants_extracted: {count}
         properties_mapped: {count}
         tier_a_candidates: {count}

       tier_10_emerging:
         sources_processed: {count}
         emerging_patterns: {count}
         l2_specific: {count}
         aa_specific: {count}
         bridge_specific: {count}

       totals:
         patterns_extracted: {count}
         patterns_merged: {count}
         patterns_created: {count}

       emerging_coverage:
         arbitrum: true/false
         optimism: true/false
         zksync: true/false
         erc4337: true/false
         eigenlayer: true/false

       quality_stats:
         avg_lines_per_doc: {number}
         semantic_only_verified: true
       ```

    2. Create protocol coverage matrix:
       - Document which DeFi protocols have VulnDocs coverage
       - Note protocol-specific vs generalized patterns

    3. Create emerging tech tracking:
       - List all emerging patterns discovered
       - Tag maturity level (theoretical, observed, exploited)

    4. Archive crawl cache to `.true_vkg/crawl_cache/archive/tier-7-8-9-10/`

    5. Quality validation:
       - Verify FV properties mapped correctly
       - Verify protocol patterns generalized appropriately
       - Verify emerging patterns tagged
  </action>
  <verify>
    - `.true_vkg/discovery/tier-7-8-9-10-complete.yaml` exists with tier breakdowns
    - Protocol coverage matrix created
    - Emerging tech properly documented
    - Cache archived
    - Quality checks pass
  </verify>
  <done>Tier 7-10 complete with specialized metrics and coverage tracking</done>
</task>

</tasks>

<verification>
1. All 30 sources tracked and processed
2. GitHub anti-patterns extracted semantically
3. Protocol-specific patterns generalized where appropriate
4. FV properties mapped to detection signals
5. Emerging tech patterns tagged appropriately
6. Completion manifest with tier-specific metrics
</verification>

<success_criteria>
- 30/30 sources processed (with documented skip reasons if any)
- DeFiHackLabs PoCs mapped to semantic patterns
- Protocol docs generalized to reusable patterns
- FV invariants integrated as detection properties
- Emerging tech documented with proper tagging
- Tier-specific completion metrics documented
</success_criteria>

<output>
After completion, create `.planning/phases/01-vulndocs-completion/01-04-SUMMARY.md`
</output>
