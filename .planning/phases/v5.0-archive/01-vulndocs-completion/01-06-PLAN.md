---
phase: 01-vulndocs-completion
plan: 06
type: execute
wave: 3
depends_on: ["01-05"]
files_modified:
  - .true_vkg/discovery/benchmark-validation.yaml
  - knowledge/vulndocs/categories/**/*.md
autonomous: true
user_setup: []

must_haves:
  truths:
    - "VulnDocs validated against DVDeFi benchmark (13 challenges)"
    - "VulnDocs validated against real-world exploits"
    - "Coverage gaps identified and documented"
    - "Quality gate passed for GA readiness"
  artifacts:
    - path: ".true_vkg/discovery/benchmark-validation.yaml"
      provides: "Complete benchmark validation results"
    - path: ".true_vkg/discovery/coverage-matrix.yaml"
      provides: "Vulnerability class coverage tracking"
  key_links:
    - from: "DVDeFi challenges"
      to: "VulnDocs categories"
      via: "Challenge-to-pattern mapping"
    - from: "Real-world exploits"
      to: "VulnDocs categories"
      via: "Exploit-to-pattern mapping"
---

<objective>
Validate the consolidated VulnDocs against known benchmarks (DVDeFi challenges, real-world exploits) to ensure coverage and quality. Identify gaps, fix issues, and produce a validation report for GA readiness.

Purpose: Validation ensures the VulnDocs knowledge base is comprehensive enough to support vulnerability detection. DVDeFi is the primary benchmark (VKG already achieves 84.6% detection). Real-world exploits provide secondary validation.

Output: Benchmark validation report, coverage matrix, gap analysis, quality gate assessment.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/intel/summary.md
@.planning/phases/01-vulndocs-completion/01-05-SUMMARY.md
@.true_vkg/discovery/consolidation-report.yaml
@knowledge/vulndocs/index.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate against DVDeFi benchmark challenges</name>
  <files>.true_vkg/discovery/dvdefi-validation.yaml</files>
  <action>
    1. Load DVDeFi challenge list (13 challenges):
       ```yaml
       challenges:
         - name: "Unstoppable"
           category: flash-loan
           vuln_type: "Strict equality check on vault balance"
           vkg_status: "FAIL"  # Known issue

         - name: "Naive Receiver"
           category: flash-loan
           vuln_type: "Missing access control on flash loan receiver"
           vkg_status: "PASS"

         - name: "Truster"
           category: delegatecall
           vuln_type: "Arbitrary function call via flash loan"
           vkg_status: "FAIL"  # Known issue

         - name: "Side Entrance"
           category: reentrancy
           vuln_type: "Callback-based reentrancy"
           vkg_status: "FAIL"  # Known issue

         - name: "The Rewarder"
           category: flash-loan
           vuln_type: "Flash loan to manipulate reward distribution"
           vkg_status: "PASS"

         - name: "Selfie"
           category: governance
           vuln_type: "Flash loan to gain governance control"
           vkg_status: "PASS"

         - name: "Compromised"
           category: oracle
           vuln_type: "Oracle price manipulation"
           vkg_status: "PASS"

         - name: "Puppet"
           category: oracle
           vuln_type: "AMM-based oracle manipulation"
           vkg_status: "PASS"

         - name: "Puppet V2"
           category: oracle
           vuln_type: "Uniswap V2 oracle manipulation"
           vkg_status: "PASS"

         - name: "Free Rider"
           category: reentrancy
           vuln_type: "NFT purchase reentrancy"
           vkg_status: "PASS"

         - name: "Backdoor"
           category: access-control
           vuln_type: "Proxy initialization exploit"
           vkg_status: "PASS"

         - name: "Climber"
           category: access-control
           vuln_type: "Timelock schedule bypass"
           vkg_status: "PASS"

         - name: "Wallet Mining"
           category: crypto
           vuln_type: "Deterministic address mining"
           vkg_status: "PASS"
       ```

    2. For each challenge, verify VulnDocs coverage:
       - Does a relevant category/subcategory exist?
       - Does core-pattern.md describe this vulnerability class?
       - Are the semantic operations correct?
       - Would an LLM with this VulnDoc detect the issue?

    3. Document validation results:
       ```yaml
       # .true_vkg/discovery/dvdefi-validation.yaml
       benchmark: "DamnVulnerableDeFi"
       version: "v3"
       total_challenges: 13
       validated_at: {timestamp}

       results:
         - challenge: "Unstoppable"
           vulndoc_category: "logic/strict-equality"
           coverage: PARTIAL
           vulndoc_path: "knowledge/vulndocs/categories/logic/subcategories/strict-equality/core-pattern.md"
           missing: "Vault balance-specific detection signal"
           vkg_notes: "Known detection failure - strict equality pattern exists but vault-specific missing"

         - challenge: "Naive Receiver"
           vulndoc_category: "access-control/missing-access"
           coverage: FULL
           vulndoc_path: "..."
           vkg_notes: "VKG detects via CHECKS_PERMISSION absence"

         # ... all 13 challenges

       summary:
         full_coverage: {count}
         partial_coverage: {count}
         no_coverage: {count}
         coverage_percentage: {percent}

       gaps:
         - challenge: "Unstoppable"
           gap_type: "Missing vault-specific strict equality pattern"
           action: "ADD to logic/strict-equality"
         # ... any gaps
       ```

    4. Fix any gaps by updating VulnDocs:
       - Add missing patterns
       - Enhance existing patterns with missing signals
       - Ensure each DVDeFi challenge has coverage
  </action>
  <verify>
    - `.true_vkg/discovery/dvdefi-validation.yaml` complete
    - All 13 DVDeFi challenges mapped to VulnDocs
    - Gaps documented with action items
    - Known BSKG failures (Truster, Unstoppable, Side Entrance) have VulnDocs even if detection fails
  </verify>
  <done>DVDeFi benchmark validation complete with gap analysis</done>
</task>

<task type="auto">
  <name>Task 2: Validate against real-world exploits</name>
  <files>.true_vkg/discovery/exploit-validation.yaml</files>
  <action>
    1. Select real-world exploit sample (from Tier 1-2 sources):
       - At least 20 major exploits from 2022-2025
       - Cover all major vulnerability categories
       - Include >$1M exploits for severity validation

       Example exploits:
       - Ronin Bridge ($600M, bridge vuln)
       - Wormhole ($320M, signature validation)
       - Nomad Bridge ($190M, message validation)
       - Beanstalk ($182M, governance flash loan)
       - Wintermute ($160M, vanity address)
       - Mango Markets ($114M, oracle manipulation)
       - Harmony Bridge ($100M, multisig compromise)
       - Euler Finance ($197M, donation attack)
       - Cream Finance ($130M, reentrancy)
       - Compound ($80M, oracle misconfiguration)
       - DAO ($60M, reentrancy - historical)
       - Parity Wallet ($30M, self-destruct)
       - Pickle Finance ($20M, jar strategy)
       - Harvest Finance ($34M, flash loan arb)
       - Rari Capital ($80M, reentrancy)
       - BadgerDAO ($120M, frontend attack - out of scope)
       - Poly Network ($610M, cross-chain call)
       - Spartan Protocol ($30M, LP manipulation)
       - BurgerSwap ($7M, reentrancy)
       - PancakeBunny ($45M, flash loan)

    2. For each exploit, validate VulnDocs coverage:
       ```yaml
       exploit_validation:
         - name: "Euler Finance"
           date: "2023-03-13"
           amount: "$197M"
           vuln_class: "token/donation-attack"
           vulndoc_exists: true
           vulndoc_path: "categories/token/subcategories/donation-attack/core-pattern.md"
           detection_signals_match: true
           notes: "Donation attack pattern well documented"

         - name: "Ronin Bridge"
           date: "2022-03-29"
           amount: "$600M"
           vuln_class: "cross-chain/bridge-validation"
           vulndoc_exists: true
           vulndoc_path: "..."
           detection_signals_match: PARTIAL
           notes: "Bridge pattern exists but Ronin-specific multisig not covered"
       ```

    3. Generate exploit validation report:
       ```yaml
       # .true_vkg/discovery/exploit-validation.yaml
       validation_type: "Real-world exploits"
       sample_size: 20
       validated_at: {timestamp}

       results:
         # Individual exploit results...

       by_category:
         reentrancy: {covered}/{total}
         flash-loan: {covered}/{total}
         oracle: {covered}/{total}
         bridge: {covered}/{total}
         governance: {covered}/{total}
         access-control: {covered}/{total}

       summary:
         full_coverage: {count}
         partial_coverage: {count}
         no_coverage: {count}
         coverage_percentage: {percent}

       critical_gaps:
         - vuln_class: "{class}"
           missing_pattern: "{description}"
           affected_exploits: [list]
       ```

    4. Fix critical gaps (if any):
       - Add missing vulnerability patterns
       - Ensure major exploit classes have VulnDocs
  </action>
  <verify>
    - `.true_vkg/discovery/exploit-validation.yaml` complete
    - 20+ real-world exploits validated
    - Coverage by category documented
    - Critical gaps identified and addressed
  </verify>
  <done>Real-world exploit validation complete with category coverage</done>
</task>

<task type="auto">
  <name>Task 3: Generate coverage matrix and quality gate assessment</name>
  <files>.true_vkg/discovery/coverage-matrix.yaml, .true_vkg/discovery/benchmark-validation.yaml</files>
  <action>
    1. Create comprehensive coverage matrix:
       ```yaml
       # .true_vkg/discovery/coverage-matrix.yaml
       generated: {timestamp}

       # Coverage by BSKG semantic operation
       operation_coverage:
         TRANSFERS_VALUE_OUT:
           vulndocs: {count}
           categories: [reentrancy, mev, token, flash-loan]
           example_patterns: ["R:bal->X:out->W:bal", "X:out->C:auth"]

         WRITES_USER_BALANCE:
           vulndocs: {count}
           categories: [reentrancy, logic, access-control]
           example_patterns: ["W:bal->X:out", "W:bal->W:bal"]

         CHECKS_PERMISSION:
           vulndocs: {count}
           categories: [access-control, governance, upgrade]
           example_patterns: ["C:auth->M:own", "!C:auth->W:bal"]

         # ... all 20 BSKG operations

       # Coverage by vulnerability class (from SWC + industry)
       vulnerability_class_coverage:
         reentrancy:
           swc_ids: ["SWC-107"]
           dvdefi: ["Side Entrance", "Free Rider"]
           exploits: ["DAO", "Cream", "Rari"]
           vulndocs: {count}
           coverage: FULL

         oracle_manipulation:
           swc_ids: ["SWC-120"]
           dvdefi: ["Compromised", "Puppet", "Puppet V2"]
           exploits: ["Mango", "Compound"]
           vulndocs: {count}
           coverage: FULL

         flash_loan_attacks:
           swc_ids: []  # No SWC
           dvdefi: ["The Rewarder", "Selfie"]
           exploits: ["Euler", "Harvest"]
           vulndocs: {count}
           coverage: FULL

         # ... all major vuln classes

       # Gap analysis
       coverage_gaps:
         missing_operations: []  # Operations with 0 VulnDocs
         missing_swc: []  # SWC entries without VulnDocs
         missing_dvdefi: []  # DVDeFi challenges without coverage
         low_coverage:  # Operations/classes with < 2 VulnDocs
           - operation: "{op}"
             current: 1
             recommended: 3
       ```

    2. Generate final benchmark validation report:
       ```yaml
       # .true_vkg/discovery/benchmark-validation.yaml
       phase: "01-vulndocs-completion"
       completed: {timestamp}

       benchmarks:
         dvdefi:
           total: 13
           covered: {count}
           percentage: {percent}
           known_gaps: ["Truster (delegatecall)", "Unstoppable (strict eq)", "Side Entrance (callback)"]

         real_world_exploits:
           total: 20
           covered: {count}
           percentage: {percent}
           critical_gaps: []

         swc_registry:
           total: {swc_count}
           mapped: {count}
           percentage: {percent}
           unmapped: []

       quality_gate:
         passed: true/false
         criteria:
           - name: "VulnDocs count"
             required: "> 50 categories/subcategories"
             actual: {count}
             status: PASS/FAIL

           - name: "DVDeFi coverage"
             required: ">= 80%"
             actual: "{percent}%"
             status: PASS/FAIL

           - name: "Exploit coverage"
             required: ">= 85%"
             actual: "{percent}%"
             status: PASS/FAIL

           - name: "Operation coverage"
             required: "All 20 BSKG operations"
             actual: "{count}/20"
             status: PASS/FAIL

           - name: "Doc quality"
             required: "All < 100 lines, semantic-only"
             actual: "0 violations"
             status: PASS/FAIL

       recommendations:
         - priority: HIGH
           action: "{description}"
         # ... any recommendations

       ga_readiness:
         status: READY/NOT_READY
         blockers: []  # If not ready
         notes: "{summary}"
       ```

    3. If quality gate fails:
       - Document specific failures
       - Create fix tasks
       - Note blockers for GA

    4. Final summary for phase completion
  </action>
  <verify>
    - `.true_vkg/discovery/coverage-matrix.yaml` complete
    - `.true_vkg/discovery/benchmark-validation.yaml` complete
    - Quality gate assessment documented
    - GA readiness status determined
  </verify>
  <done>Coverage matrix and quality gate assessment complete, GA readiness determined</done>
</task>

</tasks>

<verification>
1. DVDeFi benchmark: All 13 challenges have VulnDocs coverage (documented gaps acceptable)
2. Real-world exploits: 20+ exploits validated with >85% coverage
3. Coverage matrix complete with all 20 BSKG operations
4. Quality gate criteria evaluated
5. GA readiness status determined
</verification>

<success_criteria>
- DVDeFi coverage >= 80% (10/13 challenges with VulnDocs)
- Real-world exploit coverage >= 85%
- All 20 BSKG operations have VulnDocs coverage
- Zero quality violations (all docs < 100 lines, semantic-only)
- GA readiness status documented
- Coverage matrix provides complete vulnerability class tracking
</success_criteria>

<output>
After completion, create `.planning/phases/01-vulndocs-completion/01-06-SUMMARY.md`
</output>
