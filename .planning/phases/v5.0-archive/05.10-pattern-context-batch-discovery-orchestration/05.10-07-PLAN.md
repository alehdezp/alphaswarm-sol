---
phase: 05.10-pattern-context-batch-discovery-orchestration
plan: 07
type: execute
wave: 7
depends_on:
  - 05.10-06
  - 05.10-13
files_modified:
  - src/alphaswarm_sol/orchestration/batch.py
  - src/alphaswarm_sol/orchestration/__init__.py
  - src/alphaswarm_sol/cli/batch.py
  - tests/orchestration/test_batch_discovery.py
autonomous: true

must_haves:
  truths:
    - "Batch orchestration uses adaptive batching and fork-then-rank"
    - "Prefix cache keys include graph hash + PCP version"
    - "Batch outputs are append-only and deterministic"
  artifacts:
    - path: "src/alphaswarm_sol/orchestration/batch.py"
      provides: "BatchDiscoveryOrchestrator v2"
      exports: ["BatchDiscoveryOrchestrator", "BatchManifest"]
  key_links:
    - from: "src/alphaswarm_sol/orchestration/batch.py"
      to: "src/alphaswarm_sol/cli/batch.py"
      via: "batch-discover CLI"
---

<objective>
Implement batch discovery v2 with adaptive batching, cache versioning, and fork-then-rank orchestration to reduce cost without lowering quality.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@src/alphaswarm_sol/skills/shipped/pattern-batch.md
@src/alphaswarm_sol/agents/orchestration/main_orchestrator.py
</context>

<execution_guardrails>
- Full doc alignment: align objective, must_haves, and tasks against `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, plus phase context or research docs before execution. Any scope change must be reflected in the plan and `.planning/STATE.md`.
- Workspace isolation: run any commands or experiments that mutate state in a fresh jj workspace; do not use the main workspace and do not reuse workspaces for reruns.
- Repeat-run detection markers: include `[ALPHASWARM-START] RUN_ID TIMESTAMP` and `[ALPHASWARM-END] RUN_ID EXIT_CODE DURATION_MS` markers in transcripts or logs so reruns are detectable.
- claude-code-controller subject: for interactive runs, use `claude-code-controller` and launch Claude Code as the subject in the claude-code-agent-teams session; transcripts must show the Claude Code session.
- Seed scope boundaries: treat B-seeds (baseline or regression inputs) and N-seeds (net-new exploratory inputs) as separate scopes; do not mix them in a single run and track results separately.
</execution_guardrails>

<tasks>

<task type="auto">
  <name>Task 1: Implement BatchDiscoveryOrchestrator v2</name>
  <files>src/alphaswarm_sol/orchestration/batch.py, src/alphaswarm_sol/orchestration/__init__.py</files>
  <action>
Implement BatchDiscoveryOrchestrator v2:
- Adaptive batching by pattern cost/complexity
- Prefix cache versioning (graph hash + PCP version + budget policy)
- Fork-then-rank: multiple scouts propose, verifier ranks, contradiction can veto
  </action>
  <verify>
```bash
rg -n "BatchDiscoveryOrchestrator|adaptive|fork" src/alphaswarm_sol/orchestration/batch.py
```
  </verify>
  <done>
Batch orchestrator v2 is implemented with adaptive batching and fork-then-rank.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add CLI + batch tests</name>
  <files>src/alphaswarm_sol/cli/batch.py, tests/orchestration/test_batch_discovery.py</files>
  <action>
Extend CLI:
- `batch-discover` accepts batch size, risk weighting, and cache mode
- Manifest includes cache keys, slice hashes, evidence IDs, and protocol_context_included flag

Add tests:
- Cache key changes on PCP version change
- Adaptive batching groups by cost
- Fork-then-rank selects deterministically
  </action>
  <verify>
```bash
rg -n "batch-discover|manifest" src/alphaswarm_sol/cli/batch.py
```
  </verify>
  <done>
CLI and tests cover batch orchestration v2 behavior.
  </done>
</task>

</tasks>
