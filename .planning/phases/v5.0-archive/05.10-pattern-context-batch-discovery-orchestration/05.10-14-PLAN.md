---
phase: 05.10-pattern-context-batch-discovery-orchestration
plan: 14
type: execute
wave: 11
depends_on:
  - 05.10-11
  - 05.10-10
  - 05.10-13
files_modified:
  - tests/integration/test_phase_510_quality.py
  - tests/integration/test_pcp_determinism.py
  - tests/integration/test_batch_end_to_end.py
  - scripts/benchmarks/phase_510_quality.py
  - src/alphaswarm_sol/kg/slicing_benchmark.py
autonomous: true

must_haves:
  truths:
    - "Phase 5.10 passes meaningful integration tests for determinism, evidence gating, and batch orchestration"
    - "Quality benchmark exercises real behavior, not just schema or pass-through checks"
    - "Shuffled-input determinism test catches iteration order bugs"
  artifacts:
    - path: "scripts/benchmarks/phase_510_quality.py"
      provides: "Phase 5.10 quality benchmark"
      exports: ["phase_510_quality"]
  key_links:
    - from: "tests/integration/test_phase_510_quality.py"
      to: "scripts/benchmarks/phase_510_quality.py"
      via: "shared fixtures + metrics"
---

<objective>
Add a high-value quality gate for Phase 5.10 that validates determinism, evidence gating, and batch orchestration behavior end-to-end with meaningful assertions.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@docs/guides/testing-basics.md
</context>

<execution_guardrails>
- Full doc alignment: align objective, must_haves, and tasks against `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, plus phase context or research docs before execution. Any scope change must be reflected in the plan and `.planning/STATE.md`.
- Workspace isolation: run any commands or experiments that mutate state in a fresh jj workspace; do not use the main workspace and do not reuse workspaces for reruns.
- Repeat-run detection markers: include `[ALPHASWARM-START] RUN_ID TIMESTAMP` and `[ALPHASWARM-END] RUN_ID EXIT_CODE DURATION_MS` markers in transcripts or logs so reruns are detectable.
- claude-code-controller subject: for interactive runs, use `claude-code-controller` and launch Claude Code as the subject in the claude-code-agent-teams session; transcripts must show the Claude Code session.
- Seed scope boundaries: treat B-seeds (baseline or regression inputs) and N-seeds (net-new exploratory inputs) as separate scopes; do not mix them in a single run and track results separately.
</execution_guardrails>

<tasks>

<task type="auto">
  <name>Task 1: Determinism + evidence gating + batch integration tests</name>
  <files>tests/integration/test_pcp_determinism.py, tests/integration/test_phase_510_quality.py, tests/integration/test_batch_end_to_end.py</files>
  <action>
Create integration tests that validate:
- PCP context packs are bit-identical for same graph+PCP version
- Evidence IDs are stable across rebuilds
- Unknown handling triggers controlled expansion and never upgrades without evidence
- Omission lists are surfaced and not interpreted as safe
- Adaptive batching with cache versioning
- Fork-then-rank with verifier selection
- Protocol context gating recorded in manifest
- Append-only deltas merged deterministically

**Shuffled-input determinism test (critical):** For merge pipeline and batch orchestration, run the same operation twice with inputs in different random order (use `random.shuffle` with fixed seed for reproducibility). Assert outputs are bit-identical. This catches dict/set iteration order bugs that cause non-determinism.

Assert behavior-level outcomes (not just pass-through).
  </action>
  <verify>
```bash
rg -n "determinism|evidence_id|unknown|omission|batch|cache|fork|manifest|delta|shuffle" tests/integration/test_phase_510_quality.py
```
  </verify>
  <done>
Determinism, evidence gating, and batch orchestration are covered by integration tests, including shuffled-input invariance.
  </done>
</task>

<task type="auto">
  <name>Task 2: Phase 5.10 quality benchmark (reuse slicing benchmark)</name>
  <files>scripts/benchmarks/phase_510_quality.py, src/alphaswarm_sol/kg/slicing_benchmark.py</files>
  <action>
Implement a quality benchmark that:
- Runs representative patterns with deterministic fixtures
- Reports: precision/recall deltas, novelty yield, evidence entropy
- Fails if batch regressions occur or determinism breaks
- Reuses kg/slicing_benchmark for token reduction + coverage metrics
- Includes shuffled-order stability check: same inputs in different order must produce identical outputs

Ensure the benchmark surfaces token reduction/coverage using existing slicing benchmark utilities.
  </action>
  <verify>
```bash
rg -n "phase 5.10|quality benchmark|shuffle" scripts/benchmarks/phase_510_quality.py
```
  </verify>
  <done>
Phase 5.10 quality benchmark and testing docs are in place, with shuffled-input determinism validation.
  </done>
</task>

</tasks>
