---
phase: 05.10-pattern-context-batch-discovery-orchestration
plan: 09
type: execute
wave: 8
depends_on:
  - 05.10-07
files_modified:
  - src/alphaswarm_sol/orchestration/creative.py
  - src/alphaswarm_sol/queries/patterns.py
  - src/alphaswarm_sol/agents/orchestration/main_orchestrator.py
  - tests/orchestration/test_creative_discovery.py
autonomous: true

must_haves:
  truths:
    - "Creative discovery outputs are Tier-B and evidence-gated"
    - "Near-miss, mutation, and counterfactual probes are deterministic"
  artifacts:
    - path: "src/alphaswarm_sol/orchestration/creative.py"
      provides: "Creative discovery loop"
      exports: ["CreativeDiscoveryLoop"]
  key_links:
    - from: "src/alphaswarm_sol/orchestration/creative.py"
      to: "src/alphaswarm_sol/agents/orchestration/main_orchestrator.py"
      via: "creative loop integration"
---

<objective>
Add a controlled creative discovery loop that surfaces novel hypotheses while preserving evidence gates and determinism.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@src/alphaswarm_sol/orchestration/batch.py
@src/alphaswarm_sol/queries/patterns.py
</context>

<execution_guardrails>
- Full doc alignment: align objective, must_haves, and tasks against `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, plus phase context or research docs before execution. Any scope change must be reflected in the plan and `.planning/STATE.md`.
- Workspace isolation: run any commands or experiments that mutate state in a fresh jj workspace; do not use the main workspace and do not reuse workspaces for reruns.
- Repeat-run detection markers: include `[ALPHASWARM-START] RUN_ID TIMESTAMP` and `[ALPHASWARM-END] RUN_ID EXIT_CODE DURATION_MS` markers in transcripts or logs so reruns are detectable.
- claude-code-controller subject: for interactive runs, use `claude-code-controller` and launch Claude Code as the subject in the claude-code-agent-teams session; transcripts must show the Claude Code session.
- Seed scope boundaries: treat B-seeds (baseline or regression inputs) and N-seeds (net-new exploratory inputs) as separate scopes; do not mix them in a single run and track results separately.
</execution_guardrails>

<tasks>

<task type="auto">
  <name>Task 1: Implement creative discovery loop</name>
  <files>src/alphaswarm_sol/orchestration/creative.py, src/alphaswarm_sol/queries/patterns.py</files>
  <action>
Implement deterministic creative exploration:
- Near-miss mining (one op away)
- Pattern mutation (ordering variants / op substitutions)
- Counterfactual probes using PCP counterfactuals
- Anomaly motifs + shadow pattern proposals

Ensure all outputs are Tier-B with explicit unknowns.
  </action>
  <verify>
```bash
rg -n "near-miss|mutation|counterfactual|shadow" src/alphaswarm_sol/orchestration/creative.py
```
  </verify>
  <done>
Creative loop behaviors are implemented and evidence-gated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate creative loop + tests</name>
  <files>src/alphaswarm_sol/agents/orchestration/main_orchestrator.py, tests/orchestration/test_creative_discovery.py</files>
  <action>
Wire creative loop as an optional Tier-B stage with strict evidence gates and budget checks.
Add tests:
- Near-miss marked unknown
- Counterfactuals do not upgrade confidence
- Shadow patterns flagged as proposals
  </action>
  <verify>
```bash
rg -n "CreativeDiscovery" src/alphaswarm_sol/agents/orchestration/main_orchestrator.py
```
  </verify>
  <done>
Creative discovery is integrated and covered by tests.
  </done>
</task>

</tasks>
