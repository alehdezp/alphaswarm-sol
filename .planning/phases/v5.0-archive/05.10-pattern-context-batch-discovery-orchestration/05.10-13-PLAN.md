---
phase: 05.10-pattern-context-batch-discovery-orchestration
plan: 13
type: execute
wave: 6
depends_on:
  - 05.10-08
files_modified:
  - schemas/alphaswarm-codex-output.json
  - src/alphaswarm_sol/orchestration/schemas.py
  - tests/agents/test_role_contracts.py
autonomous: true

must_haves:
  truths:
    - "Each agent role has a strict input/output contract"
    - "Diversity policy enforces distinct reasoning modes"
    - "Role contract tests enforce evidence IDs and unknown handling"
  artifacts:
    - path: "schemas/alphaswarm-codex-output.json"
      provides: "Role output schemas"
      exports: ["role contracts"]
  key_links:
    - from: "schemas/alphaswarm-codex-output.json"
      to: "src/alphaswarm_sol/orchestration/schemas.py"
      via: "role contract enforcement"
---

<objective>
Formalize role contracts and diversity policy so role outputs are schema-validated and reasoning paths are intentionally diverse.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@schemas/alphaswarm-codex-output.json
</context>

<execution_guardrails>
- Full doc alignment: align objective, must_haves, and tasks against `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, plus phase context or research docs before execution. Any scope change must be reflected in the plan and `.planning/STATE.md`.
- Workspace isolation: run any commands or experiments that mutate state in a fresh jj workspace; do not use the main workspace and do not reuse workspaces for reruns.
- Repeat-run detection markers: include `[ALPHASWARM-START] RUN_ID TIMESTAMP` and `[ALPHASWARM-END] RUN_ID EXIT_CODE DURATION_MS` markers in transcripts or logs so reruns are detectable.
- claude-code-controller subject: for interactive runs, use `claude-code-controller` and launch Claude Code as the subject in the claude-code-agent-teams session; transcripts must show the Claude Code session.
- Seed scope boundaries: treat B-seeds (baseline or regression inputs) and N-seeds (net-new exploratory inputs) as separate scopes; do not mix them in a single run and track results separately.
</execution_guardrails>

<tasks>

<task type="auto">
  <name>Task 1: Define role contracts and diversity policy</name>
  <files>schemas/alphaswarm-codex-output.json, src/alphaswarm_sol/orchestration/schemas.py</files>
  <action>
Extend output schema with role-specific contracts:
- ScoutHypothesis
- VerificationResult
- ContradictionReport
- CompositionProposal
- SynthesizedFinding

Add diversity policy to orchestration schemas:
- operation-first, guard-first, invariant-first paths
- enforce different reasoning modes across parallel agents
  </action>
  <verify>
```bash
rg -n "ScoutHypothesis|ContradictionReport|diversity" schemas/alphaswarm-codex-output.json src/alphaswarm_sol/orchestration/schemas.py
```
  </verify>
  <done>
Role contracts and diversity policy are defined and enforced.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add role contract tests</name>
  <files>tests/agents/test_role_contracts.py</files>
  <action>
Add tests that enforce:
- Schema validation fails on missing fields
- Unknown handling is explicit
- Evidence IDs are required
  </action>
  <verify>
```bash
rg -n "role contract" tests/agents/test_role_contracts.py
```
  </verify>
  <done>
Role contracts are covered by tests.
  </done>
</task>

</tasks>
