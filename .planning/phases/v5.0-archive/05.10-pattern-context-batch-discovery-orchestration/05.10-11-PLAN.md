---
phase: 05.10-pattern-context-batch-discovery-orchestration
plan: 11
type: execute
wave: 10
depends_on:
  - 05.10-07
files_modified:
  - tests/metrics/test_batch_quality.py
  - tests/metrics/test_calibration.py
  - tests/metrics/test_budget_pareto.py
  - scripts/benchmarks/batch_quality.py
  - docs/guides/testing-basics.md
autonomous: true

must_haves:
  truths:
    - "Quality metrics cover precision, recall, novelty, and evidence entropy"
    - "Batch vs sequential regression checks exist"
    - "Benchmark runner produces CI-ready metrics output"
  artifacts:
    - path: "scripts/benchmarks/batch_quality.py"
      provides: "Batch quality benchmark"
      exports: ["batch_quality_benchmark"]
  key_links:
    - from: "docs/guides/testing-basics.md"
      to: "scripts/benchmarks/batch_quality.py"
      via: "benchmark runbook"
---

<objective>
Define quality metrics and calibration tests to ensure batch discovery improves results without regression, and provide a benchmark runner with documentation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@docs/guides/testing-basics.md
</context>

<execution_guardrails>
- Full doc alignment: align objective, must_haves, and tasks against `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, plus phase context or research docs before execution. Any scope change must be reflected in the plan and `.planning/STATE.md`.
- Workspace isolation: run any commands or experiments that mutate state in a fresh jj workspace; do not use the main workspace and do not reuse workspaces for reruns.
- Repeat-run detection markers: include `[ALPHASWARM-START] RUN_ID TIMESTAMP` and `[ALPHASWARM-END] RUN_ID EXIT_CODE DURATION_MS` markers in transcripts or logs so reruns are detectable.
- claude-code-controller subject: for interactive runs, use `claude-code-controller` and launch Claude Code as the subject in the claude-code-agent-teams session; transcripts must show the Claude Code session.
- Seed scope boundaries: treat B-seeds (baseline or regression inputs) and N-seeds (net-new exploratory inputs) as separate scopes; do not mix them in a single run and track results separately.
</execution_guardrails>

<tasks>

<task type="auto">
  <name>Task 1: Add batch quality metric tests</name>
  <files>tests/metrics/test_batch_quality.py, tests/metrics/test_calibration.py, tests/metrics/test_budget_pareto.py</files>
  <action>
Add tests that measure:
- precision/recall deltas for batch vs sequential
- novelty yield and evidence entropy
- budget-quality Pareto thresholds
  </action>
  <verify>
```bash
rg -n "batch|precision|recall|novelty|entropy|Pareto" tests/metrics
```
  </verify>
  <done>
Batch quality metrics are covered by tests.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add benchmark runner + testing guide</name>
  <files>scripts/benchmarks/batch_quality.py, docs/guides/testing-basics.md</files>
  <action>
Implement a benchmark runner that:
- Executes batch and sequential runs
- Outputs a JSON summary for CI/regression tracking

Document how to run batch benchmarks in the testing guide.
  </action>
  <verify>
```bash
rg -n "batch quality" scripts/benchmarks/batch_quality.py
```
  </verify>
  <done>
Benchmark runner and testing guide updates are in place.
  </done>
</task>

</tasks>
