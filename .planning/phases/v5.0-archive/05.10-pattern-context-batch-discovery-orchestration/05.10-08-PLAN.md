---
phase: 05.10-pattern-context-batch-discovery-orchestration
plan: 08
type: execute
wave: 5
depends_on:
  - 05.10-06
files_modified:
  - src/alphaswarm_sol/skills/shipped/agents/vrs-contradiction.md
  - src/alphaswarm_sol/skills/shipped/agents/vrs-pattern-composer.md
  - src/alphaswarm_sol/skills/shipped/agents/vrs-finding-synthesizer.md
  - src/alphaswarm_sol/skills/shipped/agents/__init__.py
  - src/alphaswarm_sol/skills/shipped/README.md
autonomous: true

must_haves:
  truths:
    - "New agent roles are defined and scoped for batch discovery"
    - "Agent registry and shipped catalog include the new roles"
  artifacts:
    - path: "src/alphaswarm_sol/skills/shipped/agents/vrs-contradiction.md"
      provides: "Contradiction agent definition"
      exports: ["vrs-contradiction"]
  key_links:
    - from: "src/alphaswarm_sol/skills/shipped/agents/__init__.py"
      to: "src/alphaswarm_sol/skills/shipped/README.md"
      via: "agent catalog registry"
---

<objective>
Add new batch-discovery agent roles and ensure they are registered and discoverable in shipped skill catalogs.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@src/alphaswarm_sol/skills/shipped/README.md
</context>

<execution_guardrails>
- Full doc alignment: align objective, must_haves, and tasks against `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, plus phase context or research docs before execution. Any scope change must be reflected in the plan and `.planning/STATE.md`.
- Workspace isolation: run any commands or experiments that mutate state in a fresh jj workspace; do not use the main workspace and do not reuse workspaces for reruns.
- Repeat-run detection markers: include `[ALPHASWARM-START] RUN_ID TIMESTAMP` and `[ALPHASWARM-END] RUN_ID EXIT_CODE DURATION_MS` markers in transcripts or logs so reruns are detectable.
- claude-code-controller subject: for interactive runs, use `claude-code-controller` and launch Claude Code as the subject in the claude-code-agent-teams session; transcripts must show the Claude Code session.
- Seed scope boundaries: treat B-seeds (baseline or regression inputs) and N-seeds (net-new exploratory inputs) as separate scopes; do not mix them in a single run and track results separately.
</execution_guardrails>

<tasks>

<task type="auto">
  <name>Task 1: Add new agent role definitions</name>
  <files>src/alphaswarm_sol/skills/shipped/agents/vrs-contradiction.md, src/alphaswarm_sol/skills/shipped/agents/vrs-pattern-composer.md, src/alphaswarm_sol/skills/shipped/agents/vrs-finding-synthesizer.md</files>
  <action>
Create role specs:
- Contradiction agent: refutation-only, evidence required
- Pattern composer: proposes composite vulnerabilities using op-signature algebra
- Finding synthesizer: merges convergent evidence and labels confidence boundaries
  </action>
  <verify>
```bash
rg -n "Contradiction|Composer|Synthesizer" src/alphaswarm_sol/skills/shipped/agents
```
  </verify>
  <done>
New agent roles exist with strict scope definitions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Register roles in shipped catalog</name>
  <files>src/alphaswarm_sol/skills/shipped/agents/__init__.py, src/alphaswarm_sol/skills/shipped/README.md</files>
  <action>
Register new roles in the shipped agent registry and document them in the shipped README.
  </action>
  <verify>
```bash
rg -n "contradiction|composer|synthesizer" src/alphaswarm_sol/skills/shipped/README.md
```
  </verify>
  <done>
New roles are discoverable in the shipped catalog.
  </done>
</task>

</tasks>
