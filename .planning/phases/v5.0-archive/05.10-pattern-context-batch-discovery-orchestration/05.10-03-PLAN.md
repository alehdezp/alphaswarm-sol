---
phase: 05.10-pattern-context-batch-discovery-orchestration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/alphaswarm_sol/kg/evidence_id.py
  - src/alphaswarm_sol/kg/schema.py
  - src/alphaswarm_sol/agents/base.py
  - tests/kg/test_evidence_id.py
autonomous: true

must_haves:
  truths:
    - "Evidence IDs are canonical, deterministic, and graph-versioned"
    - "Evidence IDs propagate to agent findings"
  artifacts:
    - path: "src/alphaswarm_sol/kg/evidence_id.py"
      provides: "Canonical evidence ID generation"
      exports: ["evidence_id_for"]
  key_links:
    - from: "src/alphaswarm_sol/kg/evidence_id.py"
      to: "src/alphaswarm_sol/kg/schema.py"
      via: "Evidence ID storage"
    - from: "src/alphaswarm_sol/agents/base.py"
      to: "tests/kg/test_evidence_id.py"
      via: "Evidence ID propagation tests"
---

<objective>
Introduce canonical evidence identifiers and propagate them into agent evidence objects so evidence gating is reproducible and auditable across runs.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@src/alphaswarm_sol/kg/schema.py
@src/alphaswarm_sol/agents/base.py
</context>

<execution_guardrails>
- Full doc alignment: align objective, must_haves, and tasks against `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, plus phase context or research docs before execution. Any scope change must be reflected in the plan and `.planning/STATE.md`.
- Workspace isolation: run any commands or experiments that mutate state in a fresh jj workspace; do not use the main workspace and do not reuse workspaces for reruns.
- Repeat-run detection markers: include `[ALPHASWARM-START] RUN_ID TIMESTAMP` and `[ALPHASWARM-END] RUN_ID EXIT_CODE DURATION_MS` markers in transcripts or logs so reruns are detectable.
- claude-code-controller subject: for interactive runs, use `claude-code-controller` and launch Claude Code as the subject in the claude-code-agent-teams session; transcripts must show the Claude Code session.
- Seed scope boundaries: treat B-seeds (baseline or regression inputs) and N-seeds (net-new exploratory inputs) as separate scopes; do not mix them in a single run and track results separately.
</execution_guardrails>

<tasks>

<task type="auto">
  <name>Task 1: Implement canonical evidence IDs and propagation</name>
  <files>src/alphaswarm_sol/kg/evidence_id.py, src/alphaswarm_sol/kg/schema.py, src/alphaswarm_sol/agents/base.py</files>
  <action>
Implement canonical evidence ID generation using:
- graph hash or build hash
- node/edge ID
- file path + line range
- semantic op (if available)
- deterministic serialization order

Extend Evidence and AgentEvidence to carry evidence_id and serialize deterministically.
  </action>
  <verify>
```bash
rg -n "evidence_id_for|evidence_id" src/alphaswarm_sol/kg/evidence_id.py src/alphaswarm_sol/kg/schema.py src/alphaswarm_sol/agents/base.py
```
  </verify>
  <done>
Evidence IDs are defined and propagate to agent evidence.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add evidence ID determinism tests</name>
  <files>tests/kg/test_evidence_id.py</files>
  <action>
Add tests that verify:
- Evidence IDs are stable across repeated builds
- Evidence ID ordering is deterministic
  </action>
  <verify>
```bash
rg -n "evidence_id" tests/kg/test_evidence_id.py
```
  </verify>
  <done>
Evidence ID determinism is covered by tests.
  </done>
</task>

</tasks>
