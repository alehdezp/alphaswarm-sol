---
phase: 05.10-pattern-context-batch-discovery-orchestration
plan: 05
type: execute
wave: 3
depends_on:
  - 05.10-04
files_modified:
  - src/alphaswarm_sol/kg/slicer.py
  - src/alphaswarm_sol/kg/subgraph.py
  - src/alphaswarm_sol/agents/context/extractor.py
  - tests/kg/test_slice_expansion.py
autonomous: true

must_haves:
  truths:
    - "Semantic dilation expands slices only when evidence coverage is insufficient"
    - "Multi-hop triggers expand through call chains deterministically"
    - "Unknown -> expand -> re-evaluate is supported before hard-fail"
  artifacts:
    - path: "src/alphaswarm_sol/kg/slicer.py"
      provides: "Slice expansion policies"
      exports: ["ContextExpander", "GraphSlicer"]
  key_links:
    - from: "src/alphaswarm_sol/kg/slicer.py"
      to: "src/alphaswarm_sol/agents/context/extractor.py"
      via: "Expansion policy integration"
---

<objective>
Add controlled context expansion so pattern evaluation can move from unknown to verified without violating budgets or determinism.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@src/alphaswarm_sol/kg/slicer.py
@src/alphaswarm_sol/agents/context/extractor.py
</context>

<execution_guardrails>
- Full doc alignment: align objective, must_haves, and tasks against `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, plus phase context or research docs before execution. Any scope change must be reflected in the plan and `.planning/STATE.md`.
- Workspace isolation: run any commands or experiments that mutate state in a fresh jj workspace; do not use the main workspace and do not reuse workspaces for reruns.
- Repeat-run detection markers: include `[ALPHASWARM-START] RUN_ID TIMESTAMP` and `[ALPHASWARM-END] RUN_ID EXIT_CODE DURATION_MS` markers in transcripts or logs so reruns are detectable.
- claude-code-controller subject: for interactive runs, use `claude-code-controller` and launch Claude Code as the subject in the claude-code-agent-teams session; transcripts must show the Claude Code session.
- Seed scope boundaries: treat B-seeds (baseline or regression inputs) and N-seeds (net-new exploratory inputs) as separate scopes; do not mix them in a single run and track results separately.
</execution_guardrails>

<tasks>

<task type="auto">
  <name>Task 1: Add coverage scoring + expansion rules</name>
  <files>src/alphaswarm_sol/kg/slicer.py, src/alphaswarm_sol/kg/subgraph.py</files>
  <action>
Implement evidence coverage scoring and semantic dilation:
- Coverage score based on required/strong/weak evidence
- Expand slice radius until coverage threshold or budget limit
- Multi-hop expansion triggers when required ops are separated by call chains
- Deterministic tie-breaking and stop conditions
  </action>
  <verify>
```bash
rg -n "coverage|dilation|multi-hop" src/alphaswarm_sol/kg/slicer.py
```
  </verify>
  <done>
Coverage scoring and expansion rules are implemented.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate escalation policy and tests</name>
  <files>src/alphaswarm_sol/agents/context/extractor.py, tests/kg/test_slice_expansion.py</files>
  <action>
Implement escalation policy in context extraction:
- If required ops missing, mark unknown
- Trigger a single expansion pass
- Re-evaluate and return unknown if still missing

Add tests for dilation, multi-hop inclusion, and single-pass expansion.
  </action>
  <verify>
```bash
rg -n "unknown|re-evaluate|expansion" src/alphaswarm_sol/agents/context/extractor.py
```
  </verify>
  <done>
Escalation policy and expansion tests are in place.
  </done>
</task>

</tasks>
