---
phase: 05.10-pattern-context-batch-discovery-orchestration
plan: 04
type: execute
wave: 2
depends_on:
  - 05.10-03
files_modified:
  - src/alphaswarm_sol/kg/slicer.py
  - src/alphaswarm_sol/queries/patterns.py
  - tests/kg/test_pattern_slice.py
  - tests/kg/test_witness_extraction.py
  - docs/reference/rich-edges.md
autonomous: true

must_haves:
  truths:
    - "Pattern-scoped slices use edge-closure over required ops"
    - "Slices emit omission lists and missing-required signals"
    - "Witness extraction is deterministic and evidence-linked"
    - "Omission entries include edge type for debugging"
  artifacts:
    - path: "src/alphaswarm_sol/kg/slicer.py"
      provides: "Pattern slicing + witness extraction"
      exports: ["GraphSlicer", "SlicedGraph"]
  key_links:
    - from: "src/alphaswarm_sol/queries/patterns.py"
      to: "src/alphaswarm_sol/kg/slicer.py"
      via: "PCP v2 slicing inputs"
    - from: "tests/kg/test_witness_extraction.py"
      to: "src/alphaswarm_sol/kg/slicer.py"
      via: "Witness determinism coverage"
---

<objective>
Implement pattern-scoped slicing v2 with edge-closure, omission lists, deterministic pruning, and witness extraction so agents always see what evidence is present and what is missing.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@docs/reference/operations.md
@docs/reference/rich-edges.md
@src/alphaswarm_sol/kg/subgraph.py
@src/alphaswarm_sol/queries/patterns.py
@src/alphaswarm_sol/kg/slicer.py
</context>

<execution_guardrails>
- Full doc alignment: align objective, must_haves, and tasks against `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, plus phase context or research docs before execution. Any scope change must be reflected in the plan and `.planning/STATE.md`.
- Workspace isolation: run any commands or experiments that mutate state in a fresh jj workspace; do not use the main workspace and do not reuse workspaces for reruns.
- Repeat-run detection markers: include `[ALPHASWARM-START] RUN_ID TIMESTAMP` and `[ALPHASWARM-END] RUN_ID EXIT_CODE DURATION_MS` markers in transcripts or logs so reruns are detectable.
- claude-code-controller subject: for interactive runs, use `claude-code-controller` and launch Claude Code as the subject in the claude-code-agent-teams session; transcripts must show the Claude Code session.
- Seed scope boundaries: treat B-seeds (baseline or regression inputs) and N-seeds (net-new exploratory inputs) as separate scopes; do not mix them in a single run and track results separately.
</execution_guardrails>

<tasks>

<task type="auto">
  <name>Task 1: Implement slicing v2 + witness extraction</name>
  <files>src/alphaswarm_sol/kg/slicer.py, src/alphaswarm_sol/queries/patterns.py</files>
  <action>
Extend pattern parsing to accept PCP v2 focus inputs and implement in GraphSlicer:
- Edge-closure slicing seeded by required ops and witness evidence
- Counter/anti-signal inclusion
- Deterministic pruning rules with omission reporting
- Witness + negative witness extraction with evidence IDs

For omission list entries, include both edge ID and edge type (e.g., `{edge_id: "e_0x1234", edge_type: "calls"}`) to enable debugging why specific context was excluded.
  </action>
  <verify>
```bash
rg -n "witness|omission|edge-closure|edge_type" src/alphaswarm_sol/kg/slicer.py
```
  </verify>
  <done>
Slicing v2 and witness extraction are implemented with deterministic reporting and typed omission entries.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add slicing tests and docs</name>
  <files>tests/kg/test_pattern_slice.py, tests/kg/test_witness_extraction.py, docs/reference/rich-edges.md</files>
  <action>
Add tests for:
- Required ops always present
- Omission list includes excluded edges with both edge_id and edge_type
- Witness extraction deterministic ordering

Update rich-edges reference to include omission and witness reporting, noting that omission entries contain edge type for debugging.
  </action>
  <verify>
```bash
rg -n "PatternSlice|witness|edge_type" tests/kg/test_pattern_slice.py
```
  </verify>
  <done>
Slicing v2 behavior and docs are covered, including typed omission entries.
  </done>
</task>

</tasks>
