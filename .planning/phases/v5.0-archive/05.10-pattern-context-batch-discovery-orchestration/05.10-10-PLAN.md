---
phase: 05.10-pattern-context-batch-discovery-orchestration
plan: 10
type: execute
wave: 9
depends_on:
  - 05.10-07
files_modified:
  - src/alphaswarm_sol/orchestration/dedup.py
  - src/alphaswarm_sol/orchestration/schemas.py
  - src/alphaswarm_sol/orchestration/handlers.py
  - tests/orchestration/test_merge_pipeline.py
autonomous: true

must_haves:
  truths:
    - "Merge pipeline is append-only and idempotent"
    - "Deterministic ordering prevents conflicts"
    - "Conflicts are quarantined for resolver agent"
  artifacts:
    - path: "src/alphaswarm_sol/orchestration/dedup.py"
      provides: "Merge pipeline v2"
      exports: ["merge_findings"]
  key_links:
    - from: "src/alphaswarm_sol/orchestration/schemas.py"
      to: "src/alphaswarm_sol/orchestration/dedup.py"
      via: "delta schema enforcement"
---

<objective>
Harden the merge pipeline so concurrent batch results can be merged deterministically with conflict quarantine and full audit trails.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@src/alphaswarm_sol/orchestration/dedup.py
@src/alphaswarm_sol/orchestration/handlers.py
</context>

<execution_guardrails>
- Full doc alignment: align objective, must_haves, and tasks against `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, plus phase context or research docs before execution. Any scope change must be reflected in the plan and `.planning/STATE.md`.
- Workspace isolation: run any commands or experiments that mutate state in a fresh jj workspace; do not use the main workspace and do not reuse workspaces for reruns.
- Repeat-run detection markers: include `[ALPHASWARM-START] RUN_ID TIMESTAMP` and `[ALPHASWARM-END] RUN_ID EXIT_CODE DURATION_MS` markers in transcripts or logs so reruns are detectable.
- claude-code-controller subject: for interactive runs, use `claude-code-controller` and launch Claude Code as the subject in the claude-code-agent-teams session; transcripts must show the Claude Code session.
- Seed scope boundaries: treat B-seeds (baseline or regression inputs) and N-seeds (net-new exploratory inputs) as separate scopes; do not mix them in a single run and track results separately.
</execution_guardrails>

<tasks>

<task type="auto">
  <name>Task 1: Deterministic delta schema + merge ordering</name>
  <files>src/alphaswarm_sol/orchestration/schemas.py, src/alphaswarm_sol/orchestration/dedup.py</files>
  <action>
Define append-only delta schema and deterministic merge ordering:
- Immutable delta entries with evidence IDs
- Deterministic ordering keys
- Idempotent merge function
- Stable hash for merged outputs
  </action>
  <verify>
```bash
rg -n "Delta|deterministic|idempotent" src/alphaswarm_sol/orchestration/schemas.py src/alphaswarm_sol/orchestration/dedup.py
```
  </verify>
  <done>
Delta schema and deterministic merges are implemented.
  </done>
</task>

<task type="auto">
  <name>Task 2: Conflict quarantine + tests</name>
  <files>src/alphaswarm_sol/orchestration/handlers.py, tests/orchestration/test_merge_pipeline.py</files>
  <action>
Add conflict quarantine:
- Detect conflicting deltas (same evidence IDs, different claims)
- Route to resolver agent
- Preserve both deltas for audit

Add tests:
- Idempotent merges across replay
- Conflicts quarantined
- Deterministic ordering for identical deltas
  </action>
  <verify>
```bash
rg -n "conflict|quarantine" src/alphaswarm_sol/orchestration/handlers.py
```
  </verify>
  <done>
Merge pipeline v2 is covered by tests.
  </done>
</task>

</tasks>
