---
phase: 05.10-pattern-context-batch-discovery-orchestration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/alphaswarm_sol/vulndocs/schema.py
  - src/alphaswarm_sol/vulndocs/validation.py
  - docs/reference/pattern-context-pack-v2.md
  - schemas/pattern_context_pack_v2.json
autonomous: true

must_haves:
  truths:
    - "PCP v2 encodes anti-signals, witnesses, evidence weights, guard taxonomy, ordering variants, risk envelope, and economic placeholders"
    - "PCP validation enforces determinism, no RAG, and explicit unknowns"
    - "PCP v1 data is accepted via a migration shim with explicit defaults"
  artifacts:
    - path: "docs/reference/pattern-context-pack-v2.md"
      provides: "PCP v2 schema spec + template"
      exports: ["PCP v2 spec"]
    - path: "schemas/pattern_context_pack_v2.json"
      provides: "JSON Schema for PCP v2"
      exports: ["PCP v2 JSON Schema"]
  key_links:
    - from: "src/alphaswarm_sol/vulndocs/schema.py"
      to: "schemas/pattern_context_pack_v2.json"
      via: "PCP v2 JSON schema export"
    - from: "docs/reference/pattern-context-pack-v2.md"
      to: "src/alphaswarm_sol/vulndocs/schema.py"
      via: "PCP v2 spec alignment"
---

<objective>
Define PCP v2 as a first-class schema (with validator and migration shim) so pattern context is deterministic, evidence-first, and cacheable. This is the foundation for batch discovery, evidence gating, and creative Tier-B exploration.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/PHILOSOPHY.md
@src/alphaswarm_sol/vulndocs/schema.py
@src/alphaswarm_sol/vulndocs/validation.py
@docs/reference/pattern-context-pack-v2.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement PCP v2 schema + migration shim + JSON schema export</name>
  <files>src/alphaswarm_sol/vulndocs/schema.py, src/alphaswarm_sol/vulndocs/validation.py, schemas/pattern_context_pack_v2.json</files>
  <action>
Define a PCP v2 model (pydantic or dataclass) with strict defaults and validation:
- Anti-signals, witnesses (minimal + negative), evidence weights
- Guard taxonomy + bypass notes
- Ordering variants and required op signatures
- Risk envelope (asset type, trust boundary, value flow)
- Economic placeholders (value flows, incentive hooks, roles, offchain flags, risk pre-score)
- Determinism constraints (no RAG, no name heuristics)
- Unknowns policy (missing signals -> unknown)

Implement a migration shim in validation to map PCP v1 into v2 with explicit defaults.
Export PCP v2 JSON schema and keep it in sync with the model.
  </action>
  <verify>
```bash
rg -n "PatternContextPackV2|PCP v2" src/alphaswarm_sol/vulndocs/schema.py
```
  </verify>
  <done>
PCP v2 schema, validation shim, and JSON schema export are implemented.
  </done>
</task>

<task type="auto">
  <name>Task 2: Publish PCP v2 reference spec</name>
  <files>docs/reference/pattern-context-pack-v2.md</files>
  <action>
Create the normative PCP v2 reference with:
- Canonical YAML template
- Defaults and validation rules
- Field-by-field short reference
- Explicit determinism and unknowns policy
  </action>
  <verify>
```bash
rg -n "PCP v2|Pattern Context Pack" docs/reference/pattern-context-pack-v2.md
```
  </verify>
  <done>
PCP v2 reference spec exists and aligns with the schema.
  </done>
</task>

</tasks>
