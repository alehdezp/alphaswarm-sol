---
phase: 05.10-pattern-context-batch-discovery-orchestration
plan: 06
type: execute
wave: 4
depends_on:
  - 05.10-05
files_modified:
  - src/alphaswarm_sol/agents/context/extractor.py
  - src/alphaswarm_sol/agents/context/types.py
  - src/alphaswarm_sol/agents/context/merger.py
  - tests/agents/test_context_budget.py
  - tests/agents/test_context_delta.py
autonomous: true

must_haves:
  truths:
    - "Budget governor enforces cheap/verify/deep passes"
    - "PCP fields are never trimmed; only slices are reduced"
    - "Verify pass sends delta from scout context"
    - "Context gating rules define when protocol context is included or omitted"
  artifacts:
    - path: "src/alphaswarm_sol/agents/context/types.py"
      provides: "BudgetPolicy + ContextDelta types"
      exports: ["BudgetPolicy", "ContextDelta"]
  key_links:
    - from: "src/alphaswarm_sol/agents/context/extractor.py"
      to: "src/alphaswarm_sol/agents/context/merger.py"
      via: "Context delta packing"
---

<objective>
Enforce token budgets and provide delta-based context packing so verification is cheaper, deterministic, and never drops PCP fields. Add explicit protocol-context gating.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@src/alphaswarm_sol/agents/context/extractor.py
@src/alphaswarm_sol/agents/context/merger.py
</context>

<execution_guardrails>
- Full doc alignment: align objective, must_haves, and tasks against `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, plus phase context or research docs before execution. Any scope change must be reflected in the plan and `.planning/STATE.md`.
- Workspace isolation: run any commands or experiments that mutate state in a fresh jj workspace; do not use the main workspace and do not reuse workspaces for reruns.
- Repeat-run detection markers: include `[ALPHASWARM-START] RUN_ID TIMESTAMP` and `[ALPHASWARM-END] RUN_ID EXIT_CODE DURATION_MS` markers in transcripts or logs so reruns are detectable.
- claude-code-controller subject: for interactive runs, use `claude-code-controller` and launch Claude Code as the subject in the claude-code-agent-teams session; transcripts must show the Claude Code session.
- Seed scope boundaries: treat B-seeds (baseline or regression inputs) and N-seeds (net-new exploratory inputs) as separate scopes; do not mix them in a single run and track results separately.
</execution_guardrails>

<tasks>

<task type="auto">
  <name>Task 1: Implement budget policy + context gating</name>
  <files>src/alphaswarm_sol/agents/context/types.py, src/alphaswarm_sol/agents/context/extractor.py</files>
  <action>
Add BudgetPolicy with:
- cheap_pass_tokens, verify_pass_tokens, deep_pass_tokens
- hard/soft limits
- escalation rules (unknown -> expand -> verify)

Implement context gating:
- Include protocol context only when available and relevant
- If omitted, annotate exclusion and mark unknowns
- Record context_included flags in pack metadata
  </action>
  <verify>
```bash
rg -n "BudgetPolicy|context_included|protocol context" src/alphaswarm_sol/agents/context/extractor.py
```
  </verify>
  <done>
Budget policy and context gating are enforced and auditable.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement context delta packing + tests</name>
  <files>src/alphaswarm_sol/agents/context/merger.py, tests/agents/test_context_budget.py, tests/agents/test_context_delta.py</files>
  <action>
Build ContextDelta:
- Compute diff between scout slice and verifier slice
- Include only added nodes/edges and required evidence IDs
- Deterministic ordering by evidence ID

Add tests:
- Budget cap never trims PCP fields
- Context gating marks exclusions and unknowns
- Delta includes only new evidence
  </action>
  <verify>
```bash
rg -n "ContextDelta|BudgetPolicy" tests/agents
```
  </verify>
  <done>
Budget, gating, and delta packing are covered by tests.
  </done>
</task>

</tasks>
