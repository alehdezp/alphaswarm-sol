---
phase: 05.10-pattern-context-batch-discovery-orchestration
plan: 12
type: execute
wave: 5
depends_on:
  - 05.10-06
files_modified:
  - src/alphaswarm_sol/skills/shipped/pattern-batch.md
  - src/alphaswarm_sol/skills/shipped/pattern-verify.md
  - src/alphaswarm_sol/skills/shipped/context-pack.md
  - docs/guides/skills-basics.md
  - docs/reference/workflows.md
autonomous: true

must_haves:
  truths:
    - "Skill docs specify when to run, with/without protocol context, and how to handle unknowns"
    - "Orchestration trigger matrix is documented for batch workflows"
  artifacts:
    - path: "docs/guides/skills-basics.md"
      provides: "Skill trigger matrix"
      exports: ["when-to-run rules"]
  key_links:
    - from: "docs/reference/workflows.md"
      to: "docs/guides/skills-basics.md"
      via: "orchestration trigger matrix"
---

<objective>
Ensure skills explicitly document when to run, how to handle context, and how to escalate unknowns for batch orchestration.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@src/alphaswarm_sol/skills/shipped/pattern-batch.md
@src/alphaswarm_sol/skills/shipped/context-pack.md
</context>

<execution_guardrails>
- Full doc alignment: align objective, must_haves, and tasks against `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, plus phase context or research docs before execution. Any scope change must be reflected in the plan and `.planning/STATE.md`.
- Workspace isolation: run any commands or experiments that mutate state in a fresh jj workspace; do not use the main workspace and do not reuse workspaces for reruns.
- Repeat-run detection markers: include `[ALPHASWARM-START] RUN_ID TIMESTAMP` and `[ALPHASWARM-END] RUN_ID EXIT_CODE DURATION_MS` markers in transcripts or logs so reruns are detectable.
- claude-code-controller subject: for interactive runs, use `claude-code-controller` and launch Claude Code as the subject in the claude-code-agent-teams session; transcripts must show the Claude Code session.
- Seed scope boundaries: treat B-seeds (baseline or regression inputs) and N-seeds (net-new exploratory inputs) as separate scopes; do not mix them in a single run and track results separately.
</execution_guardrails>

<tasks>

<task type="auto">
  <name>Task 1: Update skill invocation rules</name>
  <files>src/alphaswarm_sol/skills/shipped/pattern-batch.md, src/alphaswarm_sol/skills/shipped/pattern-verify.md, src/alphaswarm_sol/skills/shipped/context-pack.md</files>
  <action>
Update skill docs to include:
- When to run (trigger conditions) and when to defer
- Whether protocol context is required or optional
- Context gating rules (cached PCP present, graph hash match, budget constraints)
- Unknown-handling and escalation (unknown -> expand -> verify)
  </action>
  <verify>
```bash
rg -n "when to run|context|unknown|escalation" src/alphaswarm_sol/skills/shipped/pattern-batch.md
```
  </verify>
  <done>
Skill docs document when to run with/without context and how to handle unknowns.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add orchestration trigger matrix to docs</name>
  <files>docs/guides/skills-basics.md, docs/reference/workflows.md</files>
  <action>
Document a trigger matrix that defines:
- When to run context-pack vs reuse cached PCP
- When to include protocol context vs run without it
- Escalation rules (unknown -> expand -> verify)
  </action>
  <verify>
```bash
rg -n "context-pack|pattern-batch|when to run|protocol context" docs/guides/skills-basics.md
```
  </verify>
  <done>
Trigger matrix is documented for batch workflows.
  </done>
</task>

</tasks>
