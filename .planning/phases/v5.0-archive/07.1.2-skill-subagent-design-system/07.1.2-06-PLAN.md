---
phase: 07.1.2-skill-subagent-design-system
plan: 06
type: execute
wave: 3
depends_on:
  - "07.1.2-04"
  - "07.1.2-05"
files_modified:
  - tests/skills/test_skill_goldens.py
  - tests/skills/test_skill_prompts.py
  - tests/skills/goldens/README.md
  - tests/skills/goldens/secure_reviewer.json
  - tests/skills/goldens/attacker.json
  - tests/skills/goldens/defender.json
  - tests/skills/goldens/verifier.json
  - scripts/update_skill_goldens.py
  - .planning/TOOLING.md
autonomous: true

must_haves:
  truths:
    - "Skill prompts are validated against schema v2 and required sections."
    - "Golden outputs conform to the declared output contracts."
    - "Tests can run in isolation without external tool calls."
  artifacts:
    - path: "tests/skills/goldens/secure_reviewer.json"
      provides: "Golden output for secure reviewer"
      contains: "evidence"
    - path: "tests/skills/test_skill_prompts.py"
      provides: "Prompt structure tests"
      contains: "graph-first"
    - path: "tests/skills/test_skill_goldens.py"
      provides: "Golden output contract tests"
      contains: "validate_json_schema"
  key_links:
    - from: "tests/skills/test_skill_goldens.py"
      to: "schemas/secure_reviewer_output.json"
      via: "golden validation"
      pattern: "secure_reviewer_output"
---

<objective>
Add skill unit tests and golden fixtures to validate prompt structure and output contracts.

Purpose: Make skills testable and safe to evolve while preserving evidence-first behavior.
Output: Golden fixtures, prompt tests, and an update script.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/TOOLING.md
@schemas/skill_schema_v2.json
@schemas/secure_reviewer_output.json
@src/alphaswarm_sol/skills/shipped/agents/
@.claude/skills/vrs/
</context>

<skills_subagents>
- skill-architect: Define golden output fixtures and required prompt sections.
- skill-auditor: Review tests for coverage gaps and false positives.
- cost-governor: Ensure tests stay fast and avoid expensive model calls.
</skills_subagents>

<tasks>

<task type="auto">
  <name>Task 1: Create golden output fixtures and README</name>
  <files>
    tests/skills/goldens/README.md
    tests/skills/goldens/secure_reviewer.json
    tests/skills/goldens/attacker.json
    tests/skills/goldens/defender.json
    tests/skills/goldens/verifier.json
  </files>
  <action>
Add deterministic golden outputs that follow each output contract:
- Secure reviewer output per schemas/secure_reviewer_output.json
- Attacker/defender/verifier outputs per their declared JSON schemas
- README documents how to update goldens and what they represent
  </action>
  <verify>
    python - <<'PY'
import json
from pathlib import Path
json.loads(Path('tests/skills/goldens/attacker.json').read_text())
PY
  </verify>
  <done>
    Golden fixtures exist and are valid JSON.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement prompt and golden validation tests</name>
  <files>
    tests/skills/test_skill_prompts.py
    tests/skills/test_skill_goldens.py
  </files>
  <action>
Add tests that:
- Validate skill frontmatter against schema v2
- Require graph-first sections in attacker/defender/verifier/reviewer prompts
- Validate golden outputs against their JSON schemas
- Avoid external tool calls or network use
  </action>
  <verify>
    uv run pytest tests/skills/test_skill_prompts.py -v
  </verify>
  <done>
    Tests enforce prompt structure and output contracts.
  </done>
</task>

<task type="auto">
  <name>Task 3: Tooling update for golden maintenance</name>
  <files>
    scripts/update_skill_goldens.py
    .planning/TOOLING.md
  </files>
  <action>
Add a helper script to refresh goldens and update TOOLING.md:
- Script regenerates goldens from stored templates (no model calls)
- TOOLING.md lists the golden update command and test commands
  </action>
  <verify>
    python scripts/update_skill_goldens.py --help
  </verify>
  <done>
    Tooling docs include golden maintenance steps.
  </done>
</task>

</tasks>

<verification>
- Prompt tests enforce graph-first requirements
- Golden outputs validate against declared schemas
- TOOLING.md includes golden update commands
</verification>

<success_criteria>
- Skill prompts and outputs are testable via unit tests
- Goldens document expected behavior and are easy to refresh
- Schema v2 compliance is enforced by tests
</success_criteria>

<output>
After completion, create `.planning/phases/07.1.2-skill-subagent-design-system/07.1.2-06-SUMMARY.md`
</output>
