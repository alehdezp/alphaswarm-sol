---
phase: 07.1.2-skill-subagent-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - schemas/skill_schema_v2.json
  - docs/reference/skill-schema-v2.md
  - docs/guides/skills-basics.md
  - src/alphaswarm_sol/skills/skill_schema.py
  - scripts/validate_skill_schema.py
  - tests/skills/test_skill_schema_v2.py
autonomous: true

must_haves:
  truths:
    - "Skill schema v2 defines role, evidence requirements, tools, output contracts, and failure modes."
    - "A validator can load skill frontmatter and fail on schema violations."
    - "Shipped skills and repo skills can be validated against the same schema."
  artifacts:
    - path: "schemas/skill_schema_v2.json"
      provides: "Canonical JSON schema for skill frontmatter v2"
      contains: "evidence_requirements"
    - path: "scripts/validate_skill_schema.py"
      provides: "Frontmatter validator for skill schema v2"
      contains: "validate_skill_frontmatter"
    - path: "docs/reference/skill-schema-v2.md"
      provides: "Human-readable skill schema v2 spec"
      contains: "Output Contract"
  key_links:
    - from: "schemas/skill_schema_v2.json"
      to: "scripts/validate_skill_schema.py"
      via: "validator uses schema v2"
      pattern: "skill_schema_v2.json"
---

<objective>
Define Skill Schema v2 for Claude Code and Codex skills with evidence-first contracts and enforceable validation.

Purpose: Standardize skill frontmatter with role, tools, evidence, and output contract fields so prompts are consistent and testable.
Output: JSON schema, docs, validator script, and baseline tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/TOOLING.md
@.planning/phases/07.1.2-skill-subagent-design-system/07.1.2-CONTEXT.md
@.planning/phases/07.1.2-skill-subagent-design-system/07.1.2-RESEARCH.md
@docs/guides/skills-basics.md
@src/alphaswarm_sol/skills/shipped/README.md
@.claude/skills/vrs/README.md
</context>

<skills_subagents>
- skill-architect: Draft schema v2 fields and output contract requirements.
- skill-auditor: Review schema for missing guardrails or ambiguous fields.
- cost-governor: Check token budget impact for new required fields.
</skills_subagents>

<tasks>

<task type="auto">
  <name>Task 1: Define skill schema v2 and reference docs</name>
  <files>
    schemas/skill_schema_v2.json
    docs/reference/skill-schema-v2.md
  </files>
  <action>
Create a canonical JSON schema for skill frontmatter v2 and a matching doc:
- Required fields: name, description, slash_command, context, role, model_tier, tools, evidence_requirements, output_contract, failure_modes, version
- Add optional fields for deprecations (deprecated: bool, replaces, deprecates, sunset_date)
- Add role constraints (attacker|defender|verifier|auditor|researcher|triage|orchestrator)
- Define evidence_requirements (must_link_code, min_evidence_items, cite_graph_nodes)
- Define output_contract with schema reference (path to JSON schema or inline JSON schema fragment)
- Include max_token_budget default of 6000 and explain rationale in docs
  </action>
  <verify>
    python - <<'PY'
import json
from pathlib import Path
schema = json.loads(Path('schemas/skill_schema_v2.json').read_text())
assert schema.get('title')
PY
  </verify>
  <done>
    Skill schema v2 is defined and documented with required evidence and output contract fields.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement schema validator for skill frontmatter</name>
  <files>
    src/alphaswarm_sol/skills/skill_schema.py
    scripts/validate_skill_schema.py
  </files>
  <action>
Add a lightweight validator that:
- Loads YAML frontmatter from .claude/skills and src/alphaswarm_sol/skills/shipped
- Validates against schemas/skill_schema_v2.json
- Emits clear error messages with file path + missing fields
- Supports --strict and --warn modes for CI vs local use
- Avoids touching builder_legacy.py or executor.py
  </action>
  <verify>
    python scripts/validate_skill_schema.py --help
  </verify>
  <done>
    Validator can load skill files and report schema violations.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add tests and tooling update for schema v2</name>
  <files>
    tests/skills/test_skill_schema_v2.py
    docs/guides/skills-basics.md
    .planning/TOOLING.md
  </files>
  <action>
Add tests to enforce schema v2 and update tooling references:
- Add tests to validate shipped skills and .claude skills against v2 schema
- Update docs/guides/skills-basics.md with schema v2 section and migration notes
- Tooling update: add validator command to .planning/TOOLING.md (e.g., python scripts/validate_skill_schema.py)
  </action>
  <verify>
    uv run pytest tests/skills/test_skill_schema_v2.py -v
  </verify>
  <done>
    Tests pass and TOOLING.md references the schema validator.
  </done>
</task>

</tasks>

<verification>
- Schema v2 covers role, evidence, tools, output contract, and failure modes
- Validator flags missing or invalid fields across skill files
- TOOLING.md updated with schema validation step
</verification>

<success_criteria>
- Skill schema v2 is published in schemas/ and documented
- Validation script works on both shipped and repo skills
- Tests enforce schema v2 compliance
</success_criteria>

<output>
After completion, create `.planning/phases/07.1.2-skill-subagent-design-system/07.1.2-01-SUMMARY.md`
</output>
