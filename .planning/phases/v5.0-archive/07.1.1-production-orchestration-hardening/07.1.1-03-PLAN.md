---
phase: 07.1.1-production-orchestration-hardening
plan: 03
type: execute
wave: 2
depends_on:
  - "07.1.1-01"
  - "07.1.1-02"
files_modified:
  - src/alphaswarm_sol/orchestration/queue.py
  - src/alphaswarm_sol/orchestration/loop.py
  - src/alphaswarm_sol/orchestration/router.py
  - tests/orchestration/test_work_queue.py
autonomous: true

must_haves:
  truths:
    - "Orchestration enforces max concurrency and never exceeds configured limits."
    - "Backpressure pauses new work when queues are full."
    - "Queue state is persisted and recoverable across restarts."
  artifacts:
    - path: "src/alphaswarm_sol/orchestration/queue.py"
      provides: "Work queue + concurrency limiter"
      contains: "class WorkQueue"
    - path: "src/alphaswarm_sol/orchestration/loop.py"
      provides: "Queue-aware execution loop"
      contains: "enqueue"
    - path: "tests/orchestration/test_work_queue.py"
      provides: "Queue/backpressure tests"
      contains: "backpressure"
  key_links:
    - from: "src/alphaswarm_sol/orchestration/loop.py"
      to: "src/alphaswarm_sol/orchestration/queue.py"
      via: "ExecutionLoop uses WorkQueue for scheduling"
      pattern: "WorkQueue"
---

<objective>
Introduce a durable work queue with concurrency limits and backpressure for orchestration.

Purpose: Prevent runaway parallelism and enforce predictable resource usage under load.
Output: Queue module, loop/router integration, and tests covering backpressure behavior.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1.1-production-orchestration-hardening/07.1.1-CONTEXT.md
@.planning/phases/07.1.1-production-orchestration-hardening/07.1.1-RESEARCH.md
@src/alphaswarm_sol/orchestration/loop.py
@src/alphaswarm_sol/orchestration/router.py
@src/alphaswarm_sol/orchestration/pool.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build persistent work queue + limiter</name>
  <files>src/alphaswarm_sol/orchestration/queue.py</files>
  <action>
Create a WorkQueue module that supports queueing, leasing, and persistence:
- Define WorkItem (pool_id, bead_id, action, payload_hash, enqueued_at)
- Persist queue state to .vrs/pools/{pool_id}/queue.jsonl (append-only)
- API: enqueue(), reserve(max_in_flight), ack(), nack(), snapshot()
- Enforce limits: max_in_flight, max_queue_depth, per_action limits
- Deterministic ordering (FIFO with stable tie-breakers)
  </action>
  <verify>
    python - <<'PY'
from pathlib import Path
from alphaswarm_sol.orchestration.queue import WorkQueue
queue = WorkQueue(Path('.vrs/pools/test-pool'))
assert queue.snapshot()['queued'] == 0
PY
  </verify>
  <done>
    WorkQueue persists state and enforces max in-flight/queue depth limits.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate queue/backpressure into loop and router</name>
  <files>
    src/alphaswarm_sol/orchestration/loop.py
    src/alphaswarm_sol/orchestration/router.py
  </files>
  <action>
Wire WorkQueue into the execution loop:
- Update ExecutionLoop to enqueue route decisions instead of executing immediately
- Reserve work items based on concurrency limits before invoking handlers
- When queue is full, set pool status to PAUSED with metadata (reason=backpressure)
- Router should produce deterministic WorkItem payload hashes for queueing
- Keep handler interfaces unchanged; queue handles scheduling only
  </action>
  <verify>
    uv run pytest tests/orchestration/test_work_queue.py -k backpressure -v
  </verify>
  <done>
    ExecutionLoop respects queue limits and pauses pools under backpressure.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add work queue and backpressure tests</name>
  <files>tests/orchestration/test_work_queue.py</files>
  <action>
Add tests to validate:
- FIFO ordering and deterministic tie-breakers
- max_in_flight enforcement across reserve() calls
- queue overflow triggers backpressure (pool paused or enqueue rejected)
- queue persistence survives reload
  </action>
  <verify>
    uv run pytest tests/orchestration/test_work_queue.py -v
  </verify>
  <done>
    Queue and backpressure behavior is fully covered by tests.
  </done>
</task>

</tasks>

<verification>
- Queue state persists across process restarts
- Concurrency limits are enforced at execution time
- Backpressure reason recorded in pool metadata
</verification>

<success_criteria>
- WorkQueue controls concurrency and queue depth
- ExecutionLoop never exceeds configured in-flight limits
- Backpressure prevents runaway scheduling under load
</success_criteria>

<output>
After completion, create `.planning/phases/07.1.1-production-orchestration-hardening/07.1.1-03-SUMMARY.md`
</output>
