---
phase: 07.1.1-production-orchestration-hardening
plan: 05
type: execute
wave: 3
depends_on:
  - "07.1.1-03"
  - "07.1.1-04"
files_modified:
  - src/alphaswarm_sol/orchestration/worktree.py
  - src/alphaswarm_sol/agents/propulsion/engine.py
  - src/alphaswarm_sol/agents/runtime/base.py
  - src/alphaswarm_sol/agents/runtime/codex_cli.py
  - tests/agents/test_worktree_isolation.py
autonomous: true

must_haves:
  truths:
    - "Parallel agents run in isolated git worktrees with no shared writes."
    - "Worktrees are created, tracked, and cleaned up deterministically."
    - "Agent execution uses the assigned worktree as its working directory."
  artifacts:
    - path: "src/alphaswarm_sol/orchestration/worktree.py"
      provides: "Worktree manager for agent isolation"
      contains: "class WorktreeManager"
    - path: "src/alphaswarm_sol/agents/runtime/base.py"
      provides: "AgentConfig supports workdir"
      contains: "workdir"
    - path: "tests/agents/test_worktree_isolation.py"
      provides: "Worktree isolation tests"
      contains: "worktree"
  key_links:
    - from: "src/alphaswarm_sol/agents/propulsion/engine.py"
      to: "src/alphaswarm_sol/orchestration/worktree.py"
      via: "Agent spawn allocates per-agent worktree"
      pattern: "WorktreeManager"
---

<objective>
Add worktree isolation and merge discipline so parallel agents operate safely without file conflicts.

Purpose: Ensure agent runs are isolated, reproducible, and mergeable when running in parallel.
Output: Worktree manager, agent spawn integration, runtime support, and tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1.1-production-orchestration-hardening/07.1.1-CONTEXT.md
@.planning/phases/07.1.1-production-orchestration-hardening/07.1.1-RESEARCH.md
@src/alphaswarm_sol/agents/propulsion/engine.py
@src/alphaswarm_sol/agents/runtime/base.py
@src/alphaswarm_sol/agents/runtime/codex_cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement WorktreeManager for isolated agent runs</name>
  <files>src/alphaswarm_sol/orchestration/worktree.py</files>
  <action>
Create WorktreeManager that manages git worktrees per pool/agent:
- Allocate worktree paths under .vrs/worktrees/{pool_id}/{agent_id}
- Use `git worktree add` with a detached HEAD or a temp branch
- Track worktree metadata (pool_id, agent_id, base_ref, created_at) in JSON
- Provide release() and cleanup() helpers to prune worktrees
- Fail fast if git is unavailable or repo is dirty in conflicting ways
  </action>
  <verify>
    python - <<'PY'
from alphaswarm_sol.orchestration.worktree import WorktreeManager
mgr = WorktreeManager()
assert hasattr(mgr, 'allocate')
PY
  </verify>
  <done>
    WorktreeManager can allocate/release worktrees with tracked metadata.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire worktree allocation into agent spawning</name>
  <files>
    src/alphaswarm_sol/agents/propulsion/engine.py
    src/alphaswarm_sol/agents/runtime/base.py
  </files>
  <action>
Integrate worktrees with agent execution:
- Extend AgentConfig to include workdir (optional)
- In propulsion engine, allocate a worktree per spawned agent when parallelism is enabled
- Pass workdir into AgentConfig so runtimes execute in isolated dirs
- Record worktree path in agent metadata for traceability
  </action>
  <verify>
    uv run pytest tests/agents/test_worktree_isolation.py -k config -v
  </verify>
  <done>
    Agent spawn uses per-agent worktrees and passes workdir to runtimes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Ensure Codex CLI runtime honors workdir + add tests</name>
  <files>
    src/alphaswarm_sol/agents/runtime/codex_cli.py
    tests/agents/test_worktree_isolation.py
  </files>
  <action>
Update codex_cli runtime to respect AgentConfig.workdir (pass cwd to subprocess) and add tests:
- Verify each spawned agent gets a unique worktree path
- Ensure worktree cleanup is invoked after completion
- Tests should not require network or real git pushes
  </action>
  <verify>
    uv run pytest tests/agents/test_worktree_isolation.py -v
  </verify>
  <done>
    Runtime honors workdir and worktree isolation tests pass.
  </done>
</task>

</tasks>

<verification>
- Worktrees are created per agent and cleaned up reliably
- Agent runtime executes within assigned workdir
- Parallel agents do not share writable paths
</verification>

<success_criteria>
- WorktreeManager available and integrated with agent spawning
- AgentConfig supports workdir propagation
- Worktree isolation tests pass without external dependencies
</success_criteria>

<output>
After completion, create `.planning/phases/07.1.1-production-orchestration-hardening/07.1.1-05-SUMMARY.md`
</output>
