---
phase: 07.1.1-production-orchestration-hardening
plan: 04
type: execute
wave: 2
depends_on:
  - "07.1.1-01"
files_modified:
  - src/alphaswarm_sol/orchestration/replay.py
  - src/alphaswarm_sol/cli/orchestrate.py
  - tests/e2e/test_determinism.py
autonomous: true

must_haves:
  truths:
    - "A pool can be replayed deterministically from event logs."
    - "Replay output matches the original run for verdicts and evidence packets."
    - "Replay is invocable from the CLI for audits."
  artifacts:
    - path: "src/alphaswarm_sol/orchestration/replay.py"
      provides: "Deterministic replay engine"
      contains: "class ReplayEngine"
    - path: "src/alphaswarm_sol/cli/orchestrate.py"
      provides: "CLI replay command"
      contains: "replay"
    - path: "tests/e2e/test_determinism.py"
      provides: "Replay determinism tests"
      contains: "ReplayEngine"
  key_links:
    - from: "src/alphaswarm_sol/orchestration/replay.py"
      to: ".vrs/pools/{pool_id}/events/beads.jsonl"
      via: "Replay uses event log as source of truth"
      pattern: "events"
---

<objective>
Add deterministic replay for orchestration runs using the event log as the source of truth.

Purpose: Enable reproducible audits and regression testing from recorded event history.
Output: Replay engine, CLI command, and determinism tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1.1-production-orchestration-hardening/07.1.1-CONTEXT.md
@.planning/phases/07.1.1-production-orchestration-hardening/07.1.1-RESEARCH.md
@src/alphaswarm_sol/orchestration/loop.py
@src/alphaswarm_sol/orchestration/pool.py
@src/alphaswarm_sol/cli/orchestrate.py
@tests/e2e/test_determinism.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement ReplayEngine for deterministic reconstruction</name>
  <files>src/alphaswarm_sol/orchestration/replay.py</files>
  <action>
Create ReplayEngine that can reconstruct a pool deterministically from event logs:
- Load event logs (beads + pool status events) from .vrs/pools/{pool_id}/events/
- Provide replay(pool_id, strict=True, seed=0) returning reconstructed Pool + artifacts
- Enforce deterministic ordering: sort events by timestamp, event_id
- Optionally validate current pool snapshot vs replayed state (strict mode)
- Use explicit random seed and stable hashing for any nondeterministic steps
  </action>
  <verify>
    python - <<'PY'
from alphaswarm_sol.orchestration.replay import ReplayEngine
engine = ReplayEngine()
assert hasattr(engine, 'replay')
PY
  </verify>
  <done>
    ReplayEngine can reconstruct a pool deterministically from event logs.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add CLI replay command</name>
  <files>src/alphaswarm_sol/cli/orchestrate.py</files>
  <action>
Add an orchestrate replay command:
- `uv run alphaswarm orchestrate replay <pool_id>`
- Options: --strict, --seed, --diff (print diff summary vs current pool)
- Output: summary of replayed status, bead count, verdict count, mismatch count
- No changes to executor.py
  </action>
  <verify>
    uv run alphaswarm orchestrate --help | rg -n "replay"
  </verify>
  <done>
    CLI exposes a replay command with strict and diff options.
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend determinism tests to cover replay</name>
  <files>tests/e2e/test_determinism.py</files>
  <action>
Add tests that:
- Run a small orchestration flow and capture event log
- Replay and compare verdicts/evidence vs original
- Fail when events are missing or out of order (strict mode)
Reuse existing fixtures; avoid network/model calls.
  </action>
  <verify>
    uv run pytest tests/e2e/test_determinism.py -k replay -v
  </verify>
  <done>
    Determinism tests validate replay correctness and strict-mode diffs.
  </done>
</task>

</tasks>

<verification>
- Replay output matches original run for verdicts, evidence packets, and bead states
- Strict mode flags divergence with clear diff summary
- CLI command works without modifying executor.py
</verification>

<success_criteria>
- ReplayEngine reconstructs pool state deterministically
- CLI replay command available and documented in help output
- Determinism tests pass for replay paths
</success_criteria>

<output>
After completion, create `.planning/phases/07.1.1-production-orchestration-hardening/07.1.1-04-SUMMARY.md`
</output>
