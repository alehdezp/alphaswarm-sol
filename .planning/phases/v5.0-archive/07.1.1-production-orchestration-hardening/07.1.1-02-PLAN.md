---
phase: 07.1.1-production-orchestration-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/alphaswarm_sol/orchestration/idempotency.py
  - src/alphaswarm_sol/tools/runner.py
  - src/alphaswarm_sol/agents/runtime/base.py
  - src/alphaswarm_sol/orchestration/handlers.py
  - tests/orchestration/test_idempotency.py
autonomous: true

must_haves:
  truths:
    - "Retrying tool or agent actions does not duplicate side effects."
    - "Idempotency keys are persisted and reused across restarts."
    - "Retries obey bounded exponential backoff with jitter."
  artifacts:
    - path: "src/alphaswarm_sol/orchestration/idempotency.py"
      provides: "Idempotency key generation + storage"
      contains: "class IdempotencyStore"
    - path: "src/alphaswarm_sol/tools/runner.py"
      provides: "Retry/backoff with idempotent execution"
      contains: "idempotency_key"
    - path: "tests/orchestration/test_idempotency.py"
      provides: "Idempotency and retry tests"
      contains: "retry"
  key_links:
    - from: "src/alphaswarm_sol/orchestration/handlers.py"
      to: "src/alphaswarm_sol/orchestration/idempotency.py"
      via: "Handlers derive deterministic keys per pool/bead/action"
      pattern: "idempotency_key"
---

<objective>
Add idempotency keys and bounded retry/backoff to all tool and agent execution paths.

Purpose: Prevent duplicated side effects under retries while keeping orchestration resilient to transient failures.
Output: Idempotency store, retry/backoff wiring, and tests validating no duplicate execution.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1.1-production-orchestration-hardening/07.1.1-CONTEXT.md
@.planning/phases/07.1.1-production-orchestration-hardening/07.1.1-RESEARCH.md
@src/alphaswarm_sol/tools/runner.py
@src/alphaswarm_sol/agents/runtime/base.py
@src/alphaswarm_sol/orchestration/handlers.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement idempotency key store and helpers</name>
  <files>src/alphaswarm_sol/orchestration/idempotency.py</files>
  <action>
Create IdempotencyStore with deterministic key generation and on-disk persistence:
- Key format: hash(pool_id + bead_id + action + payload_hash)
- Storage: JSONL append-only under .vrs/pools/{pool_id}/idempotency.jsonl
- API: get(key), record_success(key, result), record_failure(key, error), reserve(key)
- Include idempotency metadata (timestamp, attempts, last_error)
- Keep implementation standalone; no changes to executor.py or builder_legacy.py
  </action>
  <verify>
    python - <<'PY'
from pathlib import Path
from alphaswarm_sol.orchestration.idempotency import IdempotencyStore
store = IdempotencyStore(Path('.vrs/pools/test-pool'))
key = store.make_key(pool_id='test-pool', bead_id='b1', action='tool', payload_hash='abc')
assert key
PY
  </verify>
  <done>
    IdempotencyStore can generate keys and persist results in JSONL.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire idempotent execution with retry/backoff</name>
  <files>
    src/alphaswarm_sol/tools/runner.py
    src/alphaswarm_sol/agents/runtime/base.py
    src/alphaswarm_sol/orchestration/handlers.py
  </files>
  <action>
Add idempotent execution support to tool and agent calls:
- Extend tool runner to accept idempotency_key + retry config (max_retries, base_delay, max_delay, jitter)
- Extend AgentConfig or spawn API to accept idempotency_key and propagate to runtimes
- In orchestration handlers, derive deterministic idempotency keys from pool/bead/action + payload hash
- On success, store result in IdempotencyStore; on retry, short-circuit if cached
- Use bounded exponential backoff with jitter and hard max attempts
  </action>
  <verify>
    uv run pytest tests/orchestration/test_idempotency.py -k idempotency -v
  </verify>
  <done>
    Tool/agent execution retries are idempotent, cached, and bounded by backoff policy.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add idempotency and retry tests</name>
  <files>tests/orchestration/test_idempotency.py</files>
  <action>
Create tests that validate:
- Same idempotency key returns cached result without re-executing tool/agent stub
- Retry attempts stop at max_retries and record failures
- Backoff delay values stay within configured bounds
Use stub runners to avoid real tool/agent calls.
  </action>
  <verify>
    uv run pytest tests/orchestration/test_idempotency.py -v
  </verify>
  <done>
    Tests prove idempotent caching and bounded retry/backoff behavior.
  </done>
</task>

</tasks>

<verification>
- Idempotency keys deterministic per pool/bead/action/payload
- Tool/agent retries cannot duplicate side effects
- Backoff bounded and observable in tests
</verification>

<success_criteria>
- IdempotencyStore persists results across restarts
- Handlers pass idempotency keys for all external actions
- Retry/backoff enforced across tools and agents
</success_criteria>

<output>
After completion, create `.planning/phases/07.1.1-production-orchestration-hardening/07.1.1-02-SUMMARY.md`
</output>
