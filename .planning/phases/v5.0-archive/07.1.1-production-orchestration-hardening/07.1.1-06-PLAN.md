---
phase: 07.1.1-production-orchestration-hardening
plan: 06
type: execute
wave: 3
depends_on:
  - "07.1.1-03"
  - "07.1.1-04"
files_modified:
  - src/alphaswarm_sol/orchestration/failures.py
  - src/alphaswarm_sol/orchestration/loop.py
  - src/alphaswarm_sol/orchestration/handlers.py
  - src/alphaswarm_sol/orchestration/rules.py
  - tests/orchestration/test_failure_recovery.py
autonomous: true

must_haves:
  truths:
    - "All orchestration failures are classified into a standard taxonomy."
    - "Recovery playbooks execute automatically for supported failure types."
    - "Failure metadata is persisted for audits and replay." 
  artifacts:
    - path: "src/alphaswarm_sol/orchestration/failures.py"
      provides: "Failure taxonomy + recovery playbooks"
      contains: "class FailureClassifier"
    - path: "src/alphaswarm_sol/orchestration/loop.py"
      provides: "Loop-level failure handling"
      contains: "apply_recovery"
    - path: "tests/orchestration/test_failure_recovery.py"
      provides: "Failure taxonomy and recovery tests"
      contains: "RecoveryPlaybook"
  key_links:
    - from: "src/alphaswarm_sol/orchestration/loop.py"
      to: "src/alphaswarm_sol/orchestration/failures.py"
      via: "Loop delegates error handling to classifier and playbook"
      pattern: "FailureClassifier"
---

<objective>
Define a failure taxonomy and automated recovery playbooks for orchestration.

Purpose: Make failure modes explicit and recoverable without manual intervention.
Output: Failure classifier, recovery wiring, and tests for playbook execution.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1.1-production-orchestration-hardening/07.1.1-CONTEXT.md
@.planning/phases/07.1.1-production-orchestration-hardening/07.1.1-RESEARCH.md
@src/alphaswarm_sol/orchestration/loop.py
@src/alphaswarm_sol/orchestration/handlers.py
@src/alphaswarm_sol/orchestration/rules.py
@src/alphaswarm_sol/errors.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement failure taxonomy and recovery playbooks</name>
  <files>src/alphaswarm_sol/orchestration/failures.py</files>
  <action>
Create a FailureClassifier and RecoveryPlaybook registry:
- Define FailureType enum (tool_failure, agent_failure, timeout, validation, state_corruption, backpressure, unknown)
- Classifier maps exceptions and context to FailureType + severity
- RecoveryPlaybook maps FailureType -> action (retry, pause, skip, abort) with max attempts
- Include serialization helpers for metadata storage
  </action>
  <verify>
    python - <<'PY'
from alphaswarm_sol.orchestration.failures import FailureClassifier
classifier = FailureClassifier()
assert classifier.classify(Exception('boom'))
PY
  </verify>
  <done>
    Failure taxonomy and playbook registry exist with actionable mappings.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire recovery into loop and handlers</name>
  <files>
    src/alphaswarm_sol/orchestration/loop.py
    src/alphaswarm_sol/orchestration/handlers.py
    src/alphaswarm_sol/orchestration/rules.py
  </files>
  <action>
Integrate classifier and playbooks into orchestration:
- Wrap handler execution to classify failures and apply recovery actions
- Persist failure metadata in pool.metadata (type, severity, attempts, last_error)
- Update orchestration rules to require failure metadata on FAILED pools
- Ensure recovery actions are deterministic and bounded
  </action>
  <verify>
    uv run pytest tests/orchestration/test_failure_recovery.py -k recovery -v
  </verify>
  <done>
    Failures are classified, recovery actions applied, and metadata persisted.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add failure taxonomy and recovery tests</name>
  <files>tests/orchestration/test_failure_recovery.py</files>
  <action>
Create tests that validate:
- Failure classifier assigns correct FailureType for representative exceptions
- Recovery playbooks enforce max attempts and determinism
- Loop records failure metadata when recovery fails
Use stub handlers to simulate failures without external calls.
  </action>
  <verify>
    uv run pytest tests/orchestration/test_failure_recovery.py -v
  </verify>
  <done>
    Tests cover taxonomy classification and recovery actions end-to-end.
  </done>
</task>

</tasks>

<verification>
- Failures are classified and stored with metadata
- Recovery actions are deterministic and bounded
- Rules enforce failure metadata on FAILED pools
</verification>

<success_criteria>
- Failure taxonomy exists and is used by loop/handlers
- Recovery playbooks execute automatically where applicable
- Failure metadata is visible in pool state and tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/07.1.1-production-orchestration-hardening/07.1.1-06-SUMMARY.md`
</output>
