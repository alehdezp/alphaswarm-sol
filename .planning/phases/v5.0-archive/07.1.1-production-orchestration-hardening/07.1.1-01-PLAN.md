---
phase: 07.1.1-production-orchestration-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/alphaswarm_sol/beads/event_store.py
  - src/alphaswarm_sol/beads/storage.py
  - tests/test_bead_event_store.py
autonomous: true

must_haves:
  truths:
    - "Bead state can be reconstructed from an append-only event log without reading existing bead JSON."
    - "Every bead mutation emits a deterministic event with timestamp, actor, and payload hash."
    - "Replay produces the same bead state as the latest persisted bead file."
  artifacts:
    - path: "src/alphaswarm_sol/beads/event_store.py"
      provides: "Append-only bead event log with replay API"
      contains: "class BeadEventStore"
    - path: "src/alphaswarm_sol/beads/storage.py"
      provides: "Bead storage emits events on save/update"
      contains: "record_event"
    - path: "tests/test_bead_event_store.py"
      provides: "Event log append/replay tests"
      contains: "replay"
  key_links:
    - from: "src/alphaswarm_sol/beads/storage.py"
      to: "src/alphaswarm_sol/beads/event_store.py"
      via: "BeadStorage writes BeadEvent entries for all mutations"
      pattern: "append_event|record_event"
---

<objective>
Create an event-sourced bead state store that writes append-only events and can replay into full bead state.

Purpose: Guarantee bead history can be reconstructed deterministically for audits and recovery.
Output: New event store module, storage integration, and tests proving replay fidelity.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1.1-production-orchestration-hardening/07.1.1-CONTEXT.md
@.planning/phases/07.1.1-production-orchestration-hardening/07.1.1-RESEARCH.md
@src/alphaswarm_sol/beads/storage.py
@src/alphaswarm_sol/beads/jsonl_storage.py
@src/alphaswarm_sol/beads/schema.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement append-only bead event log with replay</name>
  <files>src/alphaswarm_sol/beads/event_store.py</files>
  <action>
Create a new event log module that stores bead events as append-only JSONL and supports deterministic replay:
- Define BeadEvent dataclass with fields: event_id, bead_id, event_type, payload, payload_hash, timestamp, actor, pool_id (optional)
- Implement BeadEventStore with append_event(), list_events(), replay(bead_id=None) -> dict[bead_id, VulnerabilityBead]
- Use stable hashing for event_id (hash of bead_id + event_type + payload_hash + timestamp)
- Store logs under .vrs/beads/events.jsonl by default, with optional pool-specific path (.vrs/pools/{pool_id}/events/beads.jsonl)
- Ensure replay ordering is deterministic (timestamp + event_id tie-breaker)
- Keep file format JSONL for git-friendly append-only behavior
  </action>
  <verify>
    python - <<'PY'
from pathlib import Path
from alphaswarm_sol.beads.event_store import BeadEventStore
store = BeadEventStore(Path('.vrs/beads'))
assert store.list_events() == []
PY
  </verify>
  <done>
    BeadEventStore exists with append/list/replay APIs and deterministic ordering.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate event logging into BeadStorage mutations</name>
  <files>src/alphaswarm_sol/beads/storage.py</files>
  <action>
Wire BeadStorage to emit events for all mutations:
- Add optional event_store parameter to BeadStorage (default to BeadEventStore rooted at .vrs/beads)
- Emit events on save_bead(), save_to_pool(), update_work_state(), delete_bead() with clear event_type names
- Add replay_beads() helper that reconstructs beads from event log for a pool
- Preserve existing JSON/YAML storage behavior (event log is additive, not replacing current files)
- Avoid touching builder_legacy.py or any executor.py
  </action>
  <verify>
    python - <<'PY'
from pathlib import Path
from alphaswarm_sol.beads.storage import BeadStorage
storage = BeadStorage(Path('.vrs/beads'))
assert hasattr(storage, 'event_store')
PY
  </verify>
  <done>
    BeadStorage emits append-only events on all mutations and can replay bead state from the log.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add event log append/replay tests</name>
  <files>tests/test_bead_event_store.py</files>
  <action>
Create tests that validate event log behavior:
- Creating/updating beads appends events (event count increases, no rewrites)
- Replay reconstructs bead state equivalent to the latest stored bead
- Pool-specific event log path works when save_to_pool() is used
Use tmp_path for isolation and avoid reliance on global .vrs directories.
  </action>
  <verify>
    uv run pytest tests/test_bead_event_store.py -v
  </verify>
  <done>
    Tests pass and confirm deterministic replay from event log.
  </done>
</task>

</tasks>

<verification>
- Event log is append-only (JSONL) and replayable
- BeadStorage still writes existing JSON/YAML files without regression
- New tests pass in isolation
</verification>

<success_criteria>
- Bead event log supports append/list/replay with deterministic ordering
- All bead mutations emit events with payload hashes
- Replay produces identical bead state to persisted bead files
</success_criteria>

<output>
After completion, create `.planning/phases/07.1.1-production-orchestration-hardening/07.1.1-01-SUMMARY.md`
</output>
