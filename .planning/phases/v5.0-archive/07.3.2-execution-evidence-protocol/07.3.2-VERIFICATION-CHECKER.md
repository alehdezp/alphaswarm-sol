# 07.3.2 Execution Evidence Protocol - Plan Check

## Scope
- Plans reviewed: 07.3.2-00-PLAN.md through 07.3.2-05-PLAN.md
- Checks: task completeness, dependency correctness, file ownership, scope sanity, must_haves

## Issues

### Blocker
- None

### Major
- None

### Minor
- None

### Info
- 07.3.2-00-PLAN.md uses a cross-phase dependency (`depends_on: ["07.3.1.5"]`) rather than an in-phase plan ID; this matches repo convention but may require orchestration tooling to resolve phase-level dependencies. `.planning/phases/07.3.2-execution-evidence-protocol/07.3.2-00-PLAN.md:6`

## Improvement Suggestions (Per Plan)

### 07.3.2-00-PLAN.md
- Consider adding a single, explicit schema version field name (e.g., `schema_version`) and forward-compatibility guidance to make validation unambiguous.
- Consider mapping each gate in 07.3.2-GATES.md to a proof token type list to tighten the gate-to-evidence linkage.

### 07.3.2-01-PLAN.md
- Consider specifying output path conventions (e.g., per-run ID subdirectories under `.vrs/proofs/`) to avoid collisions across runs.
- Consider documenting CLI flags for evidence pack collection (run_id, output_dir) in the task action to reduce ambiguity.

### 07.3.2-02-PLAN.md
- Consider defining a machine-readable validator output schema (fields + exit codes) so downstream gate aggregation can rely on stable output.
- Consider documenting the exact temporal ordering rules (expected stage sequence) in the validator action.

### 07.3.2-03-PLAN.md
- Consider naming the variance metric(s) explicitly (e.g., Jaccard similarity for findings, CV for timings) to make pass/fail criteria deterministic.
- Consider including the variance report path in evidence pack manifests for traceability.

### 07.3.2-04-PLAN.md
- Consider listing explicit per-category minimum thresholds (even placeholder values) in the manifest or gates doc to remove ambiguity.
- Consider adding dataset snapshot date/commit to the manifest for provenance clarity.

### 07.3.2-05-PLAN.md
- Consider declaring the JSON/Markdown report schema (fields + version) in the gate aggregator task for stable downstream parsing.
- Consider adding a default output directory in `configs/gate_runner.yaml` to standardize report locations.
