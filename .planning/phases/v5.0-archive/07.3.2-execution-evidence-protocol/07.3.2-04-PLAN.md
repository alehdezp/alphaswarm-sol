---
phase: 07.3.2-execution-evidence-protocol
plan: "04"
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - configs/ground_truth_manifest.yaml
  - scripts/e2e/validate_ground_truth.py
  - reports/ground_truth_coverage.md
autonomous: true

must_haves:
  truths:
    - "Ground truth sources are documented with provenance"
    - "Coverage is measured per vulnerability family"
    - "G3 gate enforces category minimums"
  artifacts:
    - path: "configs/ground_truth_manifest.yaml"
      provides: "Ground truth manifest"
      contains: "sources"
    - path: "scripts/e2e/validate_ground_truth.py"
      provides: "Ground truth validator"
      contains: "coverage"
    - path: "reports/ground_truth_coverage.md"
      provides: "Coverage report"
      contains: "coverage"
  key_links:
    - from: "configs/ground_truth_manifest.yaml"
      to: "reports/ground_truth_coverage.md"
      via: "coverage"
      pattern: "category"
---

<objective>
Document external ground truth sources and enforce coverage thresholds per vulnerability family.
</objective>

<context>
@phases/07.3.2-execution-evidence-protocol/07.3.2-GATES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build ground truth manifest</name>
  <files>
    configs/ground_truth_manifest.yaml
  </files>
  <action>
Create a manifest that records:
- dataset name + URL
- hash of dataset snapshot
- vulnerability categories covered
- expected known findings (IDs)

Include a version and update policy.
  </action>
  <verify>
Confirm manifest includes URL + hash + categories.
  </verify>
  <done>
Ground truth manifest exists with provenance.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement coverage validator (G3)</name>
  <files>
    scripts/e2e/validate_ground_truth.py
    reports/ground_truth_coverage.md
  </files>
  <action>
Implement validator that:
- checks category coverage vs minimums
- outputs a coverage report
- fails if any category is below threshold

Record gate outcome.
  </action>
  <verify>
Confirm validator reports per-category coverage and pass/fail.
  </verify>
  <done>
Ground truth coverage validator exists.
  </done>
</task>

</tasks>
