---
phase: 07.3.2-execution-evidence-protocol
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - scripts/e2e/validate_proof_tokens.py
  - scripts/e2e/validate_evidence_pack.py
  - scripts/e2e/validate_gate_g1_g2.py
autonomous: true

must_haves:
  truths:
    - "Proof tokens are validated for structure, hashes, and temporal order"
    - "Evidence packs are rejected if any required artifact is missing"
    - "G1/G2 gates are enforced programmatically"
  artifacts:
    - path: "scripts/e2e/validate_proof_tokens.py"
      provides: "Proof validator"
      contains: "validate_proof"
    - path: "scripts/e2e/validate_evidence_pack.py"
      provides: "Evidence pack validator"
      contains: "validate_pack"
    - path: "scripts/e2e/validate_gate_g1_g2.py"
      provides: "Gate validator"
      contains: "G1"
  key_links:
    - from: "scripts/e2e/validate_proof_tokens.py"
      to: "phases/07.3.2-execution-evidence-protocol/07.3.2-PROOF-TOKEN-SPEC.md"
      via: "validation rules"
      pattern: "proof_type"
---

<objective>
Validate proof tokens and evidence packs, and enforce the first two gates (Evidence Integrity, Graph Soundness).
</objective>

<context>
@phases/07.3.2-execution-evidence-protocol/07.3.2-GATES.md
@phases/07.3.2-execution-evidence-protocol/07.3.2-PROOF-TOKEN-SPEC.md
@phases/07.3.2-execution-evidence-protocol/07.3.2-EVIDENCE-PACK-SCHEMA.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement proof-token validator</name>
  <files>
    scripts/e2e/validate_proof_tokens.py
  </files>
  <action>
Implement validator that checks:
- nonce presence and echo in transcript
- hash validity (graph/context/pattern/agents/debate/report)
- temporal ordering (stage ordering)
- cross-references to graph nodes

Fail fast on any invalid proof.
  </action>
  <verify>
Confirm validator reports invalid proof reasons and exits non-zero.
  </verify>
  <done>
Proof-token validator exists and enforces structure and ordering.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement evidence pack validator</name>
  <files>
    scripts/e2e/validate_evidence_pack.py
  </files>
  <action>
Validate evidence pack schema and required artifacts:
- transcripts + hashes
- proof tokens for all stages
- debug artifacts per skill/agent
- tool outputs or explicit skip reasons

Reject missing/empty artifacts.
  </action>
  <verify>
Confirm validator fails runs missing any required artifact.
  </verify>
  <done>
Evidence pack validator exists and enforces completeness.
  </done>
</task>

<task type="auto">
  <name>Task 3: Enforce G1/G2 gates</name>
  <files>
    scripts/e2e/validate_gate_g1_g2.py
  </files>
  <action>
Implement gate enforcement for:
- G1 Evidence Integrity (proof tokens + schema)
- G2 Graph Soundness (hash + property coverage)

Output machine-readable pass/fail and reasons.
  </action>
  <verify>
Confirm gate validator produces pass/fail output with reasons.
  </verify>
  <done>
G1/G2 gates are enforced programmatically.
  </done>
</task>

</tasks>
