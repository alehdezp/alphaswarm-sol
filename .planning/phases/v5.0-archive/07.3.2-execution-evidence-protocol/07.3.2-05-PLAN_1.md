---
phase: 07.3.2-execution-evidence-protocol
plan: "05"
type: execute
wave: 4
depends_on: ["02", "03", "04"]
files_modified:
  - scripts/e2e/validate_all_gates.py
  - scripts/e2e/validate_gates_cli.sh
  - configs/gate_runner.yaml

autonomous: true

must_haves:
  truths:
    - "A single CLI command evaluates G0–G7"
    - "Gate results are machine-readable and human-readable"
    - "Runs fail if any gate fails"
  artifacts:
    - path: "scripts/e2e/validate_all_gates.py"
      provides: "Gate aggregator"
      contains: "G0"
    - path: "scripts/e2e/validate_gates_cli.sh"
      provides: "CLI wrapper"
      contains: "validate-gates"
    - path: "configs/gate_runner.yaml"
      provides: "Gate runner config"
      contains: "gates"
  key_links:
    - from: "scripts/e2e/validate_all_gates.py"
      to: "phases/07.3.2-execution-evidence-protocol/07.3.2-GATES.md"
      via: "gate rules"
      pattern: "G0"
---

<objective>
Provide a single CLI command that aggregates G0–G7 and produces a unified gate report.
</objective>

<context>
@phases/07.3.2-execution-evidence-protocol/07.3.2-GATES.md
@phases/07.3.2-execution-evidence-protocol/07.3.2-EVIDENCE-PACK-SCHEMA.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement gate aggregator</name>
  <files>
    scripts/e2e/validate_all_gates.py
  </files>
  <action>
Implement a CLI that:
- loads evidence pack manifest
- runs validators for G0–G7
- emits JSON + Markdown reports
- exits non-zero on any gate failure
  </action>
  <verify>
Confirm CLI reports gate statuses and exits non-zero on failure.
  </verify>
  <done>
Gate aggregator exists.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add CLI wrapper</name>
  <files>
    scripts/e2e/validate_gates_cli.sh
  </files>
  <action>
Provide a shell wrapper that passes the run_id or evidence pack path and prints report paths.
  </action>
  <verify>
Run with --dry-run and confirm it prints the command plan.
  </verify>
  <done>
CLI wrapper exists.
  </done>
</task>

<task type="auto">
  <name>Task 3: Define gate runner config</name>
  <files>
    configs/gate_runner.yaml
  </files>
  <action>
Define defaults:
- required gates
- report output paths
- fail-fast behavior
  </action>
  <verify>
Confirm config includes required gates and output paths.
  </verify>
  <done>
Gate runner config exists.
  </done>
</task>

</tasks>
