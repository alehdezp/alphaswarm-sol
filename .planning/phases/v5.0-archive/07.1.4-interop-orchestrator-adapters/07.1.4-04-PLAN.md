---
phase: 07.1.4-interop-orchestrator-adapters
plan: 04
type: execute
wave: 3
depends_on: ["07.1.4-01"]
files_modified:
  - src/alphaswarm_sol/adapters/autogen.py
  - src/alphaswarm_sol/adapters/crewai.py
  - tests/adapters/test_autogen_crewai.py
autonomous: true

must_haves:
  truths:
    - "AutoGen adapter maps VRS agents to AutoGen team/chat structure"
    - "CrewAI adapter maps VRS agents to crew roles with task delegation"
    - "Both adapters preserve evidence contracts through execution"
  artifacts:
    - path: "src/alphaswarm_sol/adapters/autogen.py"
      provides: "AutoGen/Magentic-One adapter implementation"
      exports: ["AutoGenAdapter", "AutoGenConfig", "VrsTeam"]
    - path: "src/alphaswarm_sol/adapters/crewai.py"
      provides: "CrewAI adapter implementation"
      exports: ["CrewAIAdapter", "CrewAIConfig", "VrsCrew"]
  key_links:
    - from: "src/alphaswarm_sol/adapters/autogen.py"
      to: "src/alphaswarm_sol/adapters/base.py"
      via: "OrchestratorAdapter inheritance"
      pattern: "class AutoGenAdapter\\(OrchestratorAdapter\\)"
    - from: "src/alphaswarm_sol/adapters/crewai.py"
      to: "src/alphaswarm_sol/adapters/base.py"
      via: "OrchestratorAdapter inheritance"
      pattern: "class CrewAIAdapter\\(OrchestratorAdapter\\)"
---

<objective>
Implement AutoGen and CrewAI adapters for team/crew orchestration.

Purpose: Enable AlphaSwarm workflows to run on AutoGen (team chat) and CrewAI (crew roles) platforms, mapping VRS agent roles to their native constructs while preserving evidence contracts.

Output:
- AutoGenAdapter implementing OrchestratorAdapter
- CrewAIAdapter implementing OrchestratorAdapter
- Unit tests validating team/crew execution patterns
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1.4-interop-orchestrator-adapters/07.1.4-CONTEXT.md
@.planning/phases/07.1.4-interop-orchestrator-adapters/07.1.4-RESEARCH.md
@.planning/phases/07.1.4-interop-orchestrator-adapters/07.1.4-01-SUMMARY.md
@src/alphaswarm_sol/agents/catalog.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement AutoGenAdapter</name>
  <files>src/alphaswarm_sol/adapters/autogen.py</files>
  <action>
Create the AutoGen adapter that maps VRS agents to AutoGen team structure.

1. Define `AutoGenConfig(AdapterConfig)`:
   - `team_type`: "round_robin" | "selector" | "swarm" (default "swarm")
   - `max_rounds`: int (default 10)
   - `human_input_mode`: "NEVER" | "TERMINATE" | "ALWAYS" (default "NEVER")
   - `code_execution_enabled`: bool (default False for safety)

2. Define `VrsTeam` class:
   - `__init__(adapter: AutoGenAdapter, bead: VulnerabilityBead)`:
     - Create ConversableAgent for each VRS role
     - Configure system prompts from catalog.yaml
     - Set up team coordination based on team_type

   - `_create_vrs_agent(role: AgentRole, config: AgentConfig) -> ConversableAgent`:
     - Map VRS role to AutoGen ConversableAgent
     - Set system_message from agent prompt
     - Configure tools as function calls
     - Return agent instance

   - `async run(task: str) -> Dict[str, Any]`:
     - Execute team chat with task as initial message
     - Return final state with evidence

3. Implement `AutoGenAdapter(OrchestratorAdapter)`:
   - `__init__(config: AutoGenConfig)`:
     - Initialize team configuration
     - Load VRS agent catalog

   - `async execute_agent(config: AgentConfig, messages: List[Dict]) -> AgentResponse`:
     - Create single ConversableAgent
     - Run single turn
     - Convert response to AgentResponse

   - `async handoff(ctx: HandoffContext) -> HandoffResult`:
     - Pass context via function_map or shared context
     - Target agent receives bead state in message
     - Return HandoffResult

   - `async execute_team(bead: VulnerabilityBead) -> VulnerabilityBead`:
     - Create VrsTeam
     - Run team conversation
     - Extract verdict from team output
     - Return updated bead

   - `get_capabilities() -> Set[AdapterCapability]`:
     - Return capabilities from ADAPTER_CAPABILITIES["autogen"]

   - `preserve_evidence(bead: VulnerabilityBead, ctx: HandoffContext) -> VulnerabilityBead`:
     - Store evidence in team conversation context
     - Validate on extraction

4. Add role mapping:
   ```python
   VRS_TO_AUTOGEN_ROLE = {
       AgentRole.ATTACKER: "attacker_agent",
       AgentRole.DEFENDER: "defender_agent",
       AgentRole.VERIFIER: "verifier_agent",
       AgentRole.SUPERVISOR: "orchestrator_agent",
   }
   ```

Use lazy imports: `from autogen import ConversableAgent, GroupChat, GroupChatManager`
  </action>
  <verify>
Run: `python -c "from alphaswarm_sol.adapters.autogen import AutoGenAdapter; print('OK')"`
  </verify>
  <done>
AutoGenAdapter maps VRS agents to AutoGen team structure with chat-based handoffs.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement CrewAIAdapter</name>
  <files>src/alphaswarm_sol/adapters/crewai.py</files>
  <action>
Create the CrewAI adapter that maps VRS agents to crew roles.

1. Define `CrewAIConfig(AdapterConfig)`:
   - `process_type`: "sequential" | "hierarchical" (default "sequential")
   - `verbose`: bool (default False)
   - `memory_enabled`: bool (default True)
   - `max_iterations`: int (default 5)

2. Define `VrsCrew` class:
   - `__init__(adapter: CrewAIAdapter, bead: VulnerabilityBead)`:
     - Create Agent for each VRS role
     - Create Task for investigation workflow
     - Assemble Crew with agents and tasks

   - `_create_vrs_agent(role: AgentRole, config: AgentConfig) -> Agent`:
     - Map VRS role to CrewAI Agent
     - Set role, goal, backstory from catalog
     - Configure tools
     - Return agent

   - `_create_investigation_tasks(bead: VulnerabilityBead) -> List[Task]`:
     - Create task for attack path analysis (attacker)
     - Create task for guard detection (defender)
     - Create task for verdict synthesis (verifier)
     - Return ordered task list

   - `async kickoff() -> Dict[str, Any]`:
     - Execute crew workflow
     - Return final output with evidence

3. Implement `CrewAIAdapter(OrchestratorAdapter)`:
   - `__init__(config: CrewAIConfig)`:
     - Initialize crew configuration
     - Load VRS agent catalog

   - `async execute_agent(config: AgentConfig, messages: List[Dict]) -> AgentResponse`:
     - Create single CrewAI Agent with single Task
     - Execute task
     - Convert output to AgentResponse

   - `async handoff(ctx: HandoffContext) -> HandoffResult`:
     - Create Task delegation between agents
     - Pass evidence in task context
     - Return HandoffResult

   - `async execute_crew(bead: VulnerabilityBead) -> VulnerabilityBead`:
     - Create VrsCrew
     - Run crew kickoff
     - Extract verdict from crew output
     - Return updated bead

   - `get_capabilities() -> Set[AdapterCapability]`:
     - Return capabilities from ADAPTER_CAPABILITIES["crewai"]

   - `preserve_evidence(bead: VulnerabilityBead, ctx: HandoffContext) -> VulnerabilityBead`:
     - Store evidence in crew memory
     - Validate on extraction

4. Add role mapping:
   ```python
   VRS_TO_CREWAI_ROLE = {
       AgentRole.ATTACKER: {
           "role": "Security Attacker",
           "goal": "Find exploitation paths for potential vulnerabilities",
           "backstory": "Expert in constructing attack vectors...",
       },
       AgentRole.DEFENDER: {
           "role": "Security Defender",
           "goal": "Identify guards and mitigations",
           "backstory": "Expert in defensive security patterns...",
       },
       AgentRole.VERIFIER: {
           "role": "Security Verifier",
           "goal": "Synthesize evidence into verdicts",
           "backstory": "Expert in evidence evaluation...",
       },
   }
   ```

Use lazy imports: `from crewai import Agent, Task, Crew, Process`
  </action>
  <verify>
Run: `python -c "from alphaswarm_sol.adapters.crewai import CrewAIAdapter; print('OK')"`
  </verify>
  <done>
CrewAIAdapter maps VRS agents to crew roles with task-based handoffs.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create unit tests for AutoGen and CrewAI adapters</name>
  <files>tests/adapters/test_autogen_crewai.py</files>
  <action>
Create unit tests for AutoGenAdapter and CrewAIAdapter.

Tests to implement:
1. `test_autogen_role_mapping`: Verify VRS roles map to AutoGen agents
2. `test_autogen_team_creation`: Verify VrsTeam creates valid team structure
3. `test_autogen_handoff_preserves_evidence`: Verify evidence in chat context
4. `test_autogen_capabilities`: Verify reported capabilities correct

5. `test_crewai_role_mapping`: Verify VRS roles map to CrewAI agents
6. `test_crewai_crew_creation`: Verify VrsCrew creates valid crew structure
7. `test_crewai_task_delegation`: Verify tasks created for investigation flow
8. `test_crewai_handoff_preserves_evidence`: Verify evidence in task context
9. `test_crewai_capabilities`: Verify reported capabilities correct

Use pytest fixtures:
- `mock_autogen`: Mock autogen imports for unit testing
- `mock_crewai`: Mock crewai imports for unit testing
- `sample_bead`: Create test VulnerabilityBead

Mark tests requiring actual packages with:
- `@pytest.mark.skipif(not HAS_AUTOGEN)`
- `@pytest.mark.skipif(not HAS_CREWAI)`
  </action>
  <verify>
Run: `uv run pytest tests/adapters/test_autogen_crewai.py -v --collect-only`
Expected: At least 9 tests collected
  </verify>
  <done>
Unit tests validate role mapping, team/crew creation, and evidence preservation.
  </done>
</task>

</tasks>

<verification>
1. Adapter imports: `python -c "from alphaswarm_sol.adapters import AutoGenAdapter, CrewAIAdapter"`
2. Role mappings exist: `python -c "from alphaswarm_sol.adapters.autogen import VRS_TO_AUTOGEN_ROLE; print(len(VRS_TO_AUTOGEN_ROLE))"`
3. Test collection: `uv run pytest tests/adapters/test_autogen_crewai.py --collect-only`
</verification>

<success_criteria>
- AutoGenAdapter creates valid team chat structure
- CrewAIAdapter creates valid crew with tasks
- Both adapters preserve evidence through handoffs
- Role mappings defined for all VRS agent types
- 9+ unit tests pass validating adapter behavior
</success_criteria>

<output>
After completion, create `.planning/phases/07.1.4-interop-orchestrator-adapters/07.1.4-04-SUMMARY.md`
</output>
