---
phase: 07.1.3-cost-token-efficiency-context-engineering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/alphaswarm_sol/llm/context_budget.py
  - src/alphaswarm_sol/config.py
  - src/alphaswarm_sol/routing/router.py
  - src/alphaswarm_sol/llm/subagents.py
  - tests/test_context_budget.py
autonomous: true

must_haves:
  truths:
    - "Context budgets are defined per pool/agent and enforced before prompt assembly."
    - "Progressive disclosure loads minimal context first and can request expansions without exceeding caps."
    - "Budget reports are attached to routing outcomes for auditability."
  artifacts:
    - path: "src/alphaswarm_sol/llm/context_budget.py"
      provides: "Context budget policies, stages, and enforcement helpers"
      exports: ["ContextBudgetPolicy", "ContextBudgetStage", "ContextBudgetReport"]
    - path: "tests/test_context_budget.py"
      provides: "Budget enforcement + progressive disclosure tests"
  key_links:
    - from: "src/alphaswarm_sol/routing/router.py"
      to: "src/alphaswarm_sol/llm/context_budget.py"
      via: "Router enforces budgets and records context budget reports"
      pattern: "ContextBudgetPolicy|apply_budget"
---

<objective>
Introduce progressive disclosure and context budget policies that cap context size per pool/agent while preserving evidence-first behavior.

Purpose: Reduce token usage by enforcing explicit context budgets and staged disclosure (summary -> evidence -> raw) without degrading precision/recall.
Output: Budget policy module, router integration, and tests confirming enforcement.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1.3-cost-token-efficiency-context-engineering/07.1.3-CONTEXT.md
@.planning/phases/07.1.3-cost-token-efficiency-context-engineering/07.1.3-RESEARCH.md
@src/alphaswarm_sol/llm/limits.py
@src/alphaswarm_sol/llm/context_policy.py
@src/alphaswarm_sol/routing/router.py
@src/alphaswarm_sol/llm/subagents.py
@src/alphaswarm_sol/config.py
</context>

<skills>
Skills/Subagents to Load:
- /test-builder (auto-invoke for test authoring)
</skills>

<tasks>

<task type="auto">
  <name>Task 1: Implement context budget policy + staged disclosure model</name>
  <files>src/alphaswarm_sol/llm/context_budget.py</files>
  <action>
Create a context budget module that works alongside ContextPolicy:
- Define ContextBudgetStage (summary, evidence, raw) and ContextBudgetPolicy with per-role defaults
- Implement ContextBudgetReport capturing estimated tokens, trimmed sections, and stage decisions
- Provide apply_budget() that trims context deterministically and never exceeds the hard cap (default <= 6000 tokens)
- Use existing token estimation helpers or a 4-chars-per-token fallback (no external tokenizer)
  </action>
  <verify>
    python - <<'PY'
from alphaswarm_sol.llm.context_budget import ContextBudgetPolicy
policy = ContextBudgetPolicy()
assert policy.max_tokens <= 6000
PY
  </verify>
  <done>
    ContextBudgetPolicy and stages exist with enforceable budget reports.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire budget enforcement into routing and prompt assembly</name>
  <files>
    src/alphaswarm_sol/routing/router.py
    src/alphaswarm_sol/llm/subagents.py
    src/alphaswarm_sol/config.py
  </files>
  <action>
Integrate budgets into routing and prompt assembly:
- Load per-pool/agent budget config from config.py (use safe defaults if missing)
- Apply apply_budget() before prompt construction, recording ContextBudgetReport
- Ensure progressive disclosure can request expansion only if budget remains
- Preserve evidence-first ordering and avoid trimming evidence IDs
  </action>
  <verify>
    python - <<'PY'
from alphaswarm_sol.llm.context_budget import ContextBudgetReport
assert hasattr(ContextBudgetReport, "dropped_sections")
PY
  </verify>
  <done>
    Routing enforces budgets and captures budget reports without breaking prompt assembly.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add tests for budget enforcement and staged disclosure</name>
  <files>tests/test_context_budget.py</files>
  <action>
Create tests that validate:
- Budget enforcement trims context deterministically
- Staged disclosure respects per-stage caps and preserves evidence IDs
- Budget reports include estimated tokens and trimmed sections
Use small dummy contexts and avoid external tokenizers.
  </action>
  <verify>
    uv run pytest tests/test_context_budget.py -v
  </verify>
  <done>
    Tests pass and confirm deterministic budget enforcement behavior.
  </done>
</task>

</tasks>

<verification>
- Budget policies enforce per-agent caps before prompt assembly
- Progressive disclosure expands in stages without exceeding caps
- New tests pass in isolation
</verification>

<success_criteria>
- Context budgets are configurable and enforced for each routing decision
- Budget reports document token estimates and trimmed sections
- Progressive disclosure stages are honored across router and subagent flow
</success_criteria>

<output>
After completion, create `.planning/phases/07.1.3-cost-token-efficiency-context-engineering/07.1.3-01-SUMMARY.md`
</output>
