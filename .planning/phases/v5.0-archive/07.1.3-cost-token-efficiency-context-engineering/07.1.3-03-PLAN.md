---
phase: 07.1.3-cost-token-efficiency-context-engineering
plan: 03
type: execute
wave: 2
depends_on: ["07.1.3-01"]
files_modified:
  - src/alphaswarm_sol/context/retrieval_packer.py
  - src/alphaswarm_sol/context/integrations/evidence.py
  - src/alphaswarm_sol/llm/subagents.py
  - tests/test_retrieval_packer.py
autonomous: true

must_haves:
  truths:
    - "Evidence bundles are packed into compact TOON with minimal envelope metadata."
    - "Retrieval packing respects context budgets and preserves evidence links."
  artifacts:
    - path: "src/alphaswarm_sol/context/retrieval_packer.py"
      provides: "Retrieval packer for compact evidence bundles"
      exports: ["RetrievalPacker", "PackedEvidenceBundle"]
    - path: "tests/test_retrieval_packer.py"
      provides: "Retriever packer tests"
  key_links:
    - from: "src/alphaswarm_sol/context/integrations/evidence.py"
      to: "src/alphaswarm_sol/context/retrieval_packer.py"
      via: "Evidence assembly uses compact retrieval packing"
      pattern: "RetrievalPacker|pack_evidence"
---

<objective>
Create a retrieval packer that compacts evidence bundles using TOON while preserving evidence-first traceability.

Purpose: Reduce token footprint of evidence payloads without losing code location or risk signal context.
Output: Retrieval packer module, integration into evidence assembly, and tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1.3-cost-token-efficiency-context-engineering/07.1.3-CONTEXT.md
@.planning/phases/07.1.3-cost-token-efficiency-context-engineering/07.1.3-RESEARCH.md
@src/alphaswarm_sol/context/integrations/evidence.py
@src/alphaswarm_sol/llm/subagents.py
@src/alphaswarm_sol/kg/toon.py
</context>

<skills>
Skills/Subagents to Load:
- /test-builder (auto-invoke for test authoring)
</skills>

<tasks>

<task type="auto">
  <name>Task 1: Implement compact retrieval packer for evidence bundles</name>
  <files>src/alphaswarm_sol/context/retrieval_packer.py</files>
  <action>
Add RetrievalPacker that:
- Accepts evidence items and produces compact TOON output plus minimal envelope metadata
- Preserves exact code locations, risk scores, and evidence IDs
- Supports max token or max byte thresholds from context budgets
- Exposes pack() and unpack() helpers for testing
  </action>
  <verify>
    python - <<'PY'
from alphaswarm_sol.context.retrieval_packer import RetrievalPacker
packer = RetrievalPacker()
assert packer.pack([])
PY
  </verify>
  <done>
    RetrievalPacker outputs compact TOON payloads with required evidence metadata.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate retrieval packer into evidence assembly and add tests</name>
  <files>
    src/alphaswarm_sol/context/integrations/evidence.py
    src/alphaswarm_sol/llm/subagents.py
    tests/test_retrieval_packer.py
  </files>
  <action>
Wire RetrievalPacker into evidence assembly:
- Use packer for evidence bundles sent to subagents
- Ensure evidence links remain resolvable (file path + line anchors)
- Add tests to confirm compact output and preservation of evidence IDs
  </action>
  <verify>
    uv run pytest tests/test_retrieval_packer.py -v
  </verify>
  <done>
    Evidence assembly uses the packer and tests confirm fidelity.
  </done>
</task>

</tasks>

<verification>
- Evidence bundles are serialized in compact TOON with minimal envelope metadata
- Evidence links remain intact and resolvable
- New tests pass in isolation
</verification>

<success_criteria>
- Retrieval packer reduces evidence bundle size without losing evidence identifiers
- Evidence assembly defaults to packed bundles for subagent context
</success_criteria>

<output>
After completion, create `.planning/phases/07.1.3-cost-token-efficiency-context-engineering/07.1.3-03-SUMMARY.md`
</output>
