---
phase: 07.1.3-cost-token-efficiency-context-engineering
plan: 02
type: execute
wave: 1
depends_on: ["07.1.3-01"]
files_modified:
  - src/alphaswarm_sol/llm/routing_policy.py
  - src/alphaswarm_sol/llm/tiers.py
  - src/alphaswarm_sol/llm/subagents.py
  - src/alphaswarm_sol/routing/router.py
  - tests/test_model_routing.py
  - docs/guides/model-routing.md
  - .planning/TOOLING.md
autonomous: true

must_haves:
  truths:
    - "Routing executes cheap validator tiers before escalating to expensive experts."
    - "Escalation thresholds are configurable per pool or workflow."
    - "Routing decisions include tier rationale and cost-tier metadata."
  artifacts:
    - path: "src/alphaswarm_sol/llm/routing_policy.py"
      provides: "Tiered routing policy + escalation rules"
      exports: ["TierRoutingPolicy", "RoutingDecision"]
    - path: "src/alphaswarm_sol/llm/tiers.py"
      provides: "Tier router selects model tiers with policy metadata"
      exports: ["TierRouter"]
    - path: "tests/test_model_routing.py"
      provides: "Model routing tests for tier escalation"
  key_links:
    - from: "src/alphaswarm_sol/llm/subagents.py"
      to: "src/alphaswarm_sol/llm/routing_policy.py"
      via: "Subagent execution uses tier routing policy"
      pattern: "TierRoutingPolicy|RoutingDecision"
---

<objective>
Introduce model routing that escalates from cheap validators to expensive experts based on risk, evidence, and budget.

Purpose: Reduce costs by defaulting to low-cost validation and escalating only when required.
Output: Tier routing policy module, integrated tier router behavior, and tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1.3-cost-token-efficiency-context-engineering/07.1.3-CONTEXT.md
@.planning/phases/07.1.3-cost-token-efficiency-context-engineering/07.1.3-RESEARCH.md
@src/alphaswarm_sol/llm/tiers.py
@src/alphaswarm_sol/llm/subagents.py
@src/alphaswarm_sol/routing/router.py
@src/alphaswarm_sol/agents/runtime/factory.py
@src/alphaswarm_sol/agents/cost.py
</context>

<skills>
Skills/Subagents to Load:
- /test-builder (auto-invoke for test authoring)
- cost-governor (optional review of budget-aware routing rules)
</skills>

<tasks>

<task type="auto">
  <name>Task 1: Implement tier routing policy with escalation rules</name>
  <files>
    src/alphaswarm_sol/llm/routing_policy.py
    src/alphaswarm_sol/llm/tiers.py
  </files>
  <action>
Add a routing policy that chooses models by tier:
- Create TierRoutingPolicy with configurable thresholds (risk score, evidence completeness, budget remaining)
- Extend TierRouter to use the policy for tier selection (validator -> specialist -> expert)
- Return routing metadata (tier, rationale, estimated cost) with each decision
- Keep defaults conservative and aligned with max 6k input token caps
  </action>
  <verify>
    python - <<'PY'
from alphaswarm_sol.llm.routing_policy import TierRoutingPolicy
policy = TierRoutingPolicy()
assert policy.validator_tier
PY
  </verify>
  <done>
    TierRoutingPolicy exists and TierRouter selects a model tier with metadata.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire tier routing into subagent execution and add tests</name>
  <files>
    src/alphaswarm_sol/llm/subagents.py
    src/alphaswarm_sol/routing/router.py
    tests/test_model_routing.py
  </files>
  <action>
Integrate routing policy into subagent execution:
- Use TierRoutingPolicy decisions to select model tiers for subagent tasks
- Record tier metadata in routing outputs for audits and cost tracking
- Add tests for: validator-first behavior, escalation on risk/evidence gaps, and budget-aware downgrades
  </action>
  <verify>
    uv run pytest tests/test_model_routing.py -v
  </verify>
  <done>
    Tests confirm tier routing behavior and escalation rules.
  </done>
</task>

<task type="auto">
  <name>Task 3: Tooling Update - document routing tiers and cost controls</name>
  <files>
    docs/guides/model-routing.md
    .planning/TOOLING.md
  </files>
  <action>
Document the model routing policy and how to configure tiers:
- Add a guide outlining tiers, thresholds, and escalation conditions
- Update .planning/TOOLING.md with routing policy usage and configuration pointers
  </action>
  <verify>
    rg -n "TierRoutingPolicy|model routing" docs/guides/model-routing.md .planning/TOOLING.md
  </verify>
  <done>
    Tooling guide includes tier routing policy usage and configuration notes.
  </done>
</task>

</tasks>

<verification>
- Tier routing executes cheap validators first and escalates only when needed
- Routing metadata includes tier rationale and cost estimates
- New tests pass in isolation
</verification>

<success_criteria>
- Configurable tier routing policy governs model selection per pool
- Subagent execution respects routing tiers with audit metadata
- Documentation describes tier routing and configuration
</success_criteria>

<output>
After completion, create `.planning/phases/07.1.3-cost-token-efficiency-context-engineering/07.1.3-02-SUMMARY.md`
</output>
