---
phase: 07.1.3-cost-token-efficiency-context-engineering
plan: 06
type: execute
wave: 3
depends_on: []
files_modified:
  - src/alphaswarm_sol/metrics/cost_ledger.py
  - src/alphaswarm_sol/report/cost_dashboard.py
  - src/alphaswarm_sol/orchestration/pool.py
  - src/alphaswarm_sol/agents/cost.py
  - tests/test_cost_dashboard.py
autonomous: true

must_haves:
  truths:
    - "Cost dashboards show per-pool spend, tokens, and budget remaining."
    - "Budget enforcement prevents pools from exceeding configured caps."
    - "Cost ledger entries link back to pool and bead IDs when available."
  artifacts:
    - path: "src/alphaswarm_sol/metrics/cost_ledger.py"
      provides: "Pool-scoped cost ledger and aggregation helpers"
      exports: ["CostLedger", "PoolCostSummary"]
    - path: "src/alphaswarm_sol/report/cost_dashboard.py"
      provides: "Cost dashboard rendering for pools"
      exports: ["render_cost_dashboard"]
    - path: "tests/test_cost_dashboard.py"
      provides: "Cost dashboard and budget enforcement tests"
  key_links:
    - from: "src/alphaswarm_sol/orchestration/pool.py"
      to: "src/alphaswarm_sol/metrics/cost_ledger.py"
      via: "Pool manager records and enforces cost budgets"
      pattern: "CostLedger|budget"
---

<objective>
Add per-pool cost dashboards and enforce budget caps during orchestration runs.

Purpose: Make cost visible and enforceable at pool level while preserving evidence linkage.
Output: Cost ledger module, dashboard renderer, pool enforcement, and tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1.3-cost-token-efficiency-context-engineering/07.1.3-CONTEXT.md
@.planning/phases/07.1.3-cost-token-efficiency-context-engineering/07.1.3-RESEARCH.md
@src/alphaswarm_sol/agents/cost.py
@src/alphaswarm_sol/orchestration/pool.py
@src/alphaswarm_sol/metrics/recorder.py
</context>

<skills>
Skills/Subagents to Load:
- /test-builder (auto-invoke for test authoring)
</skills>

<tasks>

<task type="auto">
  <name>Task 1: Implement pool-scoped cost ledger and dashboard renderer</name>
  <files>
    src/alphaswarm_sol/metrics/cost_ledger.py
    src/alphaswarm_sol/report/cost_dashboard.py
  </files>
  <action>
Create pool-scoped cost tracking and dashboard output:
- Build CostLedger for recording token usage, cost, pool_id, and bead_id
- Add aggregation helpers for totals, per-agent breakdown, and budget remaining
- Implement render_cost_dashboard() to output compact report (markdown or TOON)
  </action>
  <verify>
    python - <<'PY'
from alphaswarm_sol.metrics.cost_ledger import CostLedger
ledger = CostLedger()
assert ledger.summary()
PY
  </verify>
  <done>
    CostLedger records pool spend and can render a dashboard summary.
  </done>
</task>

<task type="auto">
  <name>Task 2: Enforce pool budgets and add dashboard tests</name>
  <files>
    src/alphaswarm_sol/orchestration/pool.py
    src/alphaswarm_sol/agents/cost.py
    tests/test_cost_dashboard.py
  </files>
  <action>
Integrate budget enforcement into pool orchestration:
- Load per-pool budget limits and enforce on cost ledger updates
- Raise or warn when a pool exceeds budget caps (align with LLMLimits hard/soft behavior)
- Add tests for budget enforcement and dashboard output
  </action>
  <verify>
    uv run pytest tests/test_cost_dashboard.py -v
  </verify>
  <done>
    Pools enforce budgets and dashboard tests confirm cost reporting.
  </done>
</task>

</tasks>

<verification>
- Cost dashboards show spend, tokens, and remaining budget per pool
- Pool orchestration blocks or warns on budget breaches
- New tests pass in isolation
</verification>

<success_criteria>
- Pool cost ledger aggregates usage across beads and agents
- Budget enforcement prevents pools from exceeding configured caps
- Cost dashboards are generated from ledger data
</success_criteria>

<output>
After completion, create `.planning/phases/07.1.3-cost-token-efficiency-context-engineering/07.1.3-06-SUMMARY.md`
</output>
