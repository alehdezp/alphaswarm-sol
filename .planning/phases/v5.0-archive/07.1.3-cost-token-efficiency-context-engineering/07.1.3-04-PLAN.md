---
phase: 07.1.3-cost-token-efficiency-context-engineering
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - src/alphaswarm_sol/cache/graph_queries.py
  - src/alphaswarm_sol/queries/executor.py
  - src/alphaswarm_sol/cache/tool_results.py
  - src/alphaswarm_sol/tools/coordinator.py
  - tests/test_cache_graph_queries.py
  - tests/test_tool_cache.py
autonomous: true

must_haves:
  truths:
    - "Graph query results are cached with deterministic keys and invalidation."
    - "Tool outputs are memoized with TTL and pool-scoped cache keys."
    - "Cache entries can be cleared per pool/run without affecting others."
  artifacts:
    - path: "src/alphaswarm_sol/cache/graph_queries.py"
      provides: "Graph query result cache with TTL and invalidation"
      exports: ["GraphQueryCache"]
    - path: "tests/test_cache_graph_queries.py"
      provides: "Graph query cache tests"
  key_links:
    - from: "src/alphaswarm_sol/queries/executor.py"
      to: "src/alphaswarm_sol/cache/graph_queries.py"
      via: "Query executor memoizes graph results"
      pattern: "GraphQueryCache|cache_key"
---

<objective>
Introduce caching and memoization for graph queries and tool outputs to reduce repeated work and token spend.

Purpose: Reduce redundant computations and tool invocations while preserving correctness per pool.
Output: Graph query cache, tool result memoization extensions, and tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1.3-cost-token-efficiency-context-engineering/07.1.3-CONTEXT.md
@.planning/phases/07.1.3-cost-token-efficiency-context-engineering/07.1.3-RESEARCH.md
@src/alphaswarm_sol/cache/tool_results.py
@src/alphaswarm_sol/queries/executor.py
@src/alphaswarm_sol/tools/coordinator.py
</context>

<skills>
Skills/Subagents to Load:
- /test-builder (auto-invoke for test authoring)
</skills>

<tasks>

<task type="auto">
  <name>Task 1: Add graph query cache with TTL and invalidation</name>
  <files>
    src/alphaswarm_sol/cache/graph_queries.py
    src/alphaswarm_sol/queries/executor.py
  </files>
  <action>
Implement a graph query cache:
- Create GraphQueryCache with keying on graph hash, query text, overlay hash, and pool_id
- Support TTL, pool scoping, and explicit invalidation
- Integrate cache lookup/store into QueryExecutor with opt-in flag
  </action>
  <verify>
    python - <<'PY'
from alphaswarm_sol.cache.graph_queries import GraphQueryCache
cache = GraphQueryCache()
assert cache.is_enabled
PY
  </verify>
  <done>
    Graph query results are cached with deterministic keys and invalidation support.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend tool output cache with pool scoping</name>
  <files>
    src/alphaswarm_sol/cache/tool_results.py
    src/alphaswarm_sol/tools/coordinator.py
  </files>
  <action>
Enhance tool output caching:
- Add optional pool_id and run_id metadata to cache keys/records
- Ensure coordinator checks cache before running expensive tools
- Preserve existing TTL behavior and include cache stats in logs
  </action>
  <verify>
    python - <<'PY'
from pathlib import Path
from alphaswarm_sol.cache.tool_results import ToolResultCache
cache = ToolResultCache(Path('.vrs/cache'))
assert cache.ttl_hours >= 0
PY
  </verify>
  <done>
    Tool outputs are memoized with TTL and scoped cache keys.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add cache tests for queries and tools</name>
  <files>
    tests/test_cache_graph_queries.py
    tests/test_tool_cache.py
  </files>
  <action>
Create tests that validate:
- Graph query cache hit/miss behavior, TTL expiry, and invalidation
- Tool result caching avoids redundant tool execution and respects TTL
Use tmp_path-based caches for isolation.
  </action>
  <verify>
    uv run pytest tests/test_cache_graph_queries.py tests/test_tool_cache.py -v
  </verify>
  <done>
    Cache tests pass and confirm memoization behavior.
  </done>
</task>

</tasks>

<verification>
- Query and tool caches honor TTL and pool scoping
- Cached results match uncached outputs
- New tests pass in isolation
</verification>

<success_criteria>
- Graph query execution is memoized with deterministic cache keys
- Tool outputs reuse cached results when possible
- Cache invalidation clears stale results per pool/run
</success_criteria>

<output>
After completion, create `.planning/phases/07.1.3-cost-token-efficiency-context-engineering/07.1.3-04-SUMMARY.md`
</output>
