---
phase: 05.8-repository-cleanup-hygiene
plan: 03
type: execute
wave: 1
depends_on:
  - 05.8-02-PLAN.md
files_modified:
  - .planning/phases/05.8-repository-cleanup-hygiene/05.8-03-REMOVAL-LOG.md
autonomous: true

must_haves:
  truths:
    - "All regenerable caches deleted (crawl_cache, filtered_cache, llm_cache)"
    - "Tracking files deleted (coverage-matrix.yaml, dvdefi-validation.yaml)"
    - "Execution artifacts deleted (logs, progress reports, crawl sessions)"
    - ".true_vkg/ folder removed completely"
  artifacts:
    - path: ".planning/phases/05.8-repository-cleanup-hygiene/05.8-03-REMOVAL-LOG.md"
      provides: "Removal log with all deletions documented"
      min_lines: 80
---

<objective>
Delete .true_vkg/ and all its contents after relocation (Plan 02) is complete.

**Per CONTEXT decisions:**
- DELETE regenerable caches (not relocate) - knowledge already extracted to vulndocs
- DELETE tracking files - Phase 7 will regenerate fresh metrics dynamically
- DELETE execution artifacts - historical crawl data no longer needed

Output: 05.8-03-REMOVAL-LOG.md documenting all deletions.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05.8-repository-cleanup-hygiene/05.8-CONTEXT.md
@.planning/phases/05.8-repository-cleanup-hygiene/05.8-02-RELOCATION-LOG.md
</context>

<execution_guardrails>
- Full doc alignment: align objective, must_haves, and tasks against `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, plus phase context or research docs before execution. Any scope change must be reflected in the plan and `.planning/STATE.md`.
- Workspace isolation: run any commands or experiments that mutate state in a fresh jj workspace; do not use the main workspace and do not reuse workspaces for reruns.
- Repeat-run detection markers: include `[ALPHASWARM-START] RUN_ID TIMESTAMP` and `[ALPHASWARM-END] RUN_ID EXIT_CODE DURATION_MS` markers in transcripts or logs so reruns are detectable.
- claude-code-controller subject: for interactive runs, use `claude-code-controller` and launch Claude Code as the subject in the claude-code-agent-teams session; transcripts must show the Claude Code session.
- Seed scope boundaries: treat B-seeds (baseline or regression inputs) and N-seeds (net-new exploratory inputs) as separate scopes; do not mix them in a single run and track results separately.
</execution_guardrails>

<tasks>

<task type="auto">
  <name>Task 1: Delete regenerable caches</name>
  <files>.planning/phases/05.8-repository-cleanup-hygiene/05.8-03-REMOVAL-LOG.md</files>
  <action>
    Delete all cache directories (these are regenerable, knowledge extracted to vulndocs):

    - .true_vkg/crawl_cache/
    - .true_vkg/filtered_cache/
    - .true_vkg/llm_cache/

    Record in log:
    - Path deleted
    - Approximate size (if available)
    - Reason: "Regenerable cache - knowledge extracted to vulndocs"
  </action>
  <verify>
    - Cache directories no longer exist
    - Each deletion recorded with reason
  </verify>
  <done>Regenerable caches deleted</done>
</task>

<task type="auto">
  <name>Task 2: Delete tracking files</name>
  <files>.planning/phases/05.8-repository-cleanup-hygiene/05.8-03-REMOVAL-LOG.md</files>
  <action>
    Delete Phase 1 tracking files (Phase 7 will regenerate metrics dynamically):

    - .true_vkg/discovery/coverage-matrix.yaml
    - .true_vkg/discovery/dvdefi-validation.yaml
    - Any other static tracking/coverage files

    Record in log with reason: "Phase 1 snapshot - Phase 7 regenerates dynamically"
  </action>
  <verify>
    - Tracking files no longer exist
    - Each deletion recorded with reason
  </verify>
  <done>Tracking files deleted</done>
</task>

<task type="auto">
  <name>Task 3: Delete execution artifacts</name>
  <files>.planning/phases/05.8-repository-cleanup-hygiene/05.8-03-REMOVAL-LOG.md</files>
  <action>
    Delete execution artifacts (historical data no longer needed):

    - Logs and progress reports
    - Crawl session files
    - Raw crawl content
    - Any other execution-time artifacts

    Record in log with reason: "Execution artifact - knowledge extracted"
  </action>
  <verify>
    - Execution artifacts deleted
    - Each deletion recorded
  </verify>
  <done>Execution artifacts deleted</done>
</task>

<task type="auto">
  <name>Task 4: Verify relocation and remove .true_vkg/</name>
  <files>.planning/phases/05.8-repository-cleanup-hygiene/05.8-03-REMOVAL-LOG.md</files>
  <action>
    Final cleanup:

    1. Verify Plan 02 relocations completed:
       - swc-mapping.yaml exists at vulndocs/.meta/references/swc-mapping.yaml
       - Scripts audit complete

    2. Check for any remaining files in .true_vkg/:
       - If unexpected files remain, document and decide (delete or flag for review)

    3. Remove the .true_vkg/ directory entirely:
       ```bash
       rm -rf .true_vkg/
       ```

    4. Record final removal in log with summary stats
  </action>
  <verify>
    - .true_vkg/ directory no longer exists
    - Relocation verification passed
    - Summary stats recorded (total files removed, total size freed)
  </verify>
  <done>.true_vkg/ completely removed</done>
</task>

</tasks>
