---
phase: 05.8-repository-cleanup-hygiene
plan: 02
type: execute
wave: 1
depends_on:
  - 05.8-01-PLAN.md
files_modified:
  - vulndocs/.meta/references/swc-mapping.yaml
  - scripts/alphaswarm_crawl.py (if merge needed)
  - .planning/phases/05.8-repository-cleanup-hygiene/05.8-02-RELOCATION-LOG.md
autonomous: true

must_haves:
  truths:
    - "swc-to-vkg-mapping.yaml relocated to vulndocs/.meta/references/swc-mapping.yaml"
    - "Scripts from .true_vkg/scripts/ audited against scripts/"
    - "Useful script functionality merged or documented"
  artifacts:
    - path: ".planning/phases/05.8-repository-cleanup-hygiene/05.8-02-RELOCATION-LOG.md"
      provides: "Relocation log with actions taken"
      min_lines: 60
    - path: "vulndocs/.meta/references/swc-mapping.yaml"
      provides: "SWC-to-vulndocs category mapping"
  key_links:
    - from: ".true_vkg/discovery/swc-to-vkg-mapping.yaml"
      to: "vulndocs/.meta/references/swc-mapping.yaml"
      via: "relocation"
---

<objective>
Relocate valuable non-regenerable content from .true_vkg/ to proper locations per CONTEXT decisions.

**Key actions:**
- Move swc-to-vkg-mapping.yaml to vulndocs/.meta/references/
- Audit scripts and merge useful functionality
- Document all relocation actions

Note: Caches (crawl_cache, filtered_cache, llm_cache) are NOT relocated - they are deleted in Plan 03.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05.8-repository-cleanup-hygiene/05.8-CONTEXT.md
@.planning/phases/05.8-repository-cleanup-hygiene/05.8-01-AUDIT.md
</context>

<execution_guardrails>
- Full doc alignment: align objective, must_haves, and tasks against `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, plus phase context or research docs before execution. Any scope change must be reflected in the plan and `.planning/STATE.md`.
- Workspace isolation: run any commands or experiments that mutate state in a fresh jj workspace; do not use the main workspace and do not reuse workspaces for reruns.
- Repeat-run detection markers: include `[ALPHASWARM-START] RUN_ID TIMESTAMP` and `[ALPHASWARM-END] RUN_ID EXIT_CODE DURATION_MS` markers in transcripts or logs so reruns are detectable.
- claude-code-controller subject: for interactive runs, use `claude-code-controller` and launch Claude Code as the subject in the claude-code-agent-teams session; transcripts must show the Claude Code session.
- Seed scope boundaries: treat B-seeds (baseline or regression inputs) and N-seeds (net-new exploratory inputs) as separate scopes; do not mix them in a single run and track results separately.
</execution_guardrails>

<tasks>

<task type="auto">
  <name>Task 1: Relocate swc-to-vkg-mapping.yaml</name>
  <files>vulndocs/.meta/references/swc-mapping.yaml, .planning/phases/05.8-repository-cleanup-hygiene/05.8-02-RELOCATION-LOG.md</files>
  <action>
    Move the SWC mapping file to its permanent location:

    1. Create directory: vulndocs/.meta/references/
    2. Copy .true_vkg/discovery/swc-to-vkg-mapping.yaml → vulndocs/.meta/references/swc-mapping.yaml
    3. Verify the file is valid YAML
    4. Record action in relocation log

    This is curated reference content linking SWC IDs to vulndocs categories.
  </action>
  <verify>
    - vulndocs/.meta/references/swc-mapping.yaml exists
    - File is valid YAML
    - Action recorded in log
  </verify>
  <done>SWC mapping relocated to vulndocs</done>
</task>

<task type="auto">
  <name>Task 2: Audit .true_vkg/scripts/ against scripts/</name>
  <files>.planning/phases/05.8-repository-cleanup-hygiene/05.8-02-RELOCATION-LOG.md</files>
  <action>
    Compare scripts in .true_vkg/scripts/ with existing scripts/:

    1. List all scripts in .true_vkg/scripts/
    2. For each, check if functionality exists in scripts/alphaswarm_crawl.py or elsewhere
    3. Categorize each script:
       - DUPLICATE: functionality already exists
       - MERGE: useful functionality to add
       - DELETE: superseded or no longer needed

    Document findings in "## Scripts Audit" section.
  </action>
  <verify>
    - Every script in .true_vkg/scripts/ is categorized
    - Each categorization has a reason
  </verify>
  <done>Scripts audit complete</done>
</task>

<task type="auto">
  <name>Task 3: Merge useful script functionality (if any)</name>
  <files>scripts/alphaswarm_crawl.py (if needed), .planning/phases/05.8-repository-cleanup-hygiene/05.8-02-RELOCATION-LOG.md</files>
  <action>
    For scripts marked MERGE in Task 2:

    1. Extract the useful functionality
    2. Integrate into appropriate existing script (likely scripts/alphaswarm_crawl.py)
    3. Or create new script if standalone utility

    If no MERGE items: document "No merge needed - all functionality covered" in log.
  </action>
  <verify>
    - All MERGE items handled
    - Merged code works (basic syntax check)
    - Actions recorded in log
  </verify>
  <done>Script merge complete (or documented as not needed)</done>
</task>

</tasks>
