---
phase: 05.8-repository-cleanup-hygiene
plan: 04
type: execute
wave: 2
depends_on:
  - 05.8-03-PLAN.md
files_modified:
  - docs/**/*.md
  - .planning/phases/05.8-repository-cleanup-hygiene/05.8-04-REFERENCE-UPDATES.md
autonomous: true

must_haves:
  truths:
    - "No references to .true_vkg remain in codebase"
    - "docs/ synced with .planning/ where content overlaps"
    - "Polished content from .planning/reference/ moved to docs/"
    - ".planning/ contains only active planning artifacts"
  artifacts:
    - path: ".planning/phases/05.8-repository-cleanup-hygiene/05.8-04-REFERENCE-UPDATES.md"
      provides: "Reference update log and doc sync summary"
      min_lines: 100
---

<objective>
Update all references and synchronize documentation per CONTEXT decisions.

**Key actions:**
1. Remove/update all .true_vkg references in code, docs, skills, tests
2. Sync docs/ from .planning/ where content overlaps (planning is source of truth)
3. Move polished content from .planning/reference/ to docs/
4. Ensure .planning/ contains only: ROADMAP.md, STATE.md, PROJECT.md, phase plans, config.json

Output: 05.8-04-REFERENCE-UPDATES.md
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05.8-repository-cleanup-hygiene/05.8-CONTEXT.md
@.planning/phases/05.8-repository-cleanup-hygiene/05.8-01-AUDIT.md
</context>

<execution_guardrails>
- Full doc alignment: align objective, must_haves, and tasks against `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, plus phase context or research docs before execution. Any scope change must be reflected in the plan and `.planning/STATE.md`.
- Workspace isolation: run any commands or experiments that mutate state in a fresh jj workspace; do not use the main workspace and do not reuse workspaces for reruns.
- Repeat-run detection markers: include `[ALPHASWARM-START] RUN_ID TIMESTAMP` and `[ALPHASWARM-END] RUN_ID EXIT_CODE DURATION_MS` markers in transcripts or logs so reruns are detectable.
- claude-code-controller subject: for interactive runs, use `claude-code-controller` and launch Claude Code as the subject in the claude-code-agent-teams session; transcripts must show the Claude Code session.
- Seed scope boundaries: treat B-seeds (baseline or regression inputs) and N-seeds (net-new exploratory inputs) as separate scopes; do not mix them in a single run and track results separately.
</execution_guardrails>

<tasks>

<task type="auto">
  <name>Task 1: Update .true_vkg references in code</name>
  <files>.planning/phases/05.8-repository-cleanup-hygiene/05.8-04-REFERENCE-UPDATES.md</files>
  <action>
    Find and update all references to .true_vkg in source code:

    ```bash
    rg -n "\.true_vkg" src/ scripts/
    ```

    For each match:
    - Update path to new location (if relocated)
    - Or remove reference (if deleted content)
    - Record: file, line, old ref, new ref/action
  </action>
  <verify>
    - `rg "\.true_vkg" src/ scripts/` returns no matches
    - All updates recorded in log
  </verify>
  <done>Code references updated</done>
</task>

<task type="auto">
  <name>Task 2: Update .true_vkg references in docs, skills, tests</name>
  <files>.planning/phases/05.8-repository-cleanup-hygiene/05.8-04-REFERENCE-UPDATES.md</files>
  <action>
    Find and update all references in non-code files:

    ```bash
    rg -n "\.true_vkg" docs/ tests/ .claude/
    ```

    Update or remove each reference. Record all changes.
  </action>
  <verify>
    - `rg "\.true_vkg" docs/ tests/ .claude/` returns no matches
    - All updates recorded in log
  </verify>
  <done>Docs, skills, and tests references updated</done>
</task>

<task type="auto">
  <name>Task 3: Sync docs/ from .planning/ overlapping content</name>
  <files>docs/**/*.md, .planning/phases/05.8-repository-cleanup-hygiene/05.8-04-REFERENCE-UPDATES.md</files>
  <action>
    Identify and sync overlapping documentation:

    1. Compare docs/ files with .planning/ content
    2. For each overlap where .planning/ is more current:
       - Update docs/ to match .planning/ content
       - Or note if docs/ version should be kept

    Focus on:
    - Architecture docs
    - API references
    - Guide content

    Document in "## Documentation Sync" section.
  </action>
  <verify>
    - All overlapping content identified
    - Sync decisions documented with rationale
    - Updated files listed
  </verify>
  <done>Documentation sync complete</done>
</task>

<task type="auto">
  <name>Task 4: Move polished content from .planning/reference/ to docs/</name>
  <files>docs/**/*.md, .planning/phases/05.8-repository-cleanup-hygiene/05.8-04-REFERENCE-UPDATES.md</files>
  <action>
    Review .planning/reference/ for polished, finished content:

    1. List all files in .planning/reference/
    2. For each file, determine:
       - MOVE: polished content → move to appropriate docs/ location
       - KEEP: still active planning → leave in .planning/
       - DELETE: outdated/superseded → remove

    Move polished content to docs/ with appropriate organization.
    Document all actions in log.
  </action>
  <verify>
    - All .planning/reference/ files categorized
    - Moved files exist in docs/
    - Original planning artifacts remain in .planning/
  </verify>
  <done>Polished content moved to docs/</done>
</task>

<task type="auto">
  <name>Task 5: Verify .planning/ contains only active artifacts</name>
  <files>.planning/phases/05.8-repository-cleanup-hygiene/05.8-04-REFERENCE-UPDATES.md</files>
  <action>
    Verify .planning/ directory structure:

    **Should contain:**
    - ROADMAP.md, STATE.md, PROJECT.md
    - config.json
    - phases/ (phase plans and artifacts)
    - archive/ (completed phase archives)

    **Should NOT contain:**
    - Finished documentation (belongs in docs/)
    - Stale reference content
    - Duplicate information

    List any items that should be moved/removed.
  </action>
  <verify>
    - .planning/ directory audit complete
    - All non-planning content identified
    - Recommendations documented
  </verify>
  <done>.planning/ structure verified</done>
</task>

</tasks>
