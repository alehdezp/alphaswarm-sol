---
phase: 05.8-repository-cleanup-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05.8-repository-cleanup-hygiene/05.8-01-AUDIT.md
autonomous: true

must_haves:
  truths:
    - "Complete inventory of .true_vkg/ with categorized actions (DELETE/MOVE/AUDIT)"
    - "All content categorized per CONTEXT decisions"
    - "Repo-wide scan of .true_vkg references completed"
  artifacts:
    - path: ".planning/phases/05.8-repository-cleanup-hygiene/05.8-01-AUDIT.md"
      provides: "Audit with categorized inventory and reference map"
      min_lines: 150
---

<objective>
Audit .true_vkg/ and perform a repo-wide scan. Categorize all content per CONTEXT.md decisions.

**Categories per CONTEXT:**
- DELETE: regenerable caches, tracking files, execution artifacts
- MOVE: swc-to-vkg-mapping.yaml → vulndocs/.meta/references/
- AUDIT: scripts (compare against scripts/)
- SYNC: documentation overlaps (.planning/ → docs/)

Output: 05.8-01-AUDIT.md with categorized inventory and reference map.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05.8-repository-cleanup-hygiene/05.8-CONTEXT.md
</context>

<execution_guardrails>
- Full doc alignment: align objective, must_haves, and tasks against `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, plus phase context or research docs before execution. Any scope change must be reflected in the plan and `.planning/STATE.md`.
- Workspace isolation: run any commands or experiments that mutate state in a fresh jj workspace; do not use the main workspace and do not reuse workspaces for reruns.
- Repeat-run detection markers: include `[ALPHASWARM-START] RUN_ID TIMESTAMP` and `[ALPHASWARM-END] RUN_ID EXIT_CODE DURATION_MS` markers in transcripts or logs so reruns are detectable.
- claude-code-controller subject: for interactive runs, use `claude-code-controller` and launch Claude Code as the subject in the claude-code-agent-teams session; transcripts must show the Claude Code session.
- Seed scope boundaries: treat B-seeds (baseline or regression inputs) and N-seeds (net-new exploratory inputs) as separate scopes; do not mix them in a single run and track results separately.
</execution_guardrails>

<tasks>

<task type="auto">
  <name>Task 1: Inventory .true_vkg/ with CONTEXT-based categorization</name>
  <files>.planning/phases/05.8-repository-cleanup-hygiene/05.8-01-AUDIT.md</files>
  <action>
    Create categorized inventory of .true_vkg/:

    **DELETE - Regenerable Caches:**
    - crawl_cache/ (knowledge extracted to vulndocs)
    - filtered_cache/
    - llm_cache/

    **DELETE - Tracking Files:**
    - discovery/coverage-matrix.yaml (Phase 7 regenerates dynamically)
    - discovery/dvdefi-validation.yaml

    **DELETE - Execution Artifacts:**
    - Logs, progress reports
    - Crawl sessions, raw crawl content

    **MOVE - Valuable Content:**
    - discovery/swc-to-vkg-mapping.yaml → vulndocs/.meta/references/swc-mapping.yaml

    **AUDIT - Scripts:**
    - scripts/ (compare against existing scripts/)

    Document in "## Categorized Inventory" with file counts and sizes where available.
  </action>
  <verify>
    - Every .true_vkg/ subfolder categorized
    - Categories match CONTEXT.md decisions
    - File counts provided
  </verify>
  <done>Categorized inventory complete</done>
</task>

<task type="auto">
  <name>Task 2: Repo-wide .true_vkg reference scan</name>
  <files>.planning/phases/05.8-repository-cleanup-hygiene/05.8-01-AUDIT.md</files>
  <action>
    Scan for references to .true_vkg and legacy paths:

    ```bash
    rg -n "\.true_vkg" --type-add 'all:*' -t all
    rg -n "crawl_cache|filtered_cache|llm_cache" --type-add 'all:*' -t all
    rg -n "vulndocs_reference" --type-add 'all:*' -t all
    ```

    Record:
    - File path and line number
    - Reference type (code, doc, test, skill, config)
    - Required action (update path / remove reference)

    Document in "## Reference Map" section.
  </action>
  <verify>
    - All reference locations captured
    - Each reference has an action noted
  </verify>
  <done>Reference map complete</done>
</task>

<task type="auto">
  <name>Task 3: Documentation overlap analysis</name>
  <files>.planning/phases/05.8-repository-cleanup-hygiene/05.8-01-AUDIT.md</files>
  <action>
    Identify documentation that needs syncing per CONTEXT:

    1. List .planning/reference/ contents
    2. Compare with docs/ for overlaps
    3. Identify which .planning/ content is "polished/finished"
    4. Note what should be synced or moved

    Document in "## Documentation Analysis" section.
  </action>
  <verify>
    - .planning/reference/ files listed
    - Overlaps with docs/ identified
    - Sync/move recommendations provided
  </verify>
  <done>Documentation analysis complete</done>
</task>

<task type="auto">
  <name>Task 4: Summary statistics</name>
  <files>.planning/phases/05.8-repository-cleanup-hygiene/05.8-01-AUDIT.md</files>
  <action>
    Add summary at top of audit:

    | Category | Items | Est. Size | Action |
    |----------|-------|-----------|--------|
    | Caches   | N     | X MB      | DELETE |
    | Tracking | N     | X KB      | DELETE |
    | Artifacts| N     | X MB      | DELETE |
    | Scripts  | N     | X KB      | AUDIT  |
    | Mapping  | 1     | X KB      | MOVE   |
    | Docs     | N     | X KB      | SYNC   |

    Include total cleanup estimate.
  </action>
  <verify>
    - Summary table at top of audit
    - All categories represented
    - Totals calculated
  </verify>
  <done>Summary statistics complete</done>
</task>

</tasks>
