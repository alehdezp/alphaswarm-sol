---
phase: 06.1-rebrand-completion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/alphaswarm_sol/metrics/ci.py
  - src/alphaswarm_sol/metrics/recorder.py
  - src/alphaswarm_sol/metrics/event_store.py
  - src/alphaswarm_sol/metrics/calculator.py
  - src/alphaswarm_sol/metrics/storage.py
  - src/alphaswarm_sol/metrics/tracker.py
  - src/alphaswarm_sol/tools/config.py
  - src/alphaswarm_sol/tools/registry.py
  - src/alphaswarm_sol/benchmark/runner.py
  - src/alphaswarm_sol/benchmark/results.py
autonomous: true

must_haves:
  truths:
    - "All .vkg/ references replaced with .vrs/"
    - "vkg_version variable renamed to alphaswarm_version"
    - "No .vkg references remain in src/ Python files"
  artifacts:
    - path: "src/alphaswarm_sol/metrics/ci.py"
      provides: "Updated metrics config path"
      contains: ".vrs/metrics"
    - path: "src/alphaswarm_sol/tools/config.py"
      provides: "Updated tools config path"
      contains: ".vrs/tools.yaml"
---

<objective>
Update all .vkg/ config path references to .vrs/ in source code

Purpose: The rebrand specified .vrs/ as the config folder, but multiple modules still reference .vkg/. This creates inconsistency and potential runtime errors if users expect .vrs/.

Output: All source files updated to use .vrs/ config paths consistently.
</objective>

<execution_guardrails>
- Full doc alignment: align objective, must_haves, and tasks against `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, plus phase context or research docs before execution. Any scope change must be reflected in the plan and `.planning/STATE.md`.
- Workspace isolation: run any commands or experiments that mutate state in a fresh jj workspace; do not use the main workspace and do not reuse workspaces for reruns.
- Repeat-run detection markers: include `[ALPHASWARM-START] RUN_ID TIMESTAMP` and `[ALPHASWARM-END] RUN_ID EXIT_CODE DURATION_MS` markers in transcripts or logs so reruns are detectable.
- claude-code-controller subject: for interactive runs, use `claude-code-controller` and launch Claude Code as the subject in the claude-code-agent-teams session; transcripts must show the Claude Code session.
- Seed scope boundaries: treat B-seeds (baseline or regression inputs) and N-seeds (net-new exploratory inputs) as separate scopes; do not mix them in a single run and track results separately.
</execution_guardrails>

<tasks>

<task id="1">
<title>Update metrics module paths</title>
<instructions>
Update the following files to change .vkg/metrics → .vrs/metrics:

**src/alphaswarm_sol/metrics/ci.py:**
- Change: `storage_path: Path to metrics storage (default: .vkg/metrics)` → `.vrs/metrics`

**src/alphaswarm_sol/metrics/recorder.py:**
- Change: `".vkg/metrics"` → `".vrs/metrics"`

**src/alphaswarm_sol/metrics/event_store.py:**
- Change: `.vkg/metrics/events/` → `.vrs/metrics/events/`

**src/alphaswarm_sol/metrics/calculator.py:**
- Change: `".vkg/metrics"` → `".vrs/metrics"`

**src/alphaswarm_sol/metrics/storage.py:**
- Change: `".vkg/metrics"` → `".vrs/metrics"`

**src/alphaswarm_sol/metrics/tracker.py:**
- Change: `".vkg/metrics"` → `".vrs/metrics"`
</instructions>
</task>

<task id="2">
<title>Update tools module paths</title>
<instructions>
Update the following files:

**src/alphaswarm_sol/tools/config.py:**
Multiple occurrences to change:
- `.vkg/tools.yaml` → `.vrs/tools.yaml`
- All docstring references
- DEFAULT_CONFIG_FILE constant
- config_path construction

**src/alphaswarm_sol/tools/registry.py:**
- Change: `.vkg/install-tools.sh` → `.vrs/install-tools.sh`
</instructions>
</task>

<task id="3">
<title>Update benchmark module variables</title>
<instructions>
Update the following files:

**src/alphaswarm_sol/benchmark/runner.py:**
- Rename variable: `results.vkg_version` → `results.alphaswarm_version`
- Update: `version("alphaswarm")` call if needed

**src/alphaswarm_sol/benchmark/results.py:**
- Rename field: `vkg_version` → `alphaswarm_version`
- Update serialization: `"vkg_version"` → `"alphaswarm_version"`
</instructions>
</task>

<task id="4">
<title>Verify no .vkg references remain</title>
<instructions>
Run verification:
```bash
# Should return 0 results for .vkg path references
grep -r "\.vkg/" src/alphaswarm_sol/ --include="*.py"

# Should return 0 results for vkg_ variable prefixes
grep -r "vkg_version" src/alphaswarm_sol/ --include="*.py"
```

If any results, fix them.
</instructions>
</task>

<task id="5">
<title>Run tests to verify nothing broke</title>
<instructions>
Run relevant tests:
```bash
uv run pytest tests/test_*metrics*.py tests/test_*tools*.py tests/test_*benchmark*.py -v --tb=short
```

If tests fail due to path changes, the tests themselves may need updating.
</instructions>
</task>

</tasks>

<verification>
- [ ] `grep -r "\.vkg/" src/alphaswarm_sol/ --include="*.py"` returns 0 results
- [ ] `grep -r "vkg_version" src/alphaswarm_sol/ --include="*.py"` returns 0 results
- [ ] All modified files have .vrs/ paths
- [ ] Tests pass or are updated to match new paths
</verification>

