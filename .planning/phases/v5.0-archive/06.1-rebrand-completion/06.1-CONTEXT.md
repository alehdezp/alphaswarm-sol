# Phase 6.1: Rebrand Completion - Context

**Gathered:** 2026-01-22
**Status:** Ready for planning
**Parent Phase:** 6 (Release Preparation)
**Reason:** Phase 6 verification revealed incomplete rebrand

<domain>
## Phase Boundary

Complete the rebrand from VKG/true_vkg to VRS/alphaswarm_sol that was started in Phase 6 but left incomplete. This phase fixes:

1. Agent files in skills/shipped/agents/ still named vkg-*
2. Config directory references still using .vkg/ instead of .vrs/
3. VKG in filenames (tests, docs, scripts)
4. MkDocs artifacts that user explicitly doesn't want
5. Legacy .true_vkg/ directory cleanup

This is a cleanup phase - no new features, just completing the rebrand properly.

</domain>

<decisions>
## Implementation Decisions

### Naming Consistency

Per 06-CONTEXT.md decisions that were not fully implemented:

| Component | From | To | Status |
|-----------|------|-----|--------|
| Agent files | vkg-*.md | vrs-*.md | INCOMPLETE |
| Config folder | .vkg/ | .vrs/ | INCOMPLETE |
| Variable names | vkg_* | alphaswarm_* or vrs_* | INCOMPLETE |

### Files to Rename

**Agent files (src/alphaswarm_sol/skills/shipped/agents/):**
- vkg-attacker.md → vrs-attacker.md
- vkg-defender.md → vrs-defender.md
- vkg-integrator.md → vrs-integrator.md
- vkg-supervisor.md → vrs-supervisor.md
- vkg-verifier.md → vrs-verifier.md

**Test files:**
- tests/test_vkg_enhancements.py → tests/test_bskg_enhancements.py
- tests/test_semgrep_vkg_parity.py → tests/test_semgrep_parity.py

**Doc/script files:**
- docs/tools/vkg-crawl.md → docs/tools/alphaswarm-crawl.md OR archive
- scripts/vkg_crawl.py → scripts/alphaswarm_crawl.py OR archive
- schemas/vkg-codex-output.json → schemas/alphaswarm-codex-output.json

### Code Changes

**Config path references (.vkg → .vrs):**

Files in src/alphaswarm_sol/metrics/:
```python
# Change all occurrences of:
".vkg/metrics" → ".vrs/metrics"
```

Files in src/alphaswarm_sol/tools/:
```python
# Change all occurrences of:
".vkg/tools.yaml" → ".vrs/tools.yaml"
".vkg/install-tools.sh" → ".vrs/install-tools.sh"
```

Files in src/alphaswarm_sol/benchmark/:
```python
# Change variable name:
vkg_version → alphaswarm_version
```

### MkDocs Removal

User explicitly stated they don't want MkDocs. Remove:
- mkdocs.yml
- site/ directory
- .github/workflows/docs.yml (or update to deploy research paper)

### Legacy Cleanup

- Remove .true_vkg/ directory (or add to .gitignore if needed for local caching)

### Verification

After all changes:
1. Run `grep -r "\.vkg" src/` - should return 0 results
2. Run `grep -r "vkg-" src/alphaswarm_sol/skills/` - should return 0 results
3. Run tests to ensure nothing broke
4. Verify CLI still works: `alphaswarm --version`

</decisions>

<deferred>
## Deferred

- .planning/intel/entities/true-vkg-* files (22+ files) - internal planning docs, low priority
- Any remaining VKG references in archived docs
- Historical git commit messages (cannot change)

</deferred>

---

*Phase: 06.1-rebrand-completion*
*Context gathered: 2026-01-22*
*Verification source: Phase 6 audit via 06-CONTEXT.md requirements*
