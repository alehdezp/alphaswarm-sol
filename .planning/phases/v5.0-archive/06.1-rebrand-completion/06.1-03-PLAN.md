---
phase: 06.1-rebrand-completion
plan: 03
type: execute
wave: 2
depends_on: [06.1-01, 06.1-02]
files_modified:
  - mkdocs.yml (deleted)
  - site/ (deleted)
  - .github/workflows/docs.yml (updated or deleted)
  - .gitignore (updated)
  - .true_vkg/ (deleted or gitignored)
autonomous: true

must_haves:
  truths:
    - "MkDocs artifacts removed (mkdocs.yml, site/)"
    - "docs.yml workflow updated or removed"
    - "Legacy .true_vkg/ directory cleaned up"
    - ".gitignore updated to prevent accidental commits of build artifacts"
  artifacts:
    - path: ".gitignore"
      provides: "Updated ignore patterns"
      contains: ".vrs/"
---

<objective>
Remove MkDocs artifacts and clean up legacy directories

Purpose: User explicitly stated they don't want MkDocs - they want the research paper format. MkDocs artifacts exist from old plans and should be removed. Also clean up legacy .true_vkg/ directory.

Output: Clean repository with no MkDocs artifacts and no legacy directories.
</objective>

<execution_guardrails>
- Full doc alignment: align objective, must_haves, and tasks against `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, plus phase context or research docs before execution. Any scope change must be reflected in the plan and `.planning/STATE.md`.
- Workspace isolation: run any commands or experiments that mutate state in a fresh jj workspace; do not use the main workspace and do not reuse workspaces for reruns.
- Repeat-run detection markers: include `[ALPHASWARM-START] RUN_ID TIMESTAMP` and `[ALPHASWARM-END] RUN_ID EXIT_CODE DURATION_MS` markers in transcripts or logs so reruns are detectable.
- claude-code-controller subject: for interactive runs, use `claude-code-controller` and launch Claude Code as the subject in the claude-code-agent-teams session; transcripts must show the Claude Code session.
- Seed scope boundaries: treat B-seeds (baseline or regression inputs) and N-seeds (net-new exploratory inputs) as separate scopes; do not mix them in a single run and track results separately.
</execution_guardrails>

<tasks>

<task id="1">
<title>Remove MkDocs configuration and build output</title>
<instructions>
Remove MkDocs artifacts:
```bash
rm mkdocs.yml
rm -rf site/
```

These are artifacts from the rejected Phase 6 plan that created MkDocs documentation.
</instructions>
</task>

<task id="2">
<title>Update or remove docs.yml workflow</title>
<instructions>
The .github/workflows/docs.yml currently deploys MkDocs to GitHub Pages.

Option A (Remove): If no documentation deployment is needed for now:
```bash
rm .github/workflows/docs.yml
```

Option B (Update): If you want to deploy the research paper instead, update the workflow to:
- Build a simple static site from docs/paper/
- Or just disable the workflow temporarily

Recommend Option A for now - documentation deployment can be added later if needed.
</instructions>
</task>

<task id="3">
<title>Clean up legacy .true_vkg/ directory</title>
<instructions>
Check what's in .true_vkg/:
```bash
ls -la .true_vkg/
```

If it contains:
- Cache files: Can be safely deleted or gitignored
- Discovery/mapping files: Review if still needed

Likely action:
```bash
rm -rf .true_vkg/
```

Or if needed for local development, add to .gitignore:
```
.true_vkg/
```
</instructions>
</task>

<task id="4">
<title>Update .gitignore</title>
<instructions>
Ensure .gitignore has proper entries:
```
# Build outputs
site/
dist/
*.egg-info/

# Local config directories
.vrs/
.true_vkg/

# Cache
__pycache__/
.pytest_cache/
```

Remove any references to old directories that no longer exist.
</instructions>
</task>

<task id="5">
<title>Archive or rename remaining VKG-named files</title>
<instructions>
Handle remaining files with vkg in name:

**docs/tools/vkg-crawl.md:**
- If still relevant: rename to alphaswarm-crawl.md
- If obsolete: move to docs/.archive/

**scripts/vkg_crawl.py:**
- If still relevant: rename to alphaswarm_crawl.py and update imports
- If obsolete: delete or archive

**schemas/vkg-codex-output.json:**
- If still relevant: rename to alphaswarm-codex-output.json
- If obsolete: archive

Check each file's usage before deciding.
</instructions>
</task>

<task id="6">
<title>Final verification</title>
<instructions>
Run final checks:
```bash
# No MkDocs artifacts
ls mkdocs.yml 2>/dev/null && echo "FAIL: mkdocs.yml exists" || echo "OK"
ls -d site/ 2>/dev/null && echo "FAIL: site/ exists" || echo "OK"

# No .true_vkg in git
git ls-files | grep "\.true_vkg" && echo "FAIL: .true_vkg tracked" || echo "OK"

# No vkg in new filenames (excluding archives and planning)
find . -name "*vkg*" -type f 2>/dev/null | grep -v ".git" | grep -v ".archive" | grep -v ".planning" | grep -v "__pycache__"
```
</instructions>
</task>

</tasks>

<verification>
- [ ] mkdocs.yml does not exist
- [ ] site/ directory does not exist
- [ ] .github/workflows/docs.yml removed or updated
- [ ] .true_vkg/ removed or gitignored
- [ ] .gitignore updated with .vrs/ and other relevant entries
- [ ] No VKG-named files in active directories (archives OK)
</verification>

