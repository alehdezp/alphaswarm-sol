---
phase: 07.3.1.5-full-testing-orchestrator
plan: "09"
type: execute
wave: 3
depends_on: ["03", "05", "08"]
files_modified:
  - src/alphaswarm_sol/testing/failure_catalog.py
  - src/alphaswarm_sol/testing/self_improving_loop.py
  - scripts/e2e/run_failure_injection.sh
autonomous: true

must_haves:
  truths:
    - "Failure taxonomy is explicit and machine-readable"
    - "Self-improving loop auto-triages and re-runs"
    - "Failure injection is part of the full-testing suite"
  artifacts:
    - path: "src/alphaswarm_sol/testing/failure_catalog.py"
      provides: "Failure categories and remediation map"
      contains: "FAILURE_"
    - path: "src/alphaswarm_sol/testing/self_improving_loop.py"
      provides: "Retry and re-run orchestrator"
      contains: "requeue"
    - path: "scripts/e2e/run_failure_injection.sh"
      provides: "Failure injection runner"
      contains: "inject"
  key_links:
    - from: "src/alphaswarm_sol/testing/self_improving_loop.py"
      to: "src/alphaswarm_sol/testing/failure_catalog.py"
      via: "triage"
      pattern: "failure"
---

<objective>
Add a failure taxonomy, a self‑improving retry loop, and explicit failure‑injection runs to harden /vrs-full-testing.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@skills/vrs-claude-code-agent-teams-testing.md
@.planning/phases/07.3.1.5-full-testing-orchestrator/07.3.1.5-CRITIQUE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define failure taxonomy</name>
  <files>
    src/alphaswarm_sol/testing/failure_catalog.py
  </files>
  <action>
Define failure categories (infra, timeout, missing marker, tool error, context error, label error) and remediation suggestions.
  </action>
  <verify>
Ensure categories are enumerated and include remediation hints.
  </verify>
  <done>
Failure catalog exists with categories and remediation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement self-improving loop</name>
  <files>
    src/alphaswarm_sol/testing/self_improving_loop.py
  </files>
  <action>
Implement a loop that:
- inspects failure catalog
- queues targeted re-runs in new worktrees
- records improvements and deltas
  </action>
  <verify>
Review the loop for requeue logic and failure classification.
  </verify>
  <done>
Self-improving loop exists with requeue logic.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add failure injection runner</name>
  <files>
    scripts/e2e/run_failure_injection.sh
  </files>
  <action>
Create a runner that injects:
- missing tools
- corrupted graphs
- missing context
- invalid labels

and verifies recovery behavior.
  </action>
  <verify>
Run with --dry-run and confirm it lists injected failure cases.
  </verify>
  <done>
Failure injection runner exists with dry-run output.
  </done>
</task>

</tasks>

## Implementation Details (v2)
- Failure catalog must map category → remediation → rerun strategy (same scenario vs. subset).
- Self-improving loop records delta notes per iteration with evidence pack IDs.
- Failure injection must be real and verified via re-run (no simulated success).

## Strict Quality Controls
- Enforce claude-code-controller usage as a hard gate; missing markers invalidate the run.
  **Full claude-code-controller documentation:** See `.planning/testing/rules/claude-code-controller-REFERENCE.md` and `.planning/testing/rules/canonical/claude-code-controller-instructions.md`
- Validate debug-mode artifacts for every skill/agent per the debug matrix.
- Evidence packs are mandatory inputs to gate evaluation; missing packs fail the run.

## Iteration Protocol (max 10, real runs only)
- Run up to 10 real iterations per phase: run → evidence pack → gates → failure classification → fix → re-run.
- Any missing evidence pack artifacts, missing claude-code-agent-teams markers, or duration <= 5000ms is an automatic fail.
- No simulated results, no fabricated metrics, no mock detections; only real claude-code-agent-teams-driven runs count.
- After 10 failed iterations, stop and produce a postmortem with evidence pack links and remediation plan.

## Core Philosophy (Must Internalize)
- Real-world, human-operated CLI testing is mandatory; simulations and mocks are invalid for validation.
- Claude Code must operate as the human operator via `claude-code-controller` in `alphaswarm-claude-code-agent-teams-session`.
- Every workflow and subworkflow requires real claude-code-agent-teams transcripts and evidence packs.
- See `phases/07.3.1.5-full-testing-orchestrator/07.3.1.5-CORE-PHILOSOPHY.md`.

