---
phase: 07.3.1.5-full-testing-orchestrator
plan: "14"
type: execute
wave: 2
depends_on: ["01", "02"]
files_modified:
  - scripts/e2e/validate_claude_code_controller_usage.py
  - configs/claude_code_controller_markers.yaml
  - .planning/phases/07.3.1.5-full-testing-orchestrator/07.3.1.5-agent-teams-enforcement.md
autonomous: true

must_haves:
  truths:
    - "claude-code-controller usage is enforced as a hard gate"
    - "Validator fails if claude-code-controller is not used"
    - "Marker file defines required claude-code-controller evidence"
  artifacts:
    - path: "scripts/e2e/validate_claude_code_controller_usage.py"
      provides: "claude-code-controller usage validator"
      contains: "claude-code-controller"
    - path: "configs/claude_code_controller_markers.yaml"
      provides: "claude-code-controller evidence markers"
      contains: "markers"
    - path: ".planning/phases/07.3.1.5-full-testing-orchestrator/07.3.1.5-agent-teams-enforcement.md"
      provides: "claude-code-controller enforcement policy"
      contains: "fail"
  key_links:
    - from: "configs/claude_code_controller_markers.yaml"
      to: "scripts/e2e/validate_claude_code_controller_usage.py"
      via: "marker validation"
      pattern: "claude-code-agent-teams"
---

<objective>
Enforce claude-code-controller usage by adding a validator, a marker definition file, and a hard enforcement policy.
**Full claude-code-controller documentation:** See `.planning/testing/rules/claude-code-controller-REFERENCE.md` and `.planning/testing/rules/canonical/claude-code-controller-instructions.md`
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.3.1.5-full-testing-orchestrator/07.3.1.5-CRITIQUE.md
@.planning/phases/07.3.1.5-full-testing-orchestrator/07.3.1.5-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define claude-code-controller enforcement policy</name>
  <files>
    .planning/phases/07.3.1.5-full-testing-orchestrator/07.3.1.5-agent-teams-enforcement.md
  </files>
  <action>
Write a policy that declares all full-testing runs invalid if claude-code-controller is not invoked, and lists acceptable evidence signals.
  </action>
  <verify>
Confirm policy includes explicit fail conditions for missing claude-code-controller usage.
  </verify>
  <done>
claude-code-agent-teams enforcement policy exists with fail rules.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create claude-code-controller marker definition</name>
  <files>
    configs/claude_code_controller_markers.yaml
  </files>
  <action>
Define required markers indicating claude-code-controller usage, including:
- claude-code-agent-teams session creation
- pane capture actions
- session shutdown
  </action>
  <verify>
Ensure marker file lists required claude-code-controller signals.
  </verify>
  <done>
Marker file exists with claude-code-controller signals.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement claude-code-controller usage validator</name>
  <files>
    scripts/e2e/validate_claude_code_controller_usage.py
  </files>
  <action>
Implement a validator that scans evidence packs and claude-code-agent-teams transcripts for claude-code-controller markers and fails runs missing them.
  </action>
  <verify>
Confirm the validator exits non-zero when markers are missing.
  </verify>
  <done>
Validator exists and enforces claude-code-controller usage.
  </done>
</task>

</tasks>

## Implementation Details (v2)
- Enforcement policy must declare claude-code-agent-teams usage a hard gate.
- Marker definitions must include session create/capture/teardown with socket ID.
- Validator must fail even if all other gates pass when claude-code-agent-teams markers are missing.

## Strict Quality Controls
- Enforce claude-code-controller usage as a hard gate; missing markers invalidate the run.
- Validate debug-mode artifacts for every skill/agent per the debug matrix.
- Evidence packs are mandatory inputs to gate evaluation; missing packs fail the run.

## Iteration Protocol (max 10, real runs only)
- Run up to 10 real iterations per phase: run → evidence pack → gates → failure classification → fix → re-run.
- Any missing evidence pack artifacts, missing claude-code-agent-teams markers, or duration <= 5000ms is an automatic fail.
- No simulated results, no fabricated metrics, no mock detections; only real claude-code-agent-teams-driven runs count.
- After 10 failed iterations, stop and produce a postmortem with evidence pack links and remediation plan.

## Core Philosophy (Must Internalize)
- Real-world, human-operated CLI testing is mandatory; simulations and mocks are invalid for validation.
- Claude Code must operate as the human operator via `claude-code-controller` in `alphaswarm-claude-code-agent-teams-session`.
- Every workflow and subworkflow requires real claude-code-agent-teams transcripts and evidence packs.
- See `phases/07.3.1.5-full-testing-orchestrator/07.3.1.5-CORE-PHILOSOPHY.md`.

