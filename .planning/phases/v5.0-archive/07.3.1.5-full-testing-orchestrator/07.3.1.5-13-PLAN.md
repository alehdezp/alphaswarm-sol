---
phase: 07.3.1.5-full-testing-orchestrator
plan: "13"
type: execute
wave: 6
depends_on: ["06", "11", "12"]
files_modified:
  - .planning/phases/07.3.1.5-full-testing-orchestrator/07.3.1.5-REPORTING.md
  - scripts/e2e/render_full_testing_dashboard.py
  - tests/e2e/expected/full_testing_report_markers.yaml
autonomous: true

must_haves:
  truths:
    - "Full-testing results are summarized in a dashboard"
    - "Report includes gate status and scenario coverage"
    - "Report is validated against markers"
  artifacts:
    - path: ".planning/phases/07.3.1.5-full-testing-orchestrator/07.3.1.5-REPORTING.md"
      provides: "Reporting spec"
      contains: "coverage"
    - path: "scripts/e2e/render_full_testing_dashboard.py"
      provides: "Dashboard renderer"
      contains: "render"
    - path: "tests/e2e/expected/full_testing_report_markers.yaml"
      provides: "Report markers"
      contains: "gate_status"
  key_links:
    - from: "scripts/e2e/render_full_testing_dashboard.py"
      to: "tests/e2e/expected/full_testing_report_markers.yaml"
      via: "marker validation"
      pattern: "report"
---

<objective>
Provide a dashboard and report spec for /vrs-full-testing results, with marker validation and coverage metrics.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.3.1.5-full-testing-orchestrator/07.3.1.5-SCENARIO-MATRIX.md
@.planning/phases/07.3.1.5-full-testing-orchestrator/07.3.1.5-GATES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define reporting spec</name>
  <files>
    .planning/phases/07.3.1.5-full-testing-orchestrator/07.3.1.5-REPORTING.md
  </files>
  <action>
Define report sections including:
- gate status summary
- scenario coverage heatmap
- debug-mode compliance
- failure taxonomy breakdown
  </action>
  <verify>
Confirm report spec includes gate status and coverage.
  </verify>
  <done>
Reporting spec exists with required sections.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement dashboard renderer</name>
  <files>
    scripts/e2e/render_full_testing_dashboard.py
  </files>
  <action>
Implement a renderer that reads evidence packs and outputs a markdown dashboard.
  </action>
  <verify>
Ensure renderer outputs gate status and scenario coverage.
  </verify>
  <done>
Dashboard renderer exists.
  </done>
</task>

<task type="auto">
  <name>Task 3: Define report markers</name>
  <files>
    tests/e2e/expected/full_testing_report_markers.yaml
  </files>
  <action>
Define markers for the report output to allow automated validation.
  </action>
  <verify>
Confirm markers include gate_status and coverage fields.
  </verify>
  <done>
Report markers exist.
  </done>
</task>

</tasks>

## Implementation Details (v2)
- Dashboard must include gate status, scenario coverage, debug compliance, and failure breakdown.
- Add “no perfect metrics” check; perfect precision/recall triggers investigation.
- Report markers must allow automated validation of coverage and gate summaries.

## Strict Quality Controls
- Enforce claude-code-controller usage as a hard gate; missing markers invalidate the run.
  **Full claude-code-controller documentation:** See `.planning/testing/rules/claude-code-controller-REFERENCE.md` and `.planning/testing/rules/canonical/claude-code-controller-instructions.md`
- Validate debug-mode artifacts for every skill/agent per the debug matrix.
- Evidence packs are mandatory inputs to gate evaluation; missing packs fail the run.

## Iteration Protocol (max 10, real runs only)
- Run up to 10 real iterations per phase: run → evidence pack → gates → failure classification → fix → re-run.
- Any missing evidence pack artifacts, missing claude-code-agent-teams markers, or duration <= 5000ms is an automatic fail.
- No simulated results, no fabricated metrics, no mock detections; only real claude-code-agent-teams-driven runs count.
- After 10 failed iterations, stop and produce a postmortem with evidence pack links and remediation plan.

## Core Philosophy (Must Internalize)
- Real-world, human-operated CLI testing is mandatory; simulations and mocks are invalid for validation.
- Claude Code must operate as the human operator via `claude-code-controller` in `alphaswarm-claude-code-agent-teams-session`.
- Every workflow and subworkflow requires real claude-code-agent-teams transcripts and evidence packs.
- See `phases/07.3.1.5-full-testing-orchestrator/07.3.1.5-CORE-PHILOSOPHY.md`.

