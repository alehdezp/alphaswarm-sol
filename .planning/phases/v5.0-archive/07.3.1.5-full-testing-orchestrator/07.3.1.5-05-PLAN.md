---
phase: 07.3.1.5-full-testing-orchestrator
plan: "05"
type: execute
wave: 3
depends_on: ["03", "04"]
files_modified:
  - tests/e2e/full_testing_golden.yaml
  - tests/e2e/expected/full_testing_markers.yaml
  - scripts/e2e/run_full_testing_golden.sh
autonomous: true

must_haves:
  truths:
    - "Golden transcript baselines are captured and diffable"
    - "Regression markers cover each subworkflow"
    - "Runner can run record vs diff modes"
  artifacts:
    - path: "tests/e2e/full_testing_golden.yaml"
      provides: "Golden transcript scenarios"
      contains: "record|diff"
    - path: "tests/e2e/expected/full_testing_markers.yaml"
      provides: "Expected markers for subworkflows"
      contains: "command_suite|agent_suite"
    - path: "scripts/e2e/run_full_testing_golden.sh"
      provides: "Golden run wrapper"
      contains: "record"
  key_links:
    - from: "tests/e2e/full_testing_golden.yaml"
      to: "scripts/e2e/run_full_testing_golden.sh"
      via: "mode"
      pattern: "record"
---

<objective>
Add golden-transcript capture and diffing for the full-testing orchestrator to detect regressions in real-world workflow behavior.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.3.1.5-full-testing-orchestrator/07.3.1.5-CONTEXT.md
@.planning/phases/07.3.1.5-full-testing-orchestrator/07.3.1.5-ORCHESTRATOR-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define golden transcript scenarios</name>
  <files>
    tests/e2e/full_testing_golden.yaml
  </files>
  <action>
Create a YAML file that declares:
- scenario_id
- record mode baseline location
- diff mode comparison rules

Include at least 2 scenarios for baseline capture.
  </action>
  <verify>
Ensure each scenario has record and diff sections.
  </verify>
  <done>
Golden transcript config exists with record/diff sections.
  </done>
</task>

<task type="auto">
  <name>Task 2: Define expected markers for full-testing</name>
  <files>
    tests/e2e/expected/full_testing_markers.yaml
  </files>
  <action>
Define expected markers and artifact checks for each subworkflow:
- command suite
- agent suite
- context suite
- resume matrix

Include error markers for failure classification.
  </action>
  <verify>
Confirm markers cover all subworkflows.
  </verify>
  <done>
Markers file includes all subworkflows.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement golden runner</name>
  <files>
    scripts/e2e/run_full_testing_golden.sh
  </files>
  <action>
Implement a runner that:
- executes /vrs-full-testing in record mode
- captures transcripts and evidence packs
- runs diff mode against baseline
  </action>
  <verify>
Run with --dry-run to confirm record and diff modes.
  </verify>
  <done>
Golden runner exists with record/diff output.
  </done>
</task>

</tasks>

## Implementation Details (v2)
- Golden config must define diff tolerance rules (timing variance, non-deterministic lines).
- Marker file must include one marker per subworkflow and explicit failure markers.
- Runner must stop on first diff mismatch and emit evidence pack paths.

## Strict Quality Controls
- Enforce claude-code-controller usage as a hard gate; missing markers invalidate the run.
  **Full claude-code-controller documentation:** See `.planning/testing/rules/claude-code-controller-REFERENCE.md` and `.planning/testing/rules/canonical/claude-code-controller-instructions.md`
- Validate debug-mode artifacts for every skill/agent per the debug matrix.
- Evidence packs are mandatory inputs to gate evaluation; missing packs fail the run.

## Iteration Protocol (max 10, real runs only)
- Run up to 10 real iterations per phase: run → evidence pack → gates → failure classification → fix → re-run.
- Any missing evidence pack artifacts, missing claude-code-agent-teams markers, or duration <= 5000ms is an automatic fail.
- No simulated results, no fabricated metrics, no mock detections; only real claude-code-agent-teams-driven runs count.
- After 10 failed iterations, stop and produce a postmortem with evidence pack links and remediation plan.

## Core Philosophy (Must Internalize)
- Real-world, human-operated CLI testing is mandatory; simulations and mocks are invalid for validation.
- Claude Code must operate as the human operator via `claude-code-controller` in `alphaswarm-claude-code-agent-teams-session`.
- Every workflow and subworkflow requires real claude-code-agent-teams transcripts and evidence packs.
- See `phases/07.3.1.5-full-testing-orchestrator/07.3.1.5-CORE-PHILOSOPHY.md`.
