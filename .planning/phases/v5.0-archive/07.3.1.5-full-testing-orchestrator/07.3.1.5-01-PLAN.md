---
phase: 07.3.1.5-full-testing-orchestrator
plan: "01"
type: execute
wave: 1
depends_on: ["00"]
files_modified:
  - skills/vrs-full-testing.md
  - src/alphaswarm_sol/skills/registry.yaml
  - configs/skill_tool_policies.yaml
autonomous: true

must_haves:
  truths:
    - "/vrs-full-testing is defined as a user-invocable skill"
    - "Skill is registered with correct versioning and guardrails"
    - "Tool policy allows claude-code-controller and required VRS tools"
    **Full claude-code-controller documentation:** See `.planning/testing/rules/claude-code-controller-REFERENCE.md` and `.planning/testing/rules/canonical/claude-code-controller-instructions.md`
  artifacts:
    - path: "skills/vrs-full-testing.md"
      provides: "Full-testing skill definition"
      contains: "invocation: /vrs-full-testing"
    - path: "src/alphaswarm_sol/skills/registry.yaml"
      provides: "Skill registry entry"
      contains: "vrs-full-testing"
    - path: "configs/skill_tool_policies.yaml"
      provides: "Tool access policy"
      contains: "claude-code-controller"
  key_links:
    - from: "skills/vrs-full-testing.md"
      to: "configs/skill_tool_policies.yaml"
      via: "tool gating"
      pattern: "claude-code-agent-teams"
---

<objective>
Create the /vrs-full-testing skill, register it, and grant it the required tool access for claude-code-agent-teams-driven orchestration.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.3.1.5-full-testing-orchestrator/07.3.1.5-ORCHESTRATOR-SPEC.md
@.planning/phases/07.3.1.5-full-testing-orchestrator/07.3.1.5-DEBUG-MODE-SPEC.md
@skills/vrs-claude-code-agent-teams-testing.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add /vrs-full-testing skill file</name>
  <files>
    skills/vrs-full-testing.md
  </files>
  <action>
Create a new skill file that:
- declares invocation /vrs-full-testing
- lists required subworkflows
- requires claude-code-controller usage
- emits evidence pack paths
- supports flags: --scenario, --suite, --resume, --debug
  </action>
  <verify>
Open the skill file and confirm it includes /vrs-full-testing invocation and debug flag.
  </verify>
  <done>
Skill file exists with invocation, flags, and claude-code-agent-teams requirement.
  </done>
</task>

<task type="auto">
  <name>Task 2: Register the skill</name>
  <files>
    src/alphaswarm_sol/skills/registry.yaml
  </files>
  <action>
Add a registry entry for vrs-full-testing with version, status, and documentation path.
  </action>
  <verify>
Confirm registry entry references skills/vrs-full-testing.md.
  </verify>
  <done>
Registry includes vrs-full-testing entry.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update tool policy for claude-code-controller</name>
  <files>
    configs/skill_tool_policies.yaml
  </files>
  <action>
Ensure the full-testing skill is allowed to use claude-code-controller and required tool runners.
  </action>
  <verify>
Confirm policy lists vrs-full-testing with claude-code-controller access.
  </verify>
  <done>
Tool policy updated for vrs-full-testing.
  </done>
</task>

</tasks>

## Implementation Details (v2)
- Skill file must hard-fail if claude-code-controller is not invoked and require evidence pack paths in outputs.
- Registry entry must include version, status, and guardrail references (debug enforcement + claude-code-agent-teams enforcement).
- Tool policy should be allowlist-only; deny all tools not required by full-testing.

## Strict Quality Controls
- Enforce claude-code-controller usage as a hard gate; missing markers invalidate the run.
- Validate debug-mode artifacts for every skill/agent per the debug matrix.
- Evidence packs are mandatory inputs to gate evaluation; missing packs fail the run.

## Iteration Protocol (max 10, real runs only)
- Run up to 10 real iterations per phase: run → evidence pack → gates → failure classification → fix → re-run.
- Any missing evidence pack artifacts, missing claude-code-agent-teams markers, or duration <= 5000ms is an automatic fail.
- No simulated results, no fabricated metrics, no mock detections; only real claude-code-agent-teams-driven runs count.
- After 10 failed iterations, stop and produce a postmortem with evidence pack links and remediation plan.

## Core Philosophy (Must Internalize)
- Real-world, human-operated CLI testing is mandatory; simulations and mocks are invalid for validation.
- Claude Code must operate as the human operator via `claude-code-controller` in `alphaswarm-claude-code-agent-teams-session`.
- Every workflow and subworkflow requires real claude-code-agent-teams transcripts and evidence packs.
- See `phases/07.3.1.5-full-testing-orchestrator/07.3.1.5-CORE-PHILOSOPHY.md`.

