---
phase: 07.3.1.5-full-testing-orchestrator
plan: "15"
type: execute
wave: 3
depends_on: ["01", "07"]
files_modified:
  - .planning/phases/07.3.1.5-full-testing-orchestrator/07.3.1.5-WORKTREE-SNAPSHOT-MATRIX.md
  - configs/worktree_snapshot_policy.yaml
  - scripts/e2e/validate_snapshot_matrix.py
autonomous: true

must_haves:
  truths:
    - "Snapshot matrix defines required stage checkpoints"
    - "Worktree snapshot policy is machine-readable"
    - "Validator enforces snapshot completeness"
  artifacts:
    - path: ".planning/phases/07.3.1.5-full-testing-orchestrator/07.3.1.5-WORKTREE-SNAPSHOT-MATRIX.md"
      provides: "Snapshot matrix by scenario and stage"
      contains: "graph|context|patterns"
    - path: "configs/worktree_snapshot_policy.yaml"
      provides: "Snapshot policy definition"
      contains: "required_stages"
    - path: "scripts/e2e/validate_snapshot_matrix.py"
      provides: "Snapshot validator"
      contains: "validate_snapshots"
  key_links:
    - from: "configs/worktree_snapshot_policy.yaml"
      to: "scripts/e2e/validate_snapshot_matrix.py"
      via: "policy enforcement"
      pattern: "snapshot"
---

<objective>
Codify the worktree snapshot matrix and enforce required stage checkpoints for all scenarios.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.3.1.5-full-testing-orchestrator/07.3.1.5-SCENARIO-MATRIX.md
@.planning/phases/07.3.1.5-full-testing-orchestrator/07.3.1.5-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create snapshot matrix doc</name>
  <files>
    .planning/phases/07.3.1.5-full-testing-orchestrator/07.3.1.5-WORKTREE-SNAPSHOT-MATRIX.md
  </files>
  <action>
Document required snapshot checkpoints per scenario:
- pre-graph
- post-graph
- post-context
- post-patterns
- post-agents
- post-debate
- post-report
  </action>
  <verify>
Confirm the matrix includes all stages for at least the top 20 scenarios.
  </verify>
  <done>
Snapshot matrix exists with stage checkpoints.
  </done>
</task>

<task type="auto">
  <name>Task 2: Define snapshot policy config</name>
  <files>
    configs/worktree_snapshot_policy.yaml
  </files>
  <action>
Create a policy file listing required stages per scenario category (reentrancy, access, oracle, etc.).
  </action>
  <verify>
Ensure policy maps categories to required stages.
  </verify>
  <done>
Snapshot policy exists with category-stage mapping.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement snapshot validator</name>
  <files>
    scripts/e2e/validate_snapshot_matrix.py
  </files>
  <action>
Implement a validator that reads the snapshot policy and verifies snapshot presence for each scenario in the evidence pack.
  </action>
  <verify>
Confirm validator reports missing snapshots and exits non-zero on failure.
  </verify>
  <done>
Snapshot validator exists with missing snapshot reporting.
  </done>
</task>

</tasks>

## Implementation Details (v2)
- Snapshot matrix must require all stages: pre/post graph, context, patterns, agents, debate, report.
- Policy must map scenario categories to required stages.
- Validator must report missing stages per scenario and fail the run.

## Strict Quality Controls
- Enforce claude-code-controller usage as a hard gate; missing markers invalidate the run.
  **Full claude-code-controller documentation:** See `.planning/testing/rules/claude-code-controller-REFERENCE.md` and `.planning/testing/rules/canonical/claude-code-controller-instructions.md`
- Validate debug-mode artifacts for every skill/agent per the debug matrix.
- Evidence packs are mandatory inputs to gate evaluation; missing packs fail the run.

## Iteration Protocol (max 10, real runs only)
- Run up to 10 real iterations per phase: run → evidence pack → gates → failure classification → fix → re-run.
- Any missing evidence pack artifacts, missing claude-code-agent-teams markers, or duration <= 5000ms is an automatic fail.
- No simulated results, no fabricated metrics, no mock detections; only real claude-code-agent-teams-driven runs count.
- After 10 failed iterations, stop and produce a postmortem with evidence pack links and remediation plan.

## Core Philosophy (Must Internalize)
- Real-world, human-operated CLI testing is mandatory; simulations and mocks are invalid for validation.
- Claude Code must operate as the human operator via `claude-code-controller` in `alphaswarm-claude-code-agent-teams-session`.
- Every workflow and subworkflow requires real claude-code-agent-teams transcripts and evidence packs.
- See `phases/07.3.1.5-full-testing-orchestrator/07.3.1.5-CORE-PHILOSOPHY.md`.
