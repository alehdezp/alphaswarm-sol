---
phase: 07.3.1.5-full-testing-orchestrator
plan: "10"
type: execute
wave: 4
depends_on: ["03", "05", "07", "09"]
status: INVALIDATED
invalidated_date: 2026-02-03
invalidation_reason: |
  Implementation files were removed because they referenced non-existent CLI commands
  (`alphaswarm audit --checkpoint`, `--resume`). The code was never validated.
  Files deleted:
    - tests/e2e/human_auditor_workflows.yaml
    - scripts/e2e/run_human_workflows.sh
  The conceptual model (HUMAN-AUDITOR-MODEL.md) remains for future reference.
files_modified:
  - tests/e2e/human_auditor_workflows.yaml (DELETED)
  - scripts/e2e/run_human_workflows.sh (DELETED)
  - .planning/phases/07.3.1.5-full-testing-orchestrator/07.3.1.5-HUMAN-AUDITOR-MODEL.md
autonomous: true

must_haves:
  truths:
    - "Human-like workflows are encoded as test cases"
    - "Workflows exercise stop/resume/pivot behavior"
    - "All runs use claude-code-controller and worktree snapshots"
  artifacts:
    - path: "tests/e2e/human_auditor_workflows.yaml"
      provides: "Human-auditor workflow cases"
      contains: "pivot"
    - path: "scripts/e2e/run_human_workflows.sh"
      provides: "Runner for human-like workflows"
      contains: "resume"
    - path: ".planning/phases/07.3.1.5-full-testing-orchestrator/07.3.1.5-HUMAN-AUDITOR-MODEL.md"
      provides: "Human auditor behavior model"
      contains: "stop and resume"
  key_links:
    - from: "tests/e2e/human_auditor_workflows.yaml"
      to: "scripts/e2e/run_human_workflows.sh"
      via: "execution"
      pattern: "workflow"
---

<objective>
Encode realistic human-auditor workflows (stop, resume, pivot, debug) into the full-testing suite and run them via claude-code-agent-teams + worktrees.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.3.1.5-full-testing-orchestrator/07.3.1.5-SCENARIO-MATRIX.md
@.planning/phases/07.3.1.5-full-testing-orchestrator/07.3.1.5-CRITIQUE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Model human auditor behavior</name>
  <files>
    .planning/phases/07.3.1.5-full-testing-orchestrator/07.3.1.5-HUMAN-AUDITOR-MODEL.md
  </files>
  <action>
Write a model describing how a human auditor works:
- stop after graph
- investigate a single pattern
- pivot to another vulnerability
- resume with context generation
- request debug output
  </action>
  <verify>
Confirm the model includes stop/resume/pivot behavior.
  </verify>
  <done>
Human auditor model exists.
  </done>
</task>

<task type="auto">
  <name>Task 2: Encode human workflows</name>
  <files>
    tests/e2e/human_auditor_workflows.yaml
  </files>
  <action>
Define workflows that:
- branch on findings
- resume from snapshots
- switch to a specific agent mid-run
- stop on failure and request debug mode
  </action>
  <verify>
Ensure workflows include at least 6 cases.
  </verify>
  <done>
Workflow file exists with 6+ cases.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement human workflow runner</name>
  <files>
    scripts/e2e/run_human_workflows.sh
  </files>
  <action>
Implement a runner that executes the workflows via claude-code-controller, using worktree snapshots and debug mode.
**Full claude-code-controller documentation:** See `.planning/testing/rules/claude-code-controller-REFERENCE.md` and `.planning/testing/rules/canonical/claude-code-controller-instructions.md`
  </action>
  <verify>
Run with --dry-run to confirm it lists workflow cases.
  </verify>
  <done>
Runner exists with dry-run output.
  </done>
</task>

</tasks>

## Implementation Details (v2)
- Human workflows must include stop/resume/pivot plus explicit debug request branches.
- Workflows must depend on snapshot checkpoints; missing snapshot is a fail.
- Runner must enforce claude-code-agent-teams usage and evidence pack creation for every workflow.

## Strict Quality Controls
- Enforce claude-code-controller usage as a hard gate; missing markers invalidate the run.
- Validate debug-mode artifacts for every skill/agent per the debug matrix.
- Evidence packs are mandatory inputs to gate evaluation; missing packs fail the run.

## Iteration Protocol (max 10, real runs only)
- Run up to 10 real iterations per phase: run → evidence pack → gates → failure classification → fix → re-run.
- Any missing evidence pack artifacts, missing claude-code-agent-teams markers, or duration <= 5000ms is an automatic fail.
- No simulated results, no fabricated metrics, no mock detections; only real claude-code-agent-teams-driven runs count.
- After 10 failed iterations, stop and produce a postmortem with evidence pack links and remediation plan.

## Core Philosophy (Must Internalize)
- Real-world, human-operated CLI testing is mandatory; simulations and mocks are invalid for validation.
- Claude Code must operate as the human operator via `claude-code-controller` in `alphaswarm-claude-code-agent-teams-session`.
- Every workflow and subworkflow requires real claude-code-agent-teams transcripts and evidence packs.
- See `phases/07.3.1.5-full-testing-orchestrator/07.3.1.5-CORE-PHILOSOPHY.md`.

