---
phase: 07.3.1.5-full-testing-orchestrator
plan: "02"
type: execute
wave: 1
depends_on: ["00"]
files_modified:
  - src/alphaswarm_sol/testing/agent_teams_harness.py
  - scripts/e2e/agent_teams_harness_config.yaml
  - tests/e2e/test_agent_teams_harness_smoke.py
autonomous: true

must_haves:
  truths:
    - "claude-code-agent-teams harness can create isolated sessions per run"
    - "claude-code-agent-teams harness integrates with claude-code-controller CLI"
    - "smoke test validates harness creation and capture"
  artifacts:
    - path: "src/alphaswarm_sol/testing/agent_teams_harness.py"
      provides: "Programmatic claude-code-agent-teams harness"
      contains: "create_session"
    - path: "scripts/e2e/agent_teams_harness_config.yaml"
      provides: "Harness config"
      contains: "alphaswarm-claude-code-agent-teams-session"
    - path: "tests/e2e/test_agent_teams_harness_smoke.py"
      provides: "Harness smoke test"
      contains: "capture"
  key_links:
    - from: "src/alphaswarm_sol/testing/agent_teams_harness.py"
      to: "scripts/e2e/agent_teams_harness_config.yaml"
      via: "config loading"
      pattern: "config"
---

<objective>
Add a strict claude-code-agent-teams harness that can be driven by claude-code-controller and provides deterministic session creation and transcript capture.
**Full claude-code-controller documentation:** See `.planning/testing/rules/claude-code-controller-REFERENCE.md` and `.planning/testing/rules/canonical/claude-code-controller-instructions.md`
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@skills/vrs-claude-code-agent-teams-testing.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement claude-code-agent-teams harness wrapper</name>
  <files>
    src/alphaswarm_sol/testing/agent_teams_harness.py
  </files>
  <action>
Create a harness that:
- starts/attaches to `alphaswarm-claude-code-agent-teams-session`
- supports isolated session socket paths
- maps pane roles (controller/subject/monitor)
- captures output via claude-code-agent-teams capture-pane
  </action>
  <verify>
Review the harness to confirm it includes create_session and capture methods.
  </verify>
  <done>
Harness wrapper exists with session and capture capabilities.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add harness configuration</name>
  <files>
    scripts/e2e/agent_teams_harness_config.yaml
  </files>
  <action>
Create a config file with:
- session name
- pane roles
- capture settings
- timeouts
  </action>
  <verify>
Confirm config includes session name and pane roles.
  </verify>
  <done>
Harness config exists with required fields.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add harness smoke test</name>
  <files>
    tests/e2e/test_agent_teams_harness_smoke.py
  </files>
  <action>
Write a minimal test that:
- starts a claude-code-agent-teams session
- captures a pane
- verifies capture output is non-empty
  </action>
  <verify>
Ensure the test asserts capture output and cleans up the session.
  </verify>
  <done>
Smoke test exists and checks basic harness behavior.
  </done>
</task>

</tasks>

## Implementation Details (v2)
- Use a unique claude-code-agent-teams socket per run; map pane roles deterministically (controller/subject/monitor).
- Capture raw text + replayable recording (when available) and hash captures for diffing.
- Enforce session cleanup and verify shutdown markers in evidence pack.

## Strict Quality Controls
- Enforce claude-code-controller usage as a hard gate; missing markers invalidate the run.
- Validate debug-mode artifacts for every skill/agent per the debug matrix.
- Evidence packs are mandatory inputs to gate evaluation; missing packs fail the run.

## Iteration Protocol (max 10, real runs only)
- Run up to 10 real iterations per phase: run → evidence pack → gates → failure classification → fix → re-run.
- Any missing evidence pack artifacts, missing claude-code-agent-teams markers, or duration <= 5000ms is an automatic fail.
- No simulated results, no fabricated metrics, no mock detections; only real claude-code-agent-teams-driven runs count.
- After 10 failed iterations, stop and produce a postmortem with evidence pack links and remediation plan.

## Core Philosophy (Must Internalize)
- Real-world, human-operated CLI testing is mandatory; simulations and mocks are invalid for validation.
- Claude Code must operate as the human operator via `claude-code-controller` in `alphaswarm-claude-code-agent-teams-session`.
- Every workflow and subworkflow requires real claude-code-agent-teams transcripts and evidence packs.
- See `phases/07.3.1.5-full-testing-orchestrator/07.3.1.5-CORE-PHILOSOPHY.md`.
