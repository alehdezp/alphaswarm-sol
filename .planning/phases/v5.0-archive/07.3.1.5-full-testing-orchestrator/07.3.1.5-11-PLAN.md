---
phase: 07.3.1.5-full-testing-orchestrator
plan: "11"
type: execute
wave: 5
depends_on: ["06", "08", "10"]
files_modified:
  - scripts/e2e/full_testing_gate_check.py
  - .planning/phases/07.3.1.5-full-testing-orchestrator/07.3.1.5-GATE-FAILURES.md
  - tests/e2e/expected/full_testing_gate_markers.yaml
autonomous: true

must_haves:
  truths:
    - "Gates are evaluated programmatically"
    - "Gate failures are classified and documented"
    - "Gate markers map to evidence pack fields"
  artifacts:
    - path: "scripts/e2e/full_testing_gate_check.py"
      provides: "Programmatic gate evaluator"
      contains: "evaluate_gates"
    - path: ".planning/phases/07.3.1.5-full-testing-orchestrator/07.3.1.5-GATE-FAILURES.md"
      provides: "Gate failure taxonomy"
      contains: "critical"
    - path: "tests/e2e/expected/full_testing_gate_markers.yaml"
      provides: "Gate marker mapping"
      contains: "gate_id"
  key_links:
    - from: "tests/e2e/expected/full_testing_gate_markers.yaml"
      to: "scripts/e2e/full_testing_gate_check.py"
      via: "gate_id"
      pattern: "gate"
---

<objective>
Automate gate evaluation and create a strict failure taxonomy for /vrs-full-testing results.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.3.1.5-full-testing-orchestrator/07.3.1.5-GATES.md
@.planning/phases/07.3.1.5-full-testing-orchestrator/07.3.1.5-DEBUG-MODE-ENFORCEMENT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement gate evaluator</name>
  <files>
    scripts/e2e/full_testing_gate_check.py
  </files>
  <action>
Implement an evaluator that reads evidence packs and gate definitions, then outputs pass/fail per gate.
  </action>
  <verify>
Confirm evaluator prints failing gate IDs with reasons.
  </verify>
  <done>
Gate evaluator exists and emits pass/fail results.
  </done>
</task>

<task type="auto">
  <name>Task 2: Define gate failure taxonomy</name>
  <files>
    .planning/phases/07.3.1.5-full-testing-orchestrator/07.3.1.5-GATE-FAILURES.md
  </files>
  <action>
Define failure categories and remediation hints for each gate type.
  </action>
  <verify>
Ensure taxonomy includes critical and non-critical categories.
  </verify>
  <done>
Gate failure taxonomy exists.
  </done>
</task>

<task type="auto">
  <name>Task 3: Define gate markers</name>
  <files>
    tests/e2e/expected/full_testing_gate_markers.yaml
  </files>
  <action>
Map gate IDs to evidence pack fields and expected markers for validation.
  </action>
  <verify>
Confirm each gate in GATES.md has a marker entry.
  </verify>
  <done>
Gate markers file exists and covers all gates.
  </done>
</task>

</tasks>

## Implementation Details (v2)
- Gate evaluator must output machine-readable JSON plus a human summary.
- Failure taxonomy must include severity + required action (retry vs. redesign).
- Gate markers must map every gate ID to evidence pack fields.

## Strict Quality Controls
- Enforce claude-code-controller usage as a hard gate; missing markers invalidate the run.
  **Full claude-code-controller documentation:** See `.planning/testing/rules/claude-code-controller-REFERENCE.md` and `.planning/testing/rules/canonical/claude-code-controller-instructions.md`
- Validate debug-mode artifacts for every skill/agent per the debug matrix.
- Evidence packs are mandatory inputs to gate evaluation; missing packs fail the run.

## Iteration Protocol (max 10, real runs only)
- Run up to 10 real iterations per phase: run → evidence pack → gates → failure classification → fix → re-run.
- Any missing evidence pack artifacts, missing claude-code-agent-teams markers, or duration <= 5000ms is an automatic fail.
- No simulated results, no fabricated metrics, no mock detections; only real claude-code-agent-teams-driven runs count.
- After 10 failed iterations, stop and produce a postmortem with evidence pack links and remediation plan.

## Core Philosophy (Must Internalize)
- Real-world, human-operated CLI testing is mandatory; simulations and mocks are invalid for validation.
- Claude Code must operate as the human operator via `claude-code-controller` in `alphaswarm-claude-code-agent-teams-session`.
- Every workflow and subworkflow requires real claude-code-agent-teams transcripts and evidence packs.
- See `phases/07.3.1.5-full-testing-orchestrator/07.3.1.5-CORE-PHILOSOPHY.md`.

