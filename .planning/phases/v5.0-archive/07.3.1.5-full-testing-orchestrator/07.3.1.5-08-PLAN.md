---
phase: 07.3.1.5-full-testing-orchestrator
plan: "08"
type: execute
wave: 2
depends_on: ["04", "07"]
files_modified:
  - scripts/e2e/validate_debug_mode.py
  - configs/debug_mode_matrix.yaml
  - .planning/phases/07.3.1.5-full-testing-orchestrator/07.3.1.5-DEBUG-MODE-ENFORCEMENT.md
autonomous: true

must_haves:
  truths:
    - "Debug mode is enforced across all production skills and agents"
    - "Validator fails if debug artifacts are missing"
    - "Per-skill debug requirements are explicit"
  artifacts:
    - path: "scripts/e2e/validate_debug_mode.py"
      provides: "Debug enforcement validator"
      contains: "validate_debug"
    - path: "configs/debug_mode_matrix.yaml"
      provides: "Per-skill debug requirements"
      contains: "debug_required"
    - path: ".planning/phases/07.3.1.5-full-testing-orchestrator/07.3.1.5-DEBUG-MODE-ENFORCEMENT.md"
      provides: "Enforcement policy"
      contains: "fail"
  key_links:
    - from: "configs/debug_mode_matrix.yaml"
      to: "scripts/e2e/validate_debug_mode.py"
      via: "validation rules"
      pattern: "debug"
---

<objective>
Enforce debug-mode coverage by adding a validator, a per-skill debug requirements matrix, and a strict enforcement policy.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.3.1.5-full-testing-orchestrator/07.3.1.5-DEBUG-MODE-SPEC.md
@.planning/phases/07.3.1.5-full-testing-orchestrator/07.3.1.5-SKILL-COVERAGE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create debug-mode enforcement policy</name>
  <files>
    .planning/phases/07.3.1.5-full-testing-orchestrator/07.3.1.5-DEBUG-MODE-ENFORCEMENT.md
  </files>
  <action>
Write a policy that specifies required debug artifacts per skill/agent and declares the run invalid if any are missing.
  </action>
  <verify>
Confirm policy lists required artifacts and failure conditions.
  </verify>
  <done>
Enforcement policy exists with explicit fail rules.
  </done>
</task>

<task type="auto">
  <name>Task 2: Define debug-mode matrix</name>
  <files>
    configs/debug_mode_matrix.yaml
  </files>
  <action>
Create a matrix mapping each skill/agent to required debug outputs (inputs, tool calls, BSKG queries, outputs).
  </action>
  <verify>
Ensure matrix includes the core /vrs skills and agent set.
  </verify>
  <done>
Debug matrix exists with required fields.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement debug-mode validator</name>
  <files>
    scripts/e2e/validate_debug_mode.py
  </files>
  <action>
Implement a validator that reads evidence packs and the debug matrix to fail runs missing required artifacts.
  </action>
  <verify>
Confirm validator reports missing artifacts and exits non-zero on failure.
  </verify>
  <done>
Validator exists and reports missing debug artifacts.
  </done>
</task>

</tasks>

## Implementation Details (v2)
- Debug matrix must list required artifacts per skill/agent (inputs, tool calls, BSKG/VQL, outputs).
- Validator must check presence + non-empty content; any missing artifact fails the run.
- Enforcement policy should explicitly disallow “partial” debug mode.

## Strict Quality Controls
- Enforce claude-code-controller usage as a hard gate; missing markers invalidate the run.
  **Full claude-code-controller documentation:** See `.planning/testing/rules/claude-code-controller-REFERENCE.md` and `.planning/testing/rules/canonical/claude-code-controller-instructions.md`
- Validate debug-mode artifacts for every skill/agent per the debug matrix.
- Evidence packs are mandatory inputs to gate evaluation; missing packs fail the run.

## Iteration Protocol (max 10, real runs only)
- Run up to 10 real iterations per phase: run → evidence pack → gates → failure classification → fix → re-run.
- Any missing evidence pack artifacts, missing claude-code-agent-teams markers, or duration <= 5000ms is an automatic fail.
- No simulated results, no fabricated metrics, no mock detections; only real claude-code-agent-teams-driven runs count.
- After 10 failed iterations, stop and produce a postmortem with evidence pack links and remediation plan.

## Core Philosophy (Must Internalize)
- Real-world, human-operated CLI testing is mandatory; simulations and mocks are invalid for validation.
- Claude Code must operate as the human operator via `claude-code-controller` in `alphaswarm-claude-code-agent-teams-session`.
- Every workflow and subworkflow requires real claude-code-agent-teams transcripts and evidence packs.
- See `phases/07.3.1.5-full-testing-orchestrator/07.3.1.5-CORE-PHILOSOPHY.md`.

