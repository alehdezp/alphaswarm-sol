---
phase: 07.3.1.5-full-testing-orchestrator
plan: "03"
type: execute
wave: 2
depends_on: ["01", "02"]
files_modified:
  - src/alphaswarm_sol/testing/full_testing_orchestrator.py
  - src/alphaswarm_sol/testing/full_testing_schema.py
  - scripts/e2e/full_testing_runner.sh
autonomous: true

must_haves:
  truths:
    - "Full-testing orchestrator coordinates all suites"
    - "Schema defines inputs/outputs for the orchestrator"
    - "Runner can launch orchestrator in claude-code-agent-teams mode"
  artifacts:
    - path: "src/alphaswarm_sol/testing/full_testing_orchestrator.py"
      provides: "Orchestrator implementation"
      contains: "run_full_testing"
    - path: "src/alphaswarm_sol/testing/full_testing_schema.py"
      provides: "Input/output schema"
      contains: "FullTestingRun"
    - path: "scripts/e2e/full_testing_runner.sh"
      provides: "CLI wrapper"
      contains: "full-testing"
  key_links:
    - from: "src/alphaswarm_sol/testing/full_testing_orchestrator.py"
      to: "scripts/e2e/full_testing_runner.sh"
      via: "runner entry"
      pattern: "orchestrator"
---

<objective>
Implement the /vrs-full-testing orchestrator core, schema, and a runner that drives claude-code-agent-teams-based interactive sessions.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.3.1.5-full-testing-orchestrator/07.3.1.5-ORCHESTRATOR-SPEC.md
@.planning/phases/07.3.1.5-full-testing-orchestrator/07.3.1.5-DEBUG-MODE-SPEC.md
@.planning/phases/07.3.1.5-full-testing-orchestrator/07.3.1.5-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement orchestrator core</name>
  <files>
    src/alphaswarm_sol/testing/full_testing_orchestrator.py
  </files>
  <action>
Create an orchestrator that:
- loads scenario manifest
- launches claude-code-agent-teams sessions
- executes command suite, agent suite, context suite, resume matrix
- captures evidence packs
- triggers self-improvement loop on failures
  </action>
  <verify>
Review the file for suite ordering and evidence pack writes.
  </verify>
  <done>
Orchestrator core exists with suite execution and evidence pack output.
  </done>
</task>

<task type="auto">
  <name>Task 2: Define schema for full-testing</name>
  <files>
    src/alphaswarm_sol/testing/full_testing_schema.py
  </files>
  <action>
Define input/output schema for:
- scenarios
- suite selection
- debug mode flags
- evidence pack outputs
  </action>
  <verify>
Ensure schema includes debug mode and evidence pack fields.
  </verify>
  <done>
Schema exists with required fields.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add CLI runner</name>
  <files>
    scripts/e2e/full_testing_runner.sh
  </files>
  <action>
Add a CLI wrapper that:
- invokes /vrs-full-testing in claude-code-agent-teams
- supports --scenario, --suite, --debug, --resume
- outputs paths to evidence packs
  </action>
  <verify>
Run with --dry-run and confirm it prints the command plan.
  </verify>
  <done>
Runner exists with dry-run output and flags.
  </done>
</task>

</tasks>

## Implementation Details (v2)
- Orchestrator must be idempotent: retries create new worktrees and never mutate prior evidence packs.
- Schema should use strict enums for suites/resume stages; reject unknowns.
- Runner must support record/diff plus dry-run; print exact command plan and evidence pack output paths.

## Strict Quality Controls
- Enforce claude-code-controller usage as a hard gate; missing markers invalidate the run.
- Validate debug-mode artifacts for every skill/agent per the debug matrix.
- Evidence packs are mandatory inputs to gate evaluation; missing packs fail the run.

## Iteration Protocol (max 10, real runs only)
- Run up to 10 real iterations per phase: run → evidence pack → gates → failure classification → fix → re-run.
- Any missing evidence pack artifacts, missing claude-code-agent-teams markers, or duration <= 5000ms is an automatic fail.
- No simulated results, no fabricated metrics, no mock detections; only real claude-code-agent-teams-driven runs count.
- After 10 failed iterations, stop and produce a postmortem with evidence pack links and remediation plan.

## Core Philosophy (Must Internalize)
- Real-world, human-operated CLI testing is mandatory; simulations and mocks are invalid for validation.
- Claude Code must operate as the human operator via `claude-code-controller` in `alphaswarm-claude-code-agent-teams-session`.
  **Full claude-code-controller documentation:** See `.planning/testing/rules/claude-code-controller-REFERENCE.md` and `.planning/testing/rules/canonical/claude-code-controller-instructions.md`
- Every workflow and subworkflow requires real claude-code-agent-teams transcripts and evidence packs.
- See `phases/07.3.1.5-full-testing-orchestrator/07.3.1.5-CORE-PHILOSOPHY.md`.
