---
phase: 07.3.1.5-full-testing-orchestrator
plan: "04"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/alphaswarm_sol/testing/debug_mode.py
  - src/alphaswarm_sol/testing/debug_artifacts.py
  - configs/debug_mode_defaults.yaml
autonomous: true

must_haves:
  truths:
    - "Debug-mode capture is standardized across all skills and agents"
    - "Debug artifacts include tool calls and BSKG queries"
    - "Defaults are configured for production skills"
  artifacts:
    - path: "src/alphaswarm_sol/testing/debug_mode.py"
      provides: "Debug-mode helper"
      contains: "enable_debug_mode"
    - path: "src/alphaswarm_sol/testing/debug_artifacts.py"
      provides: "Artifact writer"
      contains: "write_debug_pack"
    - path: "configs/debug_mode_defaults.yaml"
      provides: "Default debug config"
      contains: "debug_mode"
  key_links:
    - from: "src/alphaswarm_sol/testing/debug_mode.py"
      to: "configs/debug_mode_defaults.yaml"
      via: "default config"
      pattern: "defaults"
---

<objective>
Add a production-grade debug-mode system that captures all tool calls, VQL/BSKG queries, and intermediate artifacts for every skill/agent.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.3.1.5-full-testing-orchestrator/07.3.1.5-DEBUG-MODE-SPEC.md
@.planning/phases/07.3.1.5-full-testing-orchestrator/07.3.1.5-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement debug-mode helper</name>
  <files>
    src/alphaswarm_sol/testing/debug_mode.py
  </files>
  <action>
Create a helper that:
- enables debug mode for a run
- tags outputs with run_id and skill_id
- routes debug outputs to evidence pack
  </action>
  <verify>
Review the helper for run_id tagging and output routing.
  </verify>
  <done>
Debug helper exists with run_id tagging.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement debug artifact writer</name>
  <files>
    src/alphaswarm_sol/testing/debug_artifacts.py
  </files>
  <action>
Implement a writer that stores:
- prompts/inputs
- tool calls
- BSKG/VQL queries
- outputs and intermediate artifacts
  </action>
  <verify>
Ensure writer defines paths for each artifact type.
  </verify>
  <done>
Debug artifact writer exists with required outputs.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add debug defaults config</name>
  <files>
    configs/debug_mode_defaults.yaml
  </files>
  <action>
Define default debug settings per skill and global on/off toggles.
  </action>
  <verify>
Confirm config includes per-skill defaults.
  </verify>
  <done>
Debug defaults config exists.
  </done>
</task>

</tasks>

## Implementation Details (v2)
- Centralize debug hooks to capture all tool calls and BSKG/VQL queries with run_id + skill_id.
- Write debug artifacts into evidence pack with stable naming and redaction rules.
- Defaults must be explicit per-skill; missing required artifacts is a hard fail.

## Strict Quality Controls
- Enforce claude-code-controller usage as a hard gate; missing markers invalidate the run.
- Validate debug-mode artifacts for every skill/agent per the debug matrix.
- Evidence packs are mandatory inputs to gate evaluation; missing packs fail the run.

## Iteration Protocol (max 10, real runs only)
- Run up to 10 real iterations per phase: run → evidence pack → gates → failure classification → fix → re-run.
- Any missing evidence pack artifacts, missing claude-code-agent-teams markers, or duration <= 5000ms is an automatic fail.
- No simulated results, no fabricated metrics, no mock detections; only real claude-code-agent-teams-driven runs count.
- After 10 failed iterations, stop and produce a postmortem with evidence pack links and remediation plan.

## Core Philosophy (Must Internalize)
- Real-world, human-operated CLI testing is mandatory; simulations and mocks are invalid for validation.
- Claude Code must operate as the human operator via `claude-code-controller` in `alphaswarm-claude-code-agent-teams-session`.
  **Full claude-code-controller documentation:** See `.planning/testing/rules/claude-code-controller-REFERENCE.md` and `.planning/testing/rules/canonical/claude-code-controller-instructions.md`
- Every workflow and subworkflow requires real claude-code-agent-teams transcripts and evidence packs.
- See `phases/07.3.1.5-full-testing-orchestrator/07.3.1.5-CORE-PHILOSOPHY.md`.
