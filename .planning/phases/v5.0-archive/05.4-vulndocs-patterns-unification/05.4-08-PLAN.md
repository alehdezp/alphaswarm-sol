---
phase: 05.4-vulndocs-patterns-unification
plan: 08
type: execute
wave: 7
depends_on: ["05.4-07"]
files_modified:
  - tests/vulndocs/test_integration.py
  - tests/vulndocs/test_skills.py
autonomous: true

must_haves:
  truths:
    - "Integration tests validate full framework workflow"
    - "Skill tests verify skill file structure"
    - "Tests cover scaffold -> validate -> list cycle"
    - "All tests use tmp_path for isolation"
  artifacts:
    - path: "tests/vulndocs/test_integration.py"
      provides: "End-to-end integration tests for vulndocs framework"
      min_lines: 300
    - path: "tests/vulndocs/test_skills.py"
      provides: "Skill file validation tests"
      min_lines: 100
  key_links:
    - from: "tests/vulndocs/test_integration.py"
      to: "src/true_vkg/vulndocs/"
      via: "Tests all vulndocs module components"
      pattern: "from src.true_vkg.vulndocs"
---

<objective>
Create comprehensive integration tests for the vulndocs framework.

Purpose: Validate that all framework components work together correctly,
including schema validation, discovery, CLI commands, and skill file structure.

**Note:** This plan depends on 05.4-07 (all skills complete) so that skill
tests can verify all 7 vrs: skill files exist.

Output:
- Integration tests for full workflow
- Skill file validation tests
- Coverage for all public APIs
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.4-vulndocs-patterns-unification/05.4-CONTEXT.md
@.planning/phases/05.4-vulndocs-patterns-unification/05.4-07-SUMMARY.md
</context>

<execution_guardrails>
- Full doc alignment: align objective, must_haves, and tasks against `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, plus phase context or research docs before execution. Any scope change must be reflected in the plan and `.planning/STATE.md`.
- Workspace isolation: run any commands or experiments that mutate state in a fresh jj workspace; do not use the main workspace and do not reuse workspaces for reruns.
- Repeat-run detection markers: include `[ALPHASWARM-START] RUN_ID TIMESTAMP` and `[ALPHASWARM-END] RUN_ID EXIT_CODE DURATION_MS` markers in transcripts or logs so reruns are detectable.
- claude-code-controller subject: for interactive runs, use `claude-code-controller` and launch Claude Code as the subject in the claude-code-agent-teams session; transcripts must show the Claude Code session.
- Seed scope boundaries: treat B-seeds (baseline or regression inputs) and N-seeds (net-new exploratory inputs) as separate scopes; do not mix them in a single run and track results separately.
</execution_guardrails>

<tasks>

<task type="auto">
  <name>Task 1: Create integration tests for framework workflow</name>
  <files>
    tests/vulndocs/test_integration.py
  </files>
  <action>
    Create comprehensive integration tests for the vulndocs framework.

    Test categories:

    1. Template tests:
       - test.meta/templates_exist: All template files present
       - test_template_index_valid: Template index.yaml is valid
       - test_template_pattern_valid: Template pattern.yaml is valid
       - test_template_has_phase7_fields: Phase 7 fields in templates

    2. Scaffold workflow tests:
       - test_scaffold_creates_structure: Creates all expected files
       - test_scaffold_replaces_placeholders: Variables substituted
       - test_scaffold_creates_patterns_folder: Empty patterns/ folder
       - test_scaffold_index_validates: Created index.yaml passes validation

    3. Validation workflow tests:
       - test_validate_minimal_entry: Only index.yaml -> MINIMAL
       - test_validate_standard_entry: With md files -> STANDARD
       - test_validate_complete_entry: All files -> COMPLETE
       - test_validate_excellent_entry: With tested patterns -> EXCELLENT
       - test_validate_framework_aggregates: Multiple vulns aggregated
       - test_validate_reports_errors: Missing fields -> errors
       - test_validate_reports_warnings: Missing optional -> warnings

    4. Discovery tests:
       - test_discover_categories: Finds all categories
       - test_discover_vulnerabilities: Finds all vulns
       - test_discover_patterns: Finds patterns in vulns
       - test_discover_skips.meta/templates: Ignores .meta/templates

    5. CLI integration tests:
       - test_cli_validate_runs: Command executes
       - test_cli_scaffold_runs: Command executes
       - test_cli_info_runs: Command executes
       - test_cli_list_runs: Command executes
       - test_cli_workflow: scaffold -> validate -> list

    6. End-to-end workflow:
       - test_full_workflow: Create category, add vuln, add pattern, validate

    Use pytest fixtures:
    - tmp_vulndocs: Temporary vulndocs folder with templates
    - sample_vulnerability: Pre-created vulnerability for testing

    Target: 25-30 tests.
  </action>
  <verify>
    uv run pytest tests/vulndocs/test_integration.py -v --tb=short
  </verify>
  <done>
    - test_integration.py exists with 25+ tests
    - All workflow stages tested
    - Uses tmp_path for isolation
    - CLI commands tested with CliRunner
  </done>
</task>

<task type="auto">
  <name>Task 2: Create skill file validation tests</name>
  <files>
    tests/vulndocs/test_skills.py
  </files>
  <action>
    Create tests that validate skill file structure and content.

    Test categories:

    1. Skill file existence:
       - test_vrs_skills_directory_exists
       - test_discover_skill_exists
       - test_add_vulnerability_skill_exists
       - test_refine_skill_exists
       - test_test_pattern_skill_exists
       - test_research_skill_exists
       - test_merge_findings_skill_exists
       - test_generate_tests_skill_exists

    2. Skill content validation:
       - test_skills_have_slash_command: All have vrs: prefix
       - test_skills_have_context: All specify context
       - test_skills_have_tools: All list required tools
       - test_skills_have_workflow: All document workflow steps

    3. Skill consistency:
       - test_skills_use_graph_queries: No manual code reading
       - test_skills_reference_cli: Use CLI commands where applicable
       - test_skills_have_output_format: Document expected output
       - test_skills_document_bash_invocation: Explain that CLI is invoked via Bash tool

    4. README validation:
       - test_readme_lists_all_skills: All 7 skills documented
       - test_readme_has_workflow_diagram: Shows skill relationships
       - test_readme_explains_invocation_model: Documents how agents use skills

    Parse skill files as markdown and validate structure.
    Target: 15-20 tests.
  </action>
  <verify>
    uv run pytest tests/vulndocs/test_skills.py -v --tb=short
  </verify>
  <done>
    - test_skills.py exists with 15+ tests
    - All 7 skill files validated
    - Structure and content checked
    - README completeness verified
    - Invocation model documentation verified
  </done>
</task>

<task type="auto">
  <name>Task 3: Create test fixtures and conftest</name>
  <files>
    tests/vulndocs/conftest.py
    tests/vulndocs/__init__.py
  </files>
  <action>
    Create shared fixtures for vulndocs tests.

    1. tests/vulndocs/__init__.py:
       - Empty file for package recognition

    2. tests/vulndocs/conftest.py with fixtures:

       @pytest.fixture
       def tmp_vulndocs(tmp_path):
           """Create temporary vulndocs folder with templates."""
           # Copy templates from vulndocs/.meta/templates/
           # Return path to temporary vulndocs

       @pytest.fixture
       def sample_vulnerability(tmp_vulndocs):
           """Create a sample vulnerability for testing."""
           # Use scaffold to create
           # Return path

       @pytest.fixture
       def sample_pattern(sample_vulnerability):
           """Create a sample pattern for testing."""
           # Add pattern to vulnerability
           # Return path

       @pytest.fixture
       def sample_framework(tmp_vulndocs):
           """Create a framework with multiple entries."""
           # Create 2-3 categories with vulnerabilities
           # Return path

       @pytest.fixture
       def cli_runner():
           """Typer CLI test runner."""
           from typer.testing import CliRunner
           return CliRunner()

    These fixtures ensure tests are isolated and reproducible.
  </action>
  <verify>
    python -c "from tests.vulndocs.conftest import *; print('Fixtures importable')"
    ls tests/vulndocs/
  </verify>
  <done>
    - conftest.py exists with shared fixtures
    - __init__.py makes tests/vulndocs a package
    - Fixtures provide isolated test environments
    - All fixtures documented
  </done>
</task>

</tasks>

<verification>
Overall phase checks:
- `uv run pytest tests/vulndocs/ -v` runs all vulndocs tests
- Integration tests cover full workflow
- Skill tests validate all 7 skill files
- No test failures
</verification>

<success_criteria>
- 40+ vulndocs tests across schema, validation, CLI, integration
- All 7 skill files validated for structure
- Full workflow tested end-to-end
- Tests are isolated using tmp_path
- Test coverage >= 80% for vulndocs module
</success_criteria>

<output>
After completion, create `.planning/phases/05.4-vulndocs-patterns-unification/05.4-08-SUMMARY.md`
</output>
