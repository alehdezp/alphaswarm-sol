---
phase: 05.4-vulndocs-patterns-unification
plan: 10
type: execute
wave: 8
depends_on: ["05.4-09"]
files_modified:
  - .claude/skills/vrs/README.md
  - vulndocs/README.md
autonomous: true

must_haves:
  truths:
    - "Skills README documents complete workflow"
    - "vulndocs README is final and comprehensive"
    - "All framework components verified working"
    - "Phase ready for execution"
  artifacts:
    - path: ".claude/skills/vrs/README.md"
      provides: "Complete skills documentation"
      min_lines: 150
    - path: "vulndocs/README.md"
      provides: "Final framework documentation"
      min_lines: 150
  key_links:
    - from: ".claude/skills/vrs/README.md"
      to: "vulndocs/"
      via: "Skills operate on vulndocs structure"
      pattern: "vulndocs/"
---

<objective>
Finalize documentation and verify framework readiness.

Purpose: Complete all documentation, verify all components work together,
and prepare the phase for execution. This is the final plan that ensures
the framework is ready for use.

Output:
- Final skills README with complete workflow
- Final vulndocs README with all information
- Verification that all components work
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.4-vulndocs-patterns-unification/05.4-CONTEXT.md
@.planning/phases/05.4-vulndocs-patterns-unification/05.4-09-SUMMARY.md
</context>

<execution_guardrails>
- Full doc alignment: align objective, must_haves, and tasks against `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, plus phase context or research docs before execution. Any scope change must be reflected in the plan and `.planning/STATE.md`.
- Workspace isolation: run any commands or experiments that mutate state in a fresh jj workspace; do not use the main workspace and do not reuse workspaces for reruns.
- Repeat-run detection markers: include `[ALPHASWARM-START] RUN_ID TIMESTAMP` and `[ALPHASWARM-END] RUN_ID EXIT_CODE DURATION_MS` markers in transcripts or logs so reruns are detectable.
- claude-code-controller subject: for interactive runs, use `claude-code-controller` and launch Claude Code as the subject in the claude-code-agent-teams session; transcripts must show the Claude Code session.
- Seed scope boundaries: treat B-seeds (baseline or regression inputs) and N-seeds (net-new exploratory inputs) as separate scopes; do not mix them in a single run and track results separately.
</execution_guardrails>

<tasks>

<task type="auto">
  <name>Task 1: Finalize skills README with complete workflow</name>
  <files>
    .claude/skills/vrs/README.md
  </files>
  <action>
    Create comprehensive README for vrs: skills.

    Structure:

    1. Overview (~20 lines):
       - VRS: Vulnerability Research System
       - Purpose of vrs: skills
       - Phase 6 rebrand context

    2. **Invocation Model (~30 lines):**
       - Skills are documentation files that guide agent behavior
       - When user invokes /vrs-discover, Claude Code loads the skill file
       - Agent follows the documented workflow using standard tools
       - CLI commands (like `vulndocs scaffold`) are run via Bash tool
       - No separate "skill execution engine" - skills ARE the prompts

    3. Skills Summary Table (~20 lines):
       | Skill | Purpose | Model | When to Use |
       |-------|---------|-------|-------------|
       | discover | Automated search | Sonnet | Weekly or on-demand |
       | add-vulnerability | Create entry | Sonnet | New vulnerability found |
       | refine | Improve pattern | Opus | After test failures |
       | test-pattern | Validate | Sonnet | Before promoting pattern |
       | research | Guided research | Sonnet | User-directed investigation |
       | merge-findings | Consolidate | Sonnet | Reduce redundancy |
       | generate-tests | Create tests | Sonnet | Phase 7 preparation |

    4. Workflow Diagrams (~40 lines):

       Discovery workflow:
       ```
       discover -> evaluate -> add-vulnerability or update
       ```

       Improvement workflow:
       ```
       test-pattern -> analyze results -> refine -> test-pattern
       ```

       Research workflow:
       ```
       research -> synthesize -> add-vulnerability or merge-findings
       ```

       Test generation workflow:
       ```
       generate-tests -> review -> update test_coverage
       ```

    5. Model Tier Assignments (~20 lines):
       - Explain Haiku/Sonnet/Opus choices
       - When to escalate to Opus

    6. Best Practices (~30 lines):
       - Graph-first enforcement
       - Semantic operations over names
       - Dense context over verbose
       - Always validate after changes

    7. Quick Reference (~20 lines):
       - Common invocations
       - Expected outputs

    Total: ~180 lines (increased to include invocation model)
  </action>
  <verify>
    cat .claude/skills/vrs/README.md
    wc -l .claude/skills/vrs/README.md
  </verify>
  <done>
    - README.md is comprehensive (150+ lines)
    - All 7 skills documented
    - Invocation model clearly explained
    - Workflow diagrams included
    - Model tier assignments explained
  </done>
</task>

<task type="auto">
  <name>Task 2: Finalize vulndocs README</name>
  <files>
    vulndocs/README.md
  </files>
  <action>
    Update vulndocs/README.md to be final and comprehensive.

    Ensure it includes:

    1. Purpose section:
       - Unified source of truth for vulnerability knowledge
       - LLM-optimized for detection and reasoning
       - Phase 7 test generation support

    2. Folder structure diagram:
       - Complete tree with all folders
       - Explain .meta/templates/, .meta/instructions/

    3. File structure per vulnerability:
       - index.yaml (required)
       - overview.md, detection.md, verification.md, exploits.md (optional)
       - patterns/ subfolder

    4. Quick start:
       - Add new category
       - Add new vulnerability
       - Add pattern
       - Validate structure

    5. CLI commands:
       - All commands with examples
       - Expected output

    6. Agent skills:
       - Reference to .claude/skills/vrs/
       - Brief description of each
       - Note that agents use Bash tool to invoke CLI commands

    7. Phase 7 integration:
       - semantic_triggers purpose
       - vql_queries purpose
       - reasoning_template purpose
       - test_coverage tracking

    8. Validation levels:
       - MINIMAL, STANDARD, COMPLETE, EXCELLENT
       - How to achieve each

    9. Best practices:
       - Graph-first approach
       - Semantic operations
       - Dense context

    10. **Migration note:**
        - UNIFY-08 (actual content migration) deferred to Phase 5.5
        - This phase establishes framework structure only

    Total: ~150-200 lines
  </action>
  <verify>
    cat vulndocs/README.md
    wc -l vulndocs/README.md
  </verify>
  <done>
    - README.md is final and comprehensive
    - All sections included
    - Clear quick start guide
    - Phase 7 integration explained
    - Migration deferral noted
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify framework readiness</name>
  <files>
    .planning/phases/05.4-vulndocs-patterns-unification/05.4-VERIFICATION.md
  </files>
  <action>
    Create verification document and run all checks.

    1. Run all tests:
       ```bash
       uv run pytest tests/vulndocs/ -v --tb=short
       ```

    2. Verify CLI:
       ```bash
       uv run alphaswarm vulndocs --help
       uv run alphaswarm vulndocs validate vulndocs/
       uv run alphaswarm vulndocs info
       ```

    3. Verify skills exist:
       ```bash
       ls .claude/skills/vrs/
       ```

    4. Verify templates:
       ```bash
       ls vulndocs/.meta/templates/
       ls vulndocs/.meta/templates/subcategory/
       ```

    5. Verify instructions:
       ```bash
       ls vulndocs/.meta/instructions/
       ```

    6. Create 05.4-VERIFICATION.md with results:
       - All requirements status (UNIFY-01 through UNIFY-12)
       - **Note UNIFY-08 as OUT OF SCOPE (deferred to Phase 5.5)**
       - Test results summary
       - CLI verification
       - File verification

    Format:
    ```markdown
    # Phase 5.4 Verification

    ## Requirements Status

    | ID | Requirement | Status | Evidence |
    |----|-------------|--------|----------|
    | UNIFY-01 | Flatten vulndocs structure | PASS | vulndocs/ at root |
    | UNIFY-02 | ... | PASS | ... |
    | ... | ... | ... | ... |
    | UNIFY-08 | Migration scripts | OUT OF SCOPE | Deferred to Phase 5.5 |
    | ... | ... | ... | ... |

    ## Test Results
    - Total tests: X
    - Passed: X
    - Failed: 0

    ## CLI Verification
    - vulndocs validate: PASS
    - vulndocs scaffold: PASS
    - vulndocs info: PASS
    - vulndocs list: PASS

    ## Skill Verification
    - 7 skill files present
    - README complete
    - Invocation model documented

    ## Framework Ready: YES
    ```
  </action>
  <verify>
    cat .planning/phases/05.4-vulndocs-patterns-unification/05.4-VERIFICATION.md
  </verify>
  <done>
    - All tests pass
    - All CLI commands work
    - All requirements verified (UNIFY-08 noted as out of scope)
    - VERIFICATION.md documents status
  </done>
</task>

</tasks>

<verification>
Overall phase checks:
- All 10 plans complete
- All tests pass
- All CLI commands work
- All skill files exist
- All documentation complete
- UNIFY-08 migration deferred to Phase 5.5
- Framework ready for use
</verification>

<success_criteria>
- Skills README is final (150+ lines) with invocation model
- vulndocs README is final (150+ lines) with migration note
- All requirements UNIFY-01 through UNIFY-12 verified (UNIFY-08 as out of scope)
- All tests pass
- VERIFICATION.md confirms framework ready
</success_criteria>

<output>
After completion, create `.planning/phases/05.4-vulndocs-patterns-unification/05.4-10-SUMMARY.md`
</output>
