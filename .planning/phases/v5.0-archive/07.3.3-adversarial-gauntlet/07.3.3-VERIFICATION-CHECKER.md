# 07.3.3 Adversarial Gauntlet Plan Verification

## Issues

### Blocker
- None.

### Major
- [07.3.3-01] Task 1 requires defining 10+ contracts, but no contract fixture files are listed in `<files>` or `files_modified`, so ownership and scope for the fixtures are undefined. `.planning/phases/07.3.3-adversarial-gauntlet/07.3.3-01-PLAN.md:45`
- [07.3.3-02] Task 1 defines multiple access/oracle cases, but no contract fixture files are listed in `<files>` or `files_modified`, so the cases have no declared source contracts. `.planning/phases/07.3.3-adversarial-gauntlet/07.3.3-02-PLAN.md:45`

### Minor
- [07.3.3-00] `depends_on: ["07.3.2"]` references a phase id rather than a plan id in this phase directory; clarify the intended cross-phase dependency target. `.planning/phases/07.3.3-adversarial-gauntlet/07.3.3-00-PLAN.md:6`
- [07.3.3-00/01/02] Verification requires a `--dry-run` option, but the actions do not explicitly call out adding that flag or the expected output shape. This makes the verify steps ambiguous. `.planning/phases/07.3.3-adversarial-gauntlet/07.3.3-00-PLAN.md:90` `.planning/phases/07.3.3-adversarial-gauntlet/07.3.3-01-PLAN.md:73` `.planning/phases/07.3.3-adversarial-gauntlet/07.3.3-02-PLAN.md:73`
- [07.3.3-03] Mutation generator verification lacks a concrete input/output location, so success criteria are underspecified. `.planning/phases/07.3.3-adversarial-gauntlet/07.3.3-03-PLAN.md:51`

### Info
- `tests/gauntlet/` and `reports/` do not exist yet; ensure execution includes directory creation or clarifies where artifacts live.

## Improvement Suggestions (Per Plan)

### 07.3.3-00
- Specify the exact required sections in the gauntlet spec (e.g., ground-truth schema, evidence pack fields, proof token list, binary scoring rules, failure handling).
- Make `--dry-run` a first-class requirement in the runner task (document flag name and expected output format).
- Replace `depends_on: ["07.3.2"]` with a concrete plan id (or document the cross-phase dependency convention).

### 07.3.3-01
- Add explicit contract fixture files (e.g., `tests/contracts/gauntlet/reentrancy/*.sol`) to `files_modified` and Task 1 `<files>`.
- Include a manifest field mapping each case to a concrete contract path and expected finding IDs.
- Note that the runner should consume `configs/gauntlet_scoring.yaml` and `07.3.3-GAUNTLET-SPEC.md` to avoid drift.

### 07.3.3-02
- Add explicit contract fixture files (e.g., `tests/contracts/gauntlet/access_oracle/*.sol`) to `files_modified` and Task 1 `<files>`.
- Include cross-contract fixture topology (which contracts call which) and expected ground-truth IDs in the manifest schema.
- Document negative-control acceptance criteria (what counts as a pass for safe baselines).

### 07.3.3-03
- Specify mutation input sources and output directory (seed corpus paths and where generated variants land).
- Add a concrete verification command and required metadata fields for lineage (e.g., `parent_id`, `mutation_type`, `seed_hash`).
- If seed fixtures are added or modified, list them in `files_modified` for ownership clarity.
