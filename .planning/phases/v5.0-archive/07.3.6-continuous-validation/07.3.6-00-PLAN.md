---
phase: 07.3.6-continuous-validation
plan: "00"
type: execute
wave: 0
depends_on: ["07.3.5"]
files_modified:
  - phases/07.3.6-continuous-validation/07.3.6-PIPELINE-SPEC.md
  - configs/continuous_schedule.yaml
  - scripts/e2e/run_continuous_pipeline.sh
autonomous: true

must_haves:
  truths:
    - "Continuous pipeline architecture is specified"
    - "Schedules are defined for smoke/nightly/weekly"
    - "Runner orchestrates scheduled suites"
  artifacts:
    - path: "phases/07.3.6-continuous-validation/07.3.6-PIPELINE-SPEC.md"
      provides: "Pipeline specification"
      contains: "nightly"
    - path: "configs/continuous_schedule.yaml"
      provides: "Schedule config"
      contains: "weekly"
    - path: "scripts/e2e/run_continuous_pipeline.sh"
      provides: "Pipeline runner"
      contains: "pipeline"
  key_links:
    - from: "configs/continuous_schedule.yaml"
      to: "scripts/e2e/run_continuous_pipeline.sh"
      via: "schedule"
      pattern: "nightly"
---

<objective>
Specify and implement the continuous validation pipeline (smoke, nightly, weekly).
</objective>

<context>
@phases/07.3.6-continuous-validation/07.3.6-PHASE-DESIGN.md
@phases/07.3.2-execution-evidence-protocol/07.3.2-GATES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write pipeline spec</name>
  <files>
    phases/07.3.6-continuous-validation/07.3.6-PIPELINE-SPEC.md
  </files>
  <action>
Define the continuous pipeline tiers:
- per-commit smoke
- nightly full suite
- weekly benchmark + variance

Include gate enforcement and artifact retention rules.
Explicitly include the DVD + SmartBugs E2E suites in nightly runs and require metrics aggregation outputs.
  </action>
  <verify>
Confirm spec includes all tiers and gate enforcement.
  </verify>
  <done>
Pipeline spec exists.
  </done>
</task>

<task type="auto">
  <name>Task 2: Define schedule config</name>
  <files>
    configs/continuous_schedule.yaml
  </files>
  <action>
Define schedules and suite selection per tier.
  </action>
  <verify>
Confirm schedule includes smoke/nightly/weekly.
  </verify>
  <done>
Schedule config exists.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement pipeline runner</name>
  <files>
    scripts/e2e/run_continuous_pipeline.sh
  </files>
  <action>
Implement runner that:
- selects suite by schedule
- enforces gates
- archives evidence packs
 - triggers metrics aggregation and GA gate check for nightly/weekly runs
  </action>
  <verify>
Run with --dry-run and confirm suite selection.
  </verify>
  <done>
Pipeline runner exists.
  </done>
</task>

</tasks>
