---
phase: 07.3.1.6-full-testing-hardening
plan: "06"
type: execute
wave: 6
depends_on: ["05"]

execution:
  mode: autonomous
  human_confirmation: conditional
  confidence_threshold: 0.95

files_modified:
  - .planning/testing/RUNBOOK.md
  - .planning/testing/OPERATOR-STATUS-CONTRACT.md
  - .planning/testing/DECISION-LOG-SCHEMA.md
  - docs/workflows/PLAYBOOKS.md
  - .planning/testing/ground_truth/PROVENANCE-INDEX.md
  - .planning/testing/ci/VALIDATION-PLAN.md
  - .planning/testing/scenarios/SCENARIO-MANIFEST.yaml
  - .planning/testing/skill-reviewer/reviews/

outcome_validation:
  # =============================================================================
  # T1: Runbook Promise
  # =============================================================================
  - promise: "Runbook lets users succeed at running audits"
    task: T1
    tests:
      - name: "New user can complete an audit following runbook"
        verifies: "The runbook contains sufficient information to complete an audit"
        how: |
          1. Take a fresh project directory with a sample contract
          2. Follow the runbook step-by-step without external help
          3. Track where you get stuck or need clarification
          4. Complete the audit or document exactly where it fails
        success_criteria: |
          - User reaches a completed audit OR
          - Every blocker encountered has a documented recovery step in runbook
          - Runbook covers: init, build-kg, tool setup, audit execution, report generation
          - Runbook covers: /vrs-audit init, /vrs-audit continue, /vrs-audit progress, /vrs-audit check <subcat>
          - Runbook links to playbooks for branch options (graph-only, context-skip, pattern subset, resume/rollback)
          - Time to complete is reasonable (<30 min for sample contract)
        evidence_required: |
          - Transcript of following runbook steps
          - List of any blockers encountered and how runbook addressed them
          - Final audit output or error with recovery path used

      - name: "Runbook includes failure recovery paths"
        verifies: "Users can recover from common failures"
        how: |
          1. Identify 3+ named failure modes in runbook (e.g., missing tool, graph build failure, timeout)
          2. For each failure mode, verify recovery instructions exist
          3. Simulate at least one failure and follow recovery path
        success_criteria: |
          - Runbook has section for common failure modes
          - Each failure mode has recovery steps (not just "try again")
          - At least one recovery path verified via transcript
          - Runbook references operator status contract and decision log artifacts
        evidence_required: |
          - Excerpt from runbook showing failure modes and recovery
          - Transcript of simulated failure + successful recovery

      - name: "Operator status contract is actionable"
        verifies: "Users can see tasks, progress, and outputs without ambiguity"
        how: |
          1. Read OPERATOR-STATUS-CONTRACT.md
          2. Verify it defines status queries (vrs-status, vrs-resume, /vrs-audit progress, /vrs-audit check)
          3. Verify it defines how to list tasks, completed tasks, pending tasks, and last output
          4. Verify it defines how to retrieve decision log summary for a run
        success_criteria: |
          - Operator status contract defines stage.current, stage.completed, stage.next
          - Progress output includes init/task split (init_complete, task_status)
          - Task listing fields are explicit (id, status, summary, output ref)
          - Decision log retrieval instructions exist
        evidence_required: |
          - Excerpt from OPERATOR-STATUS-CONTRACT.md showing fields
          - Example status output schema in the doc

  # =============================================================================
  # T2: Ground Truth Provenance Promise
  # =============================================================================
  - promise: "Validation runs are reproducible using provenance index"
    task: T2
    tests:
      - name: "Reproduce a validation run from provenance"
        verifies: "The provenance index contains enough information to recreate a validation"
        how: |
          1. Pick an entry from PROVENANCE-INDEX.md
          2. Using only the provenance info, fetch/locate the ground truth data
          3. Run validation against that data
          4. Compare results to original validation (if available)
        success_criteria: |
          - Provenance entry includes: source, corpus ID, version, checksum, retrieval method
          - Ground truth can be fetched using the documented method
          - Fetched data matches checksum
          - Validation produces consistent results
        evidence_required: |
          - Provenance entry excerpt
          - Transcript of fetching ground truth from provenance
          - Checksum verification output
          - Validation result comparison (or first validation result if new)

      - name: "Caching policy is documented and enforced"
        verifies: "Cached data is correctly managed"
        how: |
          1. Read caching policy in PROVENANCE-INDEX.md
          2. Verify policy specifies cache location, refresh rules, and integrity checks
          3. Test cache hit (data exists) and cache miss (data needs fetch)
        success_criteria: |
          - Cache location is documented (e.g., .vrs/cache/ground_truth/)
          - Refresh rules are explicit (TTL, manual invalidation, version change)
          - Cache hit returns data without network fetch
          - Cache miss triggers documented fetch procedure
        evidence_required: |
          - Caching policy excerpt
          - Transcript showing cache hit (fast, no fetch)
          - Transcript showing cache miss (fetch triggered)

  # =============================================================================
  # T3: CI Plan Promise
  # =============================================================================
  - promise: "CI plan enables automated validation"
    task: T3
    tests:
      - name: "CI stages are defined and actionable"
        verifies: "The CI plan can be implemented by a CI system"
        how: |
          1. Read VALIDATION-PLAN.md for CI stages
          2. For each stage, verify it has: trigger, inputs, expected outputs, pass/fail criteria
          3. Verify stages can run independently (no hidden dependencies)
        success_criteria: |
          - CI plan defines at least: PR validation, nightly validation, evidence archival
          - Each stage has clear trigger (PR merge, schedule, manual)
          - Each stage specifies artifacts to produce
          - Pass/fail criteria are machine-checkable (exit codes, file existence)
        evidence_required: |
          - CI stage table with trigger/inputs/outputs/criteria
          - Verification that stages have no undocumented dependencies

      - name: "CI plan references existing scenarios"
        verifies: "CI will run defined scenarios, not abstract tests"
        how: |
          1. Check that CI plan references SCENARIO-MANIFEST.yaml
          2. Verify at least 3 scenarios are marked for CI execution
          3. Confirm those scenarios exist and are runnable
        success_criteria: |
          - CI plan explicitly references scenario manifest
          - Scenarios tagged for CI execution are identified
          - Tagged scenarios exist in manifest with execution instructions
        evidence_required: |
          - CI plan excerpt showing scenario references
          - List of CI-tagged scenarios from manifest

  # =============================================================================
  # T4: Skill Reviewer Promise
  # =============================================================================
  - promise: "Skill reviewer validates all skills and catches problems"
    task: T4
    tests:
      - name: "Skill reviewer catches known-bad skill"
        verifies: "The reviewer identifies problems in malformed skills"
        how: |
          1. Create a deliberately malformed skill (missing required fields, invalid syntax)
          2. Run skill reviewer on this skill
          3. Verify it fails with specific error identifying the problem
        success_criteria: |
          - Reviewer detects the malformation
          - Error message identifies what is wrong (not generic "validation failed")
          - Reviewer exits with non-zero code for bad skill
        evidence_required: |
          - Malformed skill content
          - Reviewer output showing detection and specific error
          - Exit code (non-zero)

      - name: "Skill reviewer passes known-good skills"
        verifies: "Valid skills pass review without false positives"
        how: |
          1. Run skill reviewer on a known-good skill (e.g., vrs-audit)
          2. Verify it passes with no errors
          3. Review output for any false warnings
        success_criteria: |
          - vrs-audit passes skill review
          - No errors or warnings that indicate problems
          - Exit code is 0
        evidence_required: |
          - Reviewer output for vrs-audit showing pass
          - Exit code (0)
          - Review report stored in reviews/ directory

      - name: "All registry skills have reviews"
        verifies: "Coverage is complete"
        how: |
          1. Count skills in registry.yaml
          2. Count review reports in reviews/ directory
          3. Verify 1:1 mapping
        success_criteria: |
          - Every skill in registry.yaml has a corresponding review file
          - No orphan reviews (reviews for skills not in registry)
          - Review files have timestamps
        evidence_required: |
          - Count of registry skills vs review files
          - List showing 1:1 mapping
          - Sample review file with timestamp

  # =============================================================================
  # T5: Rollback/Resume Promise
  # =============================================================================
  - promise: "Interrupting and resuming an audit works correctly"
    task: T5
    tests:
      - name: "Resume continues from checkpoint (I-06)"
        verifies: "An interrupted audit can be resumed without losing progress or duplicating work"
        improvement_id: I-06
        how: |
          1. Start /vrs-audit on a medium-sized contract set
          2. Wait until:
             - Graph build complete (AUDIT-PROGRESS.md shows Phase 1 done)
             - At least 2 beads processed
          3. Interrupt the audit
          4. Verify AUDIT-PROGRESS.md contains checkpoint state
          5. Run /vrs-audit --resume
          6. Verify audit continues WITHOUT duplicating work
        success_criteria: |
          - Checkpoint is created during audit (AUDIT-PROGRESS.md contains graph hash, processed beads)
          - Resume command detects checkpoint
          - Graph is NOT rebuilt (hash matches, no "Building knowledge graph" log)
          - Completed beads are NOT reprocessed (no duplicate bead IDs in processing)
          - New beads continue processing after resume
          - Final report includes all findings (pre and post interrupt)
        evidence_required: |
          - Transcript showing checkpoint creation with graph hash
          - AUDIT-PROGRESS.md showing checkpoint state (graph hash, bead list)
          - Transcript showing interrupt
          - Transcript showing resume WITHOUT graph rebuild
          - Verification that no duplicate bead processing occurred
          - Final report showing findings from both sessions

      - name: "Rollback restores previous state"
        verifies: "Rolling back to a checkpoint actually restores that state"
        how: |
          1. Create a checkpoint at a known state
          2. Make changes (run more stages, modify state)
          3. Rollback to checkpoint
          4. Verify state matches checkpoint state, not current state
        success_criteria: |
          - Checkpoint state is recorded (hash or snapshot)
          - Post-rollback state matches checkpoint state
          - Changes made after checkpoint are reverted
        evidence_required: |
          - State snapshot at checkpoint
          - State after changes (different)
          - State after rollback (matches checkpoint)
          - Diff or comparison showing restoration

      - name: "Alternative paths on failure"
        verifies: "When one path fails, alternative recovery path works"
        how: |
          1. Define a scenario with two paths: happy path and failure recovery path
          2. Trigger failure on happy path (e.g., tool timeout, graph build failure)
          3. Verify system suggests or takes alternative path
          4. Complete via alternative path
        success_criteria: |
          - Scenario defines both paths
          - Failure triggers alternative path suggestion or automatic switch
          - Alternative path completes successfully
          - Final state is valid (not corrupted by failed attempt)
        evidence_required: |
          - Scenario definition with both paths
          - Transcript showing failure and path switch
          - Transcript showing alternative path completion

must_haves:
  truths:
    - "Runbook enables successful audit completion by new users"
    - "Provenance index allows reproducible validation runs"
    - "CI plan is actionable with defined stages and scenarios"
    - "Skill reviewer catches bad skills and passes good ones"
    - "Rollback and resume actually work (not just documented)"
    - "I-06: Resume does NOT duplicate work (no graph rebuild, no bead reprocess)"
    - "I-06: AUDIT-PROGRESS.md contains checkpoint state (graph hash, processed beads, phase)"
  artifacts:
    - path: ".planning/testing/RUNBOOK.md"
      provides: "Step-by-step guide with failure recovery"
      validation_test: "New user can complete audit following runbook"
    - path: ".planning/testing/ground_truth/PROVENANCE-INDEX.md"
      provides: "Reproducible ground truth references with caching policy"
      validation_test: "Can fetch and validate ground truth from provenance entry"
    - path: ".planning/testing/ci/VALIDATION-PLAN.md"
      provides: "Actionable CI stages referencing scenarios"
      validation_test: "CI stages have triggers, inputs, outputs, and scenario refs"
    - path: ".planning/testing/skill-reviewer/reviews/"
      provides: "Per-skill review reports for all registry skills"
      validation_test: "1:1 mapping with registry; bad skill fails, good skill passes"

human_confirmation_context:
  summary: "Phase exit requirements complete with outcome-based evidence for runbook, provenance, CI, skill reviewer, and rollback/resume"
  key_evidence:
    - "Runbook: Transcript of successful audit following runbook steps"
    - "Provenance: Transcript of reproducing validation from provenance entry"
    - "CI plan: Stage table with triggers, inputs, outputs, and scenario refs"
    - "Skill reviewer: Output showing bad skill caught, good skill passed"
    - "Rollback/resume: Transcripts showing actual checkpoint restoration"
  verification_steps:
    - "Confirm runbook enables audit completion (transcript shows all stages)"
    - "Confirm provenance entry allows ground truth fetch with checksum match"
    - "Confirm CI stages reference specific scenarios from manifest"
    - "Confirm skill reviewer rejects malformed skill with specific error"
    - "Confirm resume continues from checkpoint (not restart from scratch)"
    - "Confirm rollback restores state (diff shows restoration)"
  expected_vs_actual:
    expected: "All five promises verified with live evidence"
    actual: "{{populated_at_runtime}}"
  decision_prompt: "Do the outcome tests prove the five promises? Confirm plan completion."
---


## Mandatory claude-code-agent-teams Session Isolation

- claude-code-controller validation runs MUST be launched from a non-development shell/session (separate claude-code-agent-teams session).
- Required session naming: `vrs-demo-{workflow}-{timestamp}`.
- Never target or kill panes outside the dedicated `vrs-demo-*` session for the active run.
- Evidence must record `session_label` and `pane_id` in `manifest.json` and `report.json`.


<objective>
Complete phase exit requirements: UX runbook, ground truth provenance, automation plan, skill reviewer gate, and rollback/resume validation with outcome-based evidence proving each capability works.
</objective>

## Required Sources of Truth
- `.planning/testing/rules/canonical/RULES-ESSENTIAL.md`
- `.planning/testing/rules/canonical/VALIDATION-RULES.md`
- `.planning/testing/rules/canonical/TESTING-FRAMEWORK.md`
- `.planning/testing/rules/canonical/TESTING-PHILOSOPHY.md`
- `.planning/testing/rules/claude-code-controller-REFERENCE.md` and `.planning/testing/rules/canonical/claude-code-controller-instructions.md`
- `skills/enforce-testing-rules.md`
- `docs/reference/workflows.md`
- `docs/reference/testing-framework.md`

## Gate Prerequisites
- Definition Gate completed (Plan 01) and Alignment Campaign started.

## Plan Seed IDs
- N1, N2, N3, N4, N5, I-06

## Required Evidence Artifacts
- See `must_haves.artifacts` in frontmatter and evidence pack outputs in tasks.

## Evidence Requirements
- Any live run must use demo claude-code-agent-teams session label `vrs-demo-{workflow}-{timestamp}`.
- Evidence packs must include proof tokens per `docs/PHILOSOPHY.md` (graph hash, node/edge counts, agent spawn proof, debate proof, detection proof).
- Graph-first rule enforced: BSKG/VQL queries must run before conclusions for any reasoning step.

## Reasoning
These items turn the testing framework into a repeatable user journey. Without outcome-based validation, we only know artifacts exist, not that they deliver value.

## Purpose
Deliver the Phase 07.3.1.6 exit package where:
- Runbook actually helps users succeed
- Provenance enables reproducible validations
- CI plan is implementable
- Skill reviewer catches real problems
- Rollback/resume actually work in practice

## Why
Mechanical checks (file exists, contains keyword, line count) create false confidence. Outcome tests prove users can succeed, validations are reproducible, and recovery paths work.

## How To Implement
- Task 1: Create runbook, verify by following it to complete an audit
- Task 2: Build provenance index, verify by reproducing a validation from provenance
- Task 3: Define CI plan, verify by checking stages reference real scenarios
- Task 4: Run skill reviewer, verify by testing with known-good and known-bad skills
- Task 5: Add rollback/resume scenarios, verify by actually interrupting/resuming/rolling back

## Skills/Subagents to Load
- `/vrs-workflow-test`
- `/vrs-evaluate-workflow`
- `/vrs-claude-code-agent-teams-runner`
- `vrs-claude-controller` (subagent)
- `vrs-secure-reviewer`

<context>
@.planning/phases/07.3.1.6-full-testing-hardening/context.md
@.planning/phases/07.3.1.6-full-testing-hardening/07.3.1.6-SUPER-REPORT.md
@.planning/testing/skill-reviewer/SKILL.md
@.planning/testing/workflows/workflow-failure-recovery.md
</context>

<tasks>

<task id="T1" type="auto">
  <name>UX runbook with verified usability</name>
  <files>
    .planning/testing/RUNBOOK.md
    .planning/testing/OPERATOR-STATUS-CONTRACT.md
    .planning/testing/DECISION-LOG-SCHEMA.md
    docs/workflows/PLAYBOOKS.md
  </files>
  <action>
Create a runbook that:
- Provides step-by-step commands for complete audit workflow
- Includes expected outputs at each stage
- Documents failure modes with recovery instructions
- Is verified by actually following it to complete an audit
Add operator transparency and flexibility:
- Define operator status contract (status fields, task list, task outputs)
- Define decision-log schema (summary of decisions, evidence links)
- Link to playbook branches for graph-only, context-skip, pattern subset, resume/rollback
  </action>

  <outcome_test>
    <name>New user can complete audit</name>
    <how>Follow runbook on fresh project, track blockers, complete audit</how>
    <success>Audit completes OR every blocker has documented recovery in runbook</success>
    <evidence>Transcript of following runbook; list of blockers and recoveries used</evidence>
  </outcome_test>

  <done_when>
    - Runbook exists with step-by-step commands
    - Runbook includes failure modes and recovery instructions
    - Runbook verified by transcript of successful audit completion
    - At least one failure recovery path verified via transcript
    - Operator status contract defines fields for tasks, outputs, and stage state
    - Decision log schema exists and is referenced in the runbook
    - Runbook links to PLAYBOOKS.md for branching workflows
  </done_when>
</task>

<task id="T2" type="auto">
  <name>Ground truth provenance with reproducibility test</name>
  <files>
    .planning/testing/ground_truth/PROVENANCE-INDEX.md
  </files>
  <action>
Create provenance index that:
- Lists ground truth sources with full metadata (source, corpus, version, checksum)
- Documents caching policy (location, refresh rules, integrity checks)
- Is verified by reproducing a validation from provenance info only
  </action>

  <outcome_test>
    <name>Validation reproducible from provenance</name>
    <how>Pick entry, fetch ground truth using only provenance info, verify checksum, run validation</how>
    <success>Data fetches successfully, checksum matches, validation produces consistent result</success>
    <evidence>Transcript of fetch from provenance; checksum verification; validation output</evidence>
  </outcome_test>

  <done_when>
    - Provenance index lists sources with corpus ID, version, checksum
    - Caching policy documented with location and refresh rules
    - At least one entry verified by reproducing validation
    - Checksum match confirmed in transcript
  </done_when>
</task>

<task id="T3" type="auto">
  <name>CI automation plan with scenario references</name>
  <files>
    .planning/testing/ci/VALIDATION-PLAN.md
  </files>
  <action>
Create CI plan that:
- Defines stages (PR validation, nightly, evidence archival)
- Each stage has trigger, inputs, outputs, pass/fail criteria
- References specific scenarios from SCENARIO-MANIFEST.yaml
- Is verified by confirming referenced scenarios exist
  </action>

  <outcome_test>
    <name>CI stages are actionable</name>
    <how>Check each stage has trigger/inputs/outputs/criteria; verify scenario refs exist</how>
    <success>All stages complete; at least 3 scenarios tagged for CI exist in manifest</success>
    <evidence>CI stage table; list of CI-tagged scenarios with existence confirmation</evidence>
  </outcome_test>

  <done_when>
    - CI plan defines PR validation, nightly validation, evidence archival stages
    - Each stage has trigger, inputs, outputs, and pass/fail criteria
    - References at least 3 scenarios from SCENARIO-MANIFEST.yaml
    - Referenced scenarios verified to exist
  </done_when>
</task>

<task id="T4" type="claude-code-agent-teams">
  <name>Skill reviewer with positive and negative tests</name>
  <scenario>skill-reviewer-validation</scenario>
  <files>
    .planning/testing/skill-reviewer/reviews/
    src/alphaswarm_sol/skills/registry.yaml
    .vrs/testing/runs/06-T4-skill-reviewer/transcript.txt
  </files>
  <action>
Run skill reviewer with positive and negative tests:
1. Create a deliberately malformed skill
2. Run reviewer on malformed skill, verify it fails with specific error
3. Run reviewer on vrs-audit, verify it passes
4. Run reviewer on all registry skills, store reports
5. Verify 1:1 mapping between registry skills and review files
  </action>

  <outcome_test>
    <name>Reviewer catches bad, passes good</name>
    <how>Test with malformed skill (should fail), test with vrs-audit (should pass)</how>
    <success>Malformed fails with specific error; vrs-audit passes; all registry skills reviewed</success>
    <evidence>Malformed skill + error output; vrs-audit pass output; review file count vs registry count</evidence>
  </outcome_test>

  <done_when>
    - Malformed skill fails review with specific error identifying problem
    - vrs-audit passes review with no errors
    - All registry skills have review files in reviews/
    - 1:1 mapping verified between registry and reviews
  </done_when>
</task>

<task id="T5" type="claude-code-agent-teams">
  <name>Rollback/resume/alternative-path verification (I-06)</name>
  <scenario>rollback-resume-paths</scenario>
  <improvement_id>I-06</improvement_id>
  <files>
    .planning/testing/scenarios/SCENARIO-MANIFEST.yaml
    .vrs/testing/runs/06-T5-rollback/transcript.txt
    .vrs/testing/runs/06-T5-resume/transcript.txt
    .vrs/testing/runs/06-T5-alternative/transcript.txt
    .vrs/state/AUDIT-PROGRESS.md
  </files>
  <action>
Test rollback, resume, and alternative paths with actual execution (I-06 requirements):

**Resume Test (I-06 specific):**
1. Start /vrs-audit on a medium-sized contract set (multiple contracts)
2. Wait for interrupt conditions:
   - Graph build complete (AUDIT-PROGRESS.md shows phase 1 done)
   - At least 2 beads processed
3. Interrupt the audit (Ctrl+C or kill)
4. Verify AUDIT-PROGRESS.md contains checkpoint state:
   - Graph hash preserved
   - Processed bead IDs listed
   - Current phase marked
5. Resume with /vrs-audit --resume
6. Validate NO duplicate work:
   - Graph is NOT rebuilt (hash matches, no "Building knowledge graph" log)
   - Completed beads are NOT reprocessed (no duplicate bead IDs in processing log)
   - New beads continue processing
   - Final report includes all findings (pre and post interrupt)

**Rollback Test:**
1. Create checkpoint at a known state (after graph build, before bead processing)
2. Run additional stages (process beads, make findings)
3. Rollback to checkpoint
4. Verify state matches checkpoint state (diff shows restoration)
5. Changes made after checkpoint are reverted

**Alternative Path Test:**
1. Define scenario with happy path and failure recovery path
2. Trigger failure on happy path (e.g., tool timeout, graph build failure)
3. Verify system suggests or takes alternative path
4. Complete via alternative path
5. Verify final state is valid

Each test must show actual state changes, not just command execution.
  </action>

  <outcome_test>
    <name>Resume continues from checkpoint</name>
    <how>Interrupt after checkpoint, resume, verify no repeat of completed work</how>
    <success>Resume detects checkpoint; audit continues from checkpoint stage</success>
    <evidence>Transcript showing interrupt, resume, and stage continuation</evidence>
  </outcome_test>

  <outcome_test>
    <name>Rollback restores state</name>
    <how>Create checkpoint, make changes, rollback, diff state before/after</how>
    <success>Post-rollback state matches checkpoint state (changes reverted)</success>
    <evidence>State snapshots at checkpoint, after changes, and after rollback; diff showing restoration</evidence>
  </outcome_test>

  <outcome_test>
    <name>Alternative path completes on failure</name>
    <how>Trigger failure on happy path, follow alternative, complete successfully</how>
    <success>Alternative path suggested or taken; completes with valid final state</success>
    <evidence>Transcript showing failure, path switch, and successful completion</evidence>
  </outcome_test>

  <done_when>
    - Resume transcript shows continuation from checkpoint (not restart)
    - AUDIT-PROGRESS.md contains checkpoint state (graph hash, processed beads, phase)
    - Graph is NOT rebuilt on resume (hash matches, no rebuild log entry)
    - Completed beads are NOT reprocessed (no duplicate bead processing)
    - Final report includes findings from both pre and post interrupt
    - Rollback transcript shows state restoration (diff confirms)
    - Alternative path transcript shows failure handling and successful completion
    - Scenario manifest includes entries for all three tests
  </done_when>
</task>

</tasks>
