---
phase: 07.3.1.6-full-testing-hardening
plan: "11"
type: execute
wave: 4
depends_on: ["04", "09"]

execution:
  mode: autonomous
  human_confirmation: conditional
  confidence_threshold: 0.95

files_modified:
  - .planning/testing/vql/VQL-LIBRARY.md
  - .planning/testing/templates/PATH-EXPLORATION-TEMPLATE.md
  - .planning/testing/guides/guide-pattern-lattice.md
  - .planning/testing/scenarios/SCENARIO-MANIFEST.yaml
  - .planning/phases/07.3.1.6-full-testing-hardening/07.3.1.6-B10M-PATH-REPORT.md

# OUTCOME-BASED VALIDATION
# These tests verify that the PROMISE of Cross-Function Path Exploration was delivered,
# not just that VQL queries exist or path keywords appear.

outcome_validation:
  promise: |
    Deep paths across functions are enumerated before conclusions.
    Non-obvious attack paths are discovered through graph traversal.
    Path exploration improves vulnerability detection accuracy.

  tests:
    - name: cross_function_vuln_discovered
      verifies: "Path exploration finds cross-function vulnerabilities that single-function analysis misses"
      how: |
        1. Use CrossFunctionReentrancy.sol - has vulnerability spanning withdraw() -> transfer()
        2. Run path exploration queries on the knowledge graph
        3. Verify the cross-function attack path is enumerated: withdraw -> external call -> transfer
        4. Verify this path would be missed without cross-function analysis
      success_criteria: |
        - Attack path withdraw() -> msg.sender.call -> transfer() is explicitly enumerated
        - Path includes the reentrancy entry point and the vulnerable transfer function
        - Path exploration provides evidence that single-function analysis would miss
      evidence_required: |
        - VQL query that discovers the cross-function path
        - Enumerated path showing: function entry -> external call -> reentry target
        - Explanation of why this path matters for the vulnerability
      ground_truth_contract: tests/contracts/CrossFunctionReentrancy.sol
      known_attack_path:
        entry: "withdraw()"
        external_call: "msg.sender.call{value: amount}()"
        reentry_target: "transfer()"
        vulnerability: "balances[msg.sender] not yet updated when reentry occurs"

    - name: paths_used_in_reasoning
      verifies: "Enumerated paths are actually used in vulnerability reasoning, not just listed"
      how: |
        1. Run path exploration on SideEntranceLenderPool.sol
        2. Check if reasoning references the specific attack path
        3. Verify reasoning explains HOW the path enables the attack
        4. Not just "paths were explored" but "path A->B->C enables exploit because..."
      success_criteria: |
        - Reasoning explicitly references the enumerated attack path
        - Reasoning explains the causal relationship: path enables state manipulation
        - Path is used to construct the exploit narrative, not just listed
      evidence_required: |
        - Enumerated paths from exploration
        - Reasoning section that references specific paths
        - Causal explanation of how path enables vulnerability
      ground_truth_contract: tests/fixtures/dvd/side-entrance/src/SideEntranceLenderPool.sol
      known_attack_path:
        entry: "flashLoan()"
        callback: "execute() on attacker contract"
        vulnerable_call: "deposit() during callback"
        exploit: "deposit satisfies repayment check while crediting attacker balance"

    - name: path_exploration_improves_detection
      verifies: "Path exploration makes vulnerability detection more accurate"
      how: |
        1. Analyze contract WITHOUT path exploration (direct pattern matching only)
        2. Analyze same contract WITH cross-function path exploration
        3. Compare: does path exploration catch the vulnerability or provide stronger evidence?
      success_criteria: |
        - Path exploration enables finding that would be missed without it
        - OR path exploration provides significantly stronger evidence chain
        - Evidence includes specific graph node IDs and function call sequences
      evidence_required: |
        - Analysis without path exploration
        - Analysis with path exploration
        - Comparison showing path exploration's contribution

    - name: non_obvious_paths_discovered
      verifies: "Path exploration finds paths that are not immediately obvious from code structure"
      how: |
        1. Use a contract with multi-hop vulnerability (e.g., A calls B calls C, vuln in C)
        2. Run path exploration
        3. Verify discovery of indirect paths that span multiple function calls
        4. Paths should include indirect relationships (caller -> intermediate -> target)
      success_criteria: |
        - At least one path with 2+ intermediate steps is discovered
        - Path connects entry point to vulnerable state through multiple hops
        - Path would require deliberate tracing to find manually
      evidence_required: |
        - Multi-hop path from VQL exploration
        - Graph node IDs for each step in the path
        - Explanation of why this path is non-obvious

    - name: vql_queries_are_targeted
      verifies: "VQL path queries are targeted, not just 'enumerate everything'"
      how: |
        1. Review the VQL path query bundle
        2. Verify queries target specific path characteristics (e.g., value transfer -> state write)
        3. Run query on test contract
        4. Verify results are focused, not exhaustive enumeration
      success_criteria: |
        - Queries use semantic filters (operation types, not just function names)
        - Results highlight security-relevant paths, not all possible paths
        - Query documentation explains what the query is looking for and why
      evidence_required: |
        - VQL query bundle with semantic filters
        - Query results showing focused output
        - Explanation of query targeting strategy

must_haves:
  truths:
    - "Cross-function logic paths are enumerated before conclusions"
    - "VQL library includes a cross-function path query bundle"
    - "At least one scenario records path exploration evidence"
  artifacts:
    - path: ".planning/testing/vql/VQL-LIBRARY.md"
      provides: "Cross-function path query bundle"
      contains: "cross-function"
    - path: ".planning/testing/templates/PATH-EXPLORATION-TEMPLATE.md"
      provides: "Path exploration evidence template"
      contains: "attack_path"
    - path: ".planning/phases/07.3.1.6-full-testing-hardening/07.3.1.6-B10M-PATH-REPORT.md"
      provides: "Path exploration evidence summary"
      contains: "discovered_path"

human_confirmation_context:
  summary: "Cross-function path exploration proven to discover attack paths in test contracts"
  key_evidence:
    - "Attack path discovery: withdraw -> external call -> transfer path in CrossFunctionReentrancy"
    - "Reasoning integration: paths used to construct exploit narratives"
    - "Detection improvement: path exploration enabled finding or stronger evidence"
  verification_steps:
    - "Confirm known cross-function attack path was discovered via VQL"
    - "Confirm reasoning explains HOW path enables vulnerability (not just lists path)"
    - "Confirm VQL queries are targeted, not exhaustive enumeration"
  expected_vs_actual:
    expected: "Path exploration discovers known attack paths and improves detection"
    actual: "{{populated_at_runtime}}"
  decision_prompt: "Did path exploration deliver actionable attack path discovery?"
---


## Mandatory claude-code-agent-teams Session Isolation

- claude-code-controller validation runs MUST be launched from a non-development shell/session (separate claude-code-agent-teams session).
- Required session naming: `vrs-demo-{workflow}-{timestamp}`.
- Never target or kill panes outside the dedicated `vrs-demo-*` session for the active run.
- Evidence must record `session_label` and `pane_id` in `manifest.json` and `report.json`.


<objective>
Prove that cross-function path exploration discovers attack paths that improve vulnerability detection accuracy.
</objective>

## Required Sources of Truth
- `.planning/testing/rules/canonical/RULES-ESSENTIAL.md`
- `.planning/testing/rules/canonical/VALIDATION-RULES.md`
- `.planning/testing/rules/canonical/TESTING-FRAMEWORK.md`
- `.planning/testing/rules/canonical/TESTING-PHILOSOPHY.md`
- `.planning/testing/rules/claude-code-controller-REFERENCE.md` and `.planning/testing/rules/canonical/claude-code-controller-instructions.md`
- `skills/enforce-testing-rules.md`
- `docs/reference/workflows.md`
- `docs/reference/testing-framework.md`

## Gate Prerequisites
- Definition Gate completed (Plan 01) and Alignment Campaign started.

## Plan Seed IDs
- B10.M

## Required Evidence Artifacts
- See `must_haves.artifacts` in frontmatter and evidence pack outputs in tasks.

## Evidence Requirements
- Any live run must use demo claude-code-agent-teams session label `vrs-demo-{workflow}-{timestamp}`.
- Evidence packs must include proof tokens per `docs/PHILOSOPHY.md` (graph hash, node/edge counts, agent spawn proof, debate proof, detection proof).
- Graph-first rule enforced: BSKG/VQL queries must run before conclusions for any reasoning step.
- claude-code-agent-teams runs must use Claude Code as the subject inside claude-code-agent-teams (launch shell, then send `claude`); no direct shell runs or simulated outputs.
- Anti-fabrication checks required for claude-code-agent-teams transcripts:
  - Minimum duration: 30s for agent runs (120s if designated E2E).
  - Minimum transcript length: 100 lines (200 lines if designated E2E).
  - Transcript must include tool/agent markers (alphaswarm, agent names, graph/query markers).
  - Perfect metrics (e.g., 100%/100%) are invalid and must be flagged.

## Reasoning
The promise is not "enumerate paths" but "path exploration finds vulnerabilities."
We must prove that discovered paths are used in reasoning and lead to better detection.

## Purpose
Define a cross-function path query bundle, validate it discovers known attack paths.

## Why
B10.M is blocking because deep logic paths must be enumerated before any Tier B/C conclusions are accepted.

## Skills/Subagents to Load
- `/vrs-workflow-test`
- `/vrs-evaluate-workflow`
- `/vrs-claude-code-agent-teams-runner`
- `vrs-claude-controller` (subagent)
- `vrs-attacker`, `vrs-defender`, `vrs-verifier`

<context>
@.planning/phases/07.3.1.6-full-testing-hardening/context.md
@.planning/phases/07.3.1.6-full-testing-hardening/07.3.1.6-SUPER-REPORT.md
@.planning/testing/vql/VQL-LIBRARY.md
@.planning/testing/guides/guide-pattern-lattice.md
@.planning/testing/rules/canonical/RULES-ESSENTIAL.md
</context>

## How To Implement
<tasks>

<task id="T1" type="auto">
  <name>Add cross-function path query bundle and template</name>
  <files>
    .planning/testing/vql/VQL-LIBRARY.md
    .planning/testing/templates/PATH-EXPLORATION-TEMPLATE.md
  </files>
  <action>
Extend the VQL library with a cross-function path bundle that:
- Traverses call graph across functions and contracts
- Filters for security-relevant paths (value transfer, state write ordering)
- Uses semantic operations (TRANSFERS_VALUE_OUT, WRITES_USER_BALANCE) not just function names

Create a path exploration template that captures:
- attack_path: ordered list of functions in the path
- entry_point: where attacker gains control
- external_call: where control transfers to attacker
- vulnerable_state: what state is exploitable during path
- evidence_node_ids: graph node references for each step
  </action>

  <done_when>
    - VQL library includes cross-function path query bundle
    - Bundle uses semantic operation filters, not just function names
    - Path exploration template exists with attack_path field
    - Template captures evidence_node_ids for graph traceability
  </done_when>
</task>

<task id="T2" type="auto">
  <name>Require path exploration in Tier B/C scenarios</name>
  <files>
    .planning/testing/guides/guide-pattern-lattice.md
    .planning/testing/scenarios/SCENARIO-MANIFEST.yaml
  </files>
  <action>
Update the lattice guide and scenario manifest to require:
- path_exploration_required field for Tier B/C scenarios
- Minimum path depth (e.g., at least one 2+ hop path explored)
- Link to the path exploration template
- Failure criteria: conclusions without path evidence are rejected
  </action>

  <done_when>
    - Lattice guide references path exploration requirements
    - Scenario manifest includes path_exploration_required field
    - Failure criteria for missing path enumeration defined
  </done_when>
</task>

<task id="T3" type="claude-code-agent-teams">
  <name>Execute path exploration on cross-function vulnerability</name>
  <scenario>b10m-path-exploration</scenario>
  <files>
    .planning/phases/07.3.1.6-full-testing-hardening/07.3.1.6-B10M-PATH-REPORT.md
    .vrs/testing/runs/11-T3-path-exploration/transcript.txt
  </files>
  <action>
Run path exploration on contracts with known cross-function vulnerabilities:

Contract 1: CrossFunctionReentrancy.sol
- Known path: withdraw() -> msg.sender.call -> transfer()
- Expected discovery: reentrancy allows balance manipulation via transfer during withdraw

Contract 2: SideEntranceLenderPool.sol
- Known path: flashLoan() -> execute() callback -> deposit()
- Expected discovery: deposit during callback satisfies repayment while crediting attacker

claude-code-controller sequence:
- claude-code-controller launch "zsh"
- claude-code-controller send "claude" --pane=X
- claude-code-controller send "/vrs-audit tests/contracts/CrossFunctionReentrancy.sol --path-exploration" --pane=X
- claude-code-controller wait_idle --pane=X --idle-time=15.0
- claude-code-controller capture --pane=X --output=.vrs/testing/runs/11-T3-path-exploration/transcript.txt

Anti-fabrication enforcement:
- Ensure Claude Code is the claude-code-agent-teams subject (no local shell execution).
- Duration >= 30s (>= 120s if designated E2E).
- Transcript >= 100 lines (>= 200 lines if designated E2E).
- Tool/agent markers present; perfect metrics are invalid.

Document in B10M-PATH-REPORT.md:
- Discovered attack paths with graph node IDs
- How paths were used in vulnerability reasoning
- Comparison showing path exploration's contribution to detection
  </action>

  <outcome_test ref="cross_function_vuln_discovered">
    Verify the known attack path (withdraw -> call -> transfer) is discovered.
  </outcome_test>

  <outcome_test ref="paths_used_in_reasoning">
    Verify discovered paths appear in vulnerability reasoning with causal explanation.
  </outcome_test>

  <done_when>
    - Known attack paths are discovered via VQL exploration
    - Paths are used in reasoning (not just listed)
    - B10M-PATH-REPORT.md shows concrete path discovery examples
    - Anti-fabrication checks recorded (duration, line count, tool markers)
  </done_when>
</task>

</tasks>

## How To Verify
- Known cross-function attack paths are discovered via VQL queries
- Discovered paths are used in vulnerability reasoning, not just enumerated
- VQL queries are targeted (semantic filters), not exhaustive enumeration
