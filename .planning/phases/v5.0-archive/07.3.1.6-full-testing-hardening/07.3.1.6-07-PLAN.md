---
phase: 07.3.1.6-full-testing-hardening
plan: "07"
type: execute
wave: 4
depends_on: ["04"]

execution:
  mode: autonomous
  human_confirmation: conditional
  confidence_threshold: 0.95

files_modified:
  - docs/workflows/workflow-context.md
  - docs/reference/testing-framework.md
  - scripts/validate_context.py
  - .planning/testing/guides/guide-context-quality.md
  - .planning/testing/scenarios/SCENARIO-MANIFEST.yaml
  - .planning/phases/07.3.1.6-full-testing-hardening/07.3.1.6-B10A-CONTEXT-REPORT.md
  - tests/fixtures/incomplete-context.yaml
  - tests/fixtures/complete-context.yaml
  - .vrs/settings-context-incomplete.yaml
  - .vrs/settings-context-complete.yaml

outcome_validation:
  promise: "Context packs are validated before use; bad context gets rejected; good context enables Tier C reasoning"

  tests:
    - name: "Incomplete context rejection"
      verifies: "Bad context is rejected with a clear, actionable reason"
      how: |
        1. Create a context pack missing the 'roles' field
        2. Run: python scripts/validate_context.py tests/fixtures/incomplete-context.yaml
        3. Examine exit code and output
      success_criteria: |
        - Exit code is non-zero (rejection occurred)
        - Output explicitly names 'roles' as the missing field
        - Output provides guidance on what 'roles' should contain
      evidence_required: |
        - The incomplete context YAML file used
        - Full validator output showing the rejection message
        - Exit code captured (must be non-zero)

    - name: "Complete context acceptance"
      verifies: "Well-formed context passes validation and is usable"
      how: |
        1. Create a context pack with all required fields (roles, upgradeability, asset_flows, trust_boundaries)
        2. Run: python scripts/validate_context.py tests/fixtures/complete-context.yaml
        3. Examine exit code and output
      success_criteria: |
        - Exit code is zero (acceptance)
        - Output confirms all required fields present
        - Output includes the context pack summary suitable for downstream use
      evidence_required: |
        - The complete context YAML file used
        - Full validator output showing acceptance
        - Exit code captured (must be zero)

    - name: "Tier C gate enforcement"
      verifies: "Incomplete context actually prevents Tier C reasoning from proceeding"
      how: |
        1. Launch a claude-code-agent-teams session with Claude Code
        2. Attempt a Tier C audit with an incomplete context pack
        3. Observe whether the audit proceeds or halts at the context gate
      success_criteria: |
        - Audit does NOT produce Tier C findings when context is incomplete
        - Audit emits a clear message about why Tier C was skipped
        - Transcript shows the gate check occurred before any Tier C reasoning
      evidence_required: |
        - claude-code-agent-teams transcript showing the attempted Tier C audit
        - The incomplete context pack that was used
        - Evidence that Tier C reasoning was blocked (not just warned)

    - name: "Context enables Tier C"
      verifies: "Valid context allows Tier C reasoning to proceed and produce findings"
      how: |
        1. Launch a claude-code-agent-teams session with Claude Code
        2. Run a Tier C audit with a complete, valid context pack
        3. Observe whether Tier C reasoning engages
      success_criteria: |
        - Audit proceeds past the context gate
        - Tier C reasoning occurs (economic context referenced in findings)
        - Findings include context-derived insights (role-based access, trust boundary violations, etc.)
      evidence_required: |
        - claude-code-agent-teams transcript showing Tier C audit completing
        - The complete context pack that was used
        - At least one finding that references context information (roles, trust boundaries, etc.)

    - name: "Validation prevents bad reasoning"
      verifies: "The gate actually prevents the problem it claims to solve"
      how: |
        1. Compare two audit runs: one with incomplete context (Tier A/B only), one with complete context (Tier C enabled)
        2. Examine the reasoning depth and quality of findings
      success_criteria: |
        - Incomplete context run lacks role-aware or economic-aware findings
        - Complete context run includes deeper reasoning tied to protocol context
        - The difference is attributable to context, not random variance
      evidence_required: |
        - Both transcripts side by side
        - Comparison analysis showing the reasoning difference
        - At least one finding from the complete-context run that would be impossible without context

must_haves:
  truths:
    - "Context packs include required fields for roles, upgradeability, asset flows, and trust boundaries"
    - "Tier B/C gates fail fast when context quality checks do not pass"
    - "Context validation outputs are captured in evidence packs"
  artifacts:
    - path: ".planning/testing/guides/guide-context-quality.md"
      provides: "Context quality checklist and validation rules"
      contains: "Required Fields"
      validation_script: null
    - path: "scripts/validate_context.py"
      provides: "Context validation enforcement"
      contains: "required"
      validation_script: null
    - path: ".planning/phases/07.3.1.6-full-testing-hardening/07.3.1.6-B10A-CONTEXT-REPORT.md"
      provides: "Context gate evidence summary"
      contains: "claude-code-agent-teams"
      validation_script: null

human_confirmation_context:
  summary: "Context quality gate established with checklist, validator, and live claude-code-agent-teams evidence"
  key_evidence:
    - "Context checklist: .planning/testing/guides/guide-context-quality.md"
    - "Validation script: scripts/validate_context.py"
    - "Context gate transcript: .vrs/testing/runs/07-T3-context-gate/transcript.txt"
  verification_steps:
    - "Confirm incomplete context is rejected with clear reason"
    - "Confirm complete context passes and enables Tier C"
    - "Confirm the gate actually prevents bad reasoning"
  expected_vs_actual:
    expected: "Context quality gate prevents incomplete context from reaching Tier C reasoning"
    actual: "{{populated_at_runtime}}"
  decision_prompt: "Does the context quality gate actually prevent bad reasoning from incomplete context? Confirm plan completion."
---


## Mandatory claude-code-agent-teams Session Isolation

- claude-code-controller validation runs MUST be launched from a non-development shell/session (separate claude-code-agent-teams session).
- Required session naming: `vrs-demo-{workflow}-{timestamp}`.
- Never target or kill panes outside the dedicated `vrs-demo-*` session for the active run.
- Evidence must record `session_label` and `pane_id` in `manifest.json` and `report.json`.


<objective>
Establish a protocol context quality gate so Tier B/C reasoning cannot proceed with incomplete or low quality context packs.
</objective>

## Required Sources of Truth
- `.planning/testing/rules/canonical/RULES-ESSENTIAL.md`
- `.planning/testing/rules/canonical/VALIDATION-RULES.md`
- `.planning/testing/rules/canonical/TESTING-FRAMEWORK.md`
- `.planning/testing/rules/canonical/TESTING-PHILOSOPHY.md`
- `.planning/testing/rules/claude-code-controller-REFERENCE.md` and `.planning/testing/rules/canonical/claude-code-controller-instructions.md`
- `skills/enforce-testing-rules.md`
- `docs/reference/workflows.md`
- `docs/reference/testing-framework.md`

## Gate Prerequisites
- Definition Gate completed (Plan 01) and Alignment Campaign started.

## Plan Seed IDs
- B10.A

## Required Evidence Artifacts
- See `must_haves.artifacts` in frontmatter and evidence pack outputs in tasks.

## Evidence Requirements
- Any live run must use demo claude-code-agent-teams session label `vrs-demo-{workflow}-{timestamp}`.
- Evidence packs must include proof tokens per `docs/PHILOSOPHY.md` (graph hash, node/edge counts, agent spawn proof, debate proof, detection proof).
- Graph-first rule enforced: BSKG/VQL queries must run before conclusions for any reasoning step.
- If simulated context is used, transcript must include the explicit bypass marker `[CONTEXT_SIMULATED]`.

## Reasoning
Tier B/C scenarios depend on protocol and economic context. Without a quality gate, the system can produce confident but unfounded reasoning.

## Purpose
Define required context fields, enforce validation in tooling, and prove the gate with a live claude-code-agent-teams scenario.

## Why
B10.A is blocking because context defects invalidate complex vulnerability evaluation and undermine evidence credibility.

## Skills/Subagents to Load
- `/vrs-context-pack`
- `/vrs-economic-context`
- `/vrs-workflow-test`
- `/vrs-evaluate-workflow`
- `/vrs-claude-code-agent-teams-runner`
- `vrs-claude-controller` (subagent)

<context>
@.planning/phases/07.3.1.6-full-testing-hardening/context.md
@.planning/phases/07.3.1.6-full-testing-hardening/07.3.1.6-SUPER-REPORT.md
@docs/workflows/workflow-context.md
@docs/reference/testing-framework.md
@.planning/testing/rules/canonical/RULES-ESSENTIAL.md
</context>

## How To Implement
<tasks>

<task id="T1" type="auto">
  <name>Define context quality checklist and required fields</name>
  <files>
    .planning/testing/guides/guide-context-quality.md
    docs/workflows/workflow-context.md
    docs/reference/testing-framework.md
  </files>
  <action>
Define required fields and validation criteria for protocol context packs:
- roles and authorities
- upgradeability model
- asset flows and pricing inputs
- trust boundaries and external dependencies
- assumptions and invariants
Document pass/fail criteria and Tier B/C gating behavior.
  </action>

  <done_when>
    - File .planning/testing/guides/guide-context-quality.md exists
    - Contains "Required Fields" heading
    - Lists roles, upgradeability, asset flows, trust boundaries
    - Documents Tier B/C gating behavior
  </done_when>
</task>

<task id="T2" type="auto">
  <name>Enforce context validation in tooling</name>
  <files>
    scripts/validate_context.py
    docs/reference/testing-framework.md
  </files>
  <action>
Extend the context validation script to enforce required fields and emit:
- pass/fail summary
- missing-field list
- evidence marker for context quality status
Align testing-framework guidance to require validation output in evidence packs.
  </action>

  <done_when>
    - File scripts/validate_context.py exists
    - Script runs with --help without error
    - Script fails on incomplete packs with explicit missing-field output
  </done_when>
</task>

<task id="T3" type="claude-code-agent-teams">
  <name>Prove context gate with outcome-based tests</name>
  <scenario>b10a-context-gate</scenario>
  <files>
    .planning/testing/scenarios/SCENARIO-MANIFEST.yaml
    .planning/phases/07.3.1.6-full-testing-hardening/07.3.1.6-B10A-CONTEXT-REPORT.md
    .vrs/testing/runs/07-T3-context-gate/transcript.txt
  </files>
  <action>
Execute the outcome tests defined above:

Test 1 - Incomplete context rejection:
- Create tests/fixtures/incomplete-context.yaml missing 'roles'
- python scripts/validate_context.py tests/fixtures/incomplete-context.yaml
- Capture exit code and output

Test 2 - Complete context acceptance:
- Create tests/fixtures/complete-context.yaml with all required fields
- python scripts/validate_context.py tests/fixtures/complete-context.yaml
- Capture exit code and output

Test 3 - Tier C gate enforcement (claude-code-agent-teams):
- Create `.vrs/settings-context-incomplete.yaml` pointing to tests/fixtures/incomplete-context.yaml
- claude-code-controller launch "zsh"
- claude-code-controller send "claude" --pane=X
- claude-code-controller send "/vrs-audit tests/fixtures/foundry-vault/ --settings .vrs/settings-context-incomplete.yaml" --pane=X
- Verify Tier C was blocked

Test 4 - Context enables Tier C (claude-code-agent-teams):
- Create `.vrs/settings-context-complete.yaml` pointing to tests/fixtures/complete-context.yaml
- claude-code-controller send "/vrs-audit tests/fixtures/foundry-vault/ --settings .vrs/settings-context-complete.yaml" --pane=X
- Verify Tier C reasoning occurred

Summarize results in B10A-CONTEXT-REPORT.md with evidence of each test outcome.
  </action>

  <done_when>
    - Test 1 shows rejection with clear reason
    - Test 2 shows acceptance
    - Test 3 shows Tier C blocked without complete context
    - Test 4 shows Tier C reasoning with complete context
    - B10A-CONTEXT-REPORT.md includes all test evidence
  </done_when>
</task>

</tasks>

## Completion Criteria
- Incomplete context is rejected with actionable feedback.
- Complete context passes validation and enables Tier C.
- The gate demonstrably prevents bad reasoning (not just missing markers).
- B10.A report includes evidence for each outcome test.
