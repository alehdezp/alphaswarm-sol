# Phase 07.3.1.6 Improvements Backlog

**Purpose:** Consolidated, actionable improvements to align Phase 07.3.1.6 and the testing framework with canonical rules, progressive disclosure, lattice practices, and real claude-code-agent-teams validation.

**Status:** Active (tracked + validated)

## Progress Tracker

| IMP | Status | Validation | Notes / Blockers |
|---|---|---|---|
| IMP-01 | Done | `validate_workflow_refs.py --strict` ✅ | Registry + docs aligned; skills path clarified in guides. |
| IMP-02 | Done | Doc review ✅ | claude-code-controller CLI usage standardized; requires real claude-code-agent-teams runs to fully prove. |
| IMP-03 | Done | `validate_evidence_pack.py --strict` ✅ | Example pack validates; schemas added. |
| IMP-04 | Done | `validate_scenario_manifest.py --strict` ✅ | Schema + lint enforced. |
| IMP-05 | Done | `validate_scenario_manifest.py --strict` ✅ | Marker registry added and referenced. |
| IMP-06 | In progress (exec blocked) | Schema + docs ✅ | Graph ablation scenarios added; blocked on graph-disable flag + ground truth. |
| IMP-07 | In progress (exec blocked) | Docs ✅ | Perf plan + scenarios added; harness not executed yet. |
| IMP-08 | Done | Script + fixtures ✅ | Context validator + fixtures exist; claude-code-agent-teams proof pending. |
| IMP-09 | In progress (exec blocked) | Manifest check ✅ | Economic scenario requires ground truth; model library exists. |
| IMP-10 | Done | Doc check ✅ | Missing templates added and referenced. |
| IMP-11 | Partial | `verify_command_inventory.py` ✅ | Runner added; transcripts not captured yet. |
| IMP-12 | Done | `validate_plan_consistency.py --strict` ✅ | Plan paths + deps aligned. |
| IMP-13 | Done | `validate_workflow_refs.py --strict` ✅ | Canonical skill source documented. |
| IMP-14 | Done | Doc check ✅ | Run directory standardized across docs/plans. |
| IMP-15 | Done | `validate_scenario_manifest.py --strict` ✅ | Template + manifest aligned. |
| IMP-16 | Done | Doc check ✅ | Context markers unified. |
| IMP-17 | Done (docs) | Doc review ✅ | Workflow realism fixed; claude-code-agent-teams runs pending. |
| IMP-18 | Done | `validate_plan_consistency.py --strict` ✅ | Dependencies + thresholds aligned. |
| IMP-19 | Done | Path check ✅ | Missing fixtures/settings present. |
| IMP-20 | In progress (exec blocked) | Policy updated ✅ | Ground truth enforced in context/manifest; provenance entries pending. |
| IMP-21 | Done | `validate_evidence_pack.py --strict` ✅ | Session label enforced in schema + validator. |
| IMP-22 | Done | Doc index updated ✅ | Coverage + outputs documented. |
| IMP-23 | TODO | — | Graph-first enforcement mechanism (maps to IMP-C2). |
| IMP-24 | TODO | — | Control run mechanism for economic context (maps to IMP-C3). |
| IMP-25 | TODO | — | Debate quality baseline (maps to IMP-C4). |
| IMP-26 | TODO | — | Graph citation traceability (maps to IMP-G3). |
| IMP-27 | TODO | — | Single-agent vs multi-agent comparison (maps to IMP-H1). |
| IMP-28 | TODO | — | Debate quality assessment (maps to IMP-H2). |
| IMP-29 | TODO | — | Verifier independence validation (maps to IMP-H3). |
| IMP-30 | TODO | — | Behavioral vs name-based detection (maps to IMP-I1). |
| IMP-31 | TODO | — | Per-class pattern metrics (maps to IMP-I2). |

### Validation Log (Latest Run)

- `.venv/bin/python scripts/validate_evidence_pack.py .planning/testing/templates/evidence-pack-example --strict` → PASS
- `.venv/bin/python scripts/validate_scenario_manifest.py --strict` → PASS
- `.venv/bin/python scripts/validate_workflow_refs.py --strict` → PASS
- `.venv/bin/python scripts/validate_plan_consistency.py --strict` → PASS
- `.venv/bin/python scripts/verify_command_inventory.py` → PASS (structure; transcripts still pending)
- `.venv/bin/python scripts/validate_context.py --context tests/fixtures/complete-context.yaml` → PASS (`[CONTEXT_READY]`)
- `.venv/bin/python scripts/validate_context.py --context tests/fixtures/incomplete-context.yaml` → FAIL as expected (`[CONTEXT_INCOMPLETE]`)
- claude-code-controller smoke run (workflow-cli-install) → transcript captured at `.vrs/testing/runs/cli-install-20260204-005429/transcript.txt`; boundary markers missing (known gap)

## Improvement Items

### IMP-01 — Skill/Test-Framework Alignment (P0)
- **Problem:** Testing docs and plans reference skills (`/vrs-workflow-test`, `/vrs-evaluate-workflow`, `claude-code-controller`) that are not listed in `src/alphaswarm_sol/skills/registry.yaml` or use inconsistent locations (`skills/` vs `src/alphaswarm_sol/skills/shipped/`). This breaks traceability and registry validation gates.
- **Proposal:**
  - Update registry to include testing skills OR update docs to reference canonical skill names/paths that actually ship.
  - Normalize skill locations and slash commands (vrs-* naming) across registry, `.claude/skills`, and docs.
- **Affected Files:** `src/alphaswarm_sol/skills/registry.yaml`, `.claude/skills/*`, `docs/reference/testing-framework.md`, `.planning/testing/workflows/*.md`, phase plans 03/14.
- **Validation:** `scripts/validate_workflow_refs.py --strict` passes; claude-code-agent-teams run shows skills resolve without “skill not found”.

### IMP-02 — claude-code-controller Skill vs CLI Consistency (P0)
- **Problem:** Canonical rules previously said “use the claude-code-controller skill (slash command),” but docs and plans use the `claude-code-controller` CLI. There is no explicit claude-code-controller skill file; actual tooling is `vrs-claude-code-agent-teams-runner` and the `claude-code-controller` binary.
- **Proposal:**
  - Choose a single canonical interface (either `claude-code-controller` skill or `claude-code-controller` CLI) and update rules + docs to match.
  - If keeping `claude-code-controller`, add the skill and map to CLI; if keeping CLI, update canonical rules to require `claude-code-controller` command usage.
- **Affected Files:** `.planning/testing/rules/canonical/TESTING-FRAMEWORK.md`, `.planning/testing/rules/claude-code-controller-REFERENCE.md` and `.planning/testing/rules/canonical/claude-code-controller-instructions.md`, `.planning/testing/guides/guide-claude-code-controller.md`.
- **Validation:** claude-code-agent-teams-run instructions are executable verbatim and produce transcripts stored under `.vrs/testing/runs/<run_id>/`.

### IMP-03 — Evidence Pack Schema + Validator (P0)
- **Problem:** Evidence pack schema is described but lacks a formal JSON schema and a validator script. Plan 02 references missing templates (`evidence-pack-example/`, `evidence-checklist.md`, validator output).
- **Proposal:**
  - Add JSON schema files for `report.json`, `manifest.json`, `environment.json`.
  - Add a validator script and a working example pack that passes.
  - Ensure transcript headers and `VRS-START/VRS-END` markers are enforced.
- **Affected Files:** `.planning/testing/guides/guide-evidence.md`, new `schemas/testing/*`, new `scripts/validate_evidence_pack.py`, add example pack under `.planning/testing/templates/evidence-pack-example/`.
- **Validation:** `python scripts/validate_evidence_pack.py <example>` returns 0; example pack shows graph-first and tool markers.

### IMP-04 — Scenario Manifest Schema + Lint (P0)
- **Problem:** `SCENARIO-MANIFEST.yaml` is missing mandatory fields required by newer plans (tool requirements, context pack refs, graph/VQL requirements, ground truth). Many entries are blocked or incomplete.
- **Proposal:**
  - Define a manifest schema and linting script.
  - Add required fields: `workflow_ref`, `context_ref`, `tool_requirements`, `vql_bundle`, `ground_truth_ref`, `evidence_pack_required`, `min_markers`.
  - Add a “status reason” policy with concrete blockers.
- **Affected Files:** `.planning/testing/scenarios/SCENARIO-MANIFEST.yaml`, `.planning/testing/templates/scenario-manifest.yaml`, new `scripts/validate_scenario_manifest.py`.
- **Validation:** Lint passes; blocked entries have explicit blockers and remediation links.

### IMP-05 — Canonical Marker Registry (P0)
- **Problem:** Marker lists (preflight, graph, tools, TaskCreate/TaskUpdate, context) are duplicated across docs, risking drift.
- **Proposal:**
  - Create a single marker registry file and reference it from workflows and guides.
  - Require exact marker strings and timing/ordering rules (graph-first, stage ordering).
- **Affected Files:** `docs/reference/testing-framework.md`, `.planning/testing/workflows/*`, `.planning/testing/guides/guide-orchestration-progress.md`.
- **Validation:** Workflow docs reference the registry; marker list is single source of truth.

### IMP-06 — Graph Contribution & Ablation Testing (P0)
- **Problem:** The framework requires graph-first reasoning but does not measure whether the graph materially improves reasoning.
- **Proposal:**
  - Add “graph ablation” scenarios: run with graph disabled vs enabled; compare detection accuracy and reasoning depth.
  - Add `graph_usage` metrics to evidence packs (query count, unique nodes cited, time spent in query stage).
- **Affected Files:** `.planning/testing/guides/guide-pattern-evaluation.md`, `.planning/testing/scenarios/SCENARIO-MANIFEST.yaml`, new `docs/reference/graph-usage-metrics.md`.
- **Validation:** Evidence pack includes graph usage metrics; ablation results show measurable delta.

### IMP-07 — HENTI Multi-Orchestration Performance Suite (P1)
- **Problem:** No performance test plan exists for multi-agent orchestration (throughput, latency, queueing).
- **Proposal:**
  - Define a performance scenario suite (concurrency 1/4/8; queue depth; tool latency).
  - Capture metrics: task throughput, mean time to verdict, failure rate, tool latency.
- **Affected Files:** `.planning/testing/scenarios/SCENARIO-MANIFEST.yaml`, new `.planning/testing/perf/PERF-PLAN.md`, update `docs/reference/testing-framework.md`.
- **Validation:** Performance runs produce a metrics report with realistic variance; no 100% metrics.

### IMP-08 — Context Quality Gate Artifacts (P1)
- **Problem:** Plan 07 references `guide-context-quality.md` and `scripts/validate_context.py` but neither exist in the current testing docs.
- **Proposal:**
  - Create the guide and validator; define required context fields and gating behavior.
  - Add example complete/incomplete context fixtures.
- **Affected Files:** new `.planning/testing/guides/guide-context-quality.md`, `scripts/validate_context.py`, `tests/fixtures/*context*`.
- **Validation:** Incomplete context fails with explicit missing fields; complete context passes; Tier C gate blocks/opens accordingly.

### IMP-09 — Economic Modeling Library & Enforcement (P1)
- **Problem:** Economic modeling template exists but no library or enforced linkage to scenarios.
- **Proposal:**
  - Create `ECONOMIC-MODEL-LIBRARY.md` with models across protocol types.
  - Require `behavior_model_ref` in scenario manifest for Tier B/C economic scenarios.
- **Affected Files:** `.planning/testing/scenarios/economic/ECONOMIC-MODEL-LIBRARY.md`, `.planning/testing/scenarios/SCENARIO-MANIFEST.yaml`, `docs/reference/testing-framework.md`.
- **Validation:** Tier B/C economic scenario includes model reference and concrete profit analysis.

### IMP-10 — Missing Templates for Evaluation/Discovery/Paths (P1)
- **Problem:** Plans 09/11/13 reference templates that don’t exist (`PATTERN-EVALUATION-TEMPLATE.md`, `PATH-EXPLORATION-TEMPLATE.md`, `PATTERN-DISCOVERY-LOG.md`).
- **Proposal:**
  - Add the templates and wire them into guides and scenario requirements.
- **Affected Files:** `.planning/testing/templates/*`, `.planning/testing/guides/guide-pattern-evaluation.md`, `.planning/testing/guides/guide-pattern-lattice.md`.
- **Validation:** Templates are referenced in docs and used in at least one claude-code-agent-teams run evidence pack.

### IMP-11 — Command Inventory Completion + Verifier (P1)
- **Problem:** `COMMAND-INVENTORY.md` is unverified; workflows depend on it.
- **Proposal:**
  - Add a verification script that runs each command via claude-code-controller and records transcript paths.
  - Update the inventory with verified status and timestamps.
- **Affected Files:** `.planning/testing/COMMAND-INVENTORY.md`, new `scripts/verify_command_inventory.py`.
- **Validation:** Inventory shows verified commands with transcript refs; verification script passes.

### IMP-12 — Plan-Doc Path Corrections & Consistency (P2)
- **Problem:** Phase plans reference non-canonical paths (`skills/vrs-audit.md` vs `src/alphaswarm_sol/skills/shipped/audit.md`) and non-existent docs.
- **Proposal:**
  - Normalize paths across plans and docs to use the canonical locations.
  - Add a small linter that checks for missing referenced files.
- **Affected Files:** `.planning/phases/07.3.1.6-full-testing-hardening/*-PLAN.md`.
- **Validation:** Linter finds zero missing refs; plan file paths exist.

### IMP-13 — Canonical Skill Source-of-Truth (P0)
- **Problem:** Skills exist in `skills/`, `.claude/skills/`, and `src/alphaswarm_sol/skills/shipped/` with inconsistent references across plans/docs/registry. This breaks traceability and makes workflow refs brittle.
- **Proposal:**
  - Declare a single source-of-truth for skill files (recommend: `skills/` for dev authoring, `.claude/skills/` for installed runtime, `src/alphaswarm_sol/skills/shipped/` for shipped builds).
  - Document the sync/install flow and update all plan/docs references to the canonical path(s).
  - Add a validator that cross-checks registry entries, workflow refs, and filesystem locations.
- **Affected Files:** `src/alphaswarm_sol/skills/registry.yaml`, `.planning/testing/workflows/*.md`, `.planning/phases/07.3.1.6-full-testing-hardening/*-PLAN.md`, `docs/reference/workflows.md`.
- **Validation:** Registry refs resolve to existing files; plan/docs paths pass linter; claude-code-agent-teams runs can invoke the named skills without “skill not found”.

### IMP-14 — Evidence Pack Location & Run Directory (P0)
- **Problem:** Evidence pack location is inconsistent (`.vrs/testing/runs/<run_id>/` vs `.vrs/testing/transcripts/`), and workflows don’t specify capture output paths.
- **Proposal:**
  - Define a canonical run directory layout (e.g., `.vrs/testing/runs/<run_id>/`) and treat `transcripts/`/`reports/` as optional indexes or symlinks.
  - Update guides/workflows to capture transcripts with explicit output paths.
  - Add `manifest.json` with run metadata (session label, pane id, git commit, duration, line count).
- **Affected Files:** `.planning/testing/guides/guide-evidence.md`, `.planning/testing/guides/guide-claude-code-controller.md`, `.planning/testing/guides/guide-worktree-scenarios.md`, `.planning/testing/workflows/*.md`, `.planning/testing/DOCS-VALIDATION-STATUS.md`.
- **Validation:** A new run produces a complete run directory; transcripts captured to the expected path; evidence pack validates.

### IMP-15 — Scenario Manifest Schema Alignment (P0)
- **Problem:** `templates/scenario-manifest.yaml` does not match `scenarios/SCENARIO-MANIFEST.yaml`. Required fields for B10 (context, VQL bundle, tool requirements, behavior models) are missing or inconsistent.
- **Proposal:**
  - Align the template to the real manifest structure (list entries with `doc`, `status`, `blocked_reason`, `requires_ground_truth`).
  - Add required fields: `behavior_model_ref`, `vql_bundle`, `tool_requirements`, `workflow_ref`, `evidence_pack_required`, `min_markers`.
  - Add a schema + linter to enforce required fields.
- **Affected Files:** `.planning/testing/templates/scenario-manifest.yaml`, `.planning/testing/scenarios/SCENARIO-MANIFEST.yaml`, `.planning/testing/scenarios/economic/ECONOMIC-MODEL-LIBRARY.md`, `.planning/testing/vql/VQL-LIBRARY.md`.
- **Validation:** Linter passes; manifest entries include required fields or explicit blockers.

### IMP-16 — Context Gate Marker Unification (P1)
- **Problem:** Context markers conflict (`[CONTEXT_READY]/[CONTEXT_INCOMPLETE]` vs `CONTEXT_QUALITY_PASS/FAIL`), causing ambiguous validation.
- **Proposal:**
  - Create a canonical marker registry for context gating and update all guides/workflows to use the same markers.
  - Require the context validator to emit those markers into transcripts.
- **Affected Files:** `.planning/testing/guides/guide-context-quality.md`, `.planning/testing/workflows/workflow-orchestration.md`, `.planning/testing/scenarios/SCENARIO-MANIFEST.yaml`, `docs/reference/testing-framework.md`.
- **Validation:** All context-gated runs emit the same marker set; validation checks succeed.

### IMP-17 — claude-code-agent-teams Workflow Realism (P0)
- **Problem:** Several workflows run shell commands *after* launching `claude`, which is not executable as written. Many claude-code-agent-teams tasks omit capture output paths and required `wait_idle` steps.
- **Proposal:**
  - Split shell-only workflows to run commands before `claude` or use `claude-code-controller execute` for non-interactive steps.
  - Add explicit capture output paths for every workflow.
  - Add minimal `wait_idle` steps for command stabilization.
- **Affected Files:** `.planning/testing/workflows/workflow-cli-install.md`, `.planning/testing/workflows/workflow-tools.md`, `.planning/testing/workflows/workflow-graph.md`, `.planning/testing/workflows/workflow-real-env-validation.md`, `.planning/phases/07.3.1.6-full-testing-hardening/*-PLAN.md`.
- **Validation:** Workflows can be executed verbatim without manual edits; transcripts saved to expected paths.

### IMP-18 — Plan Dependency + Threshold Alignment (P0)
- **Problem:** Plan dependencies in `PLAN-INDEX` and plan frontmatter are inconsistent; several plans use line-count thresholds below canonical rules.
- **Proposal:**
  - Normalize dependencies across `PLAN-INDEX` and plan frontmatter.
  - Update line-count and duration thresholds to match `RULES-ESSENTIAL.md`.
- **Affected Files:** `.planning/phases/07.3.1.6-full-testing-hardening/07.3.1.6-PLAN-INDEX.md`, `.planning/phases/07.3.1.6-full-testing-hardening/*-PLAN.md`.
- **Validation:** Dependency graph is consistent; no plan uses thresholds below canonical minimums.

### IMP-19 — Missing Fixtures & Context Settings (P1)
- **Problem:** Plans reference missing fixtures (e.g., `tests/fixtures/*`) and settings files (`.vrs/settings-*.yaml`) that do not exist.
- **Proposal:**
  - Add required fixtures/settings or update plans to use existing assets.
  - Track fixtures in `files_modified` for the relevant plans.
- **Affected Files:** `tests/fixtures/*`, `.vrs/settings-*.yaml`, `.planning/phases/07.3.1.6-full-testing-hardening/07.3.1.6-07-PLAN.md`, `07.3.1.6-09-PLAN.md`, `07.3.1.6-11-PLAN.md`, `07.3.1.6-12-PLAN.md`, `07.3.1.6-13-PLAN.md`.
- **Validation:** All referenced fixtures exist; claude-code-agent-teams runs can use them as documented.

### IMP-20 — External Ground Truth Enforcement (P0)
- **Problem:** Several validation plans and scenarios do not require external ground truth, violating canonical rules.
- **Proposal:**
  - Add explicit provenance requirements to every validation plan and scenario.
  - Add `ground_truth_ref` (with URL/commit/hash) to scenario manifest entries where validation claims are made.
- **Affected Files:** `.planning/phases/07.3.1.6-full-testing-hardening/*-PLAN.md`, `.planning/testing/scenarios/SCENARIO-MANIFEST.yaml`, `.planning/testing/guides/guide-ground-truth.md`.
- **Validation:** Validation runs include external provenance; no circular validation.

### IMP-21 — Session Label Enforcement & Run Metadata (P1)
- **Problem:** `vrs-demo-{workflow}-{timestamp}` is required but not enforced or captured in evidence.
- **Proposal:**
  - Add a required field in manifest/report for session label and pane id.
  - Add a simple validator that checks session labels and captures them in transcripts.
- **Affected Files:** `.planning/testing/guides/guide-evidence.md`, `.planning/testing/rules/canonical/RULES-ESSENTIAL.md`, `.planning/testing/rules/canonical/TESTING-FRAMEWORK.md`.
- **Validation:** Evidence packs include session label; validator fails runs without the label.

### IMP-22 — Doc Index Coverage & Progressive Disclosure (P1)
- **Problem:** `DOC-INDEX.md` and `README.md` omit key docs and under-specify progressive disclosure paths.
- **Proposal:**
  - Update doc counts and include missing docs (coverage map, decision log schema, operator status contract, templates, VQL library).
  - Add “Outputs/Artifacts” section to make generated files explicit.
- **Affected Files:** `.planning/testing/DOC-INDEX.md`, `.planning/testing/README.md`, `.planning/testing/CONTEXT-OVERVIEW.md`.
- **Validation:** Index reflects all docs; session starters include newly added docs.

### IMP-23 — Graph-First Enforcement Mechanism (P0)
- **Problem:** "Graph-first reasoning" rule has no verification mechanism. Agents may skip BSKG queries before drawing conclusions.
- **Strategic Ref:** IMP-C2
- **Proposal:**
  - Create `scripts/validate_graph_first.py` to verify VQL queries precede conclusions.
  - Define query markers (`[VQL_MIN_SET_START]`, `VQL-MIN-{id}`) and conclusion markers.
  - Enforce rule: "first_query_line < first_conclusion_line" with 80% threshold.
- **Affected Files:** `scripts/validate_graph_first.py` (create), `.planning/testing/rules/canonical/RULES-ESSENTIAL.md`.
- **Validation:** Script passes on compliant transcripts; fails when conclusions precede queries.

### IMP-24 — Control Run Mechanism (Economic Context) (P1)
- **Problem:** No mechanism to disable economic context for control runs in ablation studies.
- **Strategic Ref:** IMP-C3
- **Proposal:**
  - Create `.vrs/settings-no-economic.yaml` to disable economic context.
  - Add verification markers: `[ECONOMIC_CONTEXT_DISABLED]` / `[ECONOMIC_CONTEXT_ENABLED]`.
- **Affected Files:** `.vrs/settings-no-economic.yaml` (create), Plan 12.
- **Validation:** Control run emits disabled marker; test run emits enabled marker.

### IMP-25 — Debate Quality Baseline (P1)
- **Problem:** Plan 16 claims debate is "better" but no baseline for comparison exists.
- **Strategic Ref:** IMP-C4
- **Proposal:**
  - Create baseline comparison template with metrics: finding_count, evidence_depth, reasoning_length.
  - Define comparison thresholds: multi > single * 1.5 for nuance.
- **Affected Files:** `.planning/testing/templates/DEBATE-BASELINE-COMPARISON.md` (create).
- **Validation:** Baseline collected; comparison shows measurable differences (or documents no difference).

### IMP-26 — Graph Citation Traceability (P0)
- **Problem:** Findings may cite graph nodes that don't exist or are irrelevant. Evidence must be verifiable.
- **Strategic Ref:** IMP-G3
- **Proposal:**
  - Create `scripts/verify_graph_citations.py` to extract and verify all cited node IDs.
  - Verify: node exists, properties match, node appeared in preceding VQL results.
  - Require ≥90% citation validity rate.
- **Affected Files:** `scripts/verify_graph_citations.py` (create).
- **Validation:** Evidence packs with valid citations pass; fabricated citations fail.

### IMP-27 — Single-Agent vs Multi-Agent Comparison (P0)
- **Problem:** No baseline proving multi-agent debate improves outcomes vs. single-agent review.
- **Strategic Ref:** IMP-H1
- **Proposal:**
  - Run single-agent (`vrs-secure-reviewer`) and multi-agent (`attacker/defender/verifier`) on same contracts.
  - Compare: finding_count, evidence_depth, false_positive/negative rates.
  - Document cost vs quality trade-off.
- **Affected Files:** `.planning/testing/templates/SINGLE-VS-MULTI-COMPARISON.md` (create).
- **Validation:** Comparison completed on 3+ contracts; results documented with honest assessment.

### IMP-28 — Debate Quality Assessment (P1)
- **Problem:** Debate could be performative (agents talk but don't influence each other).
- **Strategic Ref:** IMP-H2
- **Proposal:**
  - Measure: cross-references between agents, specific citations (not generic), verdict synthesis.
  - Detect boilerplate: >80% structural similarity across debates = BOILERPLATE ALERT.
  - Create `scripts/analyze_debate_quality.py`.
- **Affected Files:** `scripts/analyze_debate_quality.py` (create).
- **Validation:** Real debates score ≥0.7 quality; boilerplate detected and flagged.

### IMP-29 — Verifier Independence Validation (P1)
- **Problem:** Verifier could rubber-stamp attacker conclusions without adding value.
- **Strategic Ref:** IMP-H3
- **Proposal:**
  - Track agreement/rejection rates between verifier and attacker.
  - Require: agreement_rate ≤ 80%, rejection_rate ≥ 10%.
  - Every disagreement must cite specific defender evidence.
- **Affected Files:** `.claude/agents/vrs-verifier/` (update with independence requirements).
- **Validation:** Verifier shows independent judgment; not 100% agreement.

### IMP-30 — Behavioral vs Name-Based Detection Comparison (P0)
- **Problem:** No proof that behavioral detection (semantic ops) outperforms name-based detection.
- **Strategic Ref:** IMP-I1
- **Proposal:**
  - Create test contracts where names are misleading (renamed functions, safe names on vulnerable code).
  - Compare detection rates between name-based and behavioral approaches.
  - Document which approach catches more vulnerabilities.
- **Affected Files:** `tests/contracts/semantic-test-set/RenamedWithdraw.sol`, `SafeNameUnsafeCode.sol`, `ObfuscatedReentrancy.sol` (create), `.planning/testing/templates/BEHAVIORAL-VS-NAME-COMPARISON.md` (create).
- **Validation:** Behavioral catches ≥2 vulns that name-based misses; documented evidence.

### IMP-31 — Per-Class Pattern Metrics (P1)
- **Problem:** Patterns may work for some vulnerability classes but fail for others. No per-class breakdown.
- **Strategic Ref:** IMP-I2
- **Proposal:**
  - Measure precision/recall per vulnerability class (reentrancy, access_control, oracle_manipulation, economic_exploits).
  - Set tier-appropriate thresholds (Tier A ≥80% precision, Tier C ≥50%).
  - Document capability as "Supported", "Partial", "Limited", or "Out of Scope".
- **Affected Files:** `.planning/testing/templates/PER-CLASS-METRICS-REPORT.md` (create), `docs/reference/capability-boundaries.md` (update).
- **Validation:** Per-class metrics documented; ≥2 classes meet "Supported" threshold.

---

## Assignment (For Reviewer Agents)
- **Agent A:** IMP-01, IMP-02, IMP-13 (skills/registry + claude-code-controller alignment + skill source-of-truth)
- **Agent B:** IMP-03, IMP-04, IMP-05, IMP-14, IMP-15, IMP-21 (evidence/manifest/markers + run directory + session label enforcement)
- **Agent C:** IMP-06, IMP-07, IMP-26 (graph contribution + performance + citation traceability)
- **Agent D:** IMP-08, IMP-09, IMP-10, IMP-16, IMP-24 (context/economic/templates + context marker unification + control runs)
- **Agent E:** IMP-11, IMP-12, IMP-18, IMP-19, IMP-23 (command inventory + plan path/dep/fixtures consistency + graph-first enforcement)
- **Agent F:** IMP-17, IMP-20, IMP-22 (claude-code-agent-teams workflow realism + external ground truth enforcement + doc index coverage)
- **Agent G:** IMP-25, IMP-27, IMP-28, IMP-29 (debate baseline + single vs multi + debate quality + verifier independence)
- **Agent H:** IMP-30, IMP-31 (behavioral vs name-based detection + per-class metrics)


## Reviewer Findings (Agent Summaries)

- **Evidence Pack Drift:** Full-testing evidence pack schema (07.3.1.5) and execution evidence schema (07.3.2) are incompatible; validator expects 07.3.2 layout while `/vrs-full-testing` writes 07.3.1.5 layout. Canonical docs currently describe neither accurately. Unify or version schemas and route validation accordingly.
- **Marker Drift:** Boundary markers differ (`[VRS-START]/[VRS-END]` vs `[ALPHASWARM-START]/[ALPHASWARM-END]`) across docs and code/tests; requires a single marker registry and shared constants.
- **Missing Artifacts:** Multiple plan‑referenced files are missing (e.g., evidence pack example, scenario libraries, tool matrix, context quality guide, discovery templates). See IMP‑08–IMP‑10 for immediate creation targets.
- **Path Mismatches:** Phase plans reference non‑canonical paths (`skills/` vs `src/alphaswarm_sol/skills/shipped/` vs `.claude/skills/`). Add a linter to detect missing refs and normalize references.
- **Graph Contribution:** Add graph ablation scenarios with delta metrics and usage counters in report.json; require external ground truth for deltas.
- **HENTI Performance:** Define concurrency/queue depth scenarios and perf metrics (throughput, latency, error rates) with realistic variance.
- **Doc Index Drift:** `DOC-INDEX.md` undercounts docs and omits key references (coverage map, decision log schema, templates, VQL library). Update counts and session starters.
- **Transcript Path Inconsistency:** Evidence guide expects `.vrs/testing/runs/<run_id>/` while other guides/workflows use `.vrs/testing/transcripts/`. Standardize.
- **Workflow Realism Gap:** Several workflows run shell commands after launching `claude`, which is not executable as written. Split shell vs Claude phases or use `claude-code-controller execute`.
- **Plan Dependency Drift:** Plan dependencies in frontmatter differ from `PLAN-INDEX` and reference missing fixtures/settings; align dependencies and add missing assets.
