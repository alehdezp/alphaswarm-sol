---
phase: 07.3.1.6-full-testing-hardening
plan: "15"
type: execute
wave: 5
depends_on: ["03", "04"]

execution:
  mode: autonomous
  human_confirmation: conditional
  confidence_threshold: 0.95

files_modified:
  - .planning/testing/guides/guide-orchestration-proof.md
  - .planning/testing/templates/ORCHESTRATION-MARKER-CHECKLIST.md
  - .planning/testing/scenarios/SCENARIO-MANIFEST.yaml
  - .planning/phases/07.3.1.6-full-testing-hardening/07.3.1.6-I01-ORCHESTRATION-REPORT.md
  - .planning/phases/07.3.1.6-full-testing-hardening/07.3.1.6-I05-E2E-REPORT.md
  - .vrs/testing/runs/15-T1-orchestration-proof/transcript.txt
  - .vrs/testing/runs/15-T2-e2e-pipeline/transcript.txt
  - tests/contracts/MANIFEST.yaml

# =============================================================================
# IMPROVEMENT ROADMAP IMPLEMENTATION: I-01 + I-05
# =============================================================================
# I-01: Orchestration Behavior Proof (HIGH PRIORITY)
#   Gap: Plans describe orchestration but don't prove it happens
#   Validation: Prove orchestration markers appear in real execution
#
# I-05: End-to-End Pipeline Validation (HIGH PRIORITY)
#   Gap: Components tested individually, integration seams untested
#   Validation: Prove full pipeline works from invocation to final report
# =============================================================================

outcome_validation:
  plan_promise: |
    This plan proves TWO critical capabilities:
    1. Orchestration actually happens (markers appear, agents spawn, tasks created)
    2. The full pipeline works end-to-end (user invocation → final report)

    Without these validations, the system could be a single-agent report generator
    masquerading as a multi-agent orchestration framework.

  tests:
    # =========================================================================
    # I-01: ORCHESTRATION BEHAVIOR PROOF
    # =========================================================================
    - name: "Orchestration markers appear in execution transcript"
      verifies: "The 9-stage pipeline from PHILOSOPHY.md actually executes"
      improvement_id: I-01
      how: |
        1. Run /vrs-audit on a contract with known vulnerabilities
        2. Parse transcript for REQUIRED orchestration markers:
           - [PREFLIGHT_PASS] or [PREFLIGHT_FAIL]
           - [GRAPH_BUILD_SUCCESS] with hash
           - TaskCreate(id=..., type=...) invocations (at least 3)
           - Agent spawn markers (vrs-attacker, vrs-defender, vrs-verifier)
           - [VERIFICATION_COMPLETE] with verdict
        3. FAIL if ANY required marker is missing
        4. Record marker timestamps to prove sequencing
      success_criteria: |
        - Transcript contains ALL stage markers from PHILOSOPHY.md 9-stage pipeline
        - At least 3 TaskCreate invocations with distinct task IDs
        - At least 2 subagent spawn events visible in transcript
        - Stage markers appear in CORRECT sequence (preflight → graph → detection → verification)
        - Markers are NOT static text - they contain dynamic values (hashes, IDs, timestamps)
      evidence_required: |
        - Full transcript showing marker sequence
        - Extracted marker list with timestamps
        - Proof that markers contain dynamic values (not template strings)
      failure_indicates: |
        If markers are missing, the system is NOT orchestrating - it's just generating reports.
        This is a CRITICAL failure that invalidates the multi-agent value proposition.

    - name: "TaskCreate produces distinct task structures"
      verifies: "Tasks are real work items, not just logging"
      improvement_id: I-01
      how: |
        1. Extract TaskCreate calls from transcript
        2. Verify each task has:
           - Unique task ID
           - Meaningful subject (not generic)
           - Security-relevant type (finding, verification, etc.)
        3. Verify TaskUpdate follows TaskCreate (state transitions happen)
      success_criteria: |
        - At least 3 distinct task IDs in transcript
        - Task subjects describe specific security concerns (not "Task 1", "Task 2")
        - TaskUpdate calls show state transitions (pending → in_progress → completed)
        - Tasks can be traced to specific findings in final output
      evidence_required: |
        - Extracted TaskCreate/TaskUpdate pairs
        - Task subject analysis showing security relevance
        - State transition log

    - name: "Subagent spawns produce reasoned outputs"
      verifies: "Agents don't just spawn - they produce work"
      improvement_id: I-01
      how: |
        1. Identify subagent spawn markers in transcript
        2. For each spawn, verify:
           - Agent returned output (not timeout/error)
           - Output contains graph node references
           - Output contributed to final verdict
      success_criteria: |
        - vrs-attacker spawn followed by exploit hypothesis output
        - vrs-defender spawn followed by guard analysis output
        - vrs-verifier spawn followed by verdict output
        - Each output contains at least 1 graph node ID
      evidence_required: |
        - Agent spawn/output pairs from transcript
        - Node ID extraction from each agent output

    # =========================================================================
    # I-05: END-TO-END PIPELINE VALIDATION
    # =========================================================================
    - name: "Fresh environment audit completes successfully"
      verifies: "The full pipeline works without prior state"
      improvement_id: I-05
      how: |
        1. Start with clean .vrs/ directory (no prior state)
        2. Run /vrs-audit on DamnVulnerableDeFi challenge contract
        3. Wait for completion (max 600s timeout)
        4. Collect final report artifact
      success_criteria: |
        - Pipeline completes without error or hang
        - Duration is realistic (30s - 600s, not instant, not infinite)
        - Final report artifact exists and is non-empty
        - Report is parseable (valid structure)
      evidence_required: |
        - Transcript showing start to completion
        - Duration measurement
        - Report artifact with size > 0
      max_duration_seconds: 600

    - name: "Report contains traceable findings"
      verifies: "Findings are real, not fabricated"
      improvement_id: I-05
      how: |
        1. Parse the final report for findings
        2. For each finding, verify:
           - Contains at least 1 graph node ID (bead.evidence.nodes format)
           - Contains at least 1 code location (file:line format)
           - Graph node ID exists in the actual graph
        3. Verify at least 1 finding is a true positive against ground truth
      success_criteria: |
        - Report contains at least 1 finding
        - All findings have both graph node AND code location evidence
        - Node IDs can be verified against graph (not fabricated)
        - At least 1 true positive when compared to ground truth
      evidence_required: |
        - Findings extracted from report
        - Node ID verification against graph
        - Ground truth comparison showing TP

    - name: "Integration seams work"
      verifies: "Handoffs between stages preserve context"
      improvement_id: I-05
      how: |
        1. Verify install → audit handoff (tools available when audit starts)
        2. Verify detection → verification handoff (findings flow to verifier)
        3. Verify verification → report handoff (verdicts appear in report)
      success_criteria: |
        - Tools status shows available before detection starts
        - Detection findings appear in verifier input
        - Verifier verdicts appear in final report with matching IDs
      evidence_required: |
        - Transcript showing tool availability check
        - Finding ID flow: detection → verification → report

must_haves:
  truths:
    - "9-stage orchestration pipeline executes with all required markers"
    - "TaskCreate produces distinct, security-relevant tasks"
    - "Subagent spawns produce reasoned outputs with graph evidence"
    - "Full pipeline completes end-to-end on fresh environment"
    - "Report findings are traceable to graph nodes and code locations"
    - "Integration seams preserve context across stages"
  artifacts:
    - path: ".planning/testing/guides/guide-orchestration-proof.md"
      provides: "Orchestration validation protocol"
      validation: "Contains all 9 stage markers and verification steps"
    - path: ".planning/testing/templates/ORCHESTRATION-MARKER-CHECKLIST.md"
      provides: "Checklist of required markers"
      validation: "Lists all markers with expected format"
    - path: ".planning/phases/07.3.1.6-full-testing-hardening/07.3.1.6-I01-ORCHESTRATION-REPORT.md"
      provides: "Orchestration proof evidence"
      validation: "Contains marker sequence with timestamps"
    - path: ".planning/phases/07.3.1.6-full-testing-hardening/07.3.1.6-I05-E2E-REPORT.md"
      provides: "E2E pipeline evidence"
      validation: "Contains finding traceability proof"
    - path: "tests/contracts/MANIFEST.yaml"
      provides: "Test contract ground truth registry"
      validation: "Lists contracts with known vulnerabilities"

human_confirmation_context:
  summary: "Orchestration behavior and E2E pipeline proven with real execution evidence"
  key_evidence:
    - "Orchestration markers: .vrs/testing/runs/15-T1-orchestration-proof/transcript.txt"
    - "E2E pipeline: .vrs/testing/runs/15-T2-e2e-pipeline/transcript.txt"
    - "Orchestration report: 07.3.1.6-I01-ORCHESTRATION-REPORT.md"
    - "E2E report: 07.3.1.6-I05-E2E-REPORT.md"
  verification_steps:
    - "Confirm ALL 9 stage markers appear in correct sequence"
    - "Confirm TaskCreate produces at least 3 distinct tasks"
    - "Confirm subagent outputs contain graph node IDs"
    - "Confirm pipeline completes on fresh environment"
    - "Confirm at least 1 true positive finding against ground truth"
  expected_vs_actual:
    expected: "Orchestration proven, E2E pipeline works, findings are traceable"
    actual: "{{populated_at_runtime}}"
  decision_prompt: "Does this prove the system actually orchestrates and the pipeline works end-to-end?"
---


## Mandatory claude-code-agent-teams Session Isolation

- claude-code-controller validation runs MUST be launched from a non-development shell/session (separate claude-code-agent-teams session).
- Required session naming: `vrs-demo-{workflow}-{timestamp}`.
- Never target or kill panes outside the dedicated `vrs-demo-*` session for the active run.
- Evidence must record `session_label` and `pane_id` in `manifest.json` and `report.json`.


<objective>
Prove that orchestration actually happens (I-01) and the full pipeline works end-to-end (I-05).
</objective>

## Required Sources of Truth
- `.planning/testing/rules/canonical/RULES-ESSENTIAL.md`
- `.planning/testing/rules/canonical/VALIDATION-RULES.md`
- `.planning/testing/rules/canonical/TESTING-FRAMEWORK.md`
- `.planning/testing/rules/canonical/TESTING-PHILOSOPHY.md`
- `.planning/testing/rules/canonical/claude-code-controller-instructions.md`
- `docs/PHILOSOPHY.md` (9-stage pipeline definition)
- `skills/enforce-testing-rules.md`

## Gate Prerequisites
- Definition Gate completed (Plan 01)
- Audit orchestration hardening complete (Plan 03)
- Tier B/C harness established (Plan 04)

## Plan Seed IDs
- I-01 (Orchestration Behavior Proof)
- I-05 (End-to-End Pipeline Validation)

## Why This Plan Exists

The SUPER-REPORT identified: *"orchestration behavior is unproven and untested."*

Without this plan, the system could be:
- A single-agent report generator pretending to be multi-agent
- Individual components working but failing at integration points
- Producing findings that cannot be traced to real evidence

This plan is the **critical validation** that the framework's core value proposition is real.

## Evidence Requirements
- Demo claude-code-agent-teams session: `vrs-demo-orchestration-{timestamp}`
- Proof tokens per `docs/PHILOSOPHY.md`
- Anti-fabrication: min 200 lines, min 120s duration, tool/agent markers required
- Perfect metrics (100%/100%) are INVALID and must be flagged

## Skills/Subagents to Load
- `/vrs-audit`
- `/vrs-workflow-test`
- `/vrs-evaluate-workflow`
- `/vrs-claude-code-agent-teams-runner`
- `vrs-claude-controller` (subagent)
- `vrs-attacker`, `vrs-defender`, `vrs-verifier`

<context>
@.planning/phases/07.3.1.6-full-testing-hardening/context.md
@.planning/phases/07.3.1.6-full-testing-hardening/07.3.1.6-SUPER-REPORT.md
@.planning/phases/07.3.1.6-full-testing-hardening/IMPROVEMENT-ROADMAP.md
@docs/PHILOSOPHY.md
@.planning/testing/rules/canonical/RULES-ESSENTIAL.md
</context>

<tasks>

<task id="T1" type="auto">
  <name>Create orchestration proof protocol and marker checklist</name>
  <files>
    .planning/testing/guides/guide-orchestration-proof.md
    .planning/testing/templates/ORCHESTRATION-MARKER-CHECKLIST.md
    tests/contracts/MANIFEST.yaml
  </files>
  <action>
Create documentation that:

1. Orchestration Proof Guide (.planning/testing/guides/guide-orchestration-proof.md):
   - Lists all 9 stage markers from PHILOSOPHY.md
   - Defines expected marker format (with dynamic values)
   - Provides transcript parsing instructions
   - Defines failure criteria for missing markers
   - Includes example of correct vs incorrect marker sequence

2. Marker Checklist (.planning/testing/templates/ORCHESTRATION-MARKER-CHECKLIST.md):
   - [ ] [PREFLIGHT_PASS] or [PREFLIGHT_FAIL] with reason
   - [ ] [GRAPH_BUILD_SUCCESS] with graph hash
   - [ ] [CONTEXT_READY] or [CONTEXT_INCOMPLETE] with marker
   - [ ] [CONTEXT_SIMULATED] if simulated/bypassed context is used
   - [ ] [TOOLS_INITIALIZED] with tool list
   - [ ] [DETECTION_STARTED] with scope
   - [ ] TaskCreate(id=X, type=Y, subject=Z) at least 3 times
   - [ ] Agent spawn: vrs-attacker with bead ID
   - [ ] Agent spawn: vrs-defender with bead ID
   - [ ] Agent spawn: vrs-verifier with verdict
   - [ ] [VERIFICATION_COMPLETE] with confidence
   - [ ] [REPORT_GENERATED] with path

3. Test Contract Manifest (tests/contracts/MANIFEST.yaml):
   - List test contracts with known vulnerabilities
   - Include expected detection tier (A/B/C)
   - Include expected agents to engage
   - Provide ground truth for validation
  </action>

  <done_when>
    - Guide exists with all 9 stage markers documented
    - Checklist exists with checkbox format
    - Contract manifest exists with at least 5 contracts
    - Each contract has ground truth vulnerabilities listed
  </done_when>
</task>

<task id="T2" type="claude-code-agent-teams">
  <name>Prove orchestration markers appear in real execution</name>
  <scenario>i01-orchestration-proof</scenario>
  <files>
    .planning/phases/07.3.1.6-full-testing-hardening/07.3.1.6-I01-ORCHESTRATION-REPORT.md
    .vrs/testing/runs/15-T1-orchestration-proof/transcript.txt
  </files>
  <action>
Execute orchestration proof test:

1. Select test contract with known vulnerability:
   - Use tests/contracts/CrossFunctionReentrancy.sol or similar
   - Contract must be in MANIFEST.yaml with ground truth

2. claude-code-controller execution:
   - claude-code-controller launch "zsh"
   - claude-code-controller send "cd /path/to/project" --pane=X
   - claude-code-controller send "claude" --pane=X
   - claude-code-controller wait_idle --pane=X --idle-time=10.0
   - claude-code-controller send "/vrs-audit tests/contracts/CrossFunctionReentrancy.sol" --pane=X
   - claude-code-controller wait_idle --pane=X --idle-time=30.0 --timeout=600
   - claude-code-controller capture --pane=X --output=.vrs/testing/runs/15-T1-orchestration-proof/transcript.txt

3. Parse transcript for markers:
   - Extract each stage marker with timestamp
   - Verify sequence is correct
   - Count TaskCreate invocations (must be >= 3)
   - Count subagent spawns (must be >= 2)

4. Create I01-ORCHESTRATION-REPORT.md with:
   - Marker sequence table
   - TaskCreate analysis
   - Subagent spawn evidence
   - Checklist completion status

Anti-fabrication enforcement:
- Duration >= 120s (E2E designated)
- Transcript >= 200 lines
- Tool/agent markers present
- Perfect metrics are invalid
  </action>

  <outcome_validation>
    <test name="all_markers_present">
      <verifies>9-stage pipeline executes completely</verifies>
      <success_criteria>All markers from checklist present in correct sequence</success_criteria>
    </test>
    <test name="tasks_created">
      <verifies>Orchestration produces real work items</verifies>
      <success_criteria>At least 3 TaskCreate with distinct IDs and security-relevant subjects</success_criteria>
    </test>
    <test name="agents_spawned">
      <verifies>Multi-agent behavior occurs</verifies>
      <success_criteria>At least 2 subagent spawns with reasoned outputs</success_criteria>
    </test>
  </outcome_validation>

  <done_when>
    - Transcript captured with >= 200 lines
    - All 9 stage markers identified in transcript
    - At least 3 TaskCreate invocations extracted
    - At least 2 subagent spawn events identified
    - Marker sequence is correct (no out-of-order stages)
    - I01-ORCHESTRATION-REPORT.md shows completed checklist
  </done_when>
</task>

<task id="T3" type="claude-code-agent-teams">
  <name>Prove end-to-end pipeline on fresh environment</name>
  <scenario>i05-e2e-pipeline</scenario>
  <files>
    .planning/phases/07.3.1.6-full-testing-hardening/07.3.1.6-I05-E2E-REPORT.md
    .vrs/testing/runs/15-T2-e2e-pipeline/transcript.txt
  </files>
  <action>
Execute E2E pipeline test:

1. Prepare fresh environment:
   - Use separate worktree or clean .vrs/ directory
   - Ensure no prior state exists

2. Select DamnVulnerableDeFi challenge:
   - Use tests/fixtures/dvd/side-entrance/ or similar
   - Has known vulnerability with ground truth

3. claude-code-controller execution:
   - claude-code-controller launch "zsh"
   - claude-code-controller send "cd /path/to/clean-worktree" --pane=X
   - claude-code-controller send "rm -rf .vrs/" --pane=X  # Ensure fresh
   - claude-code-controller send "claude" --pane=X
   - claude-code-controller wait_idle --pane=X --idle-time=10.0
   - claude-code-controller send "/vrs-audit tests/fixtures/dvd/side-entrance/src/" --pane=X
   - claude-code-controller wait_idle --pane=X --idle-time=30.0 --timeout=600
   - claude-code-controller capture --pane=X --output=.vrs/testing/runs/15-T2-e2e-pipeline/transcript.txt

4. Validate report:
   - Report exists at expected path
   - Report is non-empty
   - At least 1 finding with graph node ID
   - At least 1 finding with code location
   - At least 1 true positive vs ground truth

5. Create I05-E2E-REPORT.md with:
   - Pipeline duration
   - Finding summary
   - Evidence traceability proof
   - Ground truth comparison

Anti-fabrication enforcement:
- Duration >= 120s (E2E designated)
- Transcript >= 200 lines
- Tool/agent markers present
- Perfect metrics are invalid
  </action>

  <outcome_validation>
    <test name="pipeline_completes">
      <verifies>Full pipeline works from start to finish</verifies>
      <success_criteria>Completes without error in 30s-600s range</success_criteria>
    </test>
    <test name="findings_traceable">
      <verifies>Findings have real evidence</verifies>
      <success_criteria>All findings have graph node ID AND code location</success_criteria>
    </test>
    <test name="true_positive">
      <verifies>System finds real vulnerabilities</verifies>
      <success_criteria>At least 1 TP against ground truth</success_criteria>
    </test>
  </outcome_validation>

  <done_when>
    - Pipeline completed without error
    - Duration is in realistic range (30s-600s)
    - Report exists and is non-empty
    - At least 1 finding has graph node ID
    - At least 1 finding has code location
    - At least 1 true positive vs ground truth
    - I05-E2E-REPORT.md documents all evidence
  </done_when>
</task>

</tasks>

## Verification

| Task | Promise | Verification Question |
|------|---------|----------------------|
| T1 | Protocol defined | Are all markers documented with expected formats? |
| T2 | Orchestration proven | Do ALL 9 stage markers appear in correct sequence? |
| T3 | E2E works | Does pipeline complete with traceable findings? |

## Completion Criteria
- All 9 stage markers appear in orchestration transcript
- At least 3 TaskCreate invocations with distinct IDs
- At least 2 subagent spawns with reasoned outputs
- E2E pipeline completes on fresh environment
- At least 1 true positive finding traceable to graph and code
- Both I01 and I05 reports demonstrate the promised capabilities
