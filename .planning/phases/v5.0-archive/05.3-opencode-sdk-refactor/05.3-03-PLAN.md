---
phase: 05.3-opencode-sdk-refactor
plan: 03
type: execute
wave: 3
depends_on: ["05.3-01"]
files_modified:
  - src/true_vkg/agents/runtime/codex_cli.py
autonomous: true

must_haves:
  truths:
    - "Codex CLI can be invoked for review tasks"
    - "Different perspective for double-checks is provided"
    - "Non-interactive mode works with codex exec"
  artifacts:
    - path: "src/true_vkg/agents/runtime/codex_cli.py"
      provides: "CodexCLIRuntime implementation"
      exports: ["CodexCLIRuntime", "CodexCLIConfig"]
      min_lines: 200
  key_links:
    - from: "src/true_vkg/agents/runtime/codex_cli.py"
      to: "src/true_vkg/agents/runtime/base.py"
      via: "AgentRuntime ABC inheritance"
      pattern: "class CodexCLIRuntime\\(AgentRuntime\\)"
---

<objective>
Implement CodexCLIRuntime for reviews and alternative perspective tasks.

Purpose: Codex CLI (ChatGPT Plus subscription $20/month) provides a different model perspective for double-checking and reviews. Using multiple model families reduces correlated errors.

Output: CodexCLIRuntime class implementing AgentRuntime ABC for review tasks.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05.3-opencode-sdk-refactor/05.3-CONTEXT.md
@src/true_vkg/agents/runtime/base.py
@src/true_vkg/agents/runtime/types.py
</context>

<execution_guardrails>
- Full doc alignment: align objective, must_haves, and tasks against `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, plus phase context or research docs before execution. Any scope change must be reflected in the plan and `.planning/STATE.md`.
- Workspace isolation: run any commands or experiments that mutate state in a fresh jj workspace; do not use the main workspace and do not reuse workspaces for reruns.
- Repeat-run detection markers: include `[ALPHASWARM-START] RUN_ID TIMESTAMP` and `[ALPHASWARM-END] RUN_ID EXIT_CODE DURATION_MS` markers in transcripts or logs so reruns are detectable.
- claude-code-controller subject: for interactive runs, use `claude-code-controller` and launch Claude Code as the subject in the claude-code-agent-teams session; transcripts must show the Claude Code session.
- Seed scope boundaries: treat B-seeds (baseline or regression inputs) and N-seeds (net-new exploratory inputs) as separate scopes; do not mix them in a single run and track results separately.
</execution_guardrails>

<tasks>

<task type="auto">
  <name>Task 1: Implement CodexCLIRuntime</name>
  <files>src/true_vkg/agents/runtime/codex_cli.py</files>
  <action>
Create codex_cli.py with CodexCLIRuntime class:

1. CodexCLIConfig dataclass with fields:
   - model: str = "gpt-4o" (default model)
   - timeout_seconds: int = 180
   - max_tokens: int = 4096
   - working_dir: Path | None = None

2. CodexCLIRuntime(AgentRuntime) with:
   - __init__(config, working_dir)
   - _build_command(prompt, mode) -> List[str]
   - async _run_subprocess(cmd, timeout) -> Dict
   - _parse_response(result, latency_ms) -> AgentResponse

3. Implement abstract methods:
   - async execute(config, messages) -> AgentResponse
   - async spawn_agent(config, task) -> AgentResponse
   - get_model_for_role(role) -> str (always returns config.model)
   - get_usage() -> Dict

4. CLI command format per CONTEXT.md research:
   - Non-interactive: `codex exec "prompt" --json`
   - With context: `codex exec "prompt" --json --context file.sol`

5. Use cases per 05.3-CONTEXT.md:
   - Review: Alternative perspective on findings
   - Discussion: Interactive review (future)
   - Double-check: Verify Claude's conclusions

6. Error handling:
   - Subscription limit detection
   - API timeout handling
   - Fallback behavior (return empty response vs raise)

Note: Codex CLI is subscription-based ($20/month), set cost_usd = 0.0.
Primary use is for review/discussion tasks to get GPT-4 perspective.
  </action>
  <verify>
python -c "from true_vkg.agents.runtime.codex_cli import CodexCLIRuntime, CodexCLIConfig; r = CodexCLIRuntime(CodexCLIConfig()); print(r.get_model_for_role(None))"
  </verify>
  <done>CodexCLIRuntime implements AgentRuntime with codex exec for reviews</done>
</task>

<task type="auto">
  <name>Task 2: Add unit tests</name>
  <files>tests/agents/test_codex_cli_runtime.py</files>
  <action>
Create tests for CodexCLIRuntime:

1. Test CodexCLIConfig defaults:
   - Default model is gpt-4o
   - Timeout is 180 seconds

2. Test command building:
   - codex exec format
   - --json flag present
   - Prompt escaping works

3. Test response parsing:
   - JSON output parsed correctly
   - cost_usd = 0.0

4. Test error handling (mock subprocess):
   - Timeout handling
   - Non-zero exit code

Use pytest fixtures with mocked subprocess.
At least 12 tests.
  </action>
  <verify>
cd /Volumes/ex_ssd/home/projects/python/vkg-solidity/alphaswarm && uv run pytest tests/agents/test_codex_cli_runtime.py -v --tb=short
  </verify>
  <done>All 12+ tests pass</done>
</task>

</tasks>

<verification>
- CodexCLIRuntime inherits from AgentRuntime ABC
- All abstract methods implemented
- Command uses codex exec with --json
- Unit tests pass
</verification>

<success_criteria>
- CodexCLIRuntime class is importable
- CLI command is correctly formatted
- Review tasks can be routed to this runtime
- 12+ unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05.3-opencode-sdk-refactor/05.3-03-SUMMARY.md`
</output>
