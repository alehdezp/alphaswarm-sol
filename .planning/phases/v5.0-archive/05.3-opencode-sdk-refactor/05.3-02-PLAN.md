---
phase: 05.3-opencode-sdk-refactor
plan: 02
type: execute
wave: 2
depends_on: ["05.3-01"]
files_modified:
  - src/true_vkg/agents/runtime/claude_code.py
autonomous: true

must_haves:
  truths:
    - "Claude Code CLI can be invoked as subprocess with JSON output"
    - "Session management works via --resume flag"
    - "Subagent spawning uses Task tool conceptually"
    - "Critical analysis tasks route to this runtime"
  artifacts:
    - path: "src/true_vkg/agents/runtime/claude_code.py"
      provides: "ClaudeCodeRuntime implementation"
      exports: ["ClaudeCodeRuntime", "ClaudeCodeConfig"]
      min_lines: 300
  key_links:
    - from: "src/true_vkg/agents/runtime/claude_code.py"
      to: "src/true_vkg/agents/runtime/base.py"
      via: "AgentRuntime ABC inheritance"
      pattern: "class ClaudeCodeRuntime\\(AgentRuntime\\)"
---

<objective>
Implement ClaudeCodeRuntime for subscription-based orchestration and critical analysis.

Purpose: Claude Code CLI ($20-100/month subscription) is used for critical analysis tasks where quality is paramount - attacker exploitation, verifier cross-checking, and complex orchestration.

Output: ClaudeCodeRuntime class implementing AgentRuntime ABC with session management.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05.3-opencode-sdk-refactor/05.3-CONTEXT.md
@src/true_vkg/agents/runtime/base.py
@src/true_vkg/agents/runtime/types.py
</context>

<execution_guardrails>
- Full doc alignment: align objective, must_haves, and tasks against `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, plus phase context or research docs before execution. Any scope change must be reflected in the plan and `.planning/STATE.md`.
- Workspace isolation: run any commands or experiments that mutate state in a fresh jj workspace; do not use the main workspace and do not reuse workspaces for reruns.
- Repeat-run detection markers: include `[ALPHASWARM-START] RUN_ID TIMESTAMP` and `[ALPHASWARM-END] RUN_ID EXIT_CODE DURATION_MS` markers in transcripts or logs so reruns are detectable.
- claude-code-controller subject: for interactive runs, use `claude-code-controller` and launch Claude Code as the subject in the claude-code-agent-teams session; transcripts must show the Claude Code session.
- Seed scope boundaries: treat B-seeds (baseline or regression inputs) and N-seeds (net-new exploratory inputs) as separate scopes; do not mix them in a single run and track results separately.
</execution_guardrails>

<tasks>

<task type="auto">
  <name>Task 1: Implement ClaudeCodeRuntime</name>
  <files>src/true_vkg/agents/runtime/claude_code.py</files>
  <action>
Create claude_code.py with ClaudeCodeRuntime class:

1. ClaudeCodeConfig dataclass with fields:
   - model: str = "claude-sonnet-4-20250514" (default, upgrades for critical roles)
   - opus_model: str = "claude-opus-4-20250514" (for attacker/verifier)
   - timeout_seconds: int = 300 (longer for complex analysis)
   - max_tokens: int = 8192
   - session_dir: Path | None = None (for --resume)
   - working_dir: Path | None = None

2. ClaudeCodeRuntime(AgentRuntime) with:
   - __init__(config, working_dir)
   - _get_session_file(session_id) -> Path for session persistence
   - _build_command(prompt, model, session_id, output_format) -> List[str]
   - async _run_subprocess(cmd, timeout) -> Dict with JSON response
   - _parse_response(result, model, latency_ms) -> AgentResponse

3. Implement abstract methods:
   - async execute(config, messages) -> AgentResponse
   - async spawn_agent(config, task) -> AgentResponse (no session)
   - get_model_for_role(role) -> str
     - ATTACKER, VERIFIER -> opus_model
     - Others -> model (sonnet)
   - get_usage() -> Dict

4. CLI command formats:
   - One-shot: `claude -p "prompt" --output-format json --model opus`
   - With session: `claude -p "prompt" --output-format json --resume session_id`
   - Print mode for non-interactive: `claude --print "prompt" --output-format json`

5. Role-based model selection per 05.2-CONTEXT.md:
   - ATTACKER: claude-opus-4 (deep reasoning for exploit construction)
   - DEFENDER: claude-sonnet-4 (fast guard detection)
   - VERIFIER: claude-opus-4 (accuracy for cross-checking)
   - TEST_BUILDER: claude-sonnet-4 (code generation)
   - SUPERVISOR: claude-sonnet-4 (orchestration)
   - INTEGRATOR: claude-sonnet-4 (summarization)

6. Error handling:
   - Detect subscription limit errors
   - Handle session not found gracefully
   - Retry on transient errors

Note: Claude Code CLI uses --print for non-interactive mode.
Cost is subscription-based, not per-token (set cost_usd = 0.0 in response).
  </action>
  <verify>
python -c "from true_vkg.agents.runtime.claude_code import ClaudeCodeRuntime, ClaudeCodeConfig; r = ClaudeCodeRuntime(ClaudeCodeConfig()); print(r.get_model_for_role(r.__class__.__bases__[0].__module__.split('.')[-1]))"
  </verify>
  <done>ClaudeCodeRuntime implements AgentRuntime with role-based model selection</done>
</task>

<task type="auto">
  <name>Task 2: Add unit tests</name>
  <files>tests/agents/test_claude_code_runtime.py</files>
  <action>
Create comprehensive tests for ClaudeCodeRuntime:

1. Test ClaudeCodeConfig defaults:
   - Default model is claude-sonnet-4
   - Opus model is claude-opus-4
   - Timeout is 300 seconds

2. Test role-to-model mapping:
   - ATTACKER -> opus_model
   - VERIFIER -> opus_model
   - DEFENDER -> model (sonnet)
   - TEST_BUILDER -> model (sonnet)

3. Test command building:
   - --print flag for non-interactive
   - --output-format json always present
   - --model flag set correctly
   - --resume for session continuation

4. Test session management:
   - Session file path generation
   - Session ID formatting

5. Test response parsing:
   - JSON output parsed correctly
   - Content extracted
   - cost_usd = 0.0 (subscription)

6. Test error handling (mock subprocess):
   - Timeout handling
   - Non-zero exit code
   - Session not found recovery

Use pytest fixtures with mocked subprocess.
At least 20 tests.
  </action>
  <verify>
cd /Volumes/ex_ssd/home/projects/python/vkg-solidity/alphaswarm && uv run pytest tests/agents/test_claude_code_runtime.py -v --tb=short
  </verify>
  <done>All 20+ tests pass, covering role mapping, command building, and session handling</done>
</task>

</tasks>

<verification>
- ClaudeCodeRuntime inherits from AgentRuntime ABC
- All abstract methods implemented
- Role-to-model mapping returns opus for ATTACKER/VERIFIER
- Command uses --print for non-interactive mode
- Session management works with --resume
- Unit tests pass
</verification>

<success_criteria>
- ClaudeCodeRuntime class is importable
- Role mapping correctly assigns opus to critical roles
- CLI command is correctly formatted with --print --output-format json
- Session persistence works for multi-turn conversations
- 20+ unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05.3-opencode-sdk-refactor/05.3-02-SUMMARY.md`
</output>
