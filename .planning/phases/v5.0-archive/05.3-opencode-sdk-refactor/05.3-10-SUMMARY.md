---
phase: 05.3
plan: 10
title: "Runtime Tests (E2E)"
subsystem: agents/runtime/tests
completed: 2026-01-22
duration: ~8 minutes
tags: [testing, e2e, runtime, propulsion, fixtures]
dependency_graph:
  requires: ["05.3-08", "05.3-09"]
  provides: ["tests/conftest.py", "tests/agents/test_runtime_e2e.py"]
  affects: ["07"]
tech_stack:
  added: []
  patterns: ["pytest fixtures", "mock runtimes", "parameterized tests"]
key_files:
  created:
    - tests/conftest.py
    - tests/agents/test_runtime_e2e.py
  modified: []
decisions:
  - id: "test-fixtures-in-conftest"
    choice: "Centralize runtime fixtures in tests/conftest.py"
    rationale: "Enables fixture reuse across all test files, standardizes mocking patterns"
  - id: "mock-class-pattern"
    choice: "Use context manager mock classes instead of simple patches"
    rationale: "Allows call tracking, response queueing, and state verification"
  - id: "e2e-integration-via-patched-router"
    choice: "Patch TaskRouter in engine tests instead of calling real runtimes"
    rationale: "Isolates unit tests from network, allows deterministic behavior"
metrics:
  tests_added: 66
  total_runtime_tests: 388
  all_passed: true
---

# Phase 5.3 Plan 10: Runtime Tests (E2E) Summary

**One-liner:** Comprehensive E2E test suite with 66 tests verifying runtime selection flow, cost tracking, fallback behavior, and ranking updates through PropulsionEngine integration.

## Objective

Create comprehensive end-to-end tests that verify the complete runtime system works correctly when all components are integrated: PropulsionEngine, TaskRouter, ModelSelector, FeedbackCollector, and the individual runtimes.

## Tasks Completed

| # | Task | Commit | Files |
|---|------|--------|-------|
| 1 | Add runtime fixtures to conftest | cfa7ae1 | tests/conftest.py (675 LOC) |
| 2 | Create E2E runtime tests | dbbb783 | tests/agents/test_runtime_e2e.py (1365 LOC) |
| 3 | Run full test suite | (verification) | 388 tests pass |

## Implementation Summary

### Conftest Fixtures (tests/conftest.py)

Created reusable fixtures for runtime testing:

```python
# CLI mock fixtures (context manager pattern)
mock_opencode_cli    # Tracks calls, queues responses
mock_claude_code_cli # Subscription-based responses
mock_codex_cli       # GPT-4 style responses

# Support fixtures
temp_rankings_store  # Temporary YAML store
mock_all_runtimes    # Combined mock for all CLIs

# Data factories
sample_agent_config  # Factory for AgentConfig
sample_messages      # Factory for conversation messages

# Propulsion support
mock_runtime         # Configurable MockRuntime class
mock_bead            # Mock VulnerabilityBead
mock_inbox           # Mock AgentInbox with bead queue
```

Mock classes use context manager pattern for clean setup/teardown and call tracking.

### E2E Tests (tests/agents/test_runtime_e2e.py)

66 comprehensive tests covering:

| Category | Tests | Coverage |
|----------|-------|----------|
| Complete Execution Flow | 3 | Engine -> Router -> Selector -> Runtime -> Feedback |
| Runtime Routing Matrix | 13 | Role/TaskType -> correct runtime |
| OpenCode Model Selection | 6 | TaskType -> correct model |
| Fallback Behavior | 4 | OpenCode failure -> Claude Code |
| Ranking Updates | 3 | Feedback updates rankings |
| Cost Tracking | 5 | Aggregation, free models, breakdown |
| Loop Prevention | 3 | Output dedup, token ceiling |
| Session Management | 4 | Claude Code sessions |
| Concurrent Execution | 2 | Race condition safety |
| Error Recovery | 3 | Timeout, retry, auth errors |
| ROLE_TO_TASK_TYPE | 4 | Mapping correctness |
| Factory Integration | 5 | create_runtime() variants |
| Router Statistics | 2 | Route counting |
| Quality Estimation | 3 | Heuristic scoring |
| Serialization | 2 | WorkResult, CostSummary |
| Configuration | 3 | PropulsionConfig options |
| spawn_agent | 2 | Task type derivation |

### Test Results

```
Runtime-related tests:  363 passed
All agent tests:        388 passed
E2E tests only:          66 passed
```

All tests pass with no failures or regressions.

## Key Test Patterns

### Routing Matrix Test

```python
@pytest.mark.parametrize("role,task_type,expected_runtime", [
    (AgentRole.ATTACKER, TaskType.CRITICAL, ClaudeCodeRuntime),
    (AgentRole.DEFENDER, TaskType.REVIEW, CodexCLIRuntime),
    (AgentRole.TEST_BUILDER, TaskType.CODE, OpenCodeRuntime),
])
def test_routing_matrix(self, role, task_type, expected_runtime):
    runtime = route_to_runtime(role=role, task_type=task_type)
    assert isinstance(runtime, expected_runtime)
```

### Fallback Verification

```python
async def test_fallback_on_failure(self, tmp_path):
    engine = PropulsionEngine(
        runtime=failing_runtime,
        config=PropulsionConfig(enable_fallback=True),
    )
    response = await engine.execute_agent(config, messages)
    assert response.metadata.get("fallback") is True
```

### Cost Tracking Verification

```python
async def test_costs_aggregated_across_executions(self, tmp_path):
    for _ in range(5):
        await engine.execute_agent(config, messages)
    summary = engine.get_cost_summary()
    assert summary.execution_count == 5
    assert summary.total_cost_usd == pytest.approx(0.01)
```

## Deviations from Plan

None - plan executed exactly as written.

## Verification Results

| Criterion | Status | Evidence |
|-----------|--------|----------|
| E2E runtime tests pass with mocked CLIs | PASS | 66 tests pass |
| Runtime selection flows correctly through system | PASS | Routing matrix tests |
| Cost tracking is accurate across executions | PASS | Cost tracking tests |
| Ranking updates improve model selection | PASS | Ranking update tests |
| tests/agents/test_runtime_e2e.py exists | PASS | 1365 LOC created |
| PropulsionEngine integration tested | PASS | Execute flow tests |

## Next Phase Readiness

Phase 5.3 (OpenCode SDK Refactor) is now **COMPLETE**.

All 10 plans have been executed:
- Plans 01-03: CLI runtimes (OpenCode, Claude Code, Codex)
- Plan 04: Runtime factory
- Plan 05: Task router
- Plan 06: Task types
- Plan 07: Model ranking system
- Plan 08: Propulsion engine integration
- Plan 09: CLI updates
- Plan 10: E2E tests (this plan)

**Ready to proceed to Phase 6 (Release Preparation).**
