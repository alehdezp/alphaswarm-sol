---
phase: 07.1-testing-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/true_vkg/skills/shipped/test-full.md
  - src/true_vkg/skills/shipped/test-quick.md
  - src/true_vkg/skills/shipped/test-component.md
  - src/true_vkg/skills/shipped/benchmark-model.md
  - src/true_vkg/skills/shipped/mutate-contract.md
  - src/true_vkg/skills/shipped/track-gap.md
autonomous: true

must_haves:
  truths:
    - "Six testing skills exist and are invocable"
    - "Skills follow established skill format with YAML frontmatter"
    - "Each skill has proper slash_command and context settings"
  artifacts:
    - path: "src/true_vkg/skills/shipped/test-full.md"
      provides: "Complete test orchestration skill"
      contains: "slash_command: vrs:test-full"
    - path: "src/true_vkg/skills/shipped/test-quick.md"
      provides: "Smoke test skill"
      contains: "slash_command: vrs:test-quick"
    - path: "src/true_vkg/skills/shipped/test-component.md"
      provides: "Single agent component test skill"
      contains: "slash_command: vrs:test-component"
    - path: "src/true_vkg/skills/shipped/benchmark-model.md"
      provides: "Model comparison skill"
      contains: "slash_command: vrs:benchmark-model"
    - path: "src/true_vkg/skills/shipped/mutate-contract.md"
      provides: "Contract mutation skill"
      contains: "slash_command: vrs:mutate-contract"
    - path: "src/true_vkg/skills/shipped/track-gap.md"
      provides: "Gap tracking skill"
      contains: "slash_command: vrs:track-gap"
  key_links:
    - from: "src/true_vkg/skills/shipped/test-full.md"
      to: "Testing agents"
      via: "Agent spawning"
      pattern: "vrs-test-conductor"
---

<objective>
Create the six testing skills that drive the AlphaSwarm Test Forge.

Purpose: Establish skills for test orchestration, quick testing, component testing, model benchmarking, contract mutation, and gap tracking. These skills invoke the testing agents created in 07.1-01.

Output: Six skill definition files in `src/true_vkg/skills/shipped/` following the existing audit.md format.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1-testing-infrastructure/07.1-CONTEXT.md
@.planning/phases/07.1-testing-infrastructure/07.1-RESEARCH.md
@src/true_vkg/skills/shipped/audit.md
@src/true_vkg/skills/shipped/investigate.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test orchestration skills (test-full, test-quick, test-component)</name>
  <files>
    src/true_vkg/skills/shipped/test-full.md
    src/true_vkg/skills/shipped/test-quick.md
    src/true_vkg/skills/shipped/test-component.md
  </files>
  <action>
Create three skill definition files following the audit.md format.

**test-full.md:**
```yaml
---
name: vrs-test-full
description: |
  Complete test orchestration skill. Runs all testing phases including
  health check, component tests, integration tests, E2E pipeline,
  model comparison, mutation tests, adversarial tests, and gap analysis.

  Invoke when user wants to:
  - Run full test suite: "run all tests", "complete test run", "/vrs-test-full"
  - Validate before release: "test everything", "GA validation"
  - Generate comprehensive metrics: "full benchmark", "test report"

slash_command: vrs:test-full
context: fork
disable-model-invocation: true

allowed-tools:
  - Read
  - Glob
  - Grep
  - Bash(pytest*)
  - Bash(uv run*)
---
```
Content sections:
- Philosophy (test layers, accuracy focus)
- How to Invoke with examples
- Execution Flow (9 phases from context doc)
- Usage Examples (full, with options, modes)
- Output Format (TestMetrics summary)
- Quality Gates (precision >=85%, recall_critical >=95%)
- Related Skills table
- Notes on test modes and timing

**test-quick.md:**
```yaml
---
name: vrs-test-quick
description: |
  Fast smoke test skill for development iteration. Runs quick benchmark
  (< 2 min) with critical path validation only.

  Invoke when user wants to:
  - Quick validation: "quick test", "smoke test", "/vrs-test-quick"
  - CI check: "fast check", "pre-commit test"

slash_command: vrs:test-quick
context: fork
disable-model-invocation: false

allowed-tools:
  - Read
  - Glob
  - Bash(pytest*)
---
```
Content: Quick test execution, benchmark quick.yaml reference, minimal output

**test-component.md:**
```yaml
---
name: vrs-test-component
description: |
  Single agent component test skill. Tests one agent role in isolation
  with mocked dependencies.

  Invoke when user wants to:
  - Test specific agent: "test context-merge agent", "/vrs-test-component context-merge"
  - Validate agent changes: "check vuln-discovery agent"

slash_command: vrs:test-component
context: fork
disable-model-invocation: false

allowed-tools:
  - Read
  - Glob
  - Bash(pytest*)
---
```
Content: Component test execution, TestModel usage, agent role parameter
  </action>
  <verify>
    Files exist with correct slash commands:
    ```bash
    grep "slash_command: vrs:test-full" src/true_vkg/skills/shipped/test-full.md
    grep "slash_command: vrs:test-quick" src/true_vkg/skills/shipped/test-quick.md
    grep "slash_command: vrs:test-component" src/true_vkg/skills/shipped/test-component.md
    ```
  </verify>
  <done>
    Three test orchestration skills created with proper frontmatter and complete documentation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create utility testing skills (benchmark-model, mutate-contract, track-gap)</name>
  <files>
    src/true_vkg/skills/shipped/benchmark-model.md
    src/true_vkg/skills/shipped/mutate-contract.md
    src/true_vkg/skills/shipped/track-gap.md
  </files>
  <action>
Create three utility skill definition files:

**benchmark-model.md:**
```yaml
---
name: vrs-benchmark-model
description: |
  Model comparison benchmark skill. Runs corpus against multiple models
  to compare accuracy per task type and update rankings.

  Invoke when user wants to:
  - Compare models: "benchmark models", "compare Claude vs DeepSeek", "/vrs-benchmark-model"
  - Update rankings: "recalculate rankings", "model evaluation"

slash_command: vrs:benchmark-model
context: fork
disable-model-invocation: false

allowed-tools:
  - Read
  - Glob
  - Bash(uv run*)
---
```
Content sections:
- Model Pool (Claude tiers + OpenCode models from context)
- Comparison Dimensions (accuracy 60%, task-type 30%, cost 10%)
- Ranking Integration (EMA updates to rankings.yaml)
- Usage: /vrs-benchmark-model --models "opus,sonnet,deepseek" --task-type "detect"
- Output Format (model scores, cost-accuracy curves)

**mutate-contract.md:**
```yaml
---
name: vrs-mutate-contract
description: |
  Contract mutation skill. Generates Solidity contract variants that
  preserve vulnerability semantics for testing robustness.

  Invoke when user wants to:
  - Generate mutations: "mutate this contract", "/vrs-mutate-contract"
  - Create test variants: "generate 10 variants", "mutation testing"

slash_command: vrs:mutate-contract
context: fork
disable-model-invocation: false

allowed-tools:
  - Read
  - Write
  - Glob
  - Bash(solc*)
---
```
Content sections:
- Mutation Types (identifier renaming, reordering, pattern variation, structural)
- Semantic Preservation (how to verify vulnerability still present)
- Usage: /vrs-mutate-contract ./contracts/Vault.sol --count 10 --type rename
- Output: List of variant paths with validation status

**track-gap.md:**
```yaml
---
name: vrs-track-gap
description: |
  Gap tracking skill. Records testing gaps with proper categorization
  and links to backlog items.

  Invoke when user wants to:
  - Record gap: "track this gap", "/vrs-track-gap"
  - Log limitation: "document this limitation"

slash_command: vrs:track-gap
context: inline
disable-model-invocation: false

allowed-tools:
  - Read
  - Write
---
```
Content sections:
- Gap Schema (gap_id, category, severity, title, description, root_cause, etc.)
- Categories (detection, false_positive, technical, agent, scalability, ux)
- Usage: /vrs-track-gap "Cross-contract reentrancy" --category detection --severity high
- Output: Gap YAML file created in .vrs/testing/gaps/
- Link to BACKLOG.md if actionable
  </action>
  <verify>
    Files exist with correct slash commands:
    ```bash
    grep "slash_command: vrs:benchmark-model" src/true_vkg/skills/shipped/benchmark-model.md
    grep "slash_command: vrs:mutate-contract" src/true_vkg/skills/shipped/mutate-contract.md
    grep "slash_command: vrs:track-gap" src/true_vkg/skills/shipped/track-gap.md
    ```
  </verify>
  <done>
    Three utility testing skills created with proper frontmatter and complete documentation.
  </done>
</task>

</tasks>

<verification>
All six testing skills created and accessible:
```bash
ls -la src/true_vkg/skills/shipped/test-*.md src/true_vkg/skills/shipped/benchmark-model.md src/true_vkg/skills/shipped/mutate-contract.md src/true_vkg/skills/shipped/track-gap.md
# Should show 6 files

# Verify slash commands
grep "slash_command:" src/true_vkg/skills/shipped/test-*.md src/true_vkg/skills/shipped/benchmark-model.md src/true_vkg/skills/shipped/mutate-contract.md src/true_vkg/skills/shipped/track-gap.md
# Should show:
# test-full.md: slash_command: vrs:test-full
# test-quick.md: slash_command: vrs:test-quick
# test-component.md: slash_command: vrs:test-component
# benchmark-model.md: slash_command: vrs:benchmark-model
# mutate-contract.md: slash_command: vrs:mutate-contract
# track-gap.md: slash_command: vrs:track-gap
```
</verification>

<success_criteria>
- Six skill definition files exist in src/true_vkg/skills/shipped/
- Each file has valid YAML frontmatter with name, description, slash_command, context, allowed-tools
- Slash commands use vrs: prefix as established in Phase 5.4/5.6
- Each skill has complete documentation: philosophy, how to invoke, execution flow, usage examples, output format
- Context settings appropriate: fork for test execution, inline for gap tracking
- File format matches existing skills (audit.md pattern)
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-testing-infrastructure/07.1-02-SUMMARY.md`
</output>
