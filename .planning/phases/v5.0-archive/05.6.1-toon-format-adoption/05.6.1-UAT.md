---
status: complete
phase: 05.6.1-toon-format-adoption
source: 05.6.1-01-SUMMARY.md, 05.6.1-02-SUMMARY.md, 05.6.1-03-SUMMARY.md, 05.6.1-04-SUMMARY.md
started: 2026-01-23T19:30:00Z
updated: 2026-01-23T18:55:30Z
---

## Current Test

[testing complete]

## Tests

### 1. TOON Utility Functions
expected: Run `python -c "from alphaswarm_sol.kg import toon_dumps, toon_loads; print(toon_loads(toon_dumps({'test': True})))"`. Output should show `{'test': True}` confirming round-trip encoding works.
result: pass

### 2. Build-KG Default Format (TOON)
expected: Run `uv run alphaswarm build-kg tests/contracts/NoAccessGate.sol --out /tmp/toon-uat-test`. A `graph.toon` file should be created (not graph.json). Output should mention "toon" format.
result: pass

### 3. Build-KG JSON Format Flag
expected: Run `uv run alphaswarm build-kg tests/contracts/NoAccessGate.sol --out /tmp/json-uat-test --format json`. A `graph.json` file should be created. Output should mention "json" format.
result: pass

### 4. GraphStore Load Auto-Detection
expected: Run `python -c "from pathlib import Path; from alphaswarm_sol.kg import GraphStore; gs = GraphStore(Path('/tmp/toon-uat-test')); g = gs.load(); print(f'{len(g.nodes)} nodes loaded')"`. Should load the TOON file from test 2 and show node count (should be > 0).
result: pass

### 5. TOON File Size Reduction
expected: Compare file sizes: `ls -la /tmp/toon-uat-test/graph.toon /tmp/json-uat-test/graph.json`. TOON file should be smaller than JSON file (expect ~20-25% reduction).
result: pass

### 6. CLI Invalid Format Error
expected: Run `uv run alphaswarm build-kg tests/contracts/NoAccessGate.sol --format invalid 2>&1`. Should show an error message about invalid format value.
result: pass

## Summary

total: 6
passed: 6
issues: 0
pending: 0
skipped: 0

## Gaps

[none]

## Test Execution Details

**Executed:** 2026-01-23 by Claude

| Test | Command | Result |
|------|---------|--------|
| 1 | `uv run python -c "from alphaswarm_sol.kg import toon_dumps, toon_loads; print(toon_loads(toon_dumps({'test': True})))"` | `{'test': True}` ✓ |
| 2 | `uv run alphaswarm build-kg tests/contracts/NoAccessGate.sol --out /tmp/toon-uat-test` | Created graph.toon (20KB) ✓ |
| 3 | `uv run alphaswarm build-kg tests/contracts/NoAccessGate.sol --out /tmp/json-uat-test --format json` | Created graph.json (28KB) ✓ |
| 4 | `uv run python -c "from pathlib import Path; from alphaswarm_sol.kg import GraphStore; gs = GraphStore(Path('/tmp/toon-uat-test')); g = gs.load(); print(f'{len(g.nodes)} nodes loaded')"` | `6 nodes loaded` ✓ |
| 5 | `ls -la /tmp/toon-uat-test/graph.toon /tmp/json-uat-test/graph.json` | TOON 20KB vs JSON 28KB (28% reduction) ✓ |
| 6 | `uv run alphaswarm build-kg tests/contracts/NoAccessGate.sol --format invalid 2>&1` | `Error: --format must be 'toon' or 'json', got 'invalid'` ✓ |

**Note:** Test 4 requires `Path` object for `GraphStore` constructor (not string). This is documented behavior but could be improved for usability in a future enhancement.
