---
phase: 05.7-vulndocs-knowledge-consolidation
plan: 03
type: execute
wave: 2
depends_on: ["05.7-01", "05.7-02"]
files_modified:
  - vulndocs/reentrancy/classic/patterns/*.yaml
  - vulndocs/access-control/*/patterns/*.yaml
  - vulndocs/oracle/*/patterns/*.yaml
  - vulndocs/*/index.yaml
autonomous: true

must_haves:
  truths:
    - "All 146 pattern files migrated to vulndocs/ structure"
    - "Each pattern has vulndoc field linking back to parent"
    - "YAML multiline content preserved without corruption"
    - "Lens files archived to .meta/references/"
  artifacts:
    - path: "vulndocs/reentrancy/classic/patterns/vm-001.yaml"
      provides: "Migrated reentrancy pattern with vulndoc back-reference"
      contains: "vulndoc: reentrancy/classic"
    - path: "vulndocs/.meta/references/"
      provides: "Archived lens documentation"
      min_lines: 10
  key_links:
    - from: "patterns/core/vm-001.yaml"
      to: "vulndocs/reentrancy/classic/patterns/vm-001.yaml"
      via: "pattern migration"
      pattern: "vulndoc: reentrancy/classic"
---

<objective>
Migrate all pattern files from patterns/ to vulndocs/ with proper organization.

Purpose: Move 146 YAML pattern files to co-located positions within vulndocs/ structure. Add vulndoc back-references. Preserve YAML multiline content quality.

Output: All patterns migrated to vulndocs/{category}/{subcategory}/patterns/ structure.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05.7-vulndocs-knowledge-consolidation/05.7-CONTEXT.md
@.planning/phases/05.7-vulndocs-knowledge-consolidation/05.7-RESEARCH.md
@.planning/phases/05.7-vulndocs-knowledge-consolidation/05.7-01-SUMMARY.md
@.planning/phases/05.7-vulndocs-knowledge-consolidation/05.7-02-SUMMARY.md
</context>

<execution_guardrails>
- Full doc alignment: align objective, must_haves, and tasks against `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, plus phase context or research docs before execution. Any scope change must be reflected in the plan and `.planning/STATE.md`.
- Workspace isolation: run any commands or experiments that mutate state in a fresh jj workspace; do not use the main workspace and do not reuse workspaces for reruns.
- Repeat-run detection markers: include `[ALPHASWARM-START] RUN_ID TIMESTAMP` and `[ALPHASWARM-END] RUN_ID EXIT_CODE DURATION_MS` markers in transcripts or logs so reruns are detectable.
- claude-code-controller subject: for interactive runs, use `claude-code-controller` and launch Claude Code as the subject in the claude-code-agent-teams session; transcripts must show the Claude Code session.
- Seed scope boundaries: treat B-seeds (baseline or regression inputs) and N-seeds (net-new exploratory inputs) as separate scopes; do not mix them in a single run and track results separately.
</execution_guardrails>

<tasks>

<task type="auto">
  <name>Task 1: Migrate patterns/core/*.yaml to vulndocs categories</name>
  <files>vulndocs/*/patterns/*.yaml</files>
  <action>
    Migrate core patterns (65 files) to appropriate vulndocs categories:

    1. Read each pattern from patterns/core/:
       - Parse YAML to extract id, lens, severity, description
       - Determine target category from lens field or content

    2. Mapping rules (from lens to category):
       - ValueMovement lens -> reentrancy/, vault/, token/ based on content
       - Authority lens -> access-control/
       - ExternalInfluence lens -> oracle/, cross-chain/
       - Arithmetic lens -> arithmetic/, precision-loss/
       - LogicState lens -> logic/
       - Liveness lens -> dos/
       - OrderingUpgradability lens -> upgrade/, governance/

    3. For each pattern:
       - Create target directory if not exists: vulndocs/{category}/{subcategory}/patterns/
       - Read original file preserving YAML formatting
       - Add vulndoc field: `vulndoc: {category}/{subcategory}`
       - Write to new location preserving multiline content

    4. Create category index.yaml if not exists:
       ```yaml
       id: {category}
       name: "{Category Name}"
       description: "{Description}"
       subcategories: []  # Will be populated
       ```

    5. Create subcategory directories as needed based on pattern content

    IMPORTANT: Use raw file read/write to preserve YAML multiline formatting.
    DO NOT use yaml.dump() which can corrupt multiline strings.
  </action>
  <verify>
    - Count patterns in vulndocs/*/patterns/ matches 65
    - Each pattern has vulndoc field
    - Multiline descriptions preserved (check random sample)
    - No YAML syntax errors in migrated files
  </verify>
  <done>All 65 core patterns migrated with vulndoc back-references</done>
</task>

<task type="auto">
  <name>Task 2: Migrate patterns/semantic/, patterns/label_patterns/, patterns/investigation/</name>
  <files>vulndocs/*/patterns/*.yaml</files>
  <action>
    Migrate remaining pattern directories:

    1. patterns/semantic/ (26 files):
       - Already organized by category (semantic/{category}/)
       - Map directly to vulndocs/{category}/{subcategory}/patterns/
       - Add vulndoc field based on directory structure

    2. patterns/label_patterns/ (3 files - Tier C):
       - Parse label conditions to determine category
       - Merge into appropriate vulndocs category
       - Preserve tier_c match structure

    3. patterns/investigation/ (4 files):
       - Create vulndocs/investigation/ category if needed
       - Or merge into relevant categories based on content
       - Preserve investigation-specific fields

    4. patterns/semgrep/ (3 files):
       - Create vulndocs/.meta/tool-patterns/semgrep/ or
       - Merge into relevant categories with tool: semgrep annotation

    5. For each migrated pattern:
       - Preserve original YAML structure
       - Add vulndoc field
       - Keep all existing fields (status, test_coverage, etc.)

    IMPORTANT: Investigation patterns may need special subcategory
    (e.g., vulndocs/investigation/cross-contract/)
  </action>
  <verify>
    - Count semantic patterns in vulndocs matches 26
    - Count label patterns in vulndocs matches 3
    - Count investigation patterns in vulndocs matches 4
    - Total additional patterns: 36
  </verify>
  <done>All semantic, label, and investigation patterns migrated</done>
</task>

<task type="auto">
  <name>Task 3: Archive lens files and migrate root lens YAML patterns</name>
  <files>
    vulndocs/.meta/references/lens/
    vulndocs/*/patterns/*.yaml
  </files>
  <action>
    Handle lens files and root lens YAML:

    1. Archive lens markdown documentation:
       - Create vulndocs/.meta/references/lens/
       - Copy patterns/lens/*.md to .meta/references/lens/
       - These are historical reference docs

    2. Process root lens YAML files (7 files):
       patterns/arithmetic-lens.yaml
       patterns/authority-lens.yaml
       patterns/external-influence-lens.yaml
       patterns/liveness-lens.yaml
       patterns/logic-state-lens.yaml
       patterns/ordering-upgradability-lens.yaml
       patterns/value-movement-lens.yaml

       For each lens YAML:
       - Parse to extract individual patterns within
       - If contains pattern definitions, split into individual files
       - Migrate each pattern to appropriate vulndocs category
       - If just lens metadata, archive to .meta/references/

    3. Keep patterns/PATTERN_TEMPLATE.yaml:
       - Already copied to .meta/templates/pattern.yaml in Plan 02
       - Original can be archived or left for reference

    4. Update vulndocs/index.yaml with final pattern counts:
       - Count patterns per category
       - Update statistics section
  </action>
  <verify>
    - vulndocs/.meta/references/lens/ exists with archived docs
    - Root lens files processed (split or archived)
    - No patterns left in patterns/ (only lens archives)
    - vulndocs/index.yaml has accurate counts
  </verify>
  <done>All lens files archived, lens patterns migrated, counts updated</done>
</task>

</tasks>

<verification>
- Total patterns in vulndocs/*/patterns/ matches or exceeds 146
- No pattern files remain in patterns/ (only archived docs)
- All patterns have vulndoc back-reference field
- YAML multiline content preserved (no single-line descriptions)
- vulndocs/index.yaml statistics accurate
- python -c "import yaml; yaml.safe_load(open('vulndocs/reentrancy/classic/patterns/vm-001.yaml'))" succeeds
</verification>

<success_criteria>
- [ ] All 146+ pattern files migrated to vulndocs/
- [ ] Each pattern has vulndoc field linking to parent
- [ ] YAML multiline formatting preserved
- [ ] Lens documentation archived to .meta/references/
- [ ] Category index.yaml files created
- [ ] vulndocs/index.yaml statistics updated
</success_criteria>

<output>
After completion, create `.planning/phases/05.7-vulndocs-knowledge-consolidation/05.7-03-SUMMARY.md`
</output>
