---
phase: 05.7-vulndocs-knowledge-consolidation
plan: 09
type: execute
wave: 4
depends_on: ["05.7-05", "05.7-06", "05.7-07", "05.7-08"]
files_modified:
  - .gitignore
autonomous: true

must_haves:
  truths:
    - "patterns/ directory removed from repository"
    - "knowledge/ directory removed from repository"
    - "vulndocs/.meta/templates/ and .meta/instructions/ removed"
    - "No broken imports after removal"
  artifacts:
    - path: ".planning/phases/05.7-vulndocs-knowledge-consolidation/05.7-09-REMOVAL-LOG.md"
      provides: "Log of removed directories and verification"
      min_lines: 30
  key_links:
    - from: "git rm -r patterns/"
      to: "vulndocs/*/patterns/"
      via: "content migrated in Plan 03"
---

<objective>
Remove legacy folders after migration is verified complete.

Purpose: Clean up deprecated directories (patterns/, knowledge/) after all content migrated and all references updated. This is the final cleanup step.

Output: Repository with only vulndocs/ as the knowledge location.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05.7-vulndocs-knowledge-consolidation/05.7-CONTEXT.md
@.planning/phases/05.7-vulndocs-knowledge-consolidation/05.7-05-SUMMARY.md
@.planning/phases/05.7-vulndocs-knowledge-consolidation/05.7-06-SUMMARY.md
@.planning/phases/05.7-vulndocs-knowledge-consolidation/05.7-08-SUMMARY.md
</context>

<execution_guardrails>
- Full doc alignment: align objective, must_haves, and tasks against `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, plus phase context or research docs before execution. Any scope change must be reflected in the plan and `.planning/STATE.md`.
- Workspace isolation: run any commands or experiments that mutate state in a fresh jj workspace; do not use the main workspace and do not reuse workspaces for reruns.
- Repeat-run detection markers: include `[ALPHASWARM-START] RUN_ID TIMESTAMP` and `[ALPHASWARM-END] RUN_ID EXIT_CODE DURATION_MS` markers in transcripts or logs so reruns are detectable.
- claude-code-controller subject: for interactive runs, use `claude-code-controller` and launch Claude Code as the subject in the claude-code-agent-teams session; transcripts must show the Claude Code session.
- Seed scope boundaries: treat B-seeds (baseline or regression inputs) and N-seeds (net-new exploratory inputs) as separate scopes; do not mix them in a single run and track results separately.
</execution_guardrails>

<tasks>

<task type="auto">
  <name>Task 1: Pre-removal verification</name>
  <files>.planning/phases/05.7-vulndocs-knowledge-consolidation/05.7-09-REMOVAL-LOG.md</files>
  <action>
    Verify all content migrated before removal:

    1. Count verification:
       - Count patterns in vulndocs/*/patterns/ >= 146
       - Count subcategories in vulndocs/ >= 97
       - No orphaned content in patterns/ or knowledge/

    2. Reference verification:
       Run comprehensive grep to ensure no remaining references:
       ```bash
       # Should return no matches (except comments, .git, etc.)
       grep -r '"patterns/' --include="*.py" --include="*.md" src/ .claude/
       grep -r "knowledge/vulndocs" --include="*.py" --include="*.md" src/ .claude/
       grep -r ".meta/templates/" --include="*.py" --include="*.md" src/ .claude/ vulndocs/
       grep -r ".meta/instructions/" --include="*.py" --include="*.md" src/ .claude/ vulndocs/
       ```

    3. Import verification:
       ```bash
       uv run python -c "import alphaswarm_sol; print('Import OK')"
       ```

    4. CLI verification:
       ```bash
       uv run alphaswarm vulndocs list
       uv run alphaswarm vulndocs validate vulndocs/
       ```

    5. Document verification results in REMOVAL-LOG.md:
       - Pattern count in vulndocs
       - Subcategory count in vulndocs
       - Reference check results
       - Import check results
       - CLI check results
       - GO/NO-GO decision

    If any check fails: STOP and document the issue.
    Do NOT proceed with removal if verification fails.
  </action>
  <verify>
    - REMOVAL-LOG.md created with verification results
    - All counts match expected values
    - No remaining references to old paths
    - GO decision documented
  </verify>
  <done>Pre-removal verification complete and documented</done>
</task>

<task type="auto">
  <name>Task 2: Remove legacy directories</name>
  <files>.gitignore</files>
  <action>
    Remove deprecated directories:

    1. Remove patterns/ directory:
       ```bash
       git rm -r patterns/
       ```
       Note: Content migrated to vulndocs/*/patterns/ in Plan 03

    2. Remove knowledge/ directory:
       ```bash
       git rm -r knowledge/
       ```
       Note: Content migrated to vulndocs/ in Plan 04

    3. Remove vulndocs/.meta/templates/ directory:
       ```bash
       git rm -r vulndocs/.meta/templates/
       ```
       Note: Content moved to vulndocs/.meta/templates/ in Plan 02

    4. Remove vulndocs/.meta/instructions/ directory:
       ```bash
       git rm -r vulndocs/.meta/instructions/
       ```
       Note: Content moved to vulndocs/.meta/instructions/ in Plan 02

    5. Update .gitignore if needed:
       - Remove any entries for old directories
       - Ensure vulndocs/ is tracked (not ignored)

    6. Document removal in REMOVAL-LOG.md:
       - List each removed directory
       - File counts removed
       - Git commands used
  </action>
  <verify>
    - patterns/ directory does not exist
    - knowledge/ directory does not exist
    - vulndocs/.meta/templates/ does not exist
    - vulndocs/.meta/instructions/ does not exist
    - git status shows removals staged
  </verify>
  <done>Legacy directories removed from repository</done>
</task>

<task type="auto">
  <name>Task 3: Post-removal verification</name>
  <files>.planning/phases/05.7-vulndocs-knowledge-consolidation/05.7-09-REMOVAL-LOG.md</files>
  <action>
    Verify system still works after removal:

    1. Import check:
       ```bash
       uv run python -c "import alphaswarm_sol; print('Import OK')"
       uv run python -c "from alphaswarm_sol.vulndocs import VulnDocIndex; print('VulnDoc Import OK')"
       ```

    2. CLI check:
       ```bash
       uv run alphaswarm --help
       uv run alphaswarm vulndocs list
       uv run alphaswarm vulndocs validate vulndocs/
       ```

    3. Quick test run (if fast tests exist):
       ```bash
       uv run pytest tests/vulndocs/ -v --tb=short -x
       ```
       Or skip if tests are slow - full test run in Plan 10

    4. Skill check:
       - Ensure skills don't reference removed paths
       - Skill files should only reference vulndocs/

    5. Update REMOVAL-LOG.md with post-removal verification:
       - Import results
       - CLI results
       - Test results (if run)
       - Final status: SUCCESS or ISSUES

    6. Commit the removal:
       ```bash
       git commit -m "chore(05.7): remove legacy patterns/ and knowledge/ directories

       Migration to unified vulndocs/ structure complete:
       - patterns/ content -> vulndocs/*/patterns/
       - knowledge/vulndocs/ content -> vulndocs/
       - .meta/templates/ and .meta/instructions/ -> .meta/

       Closes Phase 5.7 legacy removal.
       "
       ```
  </action>
  <verify>
    - All import checks pass
    - CLI commands work
    - Commit created with removal
    - REMOVAL-LOG.md documents success
  </verify>
  <done>Post-removal verification complete, removal committed</done>
</task>

</tasks>

<verification>
- patterns/ directory does not exist
- knowledge/ directory does not exist
- vulndocs/.meta/templates/ does not exist
- vulndocs/.meta/instructions/ does not exist
- vulndocs/.meta/templates/ exists with all templates
- vulndocs/.meta/instructions/ exists with all instructions
- vulndocs/*/patterns/ contains all migrated patterns
- uv run alphaswarm vulndocs list works
- uv run python -c "import alphaswarm_sol" succeeds
- Removal committed to git
</verification>

<success_criteria>
- [ ] Pre-removal verification passed
- [ ] patterns/ directory removed
- [ ] knowledge/ directory removed
- [ ] vulndocs/.meta/templates/ and .meta/instructions/ removed
- [ ] Post-removal verification passed
- [ ] No broken imports or CLI commands
- [ ] Removal committed to git
- [ ] REMOVAL-LOG.md documents full process
</success_criteria>

<output>
After completion, create `.planning/phases/05.7-vulndocs-knowledge-consolidation/05.7-09-SUMMARY.md`
</output>
