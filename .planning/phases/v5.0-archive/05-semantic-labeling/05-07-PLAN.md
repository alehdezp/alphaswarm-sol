---
phase: 05-semantic-labeling
plan: 07
type: execute
wave: 4
depends_on: ["05-05", "05-06"]
files_modified:
  - patterns/label_patterns/policy_mismatch.yaml
  - patterns/label_patterns/invariant_violation.yaml
  - patterns/label_patterns/state_machine.yaml
autonomous: true

must_haves:
  truths:
    - "Policy mismatch patterns detect docs-vs-code disagreement"
    - "Invariant violation patterns detect balance/supply issues"
    - "State machine patterns detect illegal transitions"
    - "All patterns use Tier C label conditions"
  artifacts:
    - path: "patterns/label_patterns/policy_mismatch.yaml"
      provides: "Policy mismatch patterns (10+)"
      min_lines: 200
    - path: "patterns/label_patterns/invariant_violation.yaml"
      provides: "Invariant violation patterns"
      min_lines: 80
    - path: "patterns/label_patterns/state_machine.yaml"
      provides: "State machine patterns"
      min_lines: 60
  key_links:
    - from: "patterns/label_patterns/*.yaml"
      to: "src/true_vkg/queries/tier_c.py"
      via: "Patterns use tier_c conditions"
      pattern: "tier_c"
---

<objective>
Create label-dependent pattern packs for policy mismatches, invariant violations, and state machine issues.

Purpose: These patterns detect complex logic bugs that pure Tier A/B matching cannot find. They rely on semantic labels to understand code intent and detect when behavior doesn't match expected policy.

Output: Three pattern YAML files in patterns/label_patterns/ directory with 10+ policy mismatch patterns to meet exit gate requirements.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-semantic-labeling/05-CONTEXT.md
@.planning/phases/05-semantic-labeling/05-RESEARCH.md

# Prior plan summaries
@.planning/phases/05-semantic-labeling/05-05-SUMMARY.md
@.planning/phases/05-semantic-labeling/05-06-SUMMARY.md

# Existing pattern examples
@patterns/reentrancy/
</context>

<execution_guardrails>
- Full doc alignment: align objective, must_haves, and tasks against `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, plus phase context or research docs before execution. Any scope change must be reflected in the plan and `.planning/STATE.md`.
- Workspace isolation: run any commands or experiments that mutate state in a fresh jj workspace; do not use the main workspace and do not reuse workspaces for reruns.
- Repeat-run detection markers: include `[ALPHASWARM-START] RUN_ID TIMESTAMP` and `[ALPHASWARM-END] RUN_ID EXIT_CODE DURATION_MS` markers in transcripts or logs so reruns are detectable.
- claude-code-controller subject: for interactive runs, use `claude-code-controller` and launch Claude Code as the subject in the claude-code-agent-teams session; transcripts must show the Claude Code session.
- Seed scope boundaries: treat B-seeds (baseline or regression inputs) and N-seeds (net-new exploratory inputs) as separate scopes; do not mix them in a single run and track results separately.
</execution_guardrails>

<tasks>

<task type="auto">
  <name>Task 1: Create policy_mismatch.yaml patterns (10+ patterns)</name>
  <files>patterns/label_patterns/policy_mismatch.yaml</files>
  <action>
Create patterns/label_patterns/ directory and policy_mismatch.yaml with 10+ policy mismatch patterns to meet exit gate requirement (LABEL-09):

```yaml
# Policy Mismatch Patterns
# Detect when code behavior doesn't match stated intent
# These patterns require semantic labels from the LLM labeler

---
id: policy-mismatch-unauthorized-transfer
name: Unauthorized Value Transfer
description: |
  Function transfers value without access control, but should be restricted.
  The function has value_handling labels but missing access_control labels.
severity: high
scope: function
lens: [AccessControl, ValueTransfer]
aggregation_mode: tier_abc_all

match:
  tier_a:
    all:
      - property: visibility
        op: in
        value: [public, external]
      - property: sends_eth
        op: eq
        value: true

  tier_c:
    all:
      - type: has_label
        value: value_handling.transfers_value_out
        min_confidence: medium
      - type: missing_label
        value: access_control.owner_only
      - type: missing_label
        value: access_control.role_based
    none:
      - type: has_label
        value: access_control.no_restriction
        # If explicitly labeled as intentionally unrestricted, don't flag

required_labels:
  - value_handling.transfers_value_out

---
id: policy-mismatch-unprotected-state
name: Unprotected Critical State Modification
description: |
  Function modifies critical state without access control.
  Has state_mutation labels but missing protection labels.
severity: high
scope: function
lens: [AccessControl, StateIntegrity]
aggregation_mode: tier_abc_all

match:
  tier_a:
    all:
      - property: visibility
        op: in
        value: [public, external]
      - property: writes_privileged_state
        op: eq
        value: true

  tier_c:
    all:
      - type: has_label
        value: state_mutation.writes_critical
        min_confidence: medium
      - type: missing_label
        value: access_control.owner_only
      - type: missing_label
        value: access_control.role_based

required_labels:
  - state_mutation.writes_critical

---
id: policy-mismatch-owner-bypass
name: Owner Check Bypass
description: |
  Function labeled as owner-only but code doesn't actually check owner.
  This detects when intent (owner restriction) doesn't match implementation.
severity: critical
scope: function
lens: [AccessControl]
aggregation_mode: tier_c_only

match:
  tier_a:
    all:
      - property: visibility
        op: in
        value: [public, external]
    none:
      - property: has_onlyowner_modifier
        op: eq
        value: true
      - property: checks_msg_sender_owner
        op: eq
        value: true

  tier_c:
    all:
      - type: has_label
        value: access_control.owner_only
        min_confidence: high
        # High confidence = LLM is sure this SHOULD be owner-only

required_labels:
  - access_control.owner_only

---
id: policy-mismatch-fee-collection
name: Missing Fee Collection Protection
description: |
  Function collects fees but lacks proper access control or validation.
severity: medium
scope: function
lens: [ValueTransfer, AccessControl]
aggregation_mode: tier_abc_all

match:
  tier_a:
    all:
      - property: visibility
        op: in
        value: [public, external]
    any:
      - property: reads_msg_value
        op: eq
        value: true
      - has_operation: RECEIVES_VALUE_IN

  tier_c:
    all:
      - type: has_label
        value: value_handling.collects_fees
        min_confidence: medium
    any:
      - type: missing_label
        value: access_control.owner_only
      - type: missing_label
        value: invariants.enforces_fee_validation

required_labels:
  - value_handling.collects_fees

---
id: policy-mismatch-initializer-unprotected
name: Unprotected Initializer
description: |
  Function labeled as initialization but lacks proper protection.
severity: critical
scope: function
lens: [StateIntegrity, Initialization]
aggregation_mode: tier_abc_all

match:
  tier_a:
    all:
      - property: visibility
        op: in
        value: [public, external]
    none:
      - property: has_initializer_modifier
        op: eq
        value: true

  tier_c:
    all:
      - type: has_label
        value: state_mutation.initializes_state
        min_confidence: medium
    none:
      - type: has_label
        value: access_control.initialization_protected

required_labels:
  - state_mutation.initializes_state

---
id: policy-mismatch-admin-action-unprotected
name: Unprotected Admin Action
description: |
  Function performs admin-level operations but lacks admin access control.
  Detects privileged operations missing proper restrictions.
severity: critical
scope: function
lens: [AccessControl, Governance]
aggregation_mode: tier_abc_all

match:
  tier_a:
    all:
      - property: visibility
        op: in
        value: [public, external]
    any:
      - has_operation: MODIFIES_OWNER
      - has_operation: MODIFIES_ROLES
      - property: modifies_admin_state
        op: eq
        value: true

  tier_c:
    all:
      - type: has_label
        value: access_control.admin_function
        min_confidence: medium
    none:
      - type: has_label
        value: access_control.owner_only
      - type: has_label
        value: access_control.role_based
      - type: has_label
        value: access_control.multisig_required

required_labels:
  - access_control.admin_function

---
id: policy-mismatch-withdrawal-unvalidated
name: Withdrawal Without Validation
description: |
  Function handles withdrawals but lacks proper balance/amount validation.
  Should verify sufficient balance before sending.
severity: high
scope: function
lens: [ValueTransfer, Invariants]
aggregation_mode: tier_abc_all

match:
  tier_a:
    all:
      - property: visibility
        op: in
        value: [public, external]
      - has_operation: TRANSFERS_VALUE_OUT
    none:
      - has_operation: VALIDATES_INPUT

  tier_c:
    all:
      - type: has_label
        value: value_handling.handles_withdrawal
        min_confidence: medium
    none:
      - type: has_label
        value: invariants.enforces_balance_check
      - type: has_label
        value: invariants.validates_amount

required_labels:
  - value_handling.handles_withdrawal

---
id: policy-mismatch-mint-unrestricted
name: Unrestricted Token Minting
description: |
  Function can mint tokens but lacks minter role check.
  Token minting should be restricted to authorized minters.
severity: critical
scope: function
lens: [TokenSecurity, AccessControl]
aggregation_mode: tier_abc_all

match:
  tier_a:
    all:
      - property: visibility
        op: in
        value: [public, external]
    any:
      - property: modifies_total_supply
        op: eq
        value: true
      - has_operation: MODIFIES_CRITICAL_STATE

  tier_c:
    all:
      - type: has_label
        value: value_handling.mints_tokens
        min_confidence: medium
    none:
      - type: has_label
        value: access_control.minter_only
      - type: has_label
        value: access_control.role_based

required_labels:
  - value_handling.mints_tokens

---
id: policy-mismatch-burn-unrestricted
name: Unrestricted Token Burning
description: |
  Function can burn tokens but allows burning others' tokens without approval.
severity: high
scope: function
lens: [TokenSecurity, AccessControl]
aggregation_mode: tier_abc_all

match:
  tier_a:
    all:
      - property: visibility
        op: in
        value: [public, external]

  tier_c:
    all:
      - type: has_label
        value: value_handling.burns_tokens
        min_confidence: medium
    none:
      - type: has_label
        value: access_control.checks_allowance
      - type: has_label
        value: access_control.owner_only_burn

required_labels:
  - value_handling.burns_tokens

---
id: policy-mismatch-pause-bypassed
name: Pausable Function Without Pause Check
description: |
  Function should respect pause state but doesn't check it.
  Critical for emergency stop functionality.
severity: high
scope: function
lens: [AccessControl, StateIntegrity]
aggregation_mode: tier_abc_all

match:
  tier_a:
    all:
      - property: visibility
        op: in
        value: [public, external]
      - property: writes_state
        op: eq
        value: true
    none:
      - property: checks_paused
        op: eq
        value: true
      - property: has_when_not_paused_modifier
        op: eq
        value: true

  tier_c:
    all:
      - type: has_label
        value: temporal.should_respect_pause
        min_confidence: medium
    none:
      - type: has_label
        value: access_control.pause_exempt

required_labels:
  - temporal.should_respect_pause

---
id: policy-mismatch-oracle-untrusted
name: Oracle Data Used Without Validation
description: |
  Function uses oracle data but doesn't validate freshness or sanity.
  Oracle manipulation attacks possible.
severity: high
scope: function
lens: [Oracle, DataValidation]
aggregation_mode: tier_abc_all

match:
  tier_a:
    all:
      - property: visibility
        op: in
        value: [public, external]
      - has_operation: READS_ORACLE

  tier_c:
    all:
      - type: has_label
        value: external_interaction.uses_oracle
        min_confidence: medium
    none:
      - type: has_label
        value: invariants.validates_oracle_staleness
      - type: has_label
        value: invariants.validates_oracle_bounds

required_labels:
  - external_interaction.uses_oracle

---
id: policy-mismatch-upgrade-unprotected
name: Unprotected Upgrade Function
description: |
  Contract upgrade function lacks proper access control or timelock.
  Upgrades should be highly restricted.
severity: critical
scope: function
lens: [AccessControl, Upgradeability]
aggregation_mode: tier_abc_all

match:
  tier_a:
    all:
      - property: visibility
        op: in
        value: [public, external]
    any:
      - property: is_upgrade_function
        op: eq
        value: true
      - property: modifies_implementation
        op: eq
        value: true

  tier_c:
    all:
      - type: has_label
        value: state_mutation.upgrades_contract
        min_confidence: medium
    none:
      - type: has_label
        value: access_control.owner_only
      - type: has_label
        value: temporal.has_timelock

required_labels:
  - state_mutation.upgrades_contract
```

Create the directory first: `mkdir -p patterns/label_patterns`
  </action>
  <verify>
    - `ls patterns/label_patterns/policy_mismatch.yaml` exists
    - `python -c "import yaml; patterns = list(yaml.safe_load_all(open('patterns/label_patterns/policy_mismatch.yaml'))); print(len(patterns), 'policy mismatch patterns')"`
    - Verify count is 12 (>= 10 required by LABEL-09)
  </verify>
  <done>
    - 12 policy mismatch patterns created (meets 10+ requirement for LABEL-09)
    - Patterns cover: unauthorized transfer, unprotected state, owner bypass, fee collection, initializer, admin action, withdrawal, mint, burn, pause, oracle, upgrade
    - All patterns use tier_c conditions
    - Each pattern has required_labels field
    - Severity and lens properly set
  </done>
</task>

<task type="auto">
  <name>Task 2: Create invariant_violation.yaml patterns</name>
  <files>patterns/label_patterns/invariant_violation.yaml</files>
  <action>
Create patterns/label_patterns/invariant_violation.yaml:

```yaml
# Invariant Violation Patterns
# Detect when code violates expected invariants
# Requires labels about balance checks, supply constraints, etc.

---
id: invariant-balance-unvalidated
name: Balance Modification Without Validation
description: |
  Function modifies user balance without proper validation.
  Balance changes should be validated against invariants.
severity: high
scope: function
lens: [ValueTransfer, Invariants]
aggregation_mode: tier_abc_all

match:
  tier_a:
    all:
      - has_operation: WRITES_USER_BALANCE
    none:
      - has_operation: VALIDATES_INPUT

  tier_c:
    all:
      - type: has_category
        value: value_handling
    none:
      - type: has_label
        value: invariants.enforces_balance_check
      - type: has_label
        value: invariants.validates_amount

required_labels: []  # Category check is sufficient

---
id: invariant-supply-manipulation
name: Supply Manipulation Without Check
description: |
  Function can manipulate token supply without invariant enforcement.
severity: critical
scope: function
lens: [TokenSecurity, Invariants]
aggregation_mode: tier_abc_all

match:
  tier_a:
    all:
      - property: visibility
        op: in
        value: [public, external]
    any:
      - property: modifies_total_supply
        op: eq
        value: true
      - has_operation: MODIFIES_CRITICAL_STATE

  tier_c:
    any:
      - type: has_label
        value: state_mutation.modifies_supply
      - type: has_label
        value: value_handling.mints_tokens
      - type: has_label
        value: value_handling.burns_tokens
    none:
      - type: has_label
        value: invariants.enforces_supply_invariant
      - type: has_label
        value: invariants.enforces_cap

---
id: invariant-underflow-risk
name: Potential Underflow Without Check
description: |
  Balance or counter subtraction without underflow protection.
severity: medium
scope: function
lens: [Arithmetic, Invariants]
aggregation_mode: tier_abc_all

match:
  tier_a:
    all:
      - property: has_subtraction
        op: eq
        value: true
    none:
      - property: uses_safe_math
        op: eq
        value: true

  tier_c:
    any:
      - type: has_label
        value: value_handling.handles_balance
      - type: has_label
        value: state_mutation.modifies_counter
    none:
      - type: has_label
        value: invariants.checks_underflow
      - type: has_label
        value: invariants.enforces_non_negative

---
id: invariant-ratio-violation
name: Ratio/Proportion Violation Risk
description: |
  Function modifies values that should maintain a ratio without validation.
severity: medium
scope: function
lens: [DeFi, Invariants]
aggregation_mode: tier_c_only

match:
  tier_c:
    all:
      - type: has_any_label
        value:
          - value_handling.modifies_reserves
          - value_handling.modifies_collateral
          - state_mutation.modifies_ratio
        min_confidence: medium
    none:
      - type: has_label
        value: invariants.enforces_ratio
      - type: has_label
        value: invariants.validates_proportion
```
  </action>
  <verify>
    - `ls patterns/label_patterns/invariant_violation.yaml` exists
    - `python -c "import yaml; patterns = list(yaml.safe_load_all(open('patterns/label_patterns/invariant_violation.yaml'))); print(len(patterns), 'invariant patterns')"`
  </verify>
  <done>
    - 4 invariant violation patterns created
    - Balance, supply, underflow, ratio patterns
    - Uses has_category and has_any_label conditions
  </done>
</task>

<task type="auto">
  <name>Task 3: Create state_machine.yaml patterns</name>
  <files>patterns/label_patterns/state_machine.yaml</files>
  <action>
Create patterns/label_patterns/state_machine.yaml:

```yaml
# State Machine Patterns
# Detect illegal state transitions and sequencing issues
# Requires labels about function relationships and ordering

---
id: statemachine-double-initialization
name: Double Initialization Risk
description: |
  Initializer function can be called multiple times.
  Should only be callable once.
severity: critical
scope: function
lens: [StateIntegrity, Initialization]
aggregation_mode: tier_abc_all

match:
  tier_a:
    all:
      - property: visibility
        op: in
        value: [public, external]
    none:
      - property: checks_initialized_flag
        op: eq
        value: true
      - property: has_initializer_modifier
        op: eq
        value: true

  tier_c:
    all:
      - type: has_label
        value: state_mutation.initializes_state
        min_confidence: medium
    none:
      - type: has_label
        value: temporal.single_use_only

required_labels:
  - state_mutation.initializes_state

---
id: statemachine-missing-dependency
name: Missing Function Dependency
description: |
  Function should be called after another but dependency not enforced.
  E.g., withdraw() callable before deposit().
severity: high
scope: function
lens: [StateIntegrity, Logic]
aggregation_mode: tier_c_only

match:
  tier_c:
    all:
      - type: has_label
        value: temporal.must_call_after
        min_confidence: medium
    # The label itself carries the dependency info
    # Pattern engine should check if dependency is enforced

---
id: statemachine-pause-bypass
name: Pause State Bypass
description: |
  Function that should respect pause state doesn't check it.
severity: high
scope: function
lens: [AccessControl, StateIntegrity]
aggregation_mode: tier_abc_all

match:
  tier_a:
    all:
      - property: visibility
        op: in
        value: [public, external]
      - property: writes_state
        op: eq
        value: true
    none:
      - property: checks_paused
        op: eq
        value: true
      - property: has_when_not_paused_modifier
        op: eq
        value: true

  tier_c:
    all:
      - type: has_label
        value: temporal.respects_pause
        min_confidence: medium
        # Function SHOULD respect pause but doesn't check

required_labels:
  - temporal.respects_pause

---
id: statemachine-deadline-unchecked
name: Deadline Not Enforced
description: |
  Time-sensitive function without deadline validation.
severity: medium
scope: function
lens: [Temporal, MEV]
aggregation_mode: tier_abc_all

match:
  tier_a:
    all:
      - property: visibility
        op: in
        value: [public, external]
    none:
      - property: checks_deadline
        op: eq
        value: true
      - property: uses_block_timestamp_for_deadline
        op: eq
        value: true

  tier_c:
    all:
      - type: has_label
        value: temporal.time_sensitive
        min_confidence: medium
    none:
      - type: has_label
        value: temporal.checks_deadline

required_labels:
  - temporal.time_sensitive

---
id: statemachine-reentrancy-guard-missing
name: Reentrant Without Guard
description: |
  Function labeled as potentially reentrant but lacks guard.
severity: critical
scope: function
lens: [Reentrancy]
aggregation_mode: tier_abc_all

match:
  tier_a:
    all:
      - has_operation: CALLS_EXTERNAL
      - has_operation: WRITES_USER_BALANCE
    none:
      - property: has_reentrancy_guard
        op: eq
        value: true

  tier_c:
    all:
      - type: has_label
        value: external_interaction.reentrancy_risk
        min_confidence: medium
    none:
      - type: has_label
        value: external_interaction.reentrancy_safe
```
  </action>
  <verify>
    - `ls patterns/label_patterns/state_machine.yaml` exists
    - `python -c "import yaml; patterns = list(yaml.safe_load_all(open('patterns/label_patterns/state_machine.yaml'))); print(len(patterns), 'state machine patterns')"`
  </verify>
  <done>
    - 5 state machine patterns created
    - Double init, dependency, pause, deadline, reentrancy patterns
    - Uses temporal and state_mutation labels
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `ls patterns/label_patterns/*.yaml | wc -l` shows 3 files
2. `python -c "import yaml; import os; count = sum(len(list(yaml.safe_load_all(open(f'patterns/label_patterns/{f}')))) for f in os.listdir('patterns/label_patterns') if f.endswith('.yaml')); print(count, 'total patterns')"`
3. Verify policy mismatch patterns >= 10 (LABEL-09 requirement)
4. All patterns have valid YAML syntax
5. All patterns reference valid tier_c condition types
</verification>

<success_criteria>
- 10+ policy mismatch patterns detecting intent vs. code issues (LABEL-09 requirement met)
- 4+ invariant violation patterns detecting constraint issues
- 5+ state machine patterns detecting sequencing issues
- All patterns use tier_c conditions properly
- Patterns have appropriate severity and lens
- required_labels field specifies prerequisites
</success_criteria>

<output>
After completion, create `.planning/phases/05-semantic-labeling/05-07-SUMMARY.md`
</output>
