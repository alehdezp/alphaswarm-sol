---
phase: 07.2-corpus-research-vulndocs-expansion
plan: 07
type: execute
wave: 4
depends_on: ["07.2-01", "07.2-02", "07.2-03"]
files_modified:
  - .vrs/corpus/contracts
  - .vrs/corpus/ground-truth
  - .vrs/corpus/corpus.db
  - .vrs/corpus/README.md
  - .vrs/corpus/metadata/urls.yaml
autonomous: true

must_haves:
  truths:
    - "Corpus DB and ground truth records are populated for 150+ contracts"
    - "Corpus ratio targets are met (80% safe / 20% vulnerable)"
    - "README reflects actual schema and corpus expectations"
  artifacts:
    - path: ".vrs/corpus/corpus.db"
      provides: "Populated corpus database"
    - path: ".vrs/corpus/README.md"
      provides: "Aligned corpus documentation"
    - path: ".vrs/corpus/contracts"
      provides: "Ingested contract corpus by category"
  key_links:
    - from: ".vrs/corpus/corpus.db"
      to: ".vrs/corpus/ground-truth"
      via: "Contract IDs and ground truth labels"
      pattern: "contract_id"
---

<objective>
Populate the initial 150+ contract corpus and align corpus documentation.

Purpose: Provide the authoritative ground truth corpus required for Phase 7.3 GA validation.
Output: Populated `.vrs/corpus/` (contracts, ground truth, DB) and updated README.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.2-corpus-research-vulndocs-expansion/07.2-CONTEXT.md
@.planning/phases/07.2-corpus-research-vulndocs-expansion/07.2-RESEARCH.md
@.vrs/corpus/README.md
@.vrs/corpus/metadata/sources.yaml
@scripts/corpus_ingest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populate corpus from manifest</name>
  <files>
    .vrs/corpus/contracts
    .vrs/corpus/ground-truth
    .vrs/corpus/corpus.db
    .vrs/corpus/metadata/urls.yaml
  </files>
  <action>
Use `scripts/corpus_ingest.py` with the manifest to populate the corpus:
- Target 150+ contracts with 80/20 safe/vulnerable ratio.
- Ensure each contract has ground-truth YAML (stub if necessary) that validates against `src/alphaswarm_sol/testing/scenarios/config_schema.py`.
- Ensure provenance URLs are appended to urls.yaml.

Note: Corpus content is generated; keep directory structure stable for reproducibility.
</action>
  <verify>
    uv run python scripts/corpus_ingest.py --manifest .vrs/corpus/metadata/sources.yaml --dry-run
  </verify>
  <done>
    Corpus directories and DB are populated with the target size and ratio.
  </done>
</task>

<task type="auto">
  <name>Task 2: Align corpus README with actual schema and ratios</name>
  <files>
    .vrs/corpus/README.md
  </files>
  <action>
Update the README to match the current `CorpusDB` schema and the Phase 7.2 corpus targets (150+ contracts, 80/20 ratio).
Clarify how DB fields map to YAML sidecar metadata and why the DB remains intentionally lean.
</action>
  <verify>
    rg -n "schema|corpus" .vrs/corpus/README.md
  </verify>
  <done>
    README accurately reflects schema, ratios, and corpus layout.
  </done>
</task>

</tasks>

<verification>
- Corpus DB and ground truth files exist for all ingested contracts.
- README reflects the actual schema and target ratios.
</verification>

<success_criteria>
- 150+ contracts in `.vrs/corpus/contracts/` with ground truth stubs.
- URLs ledger updated for all sources.
- README aligned to current corpus schema.
</success_criteria>

<output>
After completion, create `.planning/phases/07.2-corpus-research-vulndocs-expansion/07.2-07-SUMMARY.md`.
</output>
