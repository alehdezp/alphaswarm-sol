---
phase: 07.2-corpus-research-vulndocs-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/alphaswarm_sol/testing/corpus/ingest.py
  - src/alphaswarm_sol/testing/corpus/__init__.py
  - scripts/corpus_ingest.py
  - .vrs/corpus/metadata/sources.yaml
  - .vrs/corpus/metadata/urls.yaml
autonomous: true

must_haves:
  truths:
    - "Corpus ingestion is reproducible from a manifest with commit-pinned sources"
    - "Provenance for all sources is logged in urls.yaml"
  artifacts:
    - path: "src/alphaswarm_sol/testing/corpus/ingest.py"
      provides: "Manifest-driven corpus ingestion helpers"
    - path: "scripts/corpus_ingest.py"
      provides: "CLI wrapper for corpus ingestion"
    - path: ".vrs/corpus/metadata/sources.yaml"
      provides: "Corpus source manifest with repo + commit metadata"
  key_links:
    - from: "src/alphaswarm_sol/testing/corpus/ingest.py"
      to: "src/alphaswarm_sol/testing/corpus/db.py"
      via: "CorpusDB insertions"
      pattern: "CorpusDB"
    - from: "scripts/corpus_ingest.py"
      to: ".vrs/corpus/metadata/urls.yaml"
      via: "Provenance logging"
      pattern: "accessed_at"
---

<objective>
Stand up a reproducible corpus ingestion pipeline for DVDeFi, Ethernaut, and vetted PoC repositories.

Purpose: Provide the tooling needed to populate the authoritative ground truth corpus required for Phase 7.3 GA validation.
Output: Ingestion tooling and source manifest with provenance logging.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.2-corpus-research-vulndocs-expansion/07.2-CONTEXT.md
@.planning/phases/07.2-corpus-research-vulndocs-expansion/07.2-RESEARCH.md
@.vrs/corpus/README.md
@src/alphaswarm_sol/testing/corpus/db.py
@src/alphaswarm_sol/testing/corpus/ground_truth.py
@scripts/validate_dvdefi.py
@scripts/download_benchmark_corpus.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement manifest-driven corpus ingestion</name>
  <files>
    src/alphaswarm_sol/testing/corpus/ingest.py
    src/alphaswarm_sol/testing/corpus/__init__.py
    scripts/corpus_ingest.py
    .vrs/corpus/metadata/sources.yaml
    .vrs/corpus/metadata/urls.yaml
  </files>
  <action>
Create a manifest-driven ingestion pipeline that can reproduce the corpus from pinned sources.

Implementation details:
- `sources.yaml` should include: repo_url, commit_sha, category, local_path, query, notes, and expected_contracts.
- `ingest.py` should:
  - Parse the manifest, clone/pin repositories if missing, and copy Solidity files into `.vrs/corpus/contracts/{category}/`.
  - Generate stable `contract_id` values (e.g., hash of repo + path + commit).
  - Insert rows into `CorpusDB` and create/update ground truth YAML stubs.
  - Ensure ground truth YAML stubs align with `src/alphaswarm_sol/testing/scenarios/config_schema.py`.
  - Keep the DB lean; store rich metadata (provenance, findings detail) in YAML sidecars.
  - Append provenance entries to `.vrs/corpus/metadata/urls.yaml` with `accessed_at` and `agent`.
- `corpus_ingest.py` should expose `--manifest`, `--category`, and `--dry-run` flags.

Re-export any new helpers from `src/alphaswarm_sol/testing/corpus/__init__.py`.
</action>
  <verify>
    rg -n "CorpusIngest|sources.yaml|urls.yaml" src/alphaswarm_sol/testing/corpus/ingest.py scripts/corpus_ingest.py
  </verify>
  <done>
    Ingestion pipeline exists, uses the manifest, and records provenance to urls.yaml.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire CLI wrapper and public exports</name>
  <files>
    src/alphaswarm_sol/testing/corpus/__init__.py
    scripts/corpus_ingest.py
  </files>
  <action>
Ensure the CLI wrapper calls the ingestion helpers with clear flags and emits a concise summary.
Export ingestion helpers from `src/alphaswarm_sol/testing/corpus/__init__.py` for reuse in tests.
  </action>
  <verify>
    uv run python scripts/corpus_ingest.py --help
  </verify>
  <done>
    CLI wrapper is functional and ingestion helpers are importable from the corpus package.
  </done>
</task>

</tasks>

<verification>
- Corpus ingestion is reproducible from manifest.
- Provenance is logged for every source.
</verification>

<success_criteria>
- Manifest-driven ingestion pipeline exists and works in dry-run mode.
- URLs ledger is populated and schema-consistent.
</success_criteria>

<output>
After completion, create `.planning/phases/07.2-corpus-research-vulndocs-expansion/07.2-01-SUMMARY.md`.
</output>
