---
phase: 07.2-corpus-research-vulndocs-expansion
plan: 08
type: execute
wave: 5
depends_on: ["07.2-04"]
files_modified:
  - src/alphaswarm_sol/testing/tier_c_harness.py
  - src/alphaswarm_sol/testing/runner.py
  - src/alphaswarm_sol/testing/metrics.py
  - tests/testing/test_tier_c_harness.py
  - .vrs/testing/tier_c_stability.yaml
autonomous: true

must_haves:
  truths:
    - "Tier C stability runs (N=50) produce a stability score per pattern"
    - "Shadow mode consensus is enforced at 70% overlap"
    - "Tier C stability results are persisted for review"
  artifacts:
    - path: "src/alphaswarm_sol/testing/tier_c_harness.py"
      provides: "Tier C stability + shadow mode harness"
    - path: ".vrs/testing/tier_c_stability.yaml"
      provides: "Tier C stability report output"
  key_links:
    - from: "src/alphaswarm_sol/testing/tier_c_harness.py"
      to: "src/alphaswarm_sol/queries/tier_c.py"
      via: "Tier C matcher"
      pattern: "TierCMatcher"
    - from: "src/alphaswarm_sol/testing/runner.py"
      to: "src/alphaswarm_sol/testing/tier_c_harness.py"
      via: "Harness integration"
      pattern: "tier_c"
---

<objective>
Implement the Tier C stability + shadow mode harness and integrate it into the test runner.

Purpose: Detect label-driven instability and enforce the 0.85 stability threshold before GA validation.
Output: Tier C harness, integration hooks, and a persisted stability report.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.2-corpus-research-vulndocs-expansion/07.2-CONTEXT.md
@.planning/phases/07.2-corpus-research-vulndocs-expansion/07.2-RESEARCH.md
@src/alphaswarm_sol/queries/tier_c.py
@src/alphaswarm_sol/testing/harness/runner.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Tier C stability + shadow mode harness</name>
  <files>
    src/alphaswarm_sol/testing/tier_c_harness.py
    .vrs/testing/tier_c_stability.yaml
  </files>
  <action>
Create a Tier C harness that:
- Runs label-based detection N=50 times per pattern.
- Computes stability score and fails below 0.85.
- Supports shadow mode with dual labelers and 70% overlap threshold.
- Writes a structured report to `.vrs/testing/tier_c_stability.yaml`.
</action>
  <verify>
    rg -n "tier_c_stability" src/alphaswarm_sol/testing/tier_c_harness.py
  </verify>
  <done>
    Tier C harness computes stability and persists results in the report file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate harness into TestRunner + metrics</name>
  <files>
    src/alphaswarm_sol/testing/runner.py
    src/alphaswarm_sol/testing/metrics.py
    tests/testing/test_tier_c_harness.py
  </files>
  <action>
Integrate Tier C harness into `TestRunner` with a Phase 7.2 execution mode and capture metrics.
Add unit tests to validate:
- N=50 runs are performed
- Stability threshold gating
- Shadow mode disagreement logging
</action>
  <verify>
    pytest tests/testing/test_tier_c_harness.py -v
  </verify>
  <done>
    TestRunner invokes the harness and tests cover stability + shadow mode behavior.
  </done>
</task>

</tasks>

<verification>
- Tier C stability report exists and includes per-pattern stability scores.
- Shadow mode consensus is enforced at 70% overlap.
</verification>

<success_criteria>
- Tier C harness is integrated into TestRunner and produces a stable report.
</success_criteria>

<output>
After completion, create `.planning/phases/07.2-corpus-research-vulndocs-expansion/07.2-08-SUMMARY.md`.
</output>
