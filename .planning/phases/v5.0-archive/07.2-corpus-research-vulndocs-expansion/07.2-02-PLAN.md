---
phase: 07.2-corpus-research-vulndocs-expansion
plan: 02
type: execute
wave: 2
depends_on: ["07.2-00", "07.2-01"]
files_modified:
  - src/alphaswarm_sol/vulndocs/reference_extract.py
  - scripts/vulndocs_reference_extract.py
  - .vrs/corpus/metadata/urls.yaml
  - vulndocs
autonomous: true

must_haves:
  truths:
    - "All VulnDocs references are logged with URL provenance and access dates"
    - "VulnDocs entries include validated source references and example pointers"
    - "Validation pipeline runs after VulnDocs updates"
  artifacts:
    - path: "src/alphaswarm_sol/vulndocs/reference_extract.py"
      provides: "VulnDocs reference extraction utilities"
    - path: "scripts/vulndocs_reference_extract.py"
      provides: "CLI for reference extraction + URL ledger update"
    - path: ".vrs/corpus/metadata/urls.yaml"
      provides: "Authoritative URL provenance ledger"
  key_links:
    - from: "scripts/vulndocs_reference_extract.py"
      to: ".vrs/corpus/metadata/urls.yaml"
      via: "Provenance append"
      pattern: "accessed_at"
    - from: "src/alphaswarm_sol/vulndocs/reference_extract.py"
      to: "vulndocs"
      via: "Source extraction"
      pattern: "sources|references"
---

<objective>
Extract and validate VulnDocs source references, log all URLs, and attach examples for test generation.

Purpose: Build an evidence-backed VulnDocs base with complete provenance before expanding patterns and corpus.
Output: Reference extraction tooling, populated provenance ledger, and updated VulnDocs research files.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.2-corpus-research-vulndocs-expansion/07.2-CONTEXT.md
@.planning/phases/07.2-corpus-research-vulndocs-expansion/07.2-RESEARCH.md
@docs/guides/vulndocs-basics.md
@vulndocs/.meta/templates/provenance.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build reference extraction + provenance logging tools</name>
  <files>
    src/alphaswarm_sol/vulndocs/reference_extract.py
    scripts/vulndocs_reference_extract.py
    .vrs/corpus/metadata/urls.yaml
  </files>
  <action>
Create a reference extraction utility that scans `vulndocs/**/index.yaml` and `research/*.md` for URLs and updates the canonical URL ledger.

Requirements:
- Parse sources/references from index.yaml and research docs.
- Deduplicate by URL, preserve first-seen metadata, append new entries.
- Treat `.vrs/corpus/metadata/urls.yaml` as append-only; never overwrite prior provenance.
- Require `accessed_at`, `category`, `query`, `agent`, and `extracted` fields.
- Provide a CLI that supports `--dry-run`, `--category`, and `--write-ledger`.
</action>
  <verify>
    rg -n "reference_extract|urls.yaml" src/alphaswarm_sol/vulndocs/reference_extract.py scripts/vulndocs_reference_extract.py
  </verify>
  <done>
    Extraction tooling exists and writes to urls.yaml without clobbering existing entries.
  </done>
</task>

<task type="auto">
  <name>Task 2: Populate VulnDocs sources + examples and run validation</name>
  <files>
    vulndocs
  </files>
  <action>
Use `/vrs-discover` + `/vrs-ingest-url` to populate missing sources and example links for VulnDocs entries.

Process:
- Run the extraction tool to identify missing provenance entries.
- For missing items, use the mandated VulnDocs workflow to ingest URLs (no direct edits).
- Include Solodit audit findings and architecture‑shift sources (EIP‑7702, Uniswap v4 hooks) as reference examples where relevant.
- Add/update `research/sources.md` and `research/examples.md` files to capture exploit references and PoC links.
- Run `/vrs-validate-vulndocs` after updates.
</action>
  <verify>
    uv run alphaswarm vulndocs validate vulndocs/
  </verify>
  <done>
    VulnDocs entries have logged provenance and examples, and validation passes.
  </done>
</task>

</tasks>

<verification>
- urls.yaml includes all extracted URLs with access metadata.
- VulnDocs research files exist where missing.
- Validation pipeline completes without errors.
</verification>

<success_criteria>
- Provenance logging is complete for all VulnDocs URLs touched.
- Example references are added for test generation.
- `/vrs-validate-vulndocs` runs successfully after edits.
</success_criteria>

<output>
After completion, create `.planning/phases/07.2-corpus-research-vulndocs-expansion/07.2-02-SUMMARY.md`.
</output>
