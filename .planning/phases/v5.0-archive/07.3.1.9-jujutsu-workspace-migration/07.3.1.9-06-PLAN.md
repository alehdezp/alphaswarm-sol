# Plan 07.3.1.9-06: Phase 07.3.1.7 Implementation Plans Rewrite

**Phase:** 07.3.1.9 - Jujutsu Workspace Migration
**Wave:** 3 (Phase 07.3.1.7 Redesign)
**Status:** READY

---

## Objective

Rewrite all implementation plans in Phase 07.3.1.7 to use jj workspace commands and terminology.

**Purpose:** Ensure the synthetic test environments phase plans are executable with jj workspaces.

**Output:** Updated plan files with jj workspace tasks and commands.

---

## Inputs

- Renamed directory: `.planning/phases/07.3.1.7-synthetic-test-environments-workspace-harness/`
- Plan files to update:
  - `07.3.1.7-00-PLAN.md`
  - `07.3.1.7-01-PLAN.md`
  - `07.3.1.7-02-PLAN.md`
  - `07.3.1.7-03-PLAN.md`
  - `07.3.1.7-PLAN-INDEX.md`
- Completed Plan 05 (context rewrite)

---

## Tasks

### T1: Update 07.3.1.7-02-PLAN.md (Workspace Harness Plan)

This is the primary harness plan with extensive worktree references.

**Update frontmatter files_modified:**
```yaml
# Current
files_modified:
  - scripts/manage_worktrees.sh
  - configs/worktree_snapshot_policy.yaml
  ...

# New
files_modified:
  - scripts/manage_workspaces.sh
  - configs/workspace_snapshot_policy.yaml
  ...
```

**Update outcome_validation tests:**
```yaml
# Current
- name: "worktree_create_and_rollback"
  verifies: "Worktree can be created from synthetic repo and reset to checkpoint"
  how: |
    1. Create worktree from a synthetic repo fixture
    2. Checkout checkpoint commit
    ...

# New
- name: "workspace_create_and_rollback"
  verifies: "Workspace can be created from synthetic repo and reset to checkpoint"
  how: |
    1. Create jj workspace from a synthetic repo fixture
    2. Edit to checkpoint revision using `jj edit <rev>`
    ...
```

**Update must_haves:**
```yaml
# Current
must_haves:
  truths:
    - "Worktree harness can create, rollback, and preserve synthetic workflow runs"
    - "Worktree isolation prevents .vrs collisions across parallel runs"
    ...

# New
must_haves:
  truths:
    - "Workspace harness can create, rollback, and preserve synthetic workflow runs"
    - "Workspace isolation prevents .vrs collisions across parallel runs"
    ...
```

**Update task T1:**
```xml
<!-- Current -->
<task id="T1" type="auto">
  <name>Extend worktree harness for synthetic repo fixtures</name>
  <files>
    scripts/manage_worktrees.sh
    scripts/test_real_workflow.py
  </files>
  <action>
Update worktree scripts to:
- Support `tests/fixtures/repos/<repo-id>` as fixtures
- Allow checkout/reset to named checkpoints
...
  </action>
</task>

<!-- New -->
<task id="T1" type="auto">
  <name>Extend workspace harness for synthetic repo fixtures</name>
  <files>
    scripts/manage_workspaces.sh
    scripts/test_real_workflow.py
  </files>
  <action>
Update workspace scripts to:
- Support `tests/fixtures/repos/<repo-id>` as fixtures
- Allow rollback to named revisions using `jj edit`
- Use `jj workspace add` for isolation
- Use `jj workspace forget` + `rm -rf` for cleanup
...
  </action>
</task>
```

**Update task T5 (claude-code-agent-teams smoke test):**
```xml
<!-- Current -->
<task id="T5" type="claude-code-agent-teams">
  <name>claude-code-controller smoke test in synthetic worktree</name>
  <action>
...
- claude-code-controller send "cd /tmp/vrs-worktrees/<test-worktree>" --pane=X
...
  </action>
</task>

<!-- New -->
<task id="T5" type="claude-code-agent-teams">
  <name>claude-code-controller smoke test in synthetic workspace</name>
  <action>
...
- claude-code-controller send "cd /tmp/vrs-workspaces/<test-workspace>" --pane=X
...
  </action>
</task>
```

### T2: Update 07.3.1.7-00-PLAN.md

Update any worktree references in the overview plan:
- "worktree harness" → "workspace harness"
- "git worktree" → "jj workspace"
- Script references updated

### T3: Update 07.3.1.7-01-PLAN.md

Update any worktree references:
- Synthetic repo checkpoints described using jj revision terminology
- Any git checkout references → jj edit

### T4: Update 07.3.1.7-03-PLAN.md

Update any worktree references:
- Mini-stress testing terminology
- Parallel workspace execution

### T5: Update 07.3.1.7-PLAN-INDEX.md

Update the plan index to reflect new terminology:
```markdown
# Current
| Plan | Title | Status |
|------|-------|--------|
| 02 | Worktree Harness | ... |

# New
| Plan | Title | Status |
|------|-------|--------|
| 02 | Workspace Harness | ... |
```

### T6: Verify All Plans Use Consistent Terminology

Run verification:
```bash
# Check for any remaining worktree references
grep -r "worktree" .planning/phases/07.3.1.7-synthetic-test-environments-workspace-harness/*.md

# Should return empty or only explanatory historical context
```

---

## Outputs

- [ ] `07.3.1.7-00-PLAN.md` - Updated
- [ ] `07.3.1.7-01-PLAN.md` - Updated
- [ ] `07.3.1.7-02-PLAN.md` - Fully rewritten for jj workspaces
- [ ] `07.3.1.7-03-PLAN.md` - Updated
- [ ] `07.3.1.7-PLAN-INDEX.md` - Updated
- [ ] All plans use consistent jj workspace terminology

---

## Verification

```bash
# Verify no worktree references in plan files
grep -r "git worktree" .planning/phases/07.3.1.7-synthetic-test-environments-workspace-harness/*.md
# Should return empty

# Verify jj workspace references exist
grep -r "jj workspace" .planning/phases/07.3.1.7-synthetic-test-environments-workspace-harness/*.md | wc -l
# Should be > 10

# Verify script references updated
grep -r "manage_workspaces.sh" .planning/phases/07.3.1.7-synthetic-test-environments-workspace-harness/*.md | wc -l
# Should be > 0
```

---

## Dependencies

- **Depends on:** Plan 07.3.1.9-05 (directory rename and context rewrite)
- **Blocks:** Plan 07.3.1.9-07 (documentation updates)

---

## Execution Guardrails

- Workspace isolation: run any commands or experiments that mutate state in a fresh jj workspace;
  do not use the main workspace and do not reuse workspaces for reruns.
- Preserve plan structure and frontmatter format
- Update only terminology, not plan logic
