# Plan 07.3.1.9-05: Phase 07.3.1.7 Directory Rename and Context Rewrite

**Phase:** 07.3.1.9 - Jujutsu Workspace Migration
**Wave:** 3 (Phase 07.3.1.7 Redesign)
**Status:** READY

---

## Objective

Rename the Phase 07.3.1.7 directory and rewrite its context.md to reflect jj workspace architecture.

**Purpose:** Align the synthetic test environments phase with the new jj workspace implementation.

**Output:** Renamed phase directory with updated context document.

---

## Inputs

- Current directory: `.planning/phases/07.3.1.7-synthetic-test-environments-worktree-harness/`
- Current context: `.planning/phases/07.3.1.7-synthetic-test-environments-worktree-harness/context.md`
- Completed Plans 01-04 (core migration complete)

---

## Tasks

### T1: Rename Phase Directory

```bash
# Rename the phase directory
mv .planning/phases/07.3.1.7-synthetic-test-environments-worktree-harness \
   .planning/phases/07.3.1.7-synthetic-test-environments-workspace-harness
```

### T2: Update context.md Header and Overview

**Current (lines 1-5):**
```markdown
# Phase 07.3.1.7 Synthetic Test Environments + Worktree Harness Context

**Status:** Draft context
**Scope:** Synthetic repo foundation + worktree isolation for workflow testing
**Purpose:** Build a controlled, fake-but-realistic corpus and a repeatable worktree harness so workflows can be tested without full audit reruns.
```

**New:**
```markdown
# Phase 07.3.1.7 Synthetic Test Environments + Workspace Harness Context

**Status:** Draft context
**Scope:** Synthetic repo foundation + jj workspace isolation for workflow testing
**Purpose:** Build a controlled, fake-but-realistic corpus and a repeatable jj workspace harness so workflows can be tested without full audit reruns.
```

### T3: Update Phase Definition Section

**Current (lines 14-19):**
```markdown
## Phase Definition

- Build the **foundation dataset** of synthetic repos...
- Establish a **standard repo template**...
- Enforce **quality gates**...
- Provide a **worktree-based harness** for isolation, rollback, and debug-friendly iteration.
- Define **3 scenarios per workflow**...
```

**New:**
```markdown
## Phase Definition

- Build the **foundation dataset** of synthetic repos (target: 20) under `tests/fixtures/repos/`.
- Establish a **standard repo template** and **manifest schema** for synthetic repos.
- Enforce **quality gates** for vulnerability diversity, decoys, safe controls, compile, and graph build.
- Provide a **jj workspace-based harness** for isolation, rollback, and debug-friendly iteration.
- Define **3 scenarios per workflow** for baseline validation, plus a mini-stress subset.
```

### T4: Update Locked Decisions Section

**Current (lines 21-28):**
```markdown
## Locked Decisions (Non-Negotiable)

...
- Each repo must include **meaningful git history + checkpoints** for worktree branching.
- Workflows are tested using **claude-code-controller** with dedicated demo labels...
```

**New:**
```markdown
## Locked Decisions (Non-Negotiable)

- Phase 1 target: **20 synthetic repos** in `tests/fixtures/repos/`.
- Each repo must be a minimal PoC and **compile or run sufficiently** for workflow tests.
- Each repo must **build a graph** successfully (graph-first validation).
- Each repo must include **intentional vulnerabilities** (multiple types when possible), **decoy signals**, and at least one **safe control**.
- Each repo must include **meaningful jj revision history + checkpoints** for workspace branching.
- Workflows are tested using **claude-code-controller** with dedicated demo labels `vrs-demo-{workflow}-{timestamp}`.
- External ground truth is **not** derived from synthetic repos. Synthetic repos use **expectations** only.
```

### T5: Update Required Sources of Truth

**Current (lines 38-49):**
```markdown
## Required Sources Of Truth

...
- `scripts/manage_worktrees.sh`
- `configs/worktree_snapshot_policy.yaml`
```

**New:**
```markdown
## Required Sources Of Truth

- `.planning/testing/rules/canonical/RULES-ESSENTIAL.md`
- `.planning/testing/rules/canonical/VALIDATION-RULES.md`
- `.planning/testing/rules/canonical/TESTING-FRAMEWORK.md`
- `.planning/testing/rules/canonical/TESTING-PHILOSOPHY.md`
- `.planning/testing/rules/claude-code-controller-REFERENCE.md` and `.planning/testing/rules/canonical/claude-code-controller-instructions.md`
- `docs/PHILOSOPHY.md`
- `docs/reference/testing-framework.md`
- `docs/workflows/README.md`
- `docs/workflows/PLAYBOOKS.md`
- `scripts/manage_workspaces.sh`
- `configs/workspace_snapshot_policy.yaml`
```

### T6: Update Workspace Strategy Section

**Current (lines 62-67):**
```markdown
## Worktree Strategy

- Use one worktree per workflow scenario.
- Allow rollback to explicit checkpoints without full audit reruns.
- Preserve `.vrs/` state in debug mode for failure analysis.
- Enforce collision checks to prevent shared `.vrs` state across worktrees.
```

**New:**
```markdown
## Workspace Strategy

- Use one jj workspace per workflow scenario.
- Allow rollback to explicit revisions without full audit reruns (`jj edit <revision>`).
- Preserve `.vrs/` state in debug mode for failure analysis.
- Enforce collision checks to prevent shared `.vrs` state across workspaces.
- Leverage jj's automatic conflict detection for parallel workspace safety.
```

### T7: Update Success Definition

**Current (lines 69-74):**
```markdown
## Success Definition

- 20 synthetic repos exist with manifests, checkpoints, and quality gate compliance.
- Worktree harness can create/rollback isolated scenario runs with collision checks.
- Each workflow has 3 scenario variants defined and runnable via claude-code-controller.
- Mini-stress subset demonstrates expanded run size beyond baseline.
```

**New:**
```markdown
## Success Definition

- 20 synthetic repos exist with manifests, checkpoints, and quality gate compliance.
- Workspace harness can create/rollback isolated scenario runs with collision checks.
- Each workflow has 3 scenario variants defined and runnable via claude-code-controller.
- Mini-stress subset demonstrates expanded run size beyond baseline.
- `jj workspace add` / `jj workspace forget` commands work correctly in harness.
```

### T8: Update Cross-References in Other Phase Files

Update references in:
- `.planning/ROADMAP.md` (if it references the phase name)
- `.planning/STATE.md` (if it references the phase)
- Any other files that link to this phase directory

---

## Outputs

- [ ] Phase directory renamed to `07.3.1.7-synthetic-test-environments-workspace-harness`
- [ ] `context.md` fully updated with jj workspace terminology
- [ ] All cross-references updated
- [ ] Script references updated (`manage_worktrees.sh` → `manage_workspaces.sh`)

---

## Verification

```bash
# Verify directory rename
ls .planning/phases/ | grep "07.3.1.7"
# Should show: 07.3.1.7-synthetic-test-environments-workspace-harness

# Verify no worktree references in context.md
grep -i "worktree" .planning/phases/07.3.1.7-synthetic-test-environments-workspace-harness/context.md
# Should return empty (or only in historical notes)

# Verify jj/workspace references exist
grep -i "workspace" .planning/phases/07.3.1.7-synthetic-test-environments-workspace-harness/context.md | wc -l
# Should be > 5
```

---

## Dependencies

- **Depends on:** Plan 07.3.1.9-04 (core migration complete)
- **Blocks:** Plan 07.3.1.9-06 (implementation plans rewrite)

---

## Execution Guardrails

- Workspace isolation: run any commands or experiments that mutate state in a fresh jj workspace;
  do not use the main workspace and do not reuse workspaces for reruns.
- Create backup of original files before renaming
- Update git tracking after directory rename
