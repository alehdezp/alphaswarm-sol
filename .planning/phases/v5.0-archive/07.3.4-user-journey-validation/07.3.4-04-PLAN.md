---
phase: 07.3.4-user-journey-validation
plan: "04"
type: ga-validation
wave: 1
depends_on: ["07.3.1.6-02"]
files_modified:
  - reports/user_journey_execution.json
  - reports/user_journey_metrics.md
autonomous: true

must_haves:
  truths:
    - "User journeys executed via Holodeck (isolated)"
    - "Evidence packs validated by Blind Critic"
    - "Metrics reflect REAL friction points"
---

<objective>
Execute the "User Journey" scenarios using the newly hardened Holodeck machinery. Verify that the system works for a fresh user by actually becoming one.
</objective>

<tasks>
<task type="auto">
  <name>Execute Novice Journey (Holodeck)</name>
  <action>
    Run `python scripts/e2e/run_holodeck.py --scenario novice_install`.
    This spins up a fresh environment, attempts to install `alphaswarm`, and runs a basic scan.
  </action>
</task>

<task type="auto">
  <name>Execute Error Recovery (Chaos)</name>
  <action>
    Run `python scripts/e2e/run_holodeck.py --scenario chaos_recovery --chaos`.
    Verify that when components fail, the system degrades gracefully and provides helpful error messages.
  </action>
</task>

<task type="auto">
  <name>Critic Review & Metrics</name>
  <action>
    Run `scripts/e2e/verify_evidence.py` on the output.
    If valid, run `collect_user_metrics.py` to populate `reports/user_journey_metrics.md` with honest data.
  </action>
</task>
</tasks>
