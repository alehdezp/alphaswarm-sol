---
phase: 05.9-llm-graph-interface-improvements
plan: 04
type: execute
wave: 2
depends_on:
  - 05.9-01
files_modified:
  - src/alphaswarm_sol/llm/evidence_ids.py
  - src/alphaswarm_sol/kg/graph_hash.py
  - tests/test_evidence_ids.py
autonomous: true

must_haves:
  truths:
    - "Evidence references are deterministic and resolvable via build hash + node id + span."
    - "A build hash is computed consistently for graph outputs."
  artifacts:
    - path: "src/alphaswarm_sol/llm/evidence_ids.py"
      provides: "Evidence ID generator + resolver"
      exports: ["deterministic evidence IDs"]
  key_links:
    - from: "src/alphaswarm_sol/llm/evidence_ids.py"
      to: "src/alphaswarm_sol/kg/graph_hash.py"
      via: "build hash + evidence ID composition"
---

<objective>
Introduce deterministic evidence references so all findings can be reproduced and validated against a specific graph build.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.9-llm-graph-interface-improvements/05.9-CONTEXT.md
</context>

<execution_guardrails>
- Full doc alignment: align objective, must_haves, and tasks against `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, plus phase context or research docs before execution. Any scope change must be reflected in the plan and `.planning/STATE.md`.
- Workspace isolation: run any commands or experiments that mutate state in a fresh jj workspace; do not use the main workspace and do not reuse workspaces for reruns.
- Repeat-run detection markers: include `[ALPHASWARM-START] RUN_ID TIMESTAMP` and `[ALPHASWARM-END] RUN_ID EXIT_CODE DURATION_MS` markers in transcripts or logs so reruns are detectable.
- claude-code-controller subject: for interactive runs, use `claude-code-controller` and launch Claude Code as the subject in the claude-code-agent-teams session; transcripts must show the Claude Code session.
- Seed scope boundaries: treat B-seeds (baseline or regression inputs) and N-seeds (net-new exploratory inputs) as separate scopes; do not mix them in a single run and track results separately.
</execution_guardrails>

<tasks>

<task type="auto">
  <name>Task 1: Add deterministic evidence ID generator + resolver</name>
  <files>src/alphaswarm_sol/llm/evidence_ids.py</files>
  <action>
Implement evidence IDs with:
- build_hash + node_id + source span
- stable hashing and reversible resolver
- helpers for clause matrix and evidence_refs
Keep API small and deterministic.
  </action>
  <verify>
```bash
rg -n "evidence_id|build_hash|resolver" src/alphaswarm_sol/llm/evidence_ids.py
```
  </verify>
  <done>
Evidence ID generator and resolver are implemented and documented.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement graph build hash utility + tests</name>
  <files>src/alphaswarm_sol/kg/graph_hash.py, tests/test_evidence_ids.py</files>
  <action>
Add a build hash utility for graph metadata and tests that:
- Verify evidence IDs are stable across runs
- Confirm resolver maps back to source locations
  </action>
  <verify>
```bash
rg -n "graph_hash|build_hash" src/alphaswarm_sol/kg/graph_hash.py
```
  </verify>
  <done>
Graph build hash and evidence determinism are verified by tests.
  </done>
</task>

</tasks>
