---
phase: 05.9-llm-graph-interface-improvements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/reference/graph-interface-v2.md
  - schemas/graph_interface_v2.json
  - src/alphaswarm_sol/llm/interface_contract.py
autonomous: true

must_haves:
  truths:
    - "Graph Interface Contract v2 is specified as a versioned ABI with explicit unknowns and omissions."
    - "A JSON schema exists for v2 and is enforced by a validator module."
    - "A compatibility shim contract skeleton exists for v1 -> v2 mapping."
  artifacts:
    - path: "docs/reference/graph-interface-v2.md"
      provides: "Normative v2 contract spec + examples"
      exports: ["Graph Interface Contract v2"]
    - path: "schemas/graph_interface_v2.json"
      provides: "JSON Schema for v2 outputs"
      exports: ["Graph Interface v2 Schema"]
  key_links:
    - from: "src/alphaswarm_sol/llm/interface_contract.py"
      to: "schemas/graph_interface_v2.json"
      via: "schema validation"
---

<objective>
Define the Graph Interface Contract v2 as a first-class ABI: a spec, a JSON schema, and a validator/shim module. This is the hard gate for all LLM-facing outputs.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.9-llm-graph-interface-improvements/05.9-CONTEXT.md
@docs/PHILOSOPHY.md
</context>

<execution_guardrails>
- Full doc alignment: align objective, must_haves, and tasks against `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, plus phase context or research docs before execution. Any scope change must be reflected in the plan and `.planning/STATE.md`.
- Workspace isolation: run any commands or experiments that mutate state in a fresh jj workspace; do not use the main workspace and do not reuse workspaces for reruns.
- Repeat-run detection markers: include `[ALPHASWARM-START] RUN_ID TIMESTAMP` and `[ALPHASWARM-END] RUN_ID EXIT_CODE DURATION_MS` markers in transcripts or logs so reruns are detectable.
- claude-code-controller subject: for interactive runs, use `claude-code-controller` and launch Claude Code as the subject in the claude-code-agent-teams session; transcripts must show the Claude Code session.
- Seed scope boundaries: treat B-seeds (baseline or regression inputs) and N-seeds (net-new exploratory inputs) as separate scopes; do not mix them in a single run and track results separately.
</execution_guardrails>

<tasks>

<task type="auto">
  <name>Task 1: Publish Graph Interface Contract v2 spec + JSON schema</name>
  <files>docs/reference/graph-interface-v2.md, schemas/graph_interface_v2.json</files>
  <action>
Write the normative contract spec with:
- ABI/semver rules + compatibility shim expectations
- Clause lists + clause matrix rules
- Evidence refs (deterministic) + omissions ledger
- Coverage score formula + debug slice annotation
- Unknowns budget gating and failure semantics
Export a JSON schema that fully encodes the contract.
  </action>
  <verify>
```bash
rg -n "Graph Interface Contract v2|interface_version" docs/reference/graph-interface-v2.md
rg -n "interface_version" schemas/graph_interface_v2.json
```
  </verify>
  <done>
Spec and schema exist and encode all required fields and invariants.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement v2 contract validator + compatibility shim stub</name>
  <files>src/alphaswarm_sol/llm/interface_contract.py</files>
  <action>
Create a validator module that:
- Loads the JSON schema and validates outputs (fail fast)
- Implements v1 -> v2 compatibility shim with explicit defaults
- Enforces ABI semver compatibility on outputs
- Provides helper methods for unknowns budget gating
Do not wire into executors yet (that happens in a later plan).
  </action>
  <verify>
```bash
rg -n "graph interface|compatibility shim|unknowns budget" src/alphaswarm_sol/llm/interface_contract.py
```
  </verify>
  <done>
Contract validator and compatibility shim stub exist and are ready for integration.
  </done>
</task>

</tasks>
