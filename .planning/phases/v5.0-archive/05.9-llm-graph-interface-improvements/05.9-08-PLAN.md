---
phase: 05.9-llm-graph-interface-improvements
plan: 08
type: execute
wave: 4
depends_on:
  - 05.9-01
  - 05.9-02
  - 05.9-03
  - 05.9-04
  - 05.9-05
  - 05.9-06
  - 05.9-07
files_modified:
  - src/alphaswarm_sol/queries/patterns.py
  - src/alphaswarm_sol/queries/results_v2.py
  - src/alphaswarm_sol/llm/interface_contract.py
autonomous: true

must_haves:
  truths:
    - "Pattern outputs fully comply with Graph Interface Contract v2."
    - "Truth-table clause matrix is emitted with evidence/omissions links."
    - "Unknowns budget gating is enforced (insufficient evidence on excess unknowns)."
  artifacts:
    - path: "src/alphaswarm_sol/queries/results_v2.py"
      provides: "v2 result packaging"
      exports: ["v2 pattern results"]
  key_links:
    - from: "src/alphaswarm_sol/queries/patterns.py"
      to: "src/alphaswarm_sol/llm/interface_contract.py"
      via: "contract validation"
---

<objective>
Make all pattern results evidence-first and contract-compliant by default, with truth-table output and unknowns gating.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.9-llm-graph-interface-improvements/05.9-CONTEXT.md
@src/alphaswarm_sol/queries/patterns.py
@src/alphaswarm_sol/llm/interface_contract.py
</context>

<execution_guardrails>
- Full doc alignment: align objective, must_haves, and tasks against `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, plus phase context or research docs before execution. Any scope change must be reflected in the plan and `.planning/STATE.md`.
- Workspace isolation: run any commands or experiments that mutate state in a fresh jj workspace; do not use the main workspace and do not reuse workspaces for reruns.
- Repeat-run detection markers: include `[ALPHASWARM-START] RUN_ID TIMESTAMP` and `[ALPHASWARM-END] RUN_ID EXIT_CODE DURATION_MS` markers in transcripts or logs so reruns are detectable.
- claude-code-controller subject: for interactive runs, use `claude-code-controller` and launch Claude Code as the subject in the claude-code-agent-teams session; transcripts must show the Claude Code session.
- Seed scope boundaries: treat B-seeds (baseline or regression inputs) and N-seeds (net-new exploratory inputs) as separate scopes; do not mix them in a single run and track results separately.
</execution_guardrails>

<tasks>

<task type="auto">
  <name>Task 1: Emit v2-compliant pattern results + clause matrix</name>
  <files>src/alphaswarm_sol/queries/patterns.py, src/alphaswarm_sol/queries/results_v2.py, src/alphaswarm_sol/llm/interface_contract.py</files>
  <action>
Implement v2 result packaging that:
- Emits matched/failed/unknown clause lists
- Adds clause_matrix with evidence/omission links
- Applies unknowns budget gating (excess -> insufficient evidence)
- Uses deterministic evidence IDs and omissions ledger
Integrate validator at the packaging boundary (fail fast).
Do not edit src/alphaswarm_sol/queries/executor.py; if required, stop and request explicit permission.
  </action>
  <verify>
```bash
rg -n "clause_matrix|unknowns budget|interface_version" src/alphaswarm_sol/queries
```
  </verify>
  <done>
Pattern outputs are v2-compliant and validated before emission.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add v2 result type helpers (if missing)</name>
  <files>src/alphaswarm_sol/queries/results_v2.py</files>
  <action>
Define minimal helper structures (dataclass or pydantic) for v2 results and clause matrices so packaging is explicit and schema-aligned. Keep helpers small and serialization-friendly.
  </action>
  <verify>
```bash
rg -n "ResultsV2|ClauseMatrix" src/alphaswarm_sol/queries/results_v2.py
```
  </verify>
  <done>
v2 result helpers exist and align with the contract schema.
  </done>
</task>

</tasks>
