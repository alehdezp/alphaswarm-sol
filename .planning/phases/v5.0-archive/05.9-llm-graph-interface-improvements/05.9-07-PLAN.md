---
phase: 05.9-llm-graph-interface-improvements
plan: 07
type: execute
wave: 3
depends_on:
  - 05.9-03
files_modified:
  - src/alphaswarm_sol/kg/slicer.py
  - src/alphaswarm_sol/llm/slicer.py
  - src/alphaswarm_sol/llm/context_policy.py
  - src/alphaswarm_sol/routing/router.py
  - tests/test_slicing_pipeline.py
autonomous: true

must_haves:
  truths:
    - "All LLM-facing surfaces use the same slicing pipeline."
    - "Debug slice mode bypasses pruning and is annotated in omissions metadata."
  artifacts:
    - path: "tests/test_slicing_pipeline.py"
      provides: "Slicing pipeline equivalence tests"
      exports: ["slicing pipeline tests"]
  key_links:
    - from: "src/alphaswarm_sol/routing/router.py"
      to: "src/alphaswarm_sol/kg/slicer.py"
      via: "single slicing pipeline"
---

<objective>
Unify all slicing paths into one deterministic pipeline so all agents receive consistent, omission-aware context.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.9-llm-graph-interface-improvements/05.9-CONTEXT.md
@src/alphaswarm_sol/kg/slicer.py
@src/alphaswarm_sol/routing/router.py
</context>

<execution_guardrails>
- Full doc alignment: align objective, must_haves, and tasks against `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, plus phase context or research docs before execution. Any scope change must be reflected in the plan and `.planning/STATE.md`.
- Workspace isolation: run any commands or experiments that mutate state in a fresh jj workspace; do not use the main workspace and do not reuse workspaces for reruns.
- Repeat-run detection markers: include `[ALPHASWARM-START] RUN_ID TIMESTAMP` and `[ALPHASWARM-END] RUN_ID EXIT_CODE DURATION_MS` markers in transcripts or logs so reruns are detectable.
- claude-code-controller subject: for interactive runs, use `claude-code-controller` and launch Claude Code as the subject in the claude-code-agent-teams session; transcripts must show the Claude Code session.
- Seed scope boundaries: treat B-seeds (baseline or regression inputs) and N-seeds (net-new exploratory inputs) as separate scopes; do not mix them in a single run and track results separately.
</execution_guardrails>

<tasks>

<task type="auto">
  <name>Task 1: Implement unified slicing pipeline</name>
  <files>src/alphaswarm_sol/kg/slicer.py, src/alphaswarm_sol/llm/slicer.py, src/alphaswarm_sol/routing/router.py</files>
  <action>
Refactor slicing to a single pipeline:
1) PPR seed selection
2) Subgraph extraction
3) GraphSlicer property slicing
4) ContextPolicy filtering
5) Omission ledger injection
Ensure router/LLM/CLI call this pipeline with role-specific budgets.
  </action>
  <verify>
```bash
rg -n "pipeline|omission|GraphSlicer" src/alphaswarm_sol/kg/slicer.py
```
  </verify>
  <done>
All slicing flows route through a single pipeline and include omissions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add debug slice mode + pipeline tests</name>
  <files>src/alphaswarm_sol/llm/context_policy.py, tests/test_slicing_pipeline.py</files>
  <action>
Add debug slice mode to bypass pruning and mark `slice_mode: debug|standard` in omissions metadata.
Add tests that enforce equivalence across router/LLM/CLI for standard slices and validate debug mode behavior.
  </action>
  <verify>
```bash
rg -n "slice_mode|debug" tests/test_slicing_pipeline.py
```
  </verify>
  <done>
Debug slice mode is implemented and slicing equivalence is verified.
  </done>
</task>

</tasks>
