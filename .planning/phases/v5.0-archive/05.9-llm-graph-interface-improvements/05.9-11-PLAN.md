---
phase: 05.9-llm-graph-interface-improvements
plan: 11
type: execute
wave: 7
depends_on:
  - 05.9-10
files_modified:
  - src/alphaswarm_sol/agents/verifier.py
  - src/alphaswarm_sol/orchestration/handlers.py
autonomous: true

must_haves:
  truths:
    - "Orchestration references the new 5.9 skills for contract validation and evidence audit."
  artifacts:
    - path: "src/alphaswarm_sol/orchestration/handlers.py"
      provides: "Skill invocation wiring"
      exports: ["Phase 5.9 orchestration"]
  key_links:
    - from: "src/alphaswarm_sol/orchestration/handlers.py"
      to: "src/alphaswarm_sol/skills/shipped/README.md"
      via: "skill references"
---

<objective>
Wire the new Phase 5.9 skills into orchestration and verifier flows so contract/evidence checks are enforced.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.9-llm-graph-interface-improvements/05.9-CONTEXT.md
@src/alphaswarm_sol/orchestration/handlers.py
@src/alphaswarm_sol/agents/verifier.py
</context>

<execution_guardrails>
- Full doc alignment: align objective, must_haves, and tasks against `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, plus phase context or research docs before execution. Any scope change must be reflected in the plan and `.planning/STATE.md`.
- Workspace isolation: run any commands or experiments that mutate state in a fresh jj workspace; do not use the main workspace and do not reuse workspaces for reruns.
- Repeat-run detection markers: include `[ALPHASWARM-START] RUN_ID TIMESTAMP` and `[ALPHASWARM-END] RUN_ID EXIT_CODE DURATION_MS` markers in transcripts or logs so reruns are detectable.
- claude-code-controller subject: for interactive runs, use `claude-code-controller` and launch Claude Code as the subject in the claude-code-agent-teams session; transcripts must show the Claude Code session.
- Seed scope boundaries: treat B-seeds (baseline or regression inputs) and N-seeds (net-new exploratory inputs) as separate scopes; do not mix them in a single run and track results separately.
</execution_guardrails>

<tasks>

<task type="auto">
  <name>Task 1: Wire new vrs skill references into orchestration</name>
  <files>src/alphaswarm_sol/orchestration/handlers.py</files>
  <action>
Update orchestration flow to reference:
- /vrs-graph-contract-validate
- /vrs-evidence-audit
- /vrs-ordering-proof
- /vrs-taint-extend
- /vrs-taxonomy-migrate
- /vrs-slice-unify
Keep changes minimal and consistent with existing skill invocation patterns.
  </action>
  <verify>
```bash
rg -n "graph-contract-validate|evidence-audit" src/alphaswarm_sol/orchestration/handlers.py
```
  </verify>
  <done>
Orchestration handlers reference the new Phase 5.9 skills.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update verifier references to new evidence/contract skills</name>
  <files>src/alphaswarm_sol/agents/verifier.py</files>
  <action>
Update verifier guidance to align with new evidence contract checks and ordering/taint proofing. Keep edits scoped to skill references and required evidence policy notes.
  </action>
  <verify>
```bash
rg -n "evidence|contract" src/alphaswarm_sol/agents/verifier.py
```
  </verify>
  <done>
Verifier references updated to align with Phase 5.9 skill usage.
  </done>
</task>

</tasks>
