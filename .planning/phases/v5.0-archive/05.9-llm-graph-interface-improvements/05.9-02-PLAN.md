---
phase: 05.9-llm-graph-interface-improvements
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/alphaswarm_sol/kg/taxonomy.py
  - src/alphaswarm_sol/kg/operations.py
  - docs/reference/operations.md
  - tests/test_ops_taxonomy.py
autonomous: true

must_haves:
  truths:
    - "A single ops taxonomy registry maps ops, edge types, tags, and legacy aliases."
    - "Registry includes versioned deprecations + migration rules."
    - "Registry is validated with automated tests, including SARIF alias coverage."
  artifacts:
    - path: "src/alphaswarm_sol/kg/taxonomy.py"
      provides: "Ops taxonomy registry + migration map"
      exports: ["taxonomy registry"]
  key_links:
    - from: "src/alphaswarm_sol/kg/operations.py"
      to: "src/alphaswarm_sol/kg/taxonomy.py"
      via: "registry-backed ops resolution"
---

<objective>
Stabilize semantic operation naming by introducing a canonical taxonomy registry with explicit migration rules, deprecations, and automated tests tied to SARIF and VulnDocs.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.9-llm-graph-interface-improvements/05.9-CONTEXT.md
@docs/reference/operations.md
</context>

<execution_guardrails>
- Full doc alignment: align objective, must_haves, and tasks against `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, plus phase context or research docs before execution. Any scope change must be reflected in the plan and `.planning/STATE.md`.
- Workspace isolation: run any commands or experiments that mutate state in a fresh jj workspace; do not use the main workspace and do not reuse workspaces for reruns.
- Repeat-run detection markers: include `[ALPHASWARM-START] RUN_ID TIMESTAMP` and `[ALPHASWARM-END] RUN_ID EXIT_CODE DURATION_MS` markers in transcripts or logs so reruns are detectable.
- claude-code-controller subject: for interactive runs, use `claude-code-controller` and launch Claude Code as the subject in the claude-code-agent-teams session; transcripts must show the Claude Code session.
- Seed scope boundaries: treat B-seeds (baseline or regression inputs) and N-seeds (net-new exploratory inputs) as separate scopes; do not mix them in a single run and track results separately.
</execution_guardrails>

<tasks>

<task type="auto">
  <name>Task 1: Implement ops taxonomy registry + deprecations</name>
  <files>src/alphaswarm_sol/kg/taxonomy.py, src/alphaswarm_sol/kg/operations.py</files>
  <action>
Create a taxonomy registry that defines:
- Canonical SemanticOperation names
- EdgeType mappings and pattern tags
- Legacy aliases and warnings
- Deprecation metadata with versioning and sunset policy
Update operations resolution to use the registry (no behavioral changes yet).
  </action>
  <verify>
```bash
rg -n "taxonomy|deprecated|alias" src/alphaswarm_sol/kg/taxonomy.py
```
  </verify>
  <done>
Registry exists and operations resolve through the canonical map with deprecation metadata.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add migration validation + SARIF alias tests</name>
  <files>tests/test_ops_taxonomy.py, docs/reference/operations.md</files>
  <action>
Add tests that:
- Fail if deprecated ops appear without migration
- Ensure legacy aliases map deterministically
- Cover SARIF-normalized op names for tool outputs
Update operations reference to document registry + migration behavior.
  </action>
  <verify>
```bash
rg -n "taxonomy|alias|deprecated" tests/test_ops_taxonomy.py
```
  </verify>
  <done>
Taxonomy registry is documented and migration rules are enforced by tests.
  </done>
</task>

</tasks>
