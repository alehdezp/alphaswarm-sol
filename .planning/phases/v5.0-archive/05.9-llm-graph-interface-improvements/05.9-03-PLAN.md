---
phase: 05.9-llm-graph-interface-improvements
plan: 03
type: execute
wave: 2
depends_on:
  - 05.9-01
files_modified:
  - src/alphaswarm_sol/kg/subgraph.py
  - src/alphaswarm_sol/kg/ppr_subgraph.py
  - src/alphaswarm_sol/kg/slicer.py
  - tests/test_subgraph_omissions.py
autonomous: true

must_haves:
  truths:
    - "All subgraph outputs include omission ledger and coverage_score."
    - "Coverage score follows the formal definition in the v2 contract."
    - "Omissions are explicit when pruning occurs."
  artifacts:
    - path: "tests/test_subgraph_omissions.py"
      provides: "Regression tests for omission ledger + coverage"
      exports: ["omission ledger tests"]
  key_links:
    - from: "src/alphaswarm_sol/kg/subgraph.py"
      to: "schemas/graph_interface_v2.json"
      via: "omissions + coverage schema compliance"
---

<objective>
Make subgraph extraction evidence-aware by emitting an omission ledger and deterministic coverage score in all LLM-facing slices.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.9-llm-graph-interface-improvements/05.9-CONTEXT.md
@src/alphaswarm_sol/kg/subgraph.py
@src/alphaswarm_sol/kg/ppr_subgraph.py
</context>

<execution_guardrails>
- Full doc alignment: align objective, must_haves, and tasks against `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, plus phase context or research docs before execution. Any scope change must be reflected in the plan and `.planning/STATE.md`.
- Workspace isolation: run any commands or experiments that mutate state in a fresh jj workspace; do not use the main workspace and do not reuse workspaces for reruns.
- Repeat-run detection markers: include `[ALPHASWARM-START] RUN_ID TIMESTAMP` and `[ALPHASWARM-END] RUN_ID EXIT_CODE DURATION_MS` markers in transcripts or logs so reruns are detectable.
- claude-code-controller subject: for interactive runs, use `claude-code-controller` and launch Claude Code as the subject in the claude-code-agent-teams session; transcripts must show the Claude Code session.
- Seed scope boundaries: treat B-seeds (baseline or regression inputs) and N-seeds (net-new exploratory inputs) as separate scopes; do not mix them in a single run and track results separately.
</execution_guardrails>

<tasks>

<task type="auto">
  <name>Task 1: Add omission ledger + coverage score to subgraph outputs</name>
  <files>src/alphaswarm_sol/kg/subgraph.py, src/alphaswarm_sol/kg/ppr_subgraph.py</files>
  <action>
Implement omissions metadata:
- cut_set, excluded_edges, omitted_nodes
- coverage_score = captured_nodes_weight / relevant_nodes_weight
- relevant_nodes = PPR_selected ∪ query_matched ∪ dependency_closed
Ensure omissions are emitted on all paths (even no-prune cases with empty lists).
  </action>
  <verify>
```bash
rg -n "coverage_score|cut_set|excluded_edges" src/alphaswarm_sol/kg/subgraph.py
```
  </verify>
  <done>
Omission ledger + coverage score are present in subgraph outputs per contract.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add omission ledger tests</name>
  <files>tests/test_subgraph_omissions.py, src/alphaswarm_sol/kg/slicer.py</files>
  <action>
Add tests that:
- Require coverage_score for all subgraphs
- Validate cut_set/excluded_edges when pruning happens
Update slicer to pass omission metadata through unchanged.
  </action>
  <verify>
```bash
rg -n "coverage_score|omissions" tests/test_subgraph_omissions.py
```
  </verify>
  <done>
Subgraph omission metadata is verified and preserved through slicing.
  </done>
</task>

</tasks>
