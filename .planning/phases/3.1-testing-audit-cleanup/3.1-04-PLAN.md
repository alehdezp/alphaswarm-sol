---
phase: 3.1-testing-audit-cleanup
plan: 04
type: execute
wave: 2
depends_on: ["3.1-01", "3.1-02"]
files_modified:
  - .planning/testing/rules/canonical/RULES-ESSENTIAL.md
  - .planning/testing/rules/canonical/VALIDATION-RULES.md
  - .planning/archive/testing-rules/legacy/RULES-ESSENTIAL.md.bak
  - .planning/archive/testing-rules/legacy/VALIDATION-RULES.md.bak
  - .vrs/debug/phase-3.1/gates/3.1-04.json
  - .vrs/debug/phase-3.1/hitl/3.1-04.md
autonomous: true

must_haves:
  truths:
    - "Zero legacy infrastructure references (tmux, jujutsu-workspace) in RULES-ESSENTIAL.md and VALIDATION-RULES.md"
    - "Rule count consolidated from ~25 to ~15 categories"
    - "Every rule category has a stable machine-readable ID (e.g., EXEC-INTEGRITY, TRANSCRIPT-AUTH)"
    - "Every rule category maps to an enforcement mechanism (hook type or controller check)"
    - "Anti-fabrication rules (F1-F4 substance) preserved in merged categories"
    - "Rules reference Agent Teams patterns (TeamCreate, SendMessage, TaskCreate) not legacy execution"
  artifacts:
    - path: ".planning/testing/rules/canonical/RULES-ESSENTIAL.md"
      provides: "Rewritten testing rules targeting Agent Teams + claude-code-controller"
    - path: ".planning/testing/rules/canonical/VALIDATION-RULES.md"
      provides: "Rewritten validation rules with stable IDs for evaluation contracts"
    - path: ".planning/archive/testing-rules/legacy/"
      provides: "Byte-identical archives of pre-rewrite originals"
    - path: ".vrs/debug/phase-3.1/gates/3.1-04.json"
      provides: "Machine gate report"
  key_links:
    - from: ".planning/testing/rules/canonical/RULES-ESSENTIAL.md"
      to: "evaluation contracts in 3.1-05"
      via: "Stable rule IDs (EXEC-INTEGRITY, etc.) referenced by rule_refs[] in contracts"
      pattern: "EXEC-INTEGRITY|TRANSCRIPT-AUTH|METRICS-REALISM"
    - from: ".planning/testing/rules/canonical/VALIDATION-RULES.md"
      to: ".planning/testing/rules/canonical/RULES-ESSENTIAL.md"
      via: "Shared rule ID namespace"
      pattern: "rule.*ID"
---

<objective>
Rewrite RULES-ESSENTIAL.md and VALIDATION-RULES.md to target Agent Teams (Claude Code native) + claude-code-controller (npm v0.6.1). Consolidate ~25 granular rules into ~15 clear categories with stable machine-readable IDs. Map each category to an enforcement mechanism. Preserve all anti-fabrication substance.

Purpose: Rules are the contract that 3.1-05 (evaluation contract foundation) and 3.1c-06 (24 evaluation contracts) reference. If rules still reference legacy infrastructure, evaluation contracts will be built against a dead execution model. Stable IDs enable evaluation contracts to cite `rule_refs: [EXEC-INTEGRITY, GROUND-TRUTH]`.

Output: Rewritten RULES-ESSENTIAL.md and VALIDATION-RULES.md with ~15 categories, stable IDs, Agent Teams patterns. Archived originals.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/3.1-testing-audit-cleanup/context.md
@.planning/testing/rules/canonical/RULES-ESSENTIAL.md
@.planning/testing/rules/canonical/VALIDATION-RULES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Archive originals and rewrite RULES-ESSENTIAL.md</name>
  <files>
    .planning/archive/testing-rules/legacy/RULES-ESSENTIAL.md.bak
    .planning/testing/rules/canonical/RULES-ESSENTIAL.md
  </files>
  <action>
1. **Archive original:**
   - Copy `.planning/testing/rules/canonical/RULES-ESSENTIAL.md` to `.planning/archive/testing-rules/legacy/RULES-ESSENTIAL.md.bak`
   - Verify byte-identical: `diff` between original and archive shows no changes

2. **Rewrite RULES-ESSENTIAL.md** with these structural changes:

   **Header:** Add a rule ID registry table at the top:
   ```markdown
   ## Rule ID Registry
   | ID | Category | Enforcement |
   |---|---|---|
   | EXEC-INTEGRITY | Execution Integrity | Command hooks (PreToolUse) |
   | TRANSCRIPT-AUTH | Transcript Authenticity | Agent hooks (PostToolUse) |
   | METRICS-REALISM | Metrics Realism | Controller event capture |
   | GROUND-TRUTH | Ground Truth Provenance | Code-based graders |
   | REPORT-INTEGRITY | Report Integrity | Prompt hooks (single LLM call) |
   ... (all ~15 categories)
   ```

   **Replace legacy execution rules** with "Agent Teams Execution":
   - 5-step Mandatory Testing Pattern:
     1. `TeamCreate("vrs-test")` -- Create team
     2. `Task(subagent_type="BSKG Attacker")` -- Spawn teammates
     3. `TaskCreate/TaskUpdate` -- Assign and track work
     4. `SendMessage` -- Monitor and coordinate
     5. Verify via controller event capture (message, task:completed, agent:exited)

   **Replace legacy quick commands** with Agent Teams + controller commands

   **Rewrite AUTO-INVOKE triggers** -- add "team", "teammate", "controller"

   **Merge granular rules into categories (per context.md locked decision):**
   - F1+F2+F1b -> `TRANSCRIPT-AUTH` (Transcript Authenticity)
   - C1+C2+C3 -> `METRICS-REALISM` (Metrics Realism)
   - A1+A2+A3 -> `EXEC-INTEGRITY` (Execution Integrity)
   - B1+B2+B3 -> `GROUND-TRUTH` (Ground Truth Provenance)
   - E1+E2+E3 -> `REPORT-INTEGRITY` (Report Integrity)
   - Additional categories as needed for isolation, duration bounds, evidence requirements, etc.

   **Content preservation rules:**
   - EVERY original rule's substance must appear under its merged category
   - Anti-fabrication checks (nonce, hash, duration, evidence) must have corresponding rule sentences
   - Do NOT silently drop any original rule content
   - Word count of each merged category >= 60% of sum of original rule word counts

   **Enforcement mapping for each category:**
   | Rule Category | Enforcement Via |
   |---|---|
   | EXEC-INTEGRITY | Command hooks (PreToolUse) -- block non-compliant tool calls |
   | TRANSCRIPT-AUTH | Agent hooks -- verify Agent Teams DM content, min transcript length |
   | METRICS-REALISM | Controller event capture -- verify total_cost_usd > 0, duration > 5s |
   | GROUND-TRUTH | Code-based graders in controller -- compare against ground-truth.yaml |
   | REPORT-INTEGRITY | Prompt hooks -- single LLM call validates report structure |
  </action>
  <verify>
- `.planning/archive/testing-rules/legacy/RULES-ESSENTIAL.md.bak` is byte-identical to git HEAD version
- `grep -ri "tmux\|jujutsu" .planning/testing/rules/canonical/RULES-ESSENTIAL.md` returns 0 hits
- Rule ID registry table exists at the top with stable IDs
- Rules reference `TeamCreate`, `SendMessage`, `TaskCreate` (not legacy terms)
- Each merged category preserves substance from original constituent rules
  </verify>
  <done>
RULES-ESSENTIAL.md rewritten with Agent Teams patterns, ~15 categories with stable IDs, enforcement mapping. Original archived. Zero legacy references. All anti-fabrication substance preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite VALIDATION-RULES.md and create gate artifacts</name>
  <files>
    .planning/archive/testing-rules/legacy/VALIDATION-RULES.md.bak
    .planning/testing/rules/canonical/VALIDATION-RULES.md
    .vrs/debug/phase-3.1/gates/3.1-04.json
    .vrs/debug/phase-3.1/hitl/3.1-04.md
  </files>
  <action>
1. **Archive original:**
   - Copy `.planning/testing/rules/canonical/VALIDATION-RULES.md` to `.planning/archive/testing-rules/legacy/VALIDATION-RULES.md.bak`

2. **Rewrite VALIDATION-RULES.md:**
   - **Remove** legacy session naming rules
   - **Update D3 (Isolation):** Agent Teams provides isolation by design; controller manages test environments
   - **Keep all anti-fabrication rules (F1-F4):** Adapt transcript source to Agent Teams DMs and controller event log. Preserve nonce verification, hash verification, duration bounds, evidence requirements
   - **Keep all ground truth rules (B1-B3) unchanged**
   - **Keep all metrics rules (C0-C3) unchanged**
   - **Use the same stable rule IDs** from RULES-ESSENTIAL.md (shared namespace)
   - Each rule section must include: rule ID, description, enforcement mechanism, verification command/check
   - All references to legacy infrastructure replaced with Agent Teams equivalents

3. **Cross-reference verification:**
   - Every rule ID in VALIDATION-RULES.md must appear in RULES-ESSENTIAL.md registry
   - Count total unique rule IDs across both files -- should be in range [12, 18]
   - `grep -c "###" .planning/testing/rules/canonical/RULES-ESSENTIAL.md` + same for VALIDATION-RULES gives category count

4. **Merge correctness check:**
   - For each original rule (F1, F2, C1, etc.), verify its substance appears in the merged category
   - Specifically verify: nonce verification (from F1), transcript hash (from F2), duration > 5s (from C1), evidence linking (from B2)

5. **Create machine gate report** at `.vrs/debug/phase-3.1/gates/3.1-04.json`:
   - `plan_id`: "3.1-04"
   - `commands_executed[]`: grep sweeps for legacy terms, rule count checks, merge verification
   - `assertions[]`: zero legacy hits, rule count in [12,18], all original rules accounted for
   - `artifacts[]`: file hashes for both rewritten files + both archives

6. **Create HITL checkpoint** at `.vrs/debug/phase-3.1/hitl/3.1-04.md`:
   - HITL-3.1-04-rules-map scenario
   - Rule-to-enforcement mapping table (every category maps to one mechanism)
   - 3 sample merged rules showing original content preserved
   - Cross-reference: every rule ID appears in both files consistently
  </action>
  <verify>
- `grep -ri "tmux\|jujutsu" .planning/testing/rules/canonical/VALIDATION-RULES.md` returns 0 hits
- Total rule categories across both files in range [12, 18]
- Anti-fabrication rules preserved: `grep -c "nonce\|hash\|duration.*5s\|evidence" .planning/testing/rules/canonical/VALIDATION-RULES.md` >= 3
- Archive exists: `.planning/archive/testing-rules/legacy/VALIDATION-RULES.md.bak`
- `.vrs/debug/phase-3.1/gates/3.1-04.json` exists
- `.vrs/debug/phase-3.1/hitl/3.1-04.md` exists
  </verify>
  <done>
VALIDATION-RULES.md rewritten with Agent Teams patterns and stable rule IDs. Anti-fabrication, ground truth, and metrics rules preserved. Both rules files share consistent rule ID namespace. Originals archived. Machine gate and HITL artifacts created.
  </done>
</task>

</tasks>

<verification>
1. Zero legacy infrastructure references in both rules files
2. ~15 rule categories with stable machine-readable IDs
3. Every category maps to an enforcement mechanism
4. Anti-fabrication substance preserved (nonce, hash, duration, evidence)
5. Rules reference Agent Teams patterns (TeamCreate, SendMessage, TaskCreate)
6. Archives are byte-identical to pre-rewrite originals
7. Machine gate at `.vrs/debug/phase-3.1/gates/3.1-04.json`
8. HITL checkpoint at `.vrs/debug/phase-3.1/hitl/3.1-04.md`
</verification>

<success_criteria>
- Rule IDs are stable and usable by 3.1-05 evaluation contracts as rule_ref fields
- No original rule substance silently dropped (verified by merge correctness check)
- Enforcement mapping is complete (every category has a mechanism)
- 3.1c-06 can create evaluation contracts citing rule_refs: [EXEC-INTEGRITY, GROUND-TRUTH, etc.]
</success_criteria>

<output>
After completion, create `.planning/phases/3.1-testing-audit-cleanup/3.1-04-SUMMARY.md`
</output>
