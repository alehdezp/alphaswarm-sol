---
phase: 3.1-testing-audit-cleanup
plan: 03
type: execute
wave: 2
depends_on: ["3.1-01", "3.1-02"]
files_modified:
  - src/alphaswarm_sol/shipping/MANIFEST.yaml
  - .vrs/debug/phase-3.1/evaluator-audit.md
  - .vrs/debug/phase-3.1/harness-audit.md
  - tests/workflow_harness/contracts/samples/skill-audit.json
  - tests/workflow_harness/contracts/samples/agent-attacker.json
  - tests/workflow_harness/contracts/samples/orchestrator-debate.json
  - .claude/settings.json
  - .vrs/debug/phase-3.1/gates/3.1-03.json
  - .vrs/debug/phase-3.1/hitl/3.1-03.md
autonomous: true

must_haves:
  truths:
    - "MANIFEST.yaml skill count matches filesystem count (ls shipping/skills/*.md)"
    - "MANIFEST.yaml agent count matches filesystem count (ls shipping/agents/*.md)"
    - "WorkflowEvaluator and TrajectoryEvaluator APIs are documented with input/output contracts"
    - "workflow_harness readiness assessed with at least one gap or caveat identified"
    - "settings.json has no orphaned hook config from prior phases"
    - "Sample capability contracts have >= 3 capability checks each"
  artifacts:
    - path: "src/alphaswarm_sol/shipping/MANIFEST.yaml"
      provides: "Authoritative inventory of 30 shipped skills and 21 shipped agents"
    - path: ".vrs/debug/phase-3.1/evaluator-audit.md"
      provides: "API documentation for WorkflowEvaluator and TrajectoryEvaluator"
    - path: ".vrs/debug/phase-3.1/harness-audit.md"
      provides: "Readiness assessment of tests/workflow_harness/ for 3.1b"
    - path: "tests/workflow_harness/contracts/samples/"
      provides: "2-3 sample capability contracts showing schema for 3.1c-09"
    - path: ".vrs/debug/phase-3.1/gates/3.1-03.json"
      provides: "Machine gate report"
  key_links:
    - from: "src/alphaswarm_sol/shipping/MANIFEST.yaml"
      to: "src/alphaswarm_sol/shipping/skills/"
      via: "Manifest skill count verified against filesystem"
      pattern: "ls.*shipping/skills"
    - from: ".vrs/debug/phase-3.1/evaluator-audit.md"
      to: "src/alphaswarm_sol/testing/workflow/workflow_evaluator.py"
      via: "Audit documents public API methods and types"
      pattern: "workflow_evaluator"
---

<objective>
Create the authoritative shipping manifest (30 skills, 21 agents), audit existing evaluator infrastructure for 3.1c readiness, assess workflow_harness for 3.1b, clean settings.json, and create sample capability contracts.

Purpose: 3.1c-09 (skill tests) and 3.1c-10 (agent tests) need the full inventory. Without a manifest, every downstream plan re-derives it by scanning the filesystem. The evaluator audit ensures 3.1c doesn't discover API gaps mid-execution. Sample contracts give 3.1c-06 a concrete starting point.

Output: MANIFEST.yaml, evaluator audit, harness audit, sample contracts, clean settings.json.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/3.1-testing-audit-cleanup/context.md
@.planning/phases/3.1-testing-audit-cleanup/3.1-01-SUMMARY.md
@src/alphaswarm_sol/testing/workflow/workflow_evaluator.py
@src/alphaswarm_sol/testing/trajectory/evaluator.py
@tests/workflow_harness/lib/assertions.py
@.claude/settings.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shipping manifest and audit evaluators</name>
  <files>
    src/alphaswarm_sol/shipping/MANIFEST.yaml
    .vrs/debug/phase-3.1/evaluator-audit.md
    .vrs/debug/phase-3.1/harness-audit.md
  </files>
  <action>
1. **Create `src/alphaswarm_sol/shipping/MANIFEST.yaml`:**
   - Scan `src/alphaswarm_sol/shipping/skills/` and `src/alphaswarm_sol/shipping/agents/` directories
   - For each skill .md file, extract: `id` (filename without .md), `name` (from skill title), `path`, `category` (from directory or content), `tier` (core/support/experimental based on content analysis)
   - For each agent .md file, extract: `id`, `name`, `path`, `category`, `model_tier` (opus/sonnet/haiku based on content)
   - Structure:
     ```yaml
     manifest:
       version: "3.1"
       generated_at: "2026-02-11"
       skills:
         - id: vrs-audit
           name: "Full Audit Execution"
           path: "src/alphaswarm_sol/shipping/skills/vrs-audit.md"
           category: audit
           tier: core
       agents:
         - id: vrs-attacker
           name: "Attacker Agent"
           path: "src/alphaswarm_sol/shipping/agents/vrs-attacker.md"
           category: investigation
           model_tier: opus
     ```
   - **CRITICAL: Verify counts match filesystem:**
     ```bash
     ls src/alphaswarm_sol/shipping/skills/*.md | wc -l  # Must match skill count in manifest
     ls src/alphaswarm_sol/shipping/agents/*.md | wc -l  # Must match agent count in manifest
     ```
   - If counts differ from expected 30/21, use actual filesystem count (not hardcoded)

2. **Audit WorkflowEvaluator (553 LOC):**
   - Read `src/alphaswarm_sol/testing/workflow/workflow_evaluator.py`
   - Document: all public methods with signatures, input types, output types, extension points
   - Note gaps: does it support evaluation-contract-aware mode? Can it accept contract JSON? What adaptations needed for 3.1c?
   - Write to `.vrs/debug/phase-3.1/evaluator-audit.md`

3. **Audit TrajectoryEvaluator (240 LOC):**
   - Read `src/alphaswarm_sol/testing/trajectory/evaluator.py`
   - Document: 8 quality dimensions, scoring mechanism, extensibility
   - Note where LLM-graded reasoning could replace keyword heuristics in 3.1c
   - Append to `.vrs/debug/phase-3.1/evaluator-audit.md`

4. **Audit `tests/workflow_harness/` (12 files):**
   - Read each file, document purpose and status
   - Assess: assertions library completeness, transcript parser robustness, controller events coverage, workspace manager reliability
   - Identify at least one gap or caveat (a 12-file harness is unlikely perfect)
   - Write to `.vrs/debug/phase-3.1/harness-audit.md` with per-file assessment and actionable gaps assigned to specific 3.1b plans
  </action>
  <verify>
- `src/alphaswarm_sol/shipping/MANIFEST.yaml` exists and parses as valid YAML
- Skill count in manifest matches `ls src/alphaswarm_sol/shipping/skills/*.md | wc -l`
- Agent count in manifest matches `ls src/alphaswarm_sol/shipping/agents/*.md | wc -l`
- `.vrs/debug/phase-3.1/evaluator-audit.md` exists and documents public API methods for both evaluators
- `.vrs/debug/phase-3.1/harness-audit.md` exists with per-file assessment and at least one gap
  </verify>
  <done>
MANIFEST.yaml created with verified counts matching filesystem. Evaluator audit documents API contracts for WorkflowEvaluator and TrajectoryEvaluator with identified gaps. Harness audit provides per-file assessment with actionable gaps for 3.1b.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create sample contracts, clean settings.json, and create gate artifacts</name>
  <files>
    tests/workflow_harness/contracts/samples/skill-audit.json
    tests/workflow_harness/contracts/samples/agent-attacker.json
    tests/workflow_harness/contracts/samples/orchestrator-debate.json
    .claude/settings.json
    .vrs/debug/phase-3.1/gates/3.1-03.json
    .vrs/debug/phase-3.1/hitl/3.1-03.md
  </files>
  <action>
1. **Create sample capability contracts** at `tests/workflow_harness/contracts/samples/`:
   - `skill-audit.json`: A capability contract for the `/vrs-audit` skill showing what 3.1c-09 will create for all 30 skills. Must include:
     - `workflow_id`: "skill-vrs-audit"
     - `category`: "skill"
     - `capability_checks[]`: At least 3 concrete checks (e.g., "builds knowledge graph before analysis", "produces findings with evidence", "calls build-kg CLI command")
     - `expected_behaviors[]`: Observable outcomes
   - `agent-attacker.json`: A capability contract for the `vrs-attacker` agent. At least 3 capability checks (e.g., "identifies attack vectors from graph", "constructs exploit path", "cites graph node IDs in findings")
   - `orchestrator-debate.json`: A capability contract for the debate orchestration flow. At least 3 capability checks (e.g., "spawns attacker and defender agents", "collects verdicts from both sides", "verifier arbitrates disagreements")

2. **Clean `.claude/settings.json`:**
   - Read current settings.json
   - Identify any hook configurations from prior phases that reference deleted infrastructure (tmux, legacy runners, etc.)
   - Remove orphaned entries while preserving active hooks
   - Ensure valid JSON after modification
   - Verify: `python -c "import json; json.load(open('.claude/settings.json'))"`

3. **Create machine gate report** at `.vrs/debug/phase-3.1/gates/3.1-03.json`:
   - Manifest filesystem verification commands and results
   - Evaluator audit completeness check (public methods documented)
   - Harness audit completeness check (per-file assessment exists, gaps identified)
   - Sample contract validation (JSON parseable, >= 3 capability_checks each)
   - settings.json validation

4. **Create HITL checkpoint** at `.vrs/debug/phase-3.1/hitl/3.1-03.md`:
   - HITL-3.1-03-manifest-audit scenario
   - Verify MANIFEST.yaml skill count matches `ls shipping/skills/*.md`
   - Review evaluator audit for completeness (API signatures documented)
   - Inspect one sample capability contract (>= 3 capability checks)
  </action>
  <verify>
- `python -c "import json; [json.load(open(f'tests/workflow_harness/contracts/samples/{f}')) for f in ['skill-audit.json', 'agent-attacker.json', 'orchestrator-debate.json']]"` succeeds
- Each sample contract has `workflow_id`, `category`, and `capability_checks` with >= 3 entries
- `python -c "import json; json.load(open('.claude/settings.json'))"` succeeds
- `.vrs/debug/phase-3.1/gates/3.1-03.json` exists
- `.vrs/debug/phase-3.1/hitl/3.1-03.md` exists
  </verify>
  <done>
3 sample capability contracts created with concrete checks. settings.json cleaned of orphaned hooks. Machine gate and HITL artifacts created.
  </done>
</task>

</tasks>

<verification>
1. MANIFEST.yaml counts match filesystem reality
2. Evaluator audit documents public API methods, input/output types, extension points, and gaps
3. Harness audit provides per-file assessment with actionable gaps
4. 3 sample contracts are valid JSON with >= 3 capability checks each
5. settings.json is valid JSON with no orphaned hooks
6. Machine gate at `.vrs/debug/phase-3.1/gates/3.1-03.json`
7. HITL checkpoint at `.vrs/debug/phase-3.1/hitl/3.1-03.md`
</verification>

<success_criteria>
- MANIFEST.yaml exists with correct counts verified against filesystem
- Evaluator and harness audits are substantive (not cursory)
- Sample contracts demonstrate the pattern 3.1c-06 will scale
- settings.json is clean for 3.1b-03 hook registration
- All downstream phases (3.1b, 3.1c, 3.2) have the artifacts they need
</success_criteria>

<output>
After completion, create `.planning/phases/3.1-testing-audit-cleanup/3.1-03-SUMMARY.md`
</output>
