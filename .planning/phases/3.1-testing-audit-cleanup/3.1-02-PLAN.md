---
phase: 3.1-testing-audit-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/alphaswarm_sol/testing/workflow/__init__.py
  - configs/skill_tool_policies.yaml
  - src/alphaswarm_sol/skills/registry.yaml
  - .planning/archive/testing-rules/legacy/
  - .vrs/debug/phase-3.1/gates/3.1-02.json
  - .vrs/debug/phase-3.1/hitl/3.1-02.md
autonomous: true

must_haves:
  truths:
    - "Zero legacy infrastructure references in production code (src/alphaswarm_sol/)"
    - "Skills registry loads without referencing deleted skills"
    - "Config files contain no legacy enforcement fields"
    - "workflow/__init__.py exports only existing symbols"
    - "Removed configs archived for reference"
  artifacts:
    - path: ".planning/archive/testing-rules/legacy/"
      provides: "Archived original configs before cleanup"
    - path: ".vrs/debug/phase-3.1/gates/3.1-02.json"
      provides: "Machine gate report with grep sweep results"
    - path: ".vrs/debug/phase-3.1/hitl/3.1-02.md"
      provides: "HITL checkpoint for legacy sweep verification"
  key_links:
    - from: "configs/skill_tool_policies.yaml"
      to: "src/alphaswarm_sol/skills/registry.yaml"
      via: "Policy references must align with registry entries"
      pattern: "skill_tool_policies.*registry"
    - from: "src/alphaswarm_sol/testing/workflow/__init__.py"
      to: "src/alphaswarm_sol/testing/workflow/*.py"
      via: "Exports only modules that still exist"
      pattern: "from.*workflow.*import"
---

<objective>
Delete remaining legacy testing infrastructure files and references from configs, registries, and skill/agent markdown files. Clean up workflow/__init__.py exports. Archive removed configs.

Purpose: Exploration confirmed most legacy infrastructure is already gone (zero tmux in production code, clean registry/policies). This plan handles remaining stragglers — config references, registry entries for deleted skills, and associated skill/agent .md files. Must be complete before 3.1-03 creates the shipping manifest.

Output: Zero legacy references in production code. Clean configs and registries. Archived originals.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/3.1-testing-audit-cleanup/context.md
@configs/skill_tool_policies.yaml
@src/alphaswarm_sol/skills/registry.yaml
@src/alphaswarm_sol/testing/workflow/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep for legacy references and archive originals</name>
  <files>
    .planning/archive/testing-rules/legacy/
  </files>
  <action>
1. **Run comprehensive legacy sweep** across production code:
   ```bash
   grep -ri "tmux" src/alphaswarm_sol/ --include="*.py" --include="*.yaml" --include="*.yml"
   grep -ri "tmux" configs/ --include="*.yaml" --include="*.yml"
   grep -ri "jujutsu\|jj-workspace" src/alphaswarm_sol/ --include="*.py"
   ```
   Record hit counts for each pattern. Exploration said these should be zero in production, but verify.

2. **Check for skill/agent .md files referencing deleted infrastructure:**
   - Scan `.claude/skills/` and `.claude/agents/` for references to deleted runners (MasterOrchestrator, AgenticRunner, etc.), deleted subdirectories (flexible/, corpus/, etc.), or legacy execution models
   - Delete any skill .md or agent .md files that solely exist for deleted infrastructure
   - For files that reference deleted infrastructure as part of a broader purpose, update them to remove dead references

3. **Archive original configs before modification:**
   - Create `.planning/archive/testing-rules/legacy/` directory
   - Copy current `configs/skill_tool_policies.yaml` to archive
   - Copy current `src/alphaswarm_sol/skills/registry.yaml` to archive
   - Copy any deleted skill/agent .md files to archive

4. **Record the full sweep output** for the HITL checkpoint in a structured format showing: pattern searched, files hit, line counts.
  </action>
  <verify>
- `.planning/archive/testing-rules/legacy/` exists with archived originals
- Sweep output recorded with per-pattern hit counts
- Legacy skill/agent .md files identified (if any)
  </verify>
  <done>
Legacy sweep complete with documented results. Original configs archived. Dead skill/agent .md files identified for deletion.
  </done>
</task>

<task type="auto">
  <name>Task 2: Clean configs, registry, workflow exports, and create gate artifacts</name>
  <files>
    src/alphaswarm_sol/testing/workflow/__init__.py
    configs/skill_tool_policies.yaml
    src/alphaswarm_sol/skills/registry.yaml
    .vrs/debug/phase-3.1/gates/3.1-02.json
    .vrs/debug/phase-3.1/hitl/3.1-02.md
  </files>
  <action>
1. **Update `configs/skill_tool_policies.yaml`:**
   - Remove any enforcement fields referencing legacy infrastructure (tmux, jujutsu workspace, deleted runners)
   - Remove any policy entries for skills that no longer exist
   - Validate YAML still parses: `python -c "import yaml; yaml.safe_load(open('configs/skill_tool_policies.yaml'))"`

2. **Update `src/alphaswarm_sol/skills/registry.yaml`:**
   - Remove entries for any skills deleted in this plan or 3.1-01
   - Validate YAML parses and all referenced skill files exist on disk
   - Verify: `python -c "import yaml; data = yaml.safe_load(open('src/alphaswarm_sol/skills/registry.yaml')); print(f'{len(data.get(\"skills\", []))} skills registered')"`

3. **Update `src/alphaswarm_sol/testing/workflow/__init__.py`:**
   - Remove exports of any modules deleted in 3.1-01 (e.g., self_improving_runner)
   - Ensure only existing modules are exported
   - Verify: `python -c "import alphaswarm_sol.testing.workflow"` succeeds

4. **Delete identified dead skill/agent .md files** (from Task 1)

5. **Final verification sweep:**
   ```bash
   grep -ri "tmux" src/alphaswarm_sol/ --include="*.py" --include="*.yaml"
   grep -ri "tmux" configs/ --include="*.yaml"
   ```
   Must return zero hits (excluding archive/ and .planning/).

6. **Run full test suite:** `pytest tests/ -x --timeout=120`

7. **Create machine gate report** at `.vrs/debug/phase-3.1/gates/3.1-02.json`:
   - `plan_id`: "3.1-02"
   - `commands_executed[]`: all grep sweeps, yaml validations, import checks, pytest
   - `assertions[]`: zero legacy hits, yaml parses, imports work, tests pass
   - `status`: pass/fail
   - `started_at`, `ended_at`, `duration_ms`
   - `artifacts[]`: file hashes for modified configs

8. **Create HITL checkpoint** at `.vrs/debug/phase-3.1/hitl/3.1-02.md`:
   - HITL-3.1-02-legacy-sweep scenario
   - Grep sweep output summary
   - Registry diff (before/after)
   - 3 spot-checked removed legacy references with justification
  </action>
  <verify>
- `grep -ri "tmux" src/alphaswarm_sol/ --include="*.py" --include="*.yaml"` returns 0 hits
- `grep -ri "tmux" configs/ --include="*.yaml"` returns 0 hits
- `python -c "import yaml; yaml.safe_load(open('configs/skill_tool_policies.yaml'))"` succeeds
- `python -c "import yaml; yaml.safe_load(open('src/alphaswarm_sol/skills/registry.yaml'))"` succeeds
- `python -c "import alphaswarm_sol.testing.workflow"` succeeds
- `pytest tests/ -x --timeout=120` passes
- `.vrs/debug/phase-3.1/gates/3.1-02.json` exists
- `.vrs/debug/phase-3.1/hitl/3.1-02.md` exists
  </verify>
  <done>
Zero legacy infrastructure references in production code. Configs and registries cleaned and validated. workflow/__init__.py exports only existing modules. Tests pass. Machine gate and HITL artifacts created.
  </done>
</task>

</tasks>

<verification>
1. `grep -ri "tmux\|jujutsu\|jj-workspace" src/alphaswarm_sol/ --include="*.py" --include="*.yaml"` returns 0 hits
2. All YAML config files parse without error
3. `python -c "import alphaswarm_sol.testing.workflow"` succeeds
4. `pytest tests/ -x` passes
5. Archive directory `.planning/archive/testing-rules/legacy/` contains originals
6. Machine gate at `.vrs/debug/phase-3.1/gates/3.1-02.json`
7. HITL checkpoint at `.vrs/debug/phase-3.1/hitl/3.1-02.md`
</verification>

<success_criteria>
- Zero legacy infrastructure references in production code (src/alphaswarm_sol/, configs/)
- Skills registry loads cleanly with no references to deleted entries
- Config files validated and contain no legacy enforcement fields
- Removed configs archived at .planning/archive/testing-rules/legacy/
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/3.1-testing-audit-cleanup/3.1-02-SUMMARY.md`
</output>
