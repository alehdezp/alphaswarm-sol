---
phase: 3.1-testing-audit-cleanup
plan: 06
type: execute
wave: 4
depends_on: ["3.1-01", "3.1-02"]
files_modified:
  - tests/test_component_runner.py
  - tests/test_flexible_integration.py
  - tests/test_parallel_runner.py
  - tests/test_self_improving_runner.py
  - tests/test_session_state.py
  - tests/e2e/test_workflow_infrastructure.py
  - .vrs/debug/phase-3.1/gates/3.1-06.json
  - .vrs/debug/phase-3.1/hitl/3.1-06.md
autonomous: true

must_haves:
  truths:
    - "Only test files whose sole imports reference modules deleted in 3.1-01/02 are removed"
    - "pytest tests/ passes with zero errors after test file deletion"
    - "Tests for living modules (tiers, detection, generator, quality, WorkflowEvaluator, TrajectoryEvaluator) are preserved"
    - "pytest --collect-only collects all remaining tests without import errors"
    - "Test count delta matches number of deleted test functions (no silent drops)"
  artifacts:
    - path: ".vrs/debug/phase-3.1/gates/3.1-06.json"
      provides: "Machine gate report with before/after test counts and deleted file justifications"
    - path: ".vrs/debug/phase-3.1/hitl/3.1-06.md"
      provides: "HITL checkpoint mapping deleted test files to removed modules"
  key_links:
    - from: "deleted test files"
      to: ".vrs/debug/phase-3.1/import-graph.json"
      via: "Each deleted test's imports reference only TRULY_DEAD modules"
      pattern: "TRULY_DEAD"
    - from: "preserved test files"
      to: "src/alphaswarm_sol/testing/"
      via: "Remaining tests import only modules that still exist"
      pattern: "from alphaswarm_sol\\.testing"
---

<objective>
Delete test files that test modules removed in 3.1-01 and 3.1-02. Preserve tests for all living modules. Verify no cascade failures and no false test drops.

Purpose: After removing ~15K LOC of dead code and legacy infrastructure, test files that import those deleted modules become import-error sources and dead weight. Only tests whose import targets no longer exist should be deleted.

Output: ~2K LOC of dead test files removed. Full test suite passes. Test count drops accurately by number of deleted test functions.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/3.1-testing-audit-cleanup/context.md
@.planning/phases/3.1-testing-audit-cleanup/3.1-01-SUMMARY.md
@.planning/phases/3.1-testing-audit-cleanup/3.1-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Identify dead test files and capture baseline counts</name>
  <files>
    .vrs/debug/phase-3.1/test-file-analysis.json
  </files>
  <action>
1. **Capture baseline test count:**
   ```bash
   pytest tests/ --collect-only -q 2>&1 | tail -1  # Record total test count
   ```

2. **Identify test files that import ONLY deleted modules.** For each test file in `tests/`:
   - Parse its imports (using ast or grep)
   - Check if ALL imported `alphaswarm_sol.testing.*` modules were deleted in 3.1-01/02
   - Categorize as:
     - `DEAD_TEST`: All testing imports reference deleted modules -> safe to delete
     - `MIXED_TEST`: Some imports reference deleted modules, some live -> needs editing (remove dead imports, keep live tests)
     - `LIVE_TEST`: All imports reference living modules -> keep as-is

   **Known candidates for deletion** (from context.md, verify with import analysis):
   - `tests/test_component_runner.py` -> imports from deleted `flexible/component_runner.py`
   - `tests/test_flexible_integration.py` -> imports from deleted `flexible/`
   - `tests/test_parallel_runner.py` -> imports from deleted `flexible/parallel_runner.py`
   - `tests/test_self_improving_runner.py` -> imports from deleted `workflow/self_improving_runner.py`
   - `tests/test_session_state.py` -> imports from deleted `flexible/session_state.py`
   - `tests/e2e/test_workflow_infrastructure.py` -> check if imports reference deleted infrastructure

   **Must verify:**
   - `tests/test_skills_package.py` -> likely LIVE (tests skill loading)
   - `tests/test_workflow_evaluator*.py` -> MUST be preserved (tests kept runner)
   - Any test files referencing `tiers`, `detection`, `generator`, `quality` -> MUST be preserved

3. **Check for shared conftest.py dependencies:**
   - Read `tests/conftest.py` and any directory-level conftest files
   - Verify no shared fixture depends on deleted modules
   - If a conftest imports deleted modules, it needs editing (not deletion)

4. **Save analysis** to `.vrs/debug/phase-3.1/test-file-analysis.json`:
   ```json
   {
     "baseline_test_count": 11363,
     "files_to_delete": ["tests/test_component_runner.py", ...],
     "files_to_edit": [],
     "files_to_keep": ["tests/test_skills_package.py", ...],
     "justification": {
       "tests/test_component_runner.py": "All imports from deleted flexible/component_runner.py"
     }
   }
   ```
  </action>
  <verify>
- Baseline test count captured
- Every file in `files_to_delete` has ALL testing imports referencing deleted modules
- No file importing living modules appears in `files_to_delete`
- conftest.py files checked for shared fixture dependencies
- `.vrs/debug/phase-3.1/test-file-analysis.json` exists
  </verify>
  <done>
Test file analysis complete. Dead test files identified with per-file justification. Baseline test count captured. No living-module tests targeted for deletion. Shared conftest dependencies verified.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete dead test files and verify test suite integrity</name>
  <files>
    tests/test_component_runner.py
    tests/test_flexible_integration.py
    tests/test_parallel_runner.py
    tests/test_self_improving_runner.py
    tests/test_session_state.py
    tests/e2e/test_workflow_infrastructure.py
    .vrs/debug/phase-3.1/gates/3.1-06.json
    .vrs/debug/phase-3.1/hitl/3.1-06.md
  </files>
  <action>
1. **Delete identified dead test files** (from Task 1 analysis):
   - Delete each file in `files_to_delete` list
   - For any `files_to_edit` (MIXED_TEST): remove dead imports and dead test functions, preserve live ones

2. **Edit conftest files if needed:**
   - If any conftest.py imports deleted modules, remove those imports and associated fixtures
   - Preserve all fixtures used by living tests

3. **Verify no import errors in remaining tests:**
   ```bash
   pytest tests/ --collect-only -q 2>&1 | grep -i "error\|import"
   ```
   Must show zero import errors.

4. **Run full test suite:**
   ```bash
   pytest tests/ -n auto --dist loadfile --timeout=120
   ```
   Must pass with zero new failures.

5. **Verify test count delta:**
   ```bash
   pytest tests/ --collect-only -q 2>&1 | tail -1  # New count
   ```
   - Delta = baseline count (from Task 1) - new count
   - Delta must equal the number of test functions in deleted files (no silent drops of unrelated tests)

6. **Verify living-module tests preserved:**
   ```bash
   pytest tests/ --collect-only -q 2>&1 | grep -c "test_skills"  # Skills tests exist
   # Verify test files for tiers, detection, quality still exist
   ls tests/test_skills_package.py  # Must exist
   ```

7. **Create machine gate report** at `.vrs/debug/phase-3.1/gates/3.1-06.json`:
   - `plan_id`: "3.1-06"
   - `baseline_test_count`, `final_test_count`, `delta`
   - `deleted_files[]` with per-file justification
   - `commands_executed[]`: collect-only, pytest run, verification checks
   - `assertions[]`: zero import errors, tests pass, delta matches

8. **Create HITL checkpoint** at `.vrs/debug/phase-3.1/hitl/3.1-06.md`:
   - HITL-3.1-06-test-cleanup scenario
   - List deleted test files mapped to their removed modules
   - Confirm no living-module tests were deleted
   - Before/after test count comparison
  </action>
  <verify>
- `pytest tests/ --collect-only -q 2>&1 | grep -i "error"` shows zero import errors
- `pytest tests/ -n auto --dist loadfile --timeout=120` passes
- Test count delta matches deleted test function count
- `ls tests/test_skills_package.py` exists (living test preserved)
- `.vrs/debug/phase-3.1/gates/3.1-06.json` exists with baseline/final counts
- `.vrs/debug/phase-3.1/hitl/3.1-06.md` exists
  </verify>
  <done>
Dead test files deleted. Full test suite passes with zero import errors. Test count delta accurately reflects deleted test functions. All living-module tests preserved. Machine gate and HITL artifacts created.
  </done>
</task>

</tasks>

<verification>
1. Only test files importing exclusively deleted modules were removed
2. `pytest tests/ --collect-only` collects all remaining tests without import errors
3. `pytest tests/ -n auto --dist loadfile` passes
4. Test count delta = number of test functions in deleted files
5. Tests for tiers, detection, generator, quality, WorkflowEvaluator, TrajectoryEvaluator exist
6. Machine gate at `.vrs/debug/phase-3.1/gates/3.1-06.json`
7. HITL checkpoint at `.vrs/debug/phase-3.1/hitl/3.1-06.md`
</verification>

<success_criteria>
- Zero import errors in test collection
- Full test suite passes
- Deletion is surgical (per-file justified), not bulk
- Living-module test coverage unchanged
- Before/after counts documented in gate report
</success_criteria>

<output>
After completion, create `.planning/phases/3.1-testing-audit-cleanup/3.1-06-SUMMARY.md`
</output>
