---
phase: 3.1b-workflow-testing-harness
plan: 04
subsystem: testing
tags: [agent-teams, jujutsu, workspace-isolation, team-lifecycle, message-capture]

# Dependency graph
requires:
  - phase: 3.1b-02
    provides: TeamObservation, AgentObservation, InboxMessage dataclasses
  - phase: 3.1b-03
    provides: Hook infrastructure (install_hooks, _ensure_hook)
provides:
  - TeamManager with full Agent Teams lifecycle management
  - Full message content capture for 3.1c debrief
  - Jujutsu workspace isolation (create, forget, rollback, list, snapshot)
  - Sandbox .claude/ support (copy/restore production skills and agents)
affects: [3.1b-05, 3.1b-07, 3.1c-05, 3.1c-11, 3.1c-12]

# Tech tracking
tech-stack:
  added: []
  patterns: [context-manager-cleanup, idempotent-operations, subprocess-with-timeout]

key-files:
  created:
    - tests/workflow_harness/lib/team_manager.py
    - tests/workflow_harness/test_team_manager.py
  modified:
    - tests/workflow_harness/lib/workspace.py
    - tests/workflow_harness/test_workspace.py

key-decisions:
  - "TeamManager wraps Agent Teams concepts but delegates real subprocess calls to 3.1b-07 smoke test"
  - "Message capture stores FULL content (not metadata stubs) for 3.1c debrief analysis"
  - "forget_workspace is idempotent (no error on already-forgotten workspace)"
  - "list_workspaces returns empty list (not error) when jj missing or not in jj repo"

patterns-established:
  - "Context manager cleanup: __exit__ always calls shutdown_all + delete_team, even on exceptions"
  - "Idempotent operations: forget_workspace and delete_team safe to call multiple times"
  - "jj availability check via shutil.which before any Jujutsu command"

# Metrics
duration: 5min
completed: 2026-02-12
---

# Phase 3.1b Plan 04: Agent Teams Interactive Framework + Jujutsu Workspace Isolation Summary

**TeamManager with full team lifecycle, message capture, and sandbox support plus Jujutsu workspace isolation for repeatable test runs**

## Performance

- **Duration:** 5 min
- **Started:** 2026-02-12T13:55:43Z
- **Completed:** 2026-02-12T14:00:43Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- TeamManager class with complete Agent Teams lifecycle: create, spawn, message, broadcast, shutdown, delete
- Full message content capture (sender, recipient, content, timestamp, message_type) for 3.1c debrief
- TeamObservation assembly from captured messages with per-agent InboxMessage correlation
- Sandbox .claude/ support for copying production skills/agents into test workspace
- Context manager ensures no orphan teams even on exceptions
- WorkspaceManager extended with 5 Jujutsu methods: create_workspace, forget_workspace, rollback, list_workspaces, snapshot_operation
- 49 new tests (36 TeamManager + 13 Jujutsu), all passing with mocked subprocess
- 193 total harness tests passing (up from 144 at 3.1b-02 exit)

## Task Commits

Each task was committed atomically:

1. **Task 1: Create TeamManager with Agent Teams lifecycle and message capture** - `f5885834` (feat)
2. **Task 2: Extend WorkspaceManager with Jujutsu workspace isolation** - `d902d523` (feat)

## Files Created/Modified
- `tests/workflow_harness/lib/team_manager.py` - TeamManager class with lifecycle, capture, sandbox, context manager (~260 LOC)
- `tests/workflow_harness/test_team_manager.py` - 36 unit tests covering all TeamManager features
- `tests/workflow_harness/lib/workspace.py` - Extended with 5 Jujutsu methods + _require_jj/_run_jj helpers
- `tests/workflow_harness/test_workspace.py` - 13 new Jujutsu tests (29 total, was 16)

## Decisions Made
1. TeamManager wraps Agent Teams concepts as a data model and capture infrastructure. Real `claude` subprocess integration is deferred to 3.1b-07 (smoke test). This keeps the plan focused and testable without requiring real Claude Code.
2. Message capture stores FULL content text (not metadata stubs). This is critical for 3.1c-05 debrief and 3.1c-11 orchestrator evaluation.
3. forget_workspace is idempotent -- silently catches errors if workspace doesn't exist. This follows the API contract in hooks-and-workspace.md (the plan said to raise RuntimeError, but the EXPECTED doc and real-world usage demand idempotency).
4. list_workspaces returns empty list (not error) when jj is not available or not in a jj repo. Graceful fallback per API contract.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Made forget_workspace idempotent (contradicts plan, matches EXPECTED)**
- **Found during:** Task 2
- **Issue:** Plan spec said "Raises RuntimeError if workspace doesn't exist" but EXPECTED doc criteria #4 says "forget_workspace is idempotent" and the API contract says caller should catch. Idempotency is clearly the correct behavior.
- **Fix:** Wrapped subprocess error in try/except, silently ignoring already-forgotten workspace errors.
- **Files modified:** tests/workflow_harness/lib/workspace.py
- **Verification:** test_forget_workspace_is_idempotent passes
- **Committed in:** d902d523

---

**Total deviations:** 1 auto-fixed (1 bug fix aligning with EXPECTED doc over plan spec)
**Impact on plan:** Minor correction. Better API usability for downstream consumers (3.1c-12).

## Issues Encountered
None

## Next Phase Readiness
- TeamManager ready for 3.1b-05 (scenario DSL `modes: [team]` support)
- TeamManager ready for 3.1b-07 (smoke test with real Claude subprocess)
- Jujutsu workspace methods ready for 3.1c-12 (improvement loop sandbox)
- 193 harness tests passing -- regression-safe baseline for further 3.1b work

---
*Phase: 3.1b-workflow-testing-harness*
*Completed: 2026-02-12*
