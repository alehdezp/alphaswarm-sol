---
phase: 3.1.1-pattern-loading-architecture
plan: 03
type: execute
wave: 3
depends_on: ["3.1.1-02"]
files_modified:
  - tests/test_pattern_loading_regression.py
autonomous: true

must_haves:
  truths:
    - "Nonexistent directory raises PatternDirectoryNotFoundError (never returns [])"
    - "vulndocs/ directory exists and contains >= 400 patterns"
    - "Default get_patterns() matches explicit get_patterns(Path('vulndocs'))"
    - "Exception hierarchy works (PatternLoadError catches all subtypes)"
    - "PatternStore rejects non-Path arguments"
    - "No test or source file references Path('patterns')"
  artifacts:
    - path: "tests/test_pattern_loading_regression.py"
      provides: "10+ regression tests preventing silent pattern loading failures"
      contains: "class TestLoudFailure"
  key_links:
    - from: "tests/test_pattern_loading_regression.py"
      to: "src/alphaswarm_sol/queries/patterns.py"
      via: "imports get_patterns, PatternStore"
      pattern: "from alphaswarm_sol.queries.patterns import get_patterns"
    - from: "tests/test_pattern_loading_regression.py"
      to: "src/alphaswarm_sol/queries/errors.py"
      via: "imports exception classes for pytest.raises"
      pattern: "from alphaswarm_sol.queries.errors import"
    - from: "tests/test_pattern_loading_regression.py"
      to: "tests/pattern_loader.py"
      via: "imports _MIN_EXPECTED_PATTERNS constant"
      pattern: "from tests.pattern_loader import _MIN_EXPECTED_PATTERNS"
---

<objective>
Create regression tests that prevent this class of bug forever. Tests verify loud failure on bad paths, the exception hierarchy, the canonical loader, and that no legacy Path("patterns") references remain.

Purpose: These tests act as a permanent safety net. If anyone reintroduces silent pattern loading or references the old path, these tests will catch it immediately.

Output: `tests/test_pattern_loading_regression.py` with 10+ tests across 3 test classes.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/3.1.1-pattern-loading-architecture/context.md
@.planning/phases/3.1.1-pattern-loading-architecture/3.1.1-01-SUMMARY.md
@.planning/phases/3.1.1-pattern-loading-architecture/3.1.1-02-SUMMARY.md
@src/alphaswarm_sol/queries/errors.py
@tests/pattern_loader.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create regression test suite for pattern loading architecture</name>
  <files>tests/test_pattern_loading_regression.py</files>
  <action>
Create NEW file `tests/test_pattern_loading_regression.py` with exactly the tests specified in context.md:

**Class TestPatternDiscovery** (3 tests):
1. `test_vulndocs_directory_exists` — asserts `Path("vulndocs").exists()`
2. `test_get_patterns_returns_non_empty` — asserts `len(get_patterns()) >= _MIN_EXPECTED_PATTERNS`
3. `test_get_patterns_default_matches_explicit` — asserts `len(get_patterns()) == len(get_patterns(Path("vulndocs")))`

**Class TestLoudFailure** (5 tests):
1. `test_get_patterns_raises_on_missing_dir` — `pytest.raises(PatternDirectoryNotFoundError)`, asserts `exc_info.value.path == Path("nonexistent_dir")`
2. `test_load_vulndocs_patterns_raises_on_missing_dir` — `pytest.raises(PatternDirectoryNotFoundError)`
3. `test_pattern_store_load_raises_on_missing_dir` — `pytest.raises(PatternDirectoryNotFoundError)` (ignore the DeprecationWarning)
4. `test_exception_hierarchy` — `pytest.raises(PatternLoadError)` catches `PatternDirectoryNotFoundError`
5. `test_pattern_store_rejects_non_path` — `pytest.raises(TypeError)` on `PatternStore("not_a_path")`

**Class TestNoLegacyPaths** (2 tests):
1. `test_no_legacy_patterns_path_in_tests` — scans all `tests/test_*.py` files for `Path("patterns")` or `Path('patterns')`, asserts zero violations
2. `test_no_legacy_patterns_path_in_source` — scans all `src/**/*.py` files for `Path("patterns")` or `Path('patterns')`, asserts zero violations

Use pytest style (not unittest.TestCase). Import from:
- `alphaswarm_sol.queries.errors` — PatternLoadError, PatternDirectoryNotFoundError, EmptyPatternStoreError
- `alphaswarm_sol.queries.patterns` — PatternStore, get_patterns
- `tests.pattern_loader` — _MIN_EXPECTED_PATTERNS

Include module docstring explaining this prevents the silent-failure bug.
  </action>
  <verify>
```bash
uv run pytest tests/test_pattern_loading_regression.py -v
```
  </verify>
  <done>
- tests/test_pattern_loading_regression.py exists with 10 tests across 3 classes
- All 10 tests pass
- TestLoudFailure confirms exceptions fire on bad paths
- TestNoLegacyPaths confirms zero Path("patterns") in codebase
  </done>
</task>

</tasks>

<verification>
```bash
uv run pytest tests/test_pattern_loading_regression.py -v --tb=short
```
</verification>

<success_criteria>
- 10+ regression tests exist and all pass
- Tests cover: directory existence, pattern count, loud failure, exception hierarchy, TypeError, no legacy paths
- No test uses mocks — all test real behavior
</success_criteria>

<output>
After completion, create `.planning/phases/3.1.1-pattern-loading-architecture/3.1.1-03-SUMMARY.md`
</output>
