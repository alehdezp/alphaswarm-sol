---
phase: 3.1.1-pattern-loading-architecture
plan: 01
subsystem: testing
tags: [patterns, exceptions, deprecation, vulndocs, loader]

# Dependency graph
requires:
  - phase: 3.1
    provides: "Sed fix replacing Path('patterns') with Path('vulndocs') across 82 occurrences"
provides:
  - "PatternLoadError exception hierarchy (3 classes in errors.py)"
  - "get_patterns() canonical loader function"
  - "PatternStore hardened with __init__ validation and @deprecated load()"
  - "All 7 source files migrated to get_patterns()"
affects: [3.1.1-02, 3.1.1-03, 3.1.1-04, 3.1b, 3.1c, 3.2]

# Tech tracking
tech-stack:
  added: []
  patterns: [custom-exception-hierarchy, canonical-loader, deprecated-decorator, fail-loud]

key-files:
  created:
    - "src/alphaswarm_sol/queries/errors.py"
  modified:
    - "src/alphaswarm_sol/queries/patterns.py"
    - "src/alphaswarm_sol/queries/__init__.py"
    - "src/alphaswarm_sol/queries/executor.py"
    - "src/alphaswarm_sol/queries/report.py"
    - "src/alphaswarm_sol/queries/schema_snapshot.py"
    - "src/alphaswarm_sol/queries/schema_hints.py"
    - "src/alphaswarm_sol/queries/intent.py"
    - "src/alphaswarm_sol/vql2/executor.py"
    - "src/alphaswarm_sol/vql2/guidance.py"

key-decisions:
  - "executor.py modification APPROVED: 2-line semantic change for single source of truth"
  - "PatternEngine._load_patterns() remains lenient (returns [] with WARNING) since patterns are optional there"
  - "testing/mutations.py kept as-is: already handles errors with try/except"

patterns-established:
  - "Custom exception hierarchy: PatternLoadError -> PatternDirectoryNotFoundError / EmptyPatternStoreError"
  - "Canonical loader pattern: get_patterns() as THE single entry point for all pattern loading"
  - "Deprecated decorator: _deprecated(reason) for marking old APIs with migration instructions"

# Metrics
duration: 4min
completed: 2026-02-11
---

# Phase 3.1.1 Plan 01: Exception Hierarchy + Canonical Loader + Source Migration Summary

**Custom exception hierarchy with get_patterns() canonical loader, PatternStore hardening, and all 7 source files migrated to single entry point**

## Performance

- **Duration:** 4 min
- **Started:** 2026-02-11T23:20:53Z
- **Completed:** 2026-02-11T23:25:13Z
- **Tasks:** 3 (1 auto + 1 checkpoint pre-approved + 1 auto)
- **Files modified:** 10 (1 created, 9 modified)

## Accomplishments
- Created 3-class exception hierarchy (PatternLoadError, PatternDirectoryNotFoundError, EmptyPatternStoreError) with .path attribute and from-e chaining
- Established get_patterns() as single canonical entry point -- loads 466 patterns, raises loud on bad paths
- Hardened PatternStore: __init__ validates Path type, load() has @deprecated decorator, load_vulndocs_patterns() raises on nonexistent dir
- Migrated all 7 source files from inline PatternStore calls to get_patterns()
- Exported get_patterns + exception hierarchy from queries/__init__.py

## Task Commits

Each task was committed atomically:

1. **Task 1: Create exception hierarchy and canonical get_patterns() loader** - `10d1cd7a` (feat)
2. **Task 2: Approve executor.py modification** - Pre-approved, no commit needed
3. **Task 3: Migrate all source files to get_patterns()** - `75e15703` (refactor)

## Files Created/Modified
- `src/alphaswarm_sol/queries/errors.py` - NEW: 3-class exception hierarchy (PatternLoadError base, PatternDirectoryNotFoundError, EmptyPatternStoreError)
- `src/alphaswarm_sol/queries/patterns.py` - Added get_patterns(), _deprecated decorator, PatternStore __init__ validation, load() deprecation, load_vulndocs_patterns() raises on missing, PatternEngine._load_patterns() lenient with logging
- `src/alphaswarm_sol/queries/__init__.py` - Added exports for get_patterns + exception hierarchy
- `src/alphaswarm_sol/queries/executor.py` - Replaced inline vulndocs detection with get_patterns() (APPROVED)
- `src/alphaswarm_sol/queries/report.py` - Replaced PatternStore.load_vulndocs_patterns with get_patterns
- `src/alphaswarm_sol/queries/schema_snapshot.py` - Replaced PatternStore.load_vulndocs_patterns with get_patterns
- `src/alphaswarm_sol/queries/schema_hints.py` - Replaced PatternStore.load_vulndocs_patterns with get_patterns
- `src/alphaswarm_sol/queries/intent.py` - Replaced PatternStore.load_vulndocs_patterns with get_patterns
- `src/alphaswarm_sol/vql2/executor.py` - Replaced 3 PatternStore.load_vulndocs_patterns calls with get_patterns
- `src/alphaswarm_sol/vql2/guidance.py` - Replaced 2 PatternStore.load_vulndocs_patterns calls with get_patterns

## Decisions Made
1. **executor.py modification APPROVED:** The change is a 2-line semantic change replacing inline PatternStore calls with get_patterns(). Required for single source of truth. Pre-approved in execution context.
2. **PatternEngine._load_patterns() stays lenient:** Returns [] with WARNING on missing dirs. This is the internal engine wrapper where patterns are optional. Unlike get_patterns() which is strict.
3. **testing/mutations.py kept as-is:** Already has try/except error handling around its PatternStore call. Not worth changing.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- `test_schema_snapshot.py` fails because it passes `pattern_dir=Path("patterns")` -- the old broken path from the sed miss. This is a pre-existing bug documented in the plan, to be fixed in Plan 02 (shared test loader).
- `test_authority_lens.py` fails on a pre-existing pattern quality gap (proposeAddition function not flagged by auth-005). Not related to our changes.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Plan 02 (shared test loader + fix all test files) can now proceed
- get_patterns() is the single canonical API for all pattern loading
- Exception hierarchy is in place for callers to handle loading errors
- Zero legacy Path("patterns") references remain in source code

## Verification Evidence

All verification checks passed:
- Exception hierarchy importable: OK
- get_patterns() loads 466 patterns: OK
- get_patterns() re-exported from queries/__init__: OK
- get_patterns(Path("nonexistent")) raises PatternDirectoryNotFoundError: OK
- PatternStore("string") raises TypeError: OK
- PatternStore.load() emits DeprecationWarning: OK
- No legacy Path("patterns") in src/: OK
- test_patterns.py: 4 passed
- test_schema_snapshot.py: 1 failed (pre-existing Path("patterns") bug, Plan 02 scope)
- test_authority_lens.py: 1 failed (pre-existing pattern quality gap)

## Self-Check: PASSED

- errors.py: FOUND
- patterns.py: FOUND
- SUMMARY.md: FOUND
- Commit 10d1cd7a: FOUND
- Commit 75e15703: FOUND

---
*Phase: 3.1.1-pattern-loading-architecture*
*Completed: 2026-02-11*
