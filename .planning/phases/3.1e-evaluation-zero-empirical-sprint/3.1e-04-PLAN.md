---
phase: 3.1e-evaluation-zero-empirical-sprint
plan: "04"
type: execute
wave: 2
depends_on: ["3.1e-01", "3.1e-02", "3.1e-03"]
files_modified:
  - .vrs/experiments/plan-05/field-provenance.md
  - .vrs/experiments/plan-05/field-provenance.json
  - .vrs/experiments/plan-05/evaluation_observations.md
  - .vrs/experiments/plan-05/sprint-block.md
  - .vrs/experiments/plan-05/smoke-test-results.json
  - .vrs/experiments/plan-05/phase-entry.json
autonomous: false
requirements:
  - "3.1e-binding-1"
  - "3.1e-binding-4"
  - "3.1e-binding-5"
  - "3.1e-exit-gate"

must_haves:
  truths:
    - "Every schema field traces to an observed data artifact (no speculative fields)"
    - "Unobserved Phenomena list has falsifiable questions, not topic labels"
    - "Three contradiction pairs explicitly checked and resolved or escalated"
    - "3.1c conditional entry protocol determined (data_rich | partial_data | blocked_recovery)"
    - "Layer-qualified naming applied to shared heuristic/LLM dimensions"
  artifacts:
    - path: ".vrs/experiments/plan-05/field-provenance.md"
      provides: "Schema field -> experiment artifact mapping with verdict_conditions"
    - path: ".vrs/experiments/plan-05/field-provenance.json"
      provides: "Machine-readable companion to field-provenance.md (same data, JSON format)"
    - path: ".vrs/experiments/plan-05/smoke-test-results.json"
      provides: "Integration smoke test pass/fail keyed to field-provenance fields"
    - path: ".vrs/experiments/plan-05/phase-entry.json"
      provides: "3.1c entry protocol: {mode, available_tracks, deferred_items}"
  key_links:
    - from: ".vrs/experiments/plan-05/field-provenance.md"
      to: ".vrs/experiments/plan-01/cost-summary.json"
      via: "Track A: detection schema fields from cost measurement"
    - from: ".vrs/experiments/plan-05/field-provenance.md"
      to: ".vrs/experiments/plan-02/llm-recommendation.md"
      via: "Track B: evaluation schema fields from LLM assessment"
    - from: ".vrs/experiments/plan-05/field-provenance.md"
      to: ".vrs/experiments/plan-03-04/descriptive_clusters.json"
      via: "Track C: taxonomy schema fields from failure classification"
---

<objective>
Plan 05: Extract Schemas From Reality — write minimal Pydantic models that capture phenomena observed in Plans 01-03/04, not theoretical ones.

Purpose: The original proposal had 600 LOC of Pydantic models before any real evaluation run (v5.0 anti-pattern). This plan extracts only what the data demands. Every schema field must trace to an observed data artifact. Unobserved phenomena are documented with falsifiable questions and routed to downstream phases.

Output: field-provenance.md mapping every schema field to its source experiment, field-provenance.json (machine-readable companion), contradiction analysis for 3 predictable pairs, 3.1c conditional entry protocol, Unobserved Phenomena list with routing, smoke-test-results.json, phase-entry.json.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/3.1e-evaluation-zero-empirical-sprint/3.1e-CONTEXT.md
@.planning/phases/3.1e-evaluation-zero-empirical-sprint/3.1e-01-SUMMARY.md
@.planning/phases/3.1e-evaluation-zero-empirical-sprint/3.1e-02-SUMMARY.md
@.planning/phases/3.1e-evaluation-zero-empirical-sprint/3.1e-03-SUMMARY.md
@.vrs/experiments/plan-01/cost-summary.json
@.vrs/experiments/plan-02/llm-recommendation.md
@.vrs/experiments/plan-03-04/descriptive_clusters.json
@.vrs/experiments/plan-03-04/action_map.json
@.vrs/experiments/plan-03-04/validity-matrix.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema extraction from observed data + contradiction analysis (target 0.5 sessions)</name>
  <files>
    .vrs/experiments/plan-05/field-provenance.md
    .vrs/experiments/plan-05/field-provenance.json
    .vrs/experiments/plan-05/smoke-test-results.json
  </files>
  <action>
    **Pre-check: context file existence**
    Before the integration contract check, verify which SUMMARY.md files exist: `3.1e-01-SUMMARY.md`, `3.1e-02-SUMMARY.md`, `3.1e-03-SUMMARY.md`. For any missing file, substitute from the corresponding PLAN.md's `<objective>` section as degraded context and note as `context_degradation: true` in field-provenance.md. Also verify CORPUS has not drifted: re-check `_corpus_version.sha256` from `graph-fingerprints.json` against current `tests/evaluation/test_detection_baseline.py` SHA256. If drift detected: document as `corpus_drift_detected: true` and annotate all is_tp-derived fields as `validity_status: corpus_drift_risk`.

    **Upstream Integration Contract check:**
    Before consuming any artifact, verify minimum format requirements:

    - **Track A** (always available): Plan 01 baseline-before.json and baseline-after.json exist with `{contract, pattern_id, function_name, node_id, is_tp, fired_conditions}`. If missing: Track A degraded, continue with Track B.

      **Track A tiered artifact manifest:**
      - *Load-bearing* (Track A requires ALL): `baseline-before.json`, `baseline-after.json`, `comparison-script.py`, `cost-summary.json`. If any load-bearing artifact is missing, Track A state is `unavailable`.
      - *Supplementary* (Track A degrades gracefully without): per-stage cost breakdown, retry logs. Missing supplementary artifacts reduce provenance depth but do not block synthesis.

      Track A states: `available` = all load-bearing artifacts present and supplementary complete. `partial` = all load-bearing artifacts present but one or more supplementary artifacts missing — downstream synthesis proceeds with degraded provenance (cost breakdowns may lack per-stage granularity, retry context unavailable). `unavailable` = one or more load-bearing artifacts missing — continue with Track B.

    - **Track B** (graduated activation): Read `plan-02-verdict-final.json` (written by Plan 02 Task 3 checkpoint) — gate on `final_verdict`, NOT on `llm-recommendation.md` directly (the human may have overridden the automated verdict). States: `active` = `final_verdict` contains "Go" and `verdict_tier == "confirmed"`. `constrained` = "Go (preliminary)" or "Ambiguous" — proceed but mark all Track B fields `confidence_qualifier: constrained` and produce `evaluation_observations_constrained.md`. `absent` = "No-go" — write `evaluation_observations.md` as fallback. `pending_human` = `llm-recommendation.md` exists but `plan-02-verdict-final.json` does not — emit a human-action-required prompt ("Plan 02 LLM recommendation exists but awaits human verdict. Please run Plan 02 Task 3 checkpoint or provide verdict.") and PAUSE. If no response after 5 minutes, fall back to `constrained` mode with `constraint_reason: pending_human_timeout`.

    - **Track C** (optional): Plan 03/04 Phase 0+1 outputs. Minimum: at least one taxonomy category + 3 gaming archetypes. If < 2 categories AND scope_reduced=true in archetype-count.json: Track C degradation is pre-authorized — skip Unobserved Phenomena routing.

    **Compound state table:**
    | Track A | Track B | Result |
    |---------|---------|--------|
    | available | active | full synthesis |
    | available | constrained | synthesis with Track B confidence qualifiers |
    | available | absent | Track A-only synthesis + evaluation_observations.md |
    | available | pending_human | PAUSE for human verdict; fallback to constrained after 5 min |
    | partial | active | synthesis with degraded Track A provenance |
    | partial | available | constrained synthesis with degraded provenance |
    | unavailable | active | Track B-only synthesis |
    | unavailable | absent | blocked_recovery → sprint-block.md |

    **Schema extraction — one field at a time:**

    For each observed data artifact, extract minimal schema fields:

    **Track A fields (from Plans 01 + 03/04 Phase 0):**
    - Per-function firing record schema (contract, pattern_id, function_name, node_id, is_tp, fired_conditions)
    - Cost fields (compute_seconds, cost_per_FP_removed, LOC_per_FP_removed) — add `cost_unit: compute_seconds` for LLM-free plans
    - Taxonomy category fields (cluster_name, scope_tag, action_category, pattern_ids)

    **Track B fields (from Plan 02, graduated):**
    - Per-dimension matrix: dimension name (layer-qualified: `heuristic_reasoning_depth`, `llm_reasoning_depth`), validity status, LLM_adds_signal flag
    - Rubric reference (SHA256, version)
    - Unification exception: if validity-matrix.json case (a) confirmed for a dimension (rank-correlation high), unified field MAY be used with `construct_note` annotation

    **Track C fields (from Plan 03/04 Phases 1-2):**
    - Archetype schema: mechanism_cluster, model_tier, heuristic_score, LLM_score, gamed_heuristic, gamed_LLM, LLM_identifies_mechanism
    - Validity coefficient per dimension

    **Schema naming constraint:** Layer-qualified names for shared dimensions (`heuristic_reasoning_depth`, `llm_reasoning_depth`). Unification requires explicit validity evidence.

    **Integration smoke test baseline:** Use `baseline-before.json` (pre-fix, which is what Phase 0 taxonomy clusters were built from). Do NOT use `baseline-after.json` — the post-fix data removes FPs that defined some clusters, making cluster membership inconsistent.

    **Contradiction analysis (3 pairs, gated on artifact existence):**

    Before analyzing each pair, verify the required artifacts exist. If artifacts are absent for a pair, record `status: skipped, reason: upstream_missing` in contradictions-summary.json and proceed to the next pair. If artifacts exist but contradict without any resolution path from available data, record `status: unresolvable, escalate: human` with data from both sides.

    **Pair 1:** Plan 02 LLM_adds_signal vs Plan 03/04 validity coefficient. Required artifacts: Plan 01 execution log AND Plan 02 `plan-02-verdict-final.json`. If either missing: skip. Otherwise: affects 3.1c LLM evaluation investment. Document: Finding A (source, artifact, quote), Finding B, resolvable from data?, if irresolvable: "Arbitration required by 3.1c: [specific decision question]."

    **Pair 2:** Plan 01 YAML-fix-sufficient vs Plan 03/04 Phase 0 builder-fix-required for same FP cluster. Required artifacts: Plan 03/04 output (`action_map.json`) AND Plan 01 output (`baseline-after.json`). If either missing: skip. Pre-condition: assert `addressed_count` is an integer for ALL action_map.json clusters — if any cluster still has `addressed_count = 'pending-plan-01'`, run the update step (read baseline-after.json delta, patch addressed_count per cluster, note `data_state: updated_at_plan05` in each entry). Only then proceed. Use `pattern_ids` from action_map.json as join key. Affects fix-strategy recommendation.

    **Pair 3:** Plan 02 rubric-calibration-succeeded vs Plan 03/04 Phase 2 rubric-uncalibrated annotation. Required artifacts: Plan 01 output AND Plan 03/04 output. If either missing: skip. Affects rubric quality confidence.

    For each non-skipped contradiction: (a) Finding A, (b) Finding B, (c) resolvable from data?, (d) if irresolvable: arbitration question for 3.1c with data on both sides.

    **Integration smoke test:**
    After field-provenance.md is written, derive the smoke test pass criterion from its contents. Read the field list from field-provenance.md and, for each field, assert:
    - Source plan identified (source_experiment column is non-empty)
    - Confidence level present (validity_status column is non-empty)
    - At least one value example present (observed_data_example column is non-empty)

    Systematic coverage of all Plan 01 output fields. One contract from each Phase 0 taxonomy cluster. Document:
    - (a) Whether raw detection JSON is interpretable by LLM as evidence, or
    - (b) What transformation is needed
    If non-trivial transformation: route to Unobserved Phenomena as 'integration gap: architectural, target: 3.1c' with minimum 2-step transformation sketch. Add `detection_evaluation_bridge` row to field-provenance.md with `bridge_complexity: {steps, estimated_loc}`.

    Write `smoke-test-results.json`: `{ "fields_covered": [...], "fields_missing": [...], "pass": bool, "field_provenance_source": "field-provenance.json" }` — pass is true if and only if fields_missing is empty. Verify: smoke-test-results.json exists AND `pass: true` before emitting phase-entry signal to 3.1c.

    **Output: field-provenance.md**
    Columns: schema_name, field_name, source_experiment, source_artifact, observed_data_example, verdict_conditions, layer_qualified (yes/no), cost_unit (compute_seconds | tokens | mixed), validity_status (measured | constrained | corpus_drift_risk | pilot_required), pilot_required (yes/no), data_state (observed | updated_at_plan05 | pending | incomplete).
    - `verdict_conditions` format: `{verdict_source: plan-02|plan-03-04, N: int, threshold: str}` — specifying which plan's verdict this field reflects
    - The output of this plan is a MARKDOWN TABLE documenting field provenance, NOT Pydantic class implementations (those come after 3.1c confirms which fields are stable)
    - Pending-phase-N annotations count as fulfilling criterion for fields whose source artifacts don't yet exist
    - Fields with ambiguous structure annotated `data_state: incomplete`

    **Output: field-provenance.json**
    Machine-readable companion containing the same data as field-provenance.md in JSON format: `{ "fields": [ { "schema_name": str, "field_name": str, "source_experiment": str, "source_artifact": str, "observed_data_example": any, "verdict_conditions": obj, "layer_qualified": bool, "cost_unit": str, "validity_status": str, "pilot_required": bool, "data_state": str } ] }`. This enables downstream tools (smoke test, phase-entry) to consume field provenance programmatically.
  </action>
  <verify>
    <automated>python -c "import pathlib, json; f=pathlib.Path('.vrs/experiments/plan-05/field-provenance.md'); assert f.exists(); content=f.read_text(); assert 'source_experiment' in content; assert 'verdict_conditions' in content or 'observed_data_example' in content; fj=pathlib.Path('.vrs/experiments/plan-05/field-provenance.json'); assert fj.exists(), 'field-provenance.json must exist'; data=json.loads(fj.read_text()); assert 'fields' in data; st=pathlib.Path('.vrs/experiments/plan-05/smoke-test-results.json'); assert st.exists(), 'smoke-test-results.json must exist'; sr=json.loads(st.read_text()); assert sr.get('pass') is True, 'smoke test must pass before emitting phase-entry signal'; print('field-provenance.md/.json and smoke-test-results.json verified')"</automated>
    <manual>Verify no schema field exists without an observed data justification. Verify 3 contradiction pairs explicitly checked (or skipped with upstream_missing reason). Verify layer-qualified naming for shared dimensions.</manual>
  </verify>
  <done>field-provenance.md and field-provenance.json map every schema field to source experiment artifact. Three contradiction pairs analyzed (resolved, escalated, or skipped with reason). Layer-qualified naming applied. Integration smoke test completed with smoke-test-results.json pass: true.</done>
</task>

<task type="auto">
  <name>Task 2: Unobserved Phenomena list + 3.1c entry protocol</name>
  <files>
    .vrs/experiments/plan-05/field-provenance.md
    .vrs/experiments/plan-05/evaluation_observations.md
    .vrs/experiments/plan-05/phase-entry.json
  </files>
  <action>
    **Step 0 — DECISION-LOCK gate:**
    Before any schema extraction, verify `DECISION-LOCK.md` exists at `.planning/phases/3.1e-evaluation-zero-empirical-sprint/DECISION-LOCK.md` (created by Plan 00) and was created before sprint start. If absent: the sprint ran without pre-committed decision rules — document as `decision_lock_missing: true` in field-provenance.md and proceed with extra caution on any field that required a judgment call.

    **Unobserved Phenomena list:**
    For each gap discovered across Plans 01-03/04, create an entry with:
    - (a) Phenomenon
    - (b) Why it matters
    - (c) Measurement difficulty: trivial | moderate | requires-infrastructure | impossible
    - (d) Target phase: 3.1c | 3.1f | Phase 5
    - (e) Concrete experimental question — falsifiable "does X cause Y?" or "can we measure Z?" NOT a topic label
    - (f) Evidence anchor — at least one artifact (filename + finding) that revealed the gap

    **Explicit REJECT criterion:** If field (c) is "impossible" AND field (e) cannot be stated as a falsifiable question, REJECT the phenomenon with a one-line reason. Rejected items are logged but not routed.

    **REJECT example:** "REJECT: 'Agent creativity' — measurement difficulty: impossible, no falsifiable question, no evidence anchor. Rejected because no target phase can resolve and no experimental question can be formulated."

    **REJECT pattern:** "cross-contract interaction: matters because patterns interact: moderate: 3.1c" — no evidence cited, no testable question.
    **ACCEPT pattern:** "Plan 03/04 Phase 0 action_map.json: cluster 'modifier-based-access-control' has no YAML-fixable action — reveals gap in has_access_gate recognition."

    For phenomena where experiment ran but produced INCONCLUSIVE: evidence anchor = 'INCONCLUSIVE: {experiment}, {artifact}, {why no signal}.'

    **Routing:**
    - 3.1c items → 3.1c experiment design input
    - 3.1f items → loop design input
    - Phase 5 → appended to `<deferred>` section
    If > 5 items tagged 3.1c: "Scope overflow" — route the FULL list to 3.1c (do not pre-select top 3 here). Tag each item with difficulty but defer priority assignment to 3.1c itself, which has better context to judge. All 3.1c items must have difficulty assigned against this rubric: `trivial` = measurable with existing artifacts; `moderate` = requires new experiment design; `requires-infrastructure` = requires new code or corpus.
    3.1c anchor: "Must review Plan 05 Unobserved Phenomena as required input."

    **Construct disambiguation entries:**
    For any dimension where construct_alignment_note shows heuristic and LLM measure different constructs: add targeted 3.1c Unobserved Phenomena item: 'construct disambiguation: schema field {dimension} must declare which construct it captures per evaluation layer.' Tag: `measurement_difficulty: trivial, target: 3.1c`.

    **3.1c conditional entry protocol:**
    Determine `phase_entry_mode` and write to `phase-entry.json`: `{ "mode": "<data_rich|partial_data|blocked_recovery>", "available_tracks": ["A"|"A_partial"|"B"|"B_constrained"|"C"], "deferred_items": [...] }`:
    1. **data_rich** — all plans complete → 3.1c receives full data package
    2. **partial_data** — some plans complete → 3.1c receives partial data + sprint-block.md
    3. **blocked_recovery** — Track A + B both fail → 3.1c receives sprint-block.md only

    **Compound failure handling (Track A + Track B both unavailable):**
    Write `.vrs/experiments/plan-05/sprint-block.md` documenting which plans failed, what data exists, what 3.1c must address.

    **If Track B unavailable or constrained:**
    Write `.vrs/experiments/plan-05/evaluation_observations.md` (unavailable) or `.vrs/experiments/plan-05/evaluation_observations_constrained.md` (constrained) with minimum content schema:
    ```
    ## Dimensions Attempted
    <list of dimension names>
    ## Per-Dimension Failure Classification
    <dimension>: no_signal | below_threshold | ambiguous | infrastructure_error
    ## Required Change in 3.1c
    <one sentence per dimension specifying what must change>
    ## Verdict Tier
    <synthetic_preliminary | confirmed | n/a>
    ```
    This schema is required — unstructured prose is not acceptable for 3.1c consumption.

    **If Track A failed AND Track B constrained:** set `phase_entry_mode: partial_data` with `constraint_flag: track_a_failed` in field-provenance.md. This is the `partial_recovery` compound state mapped to the closest entry mode.

    **Pre-registration success criteria:**
    - (a) No schema field exists without observed data justification
    - (b) Field-provenance annotation exists for every schema field
    - (c) Schemas annotated 'incomplete' for ambiguous phenomena
    - (d) Each contradiction pair explicitly checked and resolved or documented
    - Failure: schemas require fields no experiment produced data for
  </action>
  <verify>
    <automated>python -c "import pathlib, json; f=pathlib.Path('.vrs/experiments/plan-05/field-provenance.md'); assert f.exists(); content=f.read_text(); assert 'phase_entry_mode' in content, 'field-provenance.md must contain phase_entry_mode'; assert 'source_experiment' in content, 'field-provenance.md must contain source_experiment'; pe=pathlib.Path('.vrs/experiments/plan-05/phase-entry.json'); assert pe.exists(), 'phase-entry.json must exist'; ped=json.loads(pe.read_text()); assert 'mode' in ped and 'available_tracks' in ped, 'phase-entry.json must contain mode and available_tracks'; print('Plan 05 field-provenance.md and phase-entry.json verified')"</automated>
    <manual>Verify Unobserved Phenomena have falsifiable questions (not topic labels). Verify each entry has evidence anchor. Verify rejected phenomena have one-line reason. Verify 3.1c entry protocol determined.</manual>
  </verify>
  <done>Unobserved Phenomena list complete with falsifiable questions and evidence anchors (rejected items logged with reason). 3.1c conditional entry protocol determined and written to phase-entry.json. Routing completed (3.1c / 3.1f / Phase 5). If Track B unavailable, evaluation_observations.md documents what was attempted.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human review of extracted schemas + 3.1c entry decision</name>
  <files>.vrs/experiments/plan-05/field-provenance.md</files>
  <action>
    Present schema extraction and 3.1c entry protocol for human review. This is the final gate.
    What was built: Schema extraction from all 3.1e experimental data. Field-provenance mapping (md + json). Unobserved Phenomena list with routing. 3.1c conditional entry protocol. Smoke test results. Phase entry protocol.
    How to verify:
    1. Review field-provenance.md — does every field trace to a real artifact? Any speculative fields?
    2. Review field-provenance.json — does it match the markdown version?
    3. Review Unobserved Phenomena — are questions falsifiable? Are evidence anchors specific? Are rejected items justified?
    4. Review contradiction analysis — are the 3 pairs resolved, properly escalated, or skipped with upstream_missing reason?
    5. Check `phase_entry_mode` in phase-entry.json — does it match actual experiment outcomes?
    6. Verify smoke-test-results.json shows pass: true
    7. If evaluation_observations.md exists — is the LLM scoring failure analysis actionable for 3.1c?
    8. Verify layer-qualified naming for shared dimensions
    9. This is the final gate before 3.1e closes and 3.1c receives findings
    Resume signal: Type "approved" to close 3.1e and hand off to 3.1c, or describe issues.
  </action>
  <verify>Human approves schemas and 3.1c entry protocol</verify>
  <done>Human has approved extracted schemas and 3.1c conditional entry protocol. Phase 3.1e closed.</done>
</task>

</tasks>

<verification>
1. Every schema field in field-provenance.md traces to an observed data artifact
2. field-provenance.json exists as machine-readable companion with matching data
3. No speculative fields (fields requiring data no experiment produced = failure)
4. Three contradiction pairs gated on artifact existence (analyzed, or skipped with upstream_missing reason)
5. Unobserved Phenomena entries have falsifiable questions and evidence anchors; impossible/unfalsifiable items rejected with reason
6. 3.1c entry protocol determined and written to phase-entry.json
7. Layer-qualified naming applied to shared dimensions
8. smoke-test-results.json exists with pass: true (derived from field-provenance fields)
9. Session estimate: 0.5 sessions + 10-15 min for contradiction analysis
</verification>

<success_criteria>
Minimal schemas extracted from real experimental data. Every field justified by observation. Gaps documented as Unobserved Phenomena with routing to 3.1c/3.1f/Phase 5. 3.1c knows exactly what data it receives and what questions remain open. This is the anti-v5.0 pattern: schemas from data, not theory.
</success_criteria>

<output>
After completion, create `.planning/phases/3.1e-evaluation-zero-empirical-sprint/3.1e-04-SUMMARY.md`
</output>
