---
phase: 3.1e-evaluation-zero-empirical-sprint
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - .vrs/experiments/counting-policy.md
  - .vrs/experiments/eligible-dimensions.md
  - .planning/phases/3.1e-evaluation-zero-empirical-sprint/session-0-baseline-script.py
  - .planning/phases/3.1e-evaluation-zero-empirical-sprint/DECISION-LOCK.md
  - .planning/phases/3.1e-evaluation-zero-empirical-sprint/pre-commitment.json
  - .vrs/experiments/session-0/graph-fingerprints.json
  - .vrs/experiments/plan-01/baseline-before.json
  - .vrs/experiments/session-0/property-diagnostic.md
  - .vrs/experiments/rubrics/reasoning_depth_v1.0.txt
  - .vrs/experiments/rubrics/evidence_quality_v1.0.txt
autonomous: true
requirements:
  - "3.1e-binding-1"
  - "3.1e-binding-2"
  - "3.1e-exit-gate"

must_haves:
  truths:
    - "Fresh detection baseline exists with per-function firings on 7 contracts"
    - "Graph fingerprints recorded for all 7 corpus contracts"
    - "has_access_gate property diagnostic completed for 3 functions"
    - "Two rubrics (reasoning_depth, evidence_quality) exist in LLM-injectable format"
    - "Counting policy and eligible dimensions documented before any experiment runs"
    - "DECISION-LOCK.md and pre-commitment.json created with locked_at timestamp before any experiment execution"
    - "counting-policy.md contains derivation from two baseline runs with decision rule identifying authoritative source"
  artifacts:
    - path: ".vrs/experiments/counting-policy.md"
      provides: "Finding counting policy with derivation from two baselines and authoritative-source decision rule"
    - path: ".vrs/experiments/eligible-dimensions.md"
      provides: "Primary-tier dimension enumeration from reasoning_evaluator.py"
    - path: ".planning/phases/3.1e-evaluation-zero-empirical-sprint/DECISION-LOCK.md"
      provides: "Human-readable decision rules for all plans (immutable after lock)"
    - path: ".planning/phases/3.1e-evaluation-zero-empirical-sprint/pre-commitment.json"
      provides: "Machine-readable decision rules with locked_at timestamp"
    - path: ".vrs/experiments/plan-01/baseline-before.json"
      provides: "Per-function detection firings with is_tp classification and query params metadata"
    - path: ".vrs/experiments/session-0/graph-fingerprints.json"
      provides: "64-char hex fingerprint per contract graph"
    - path: ".vrs/experiments/session-0/property-diagnostic.md"
      provides: "has_access_gate verification for 3 functions"
    - path: ".vrs/experiments/rubrics/reasoning_depth_v1.0.txt"
      provides: "Scoring bands (90+, 50-70, <30) for reasoning depth"
    - path: ".vrs/experiments/rubrics/evidence_quality_v1.0.txt"
      provides: "Scoring bands for evidence quality"
  key_links:
    - from: ".vrs/experiments/plan-01/baseline-before.json"
      to: "tests/evaluation/test_detection_baseline.py"
      via: "CORPUS ground truth join"
      pattern: "corpus_by_contract.*expected_patterns"
    - from: ".vrs/experiments/session-0/property-diagnostic.md"
      to: "vulndocs/access-control/general/patterns/access-tierb-001-trust-assumption-violation.yaml"
      via: "has_access_gate property validation"
    - from: ".planning/phases/3.1e-evaluation-zero-empirical-sprint/pre-commitment.json"
      to: ".vrs/experiments/plan-05/field-provenance.md"
      via: "Plan 05 step 0 verifies locked_at precedes first execution"
    - from: ".vrs/experiments/counting-policy.md"
      to: ".planning/phases/3.1e-evaluation-zero-empirical-sprint/DECISION-LOCK.md"
      via: "Branch table thresholds derived from authoritative baseline in counting-policy.md"
---

<objective>
Session 0: Measurement Prerequisites — establish all baselines, instrumentation, decision locks, and rubrics required by Plans 01-05.

Purpose: Every downstream experiment depends on verified measurement infrastructure. Session 0 ensures the detection pipeline is reproducible, the ground truth join is correct, decision rules are pre-committed and immutable, and evaluation rubrics exist before any experiment begins. This prevents measurement drift, circular reasoning, and post-hoc decision modification.

Output: Pre-generated policy docs + decision lock artifacts + baseline detection data + graph fingerprints + property diagnostic + two LLM-injectable rubrics. All artifacts gate downstream plans.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/3.1e-evaluation-zero-empirical-sprint/3.1e-CONTEXT.md
@.planning/phases/3.1e-evaluation-zero-empirical-sprint/3.1e-RESEARCH.md
@tests/evaluation/test_detection_baseline.py
@tests/workflow_harness/graders/reasoning_evaluator.py
@vulndocs/access-control/general/patterns/access-tierb-001-trust-assumption-violation.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight instrumentation — decision lock, counting policy, eligible dimensions, and baseline script</name>
  <files>
    .planning/phases/3.1e-evaluation-zero-empirical-sprint/DECISION-LOCK.md
    .planning/phases/3.1e-evaluation-zero-empirical-sprint/pre-commitment.json
    .vrs/experiments/counting-policy.md
    .vrs/experiments/eligible-dimensions.md
    .planning/phases/3.1e-evaluation-zero-empirical-sprint/session-0-baseline-script.py
  </files>
  <action>
    **Pre-flight (before session clock starts):**

    0. **DECISION-LOCK.md + pre-commitment.json (MUST — per locked decision "Pre-Sprint Decision Lock"):**
       Create both artifacts BEFORE any experiment execution begins. These are immutable after creation.

       **DECISION-LOCK.md** at `.planning/phases/3.1e-evaluation-zero-empirical-sprint/DECISION-LOCK.md`:
       - Sections for each Plan (01, 02, 03/04, 05) with:
         (a) outcome variables observed
         (b) decision rules in JSON format
         (c) no-modification clause (rules locked before Session A execution)
       - Plan 01 rules: `outcome_A: "precision_delta >= 5pp AND zero regressions AND negative control passes"`, `outcome_B: "0 < precision_delta < 5pp AND zero regressions"`, `outcome_C: "YAML fix requires > ~200 LOC builder changes with no YAML-addressable subset"`
       - Plan 01 branch table: thresholds reference counting-policy.md — use `if observed_tp < (authoritative_tp - tolerance)` where `authoritative_tp` and `tolerance` are imported from counting-policy.md derivation, NOT hardcoded absolute values
       - Plan 02 rules: go/no-go decision table (H1+H2+H3 scoring, >= 2.0 = Go, 1.0-1.5 = Ambiguous, < 1.0 = No-go)
       - Plan 03/04 rules: taxonomy discovery (undirected), validity coefficient thresholds (normalized IC < 0.2 = high, 0.2-0.4 = near zero, > 0.4 = counter-productive), gameability go threshold (ceil(N/2))
       - Plan 05 rules: Track A/B/C activation conditions, phase_entry_mode determination

       **pre-commitment.json** at `.planning/phases/3.1e-evaluation-zero-empirical-sprint/pre-commitment.json`:
       Schema: `{ "plan_01": {rules}, "plan_02": {rules}, "plan_03_04": {rules}, "plan_05": {rules}, "locked_at": "ISO timestamp", "locked_by": "executor" }`
       The `locked_at` timestamp must precede any Session A execution. Plan 05 step 0 verifies this.

    1. **counting-policy.md:** Extract verbatim the "Finding Counting Policy" decision from CONTEXT.md. Content: per-function-firing for delta measurement (Plan 01), per-pattern for regression detection (test suite). Document `_unique_patterns()` masking defect. Path: `.vrs/experiments/counting-policy.md`.
       - **Derivation section (REQUIRED):** Include the two contradictory baseline runs with their configuration parameters (limit value, CORPUS type, date of run). State a decision rule identifying which baseline is authoritative and why. Derive the numeric threshold for "expected TP" on the 7-contract corpus from the authoritative baseline. If the ground truth TP counts cannot be determined in Session 0, record that as an explicit blocker for Plan 01 execution.
       - counting-policy.md is a BLOCKING prerequisite: Plan 01 execution cannot begin until it is committed.

    2. **eligible-dimensions.md:** Read `tests/workflow_harness/graders/reasoning_evaluator.py` source. Enumerate all primary-tier dimensions with heuristic handlers (non-default-30) from `_heuristic_dimension_score`. Record each dimension name and its heuristic formula. Expected: ALL 7 non-default dimensions are count-based proxies. Flag 'structural proxy homogeneity' if confirmed. This is a design constraint for Plan 02 anchors, not a failure.
       - Part A (count gate): If fewer than 2 eligible dimensions found — BLOCK Session 0 task (b), escalate to 3.1c.
       - Part B (testability gate): Record heuristic formula per dimension. If ALL eligible dimensions compute solely from count-based inputs — flag and document as Plan 02 design constraint.
       Path: `.vrs/experiments/eligible-dimensions.md`.

    3. **session-0-baseline-script.py:** Create standalone Python script (~80+ LOC) that:
       - Imports `VKGBuilder`, `build_lens_report`, `graph_fingerprint` from production modules
       - Imports `CORPUS` from `tests.evaluation.test_detection_baseline`
       - **CORPUS validation (`validate_corpus()` — FIRST step, before any graph build or query):**
         (a) `assert isinstance(CORPUS, list) and len(CORPUS) > 0`, else `SystemExit(1)` with message
         (b) For each element at index `i`, assert `isinstance(CORPUS[i], GroundTruth)` — on failure, `SystemExit(1)` printing the offending index and `type(CORPUS[i])`
         (c) For each element, assert fields `contract_name`, `expected_tp`, `expected_fp` are present and non-null — on failure, `SystemExit(1)` printing the index and which field is missing/null
         This function runs unconditionally as the script's first action.
       - **Runtime limit invariant in `run_query()` wrapper:** Immediately after constructing the query object, assert `query.limit >= 200` with message `f"BLOCKING: limit={query.limit} will truncate results"`. This is a runtime check, not a source-grep — the assertion fires if any code path sets limit below 200.
       - Builds join: `corpus_by_contract = {gt.target_contract_label: set(gt.expected_patterns) for gt in CORPUS}`
       - For each of 7 contracts: builds graph via `VKGBuilder().build(contract_path)`, calls `PatternEngine().run(graph, patterns, lens=DEFAULT_LENSES, limit=200, explain=True)` directly (NOT `build_lens_report` — it does not forward `explain=True`, so `fired_conditions` would always be absent), computes `graph_fingerprint(graph)` (returns 64-char hex string, NOT dict — use `graph_fingerprint` not `fingerprint_graph`)
       - Extracts per-function firings: `{contract, pattern_id, function_name, node_id, is_tp, fired_conditions: {all, any, none, edges}, _run_params: {limit: 200, lens: DEFAULT_LENSES}}` where `fired_conditions` comes from `finding['explain']`, `function_name` from `finding['node_label']` (NOT a direct field — `node_label` is the available field in PatternEngine output), and `contract` resolved by calling `_function_contract_map(graph)` from `alphaswarm_sol.queries.report` to map `node_id -> contract_label` — assert at least one resolved label matches a `target_contract_label` in CORPUS
       - Outputs `baseline-before.json` with schema: `{meta: {timestamp: str, query_params: {limit: int}, graph_fingerprint: str, corpus_size: int}, per_contract: {[name]: {findings: [...], tp_count: int, fp_count: int, total: int}}, totals: {tp: int, fp: int, total: int, contracts_tested: int}}` — the `meta.query_params.limit` field records the actual runtime limit used
       - Outputs `graph-fingerprints.json`
       - Also capture and record `corpus_sha256 = hashlib.sha256(pathlib.Path('tests/evaluation/test_detection_baseline.py').read_bytes()).hexdigest()` — store in graph-fingerprints.json as `_corpus_version.sha256` so downstream plans can verify CORPUS has not drifted
       Path: `.planning/phases/3.1e-evaluation-zero-empirical-sprint/session-0-baseline-script.py`

    Ensure `.vrs/experiments/` directory tree exists (create if needed).
  </action>
  <verify>
    <automated>python -c "import pathlib, json; assert pathlib.Path('.planning/phases/3.1e-evaluation-zero-empirical-sprint/DECISION-LOCK.md').exists(); pc=json.loads(pathlib.Path('.planning/phases/3.1e-evaluation-zero-empirical-sprint/pre-commitment.json').read_text()); assert 'locked_at' in pc; assert pathlib.Path('.vrs/experiments/counting-policy.md').exists(); assert pathlib.Path('.vrs/experiments/eligible-dimensions.md').exists(); assert pathlib.Path('.planning/phases/3.1e-evaluation-zero-empirical-sprint/session-0-baseline-script.py').exists(); print('Pre-flight artifacts exist, decision lock verified')"</automated>
    <manual>Verify DECISION-LOCK.md has sections for Plans 01, 02, 03/04, 05 with decision rules. Verify Plan 01 branch table references counting-policy.md thresholds (not hardcoded absolutes). Verify pre-commitment.json has locked_at timestamp. Verify counting-policy.md matches CONTEXT.md decision verbatim AND contains derivation section with two baselines, decision rule, and authoritative TP threshold. Verify eligible-dimensions.md lists all non-default-30 dimensions with formulas.</manual>
  </verify>
  <done>Decision lock artifacts created with locked_at timestamp. Branch table references counting-policy.md derivation. counting-policy.md includes two-baseline derivation with decision rule identifying authoritative source. Three pre-flight documents exist. Eligible dimensions enumerated with count gate and testability gate results recorded. Baseline script is syntactically valid Python with validate_corpus() function and runtime limit assertion.</done>
</task>

<task type="auto">
  <name>Task 2: Raw baseline capture (a1) + graph fingerprints + property diagnostic (c) + rubric drafts (b)</name>
  <files>
    .vrs/experiments/plan-01/baseline-before.json
    .vrs/experiments/session-0/graph-fingerprints.json
    .vrs/experiments/session-0/property-diagnostic.md
    .vrs/experiments/rubrics/reasoning_depth_v1.0.txt
    .vrs/experiments/rubrics/evidence_quality_v1.0.txt
  </files>
  <action>
    **Session 0 core tasks (1 session, ~100 min max):**

    **Pre-flight validation (before any capture):**
    - Run `python session-0-baseline-script.py --dry-run --limit 49` and confirm it exits with code 1 and prints the BLOCKING limit message — proving the runtime limit assertion is exercised.
    - Run a dry-run smoke test with one intentionally malformed CORPUS element (a plain dict injected into the list) and assert the script exits with code 1 and prints the diagnostic identifying the offending element index and its type — proving validate_corpus() rejects non-GroundTruth elements.
    - CORPUS type+field validation passes on the real 7-contract corpus (validate_corpus() completes without error).

    **Task (a1) — Raw baseline capture (MUST, ~35 min max):**
    Run `session-0-baseline-script.py` on all 7 CORPUS contracts. Build and query each contract graph separately (7 builds, 7 queries) to avoid cross-contract limit exhaustion. Use `limit=200`.
    - Save per-function firings to `.vrs/experiments/plan-01/baseline-before.json` with schema: `{meta: {timestamp, query_params: {limit}, graph_fingerprint, corpus_size}, per_contract: {[name]: {findings: [...], tp_count, fp_count, total}}, totals: {tp, fp, total, contracts_tested}}`
    - Save graph fingerprints to `.vrs/experiments/session-0/graph-fingerprints.json` with schema: `{contract: str, fingerprint: str, node_count: int, edge_count: int}` per contract
    - Record BOTH per-pattern and per-function counts as summary statistics
    - Overflow rule: if task (a1) exceeds 35 min, record partial baseline, complete in Plan 01 first action

    **Task (a2) — FP annotation (SHOULD, Plan 01 first action if incomplete):**
    For each FP in baseline-before.json, annotate with access-tierb-001 firing status. This enables Plan 01 to know exactly which FPs the pattern fix should remove. If time-constrained, defer to Plan 01.

    **Task (b) — Rubric creation (~20-25 min):**
    Create two rubrics in LLM-injectable format:
    - `reasoning_depth_v1.0.txt`: Scoring bands — 90+ (deep causal chains with node IDs, multi-step reasoning connecting graph observations to vulnerability hypothesis), 50-70 (surface-level observations, correct tools used but weak evidence chain), <30 (no reasoning chain, conclusion without support). Security-reasoning specific: reference BSKG queries, evidence interpretation, hypothesis formation.
    - `evidence_quality_v1.0.txt`: Scoring bands — 90+ (graph-grounded evidence with specific node/edge citations, cross-referenced findings), 50-70 (some evidence cited but incomplete chain), <30 (no evidence cited, assertion-only conclusions).
    - IMPORTANT FORMAT: These `.txt` files are standalone reference documents used by a human to write anchor transcripts and calibrate scoring. When injected into `REASONING_PROMPT_TEMPLATE` as the `{expected}` field, the injected content must be a 1-2 sentence behavioral description (e.g., "Good reasoning traces the vulnerability cause-and-effect chain using BSKG node IDs"), NOT the full multi-band rubric text. The multi-band format in the `.txt` is for human reference; the `{expected}` injection is a behavioral target, not a scoring rubric.

    **Task (c) — Property diagnostic (~15-20 min):**
    Validate `has_access_gate` and `state_mutability` properties for 3 functions from the corpus:
    - SideEntranceLenderPool.withdraw (expected: has_access_gate likely false — no modifier)
    - SideEntranceLenderPool.flashLoan (expected: has_access_gate likely false)
    - One function from NaiveReceiverPool with known access control
    Record expected vs actual values using three-state table: `true | false | absent` (absent = property key not present in node.properties at all, treated differently from `false` — indicates a builder gap, not a detection gap). Flag over-reporting if SideEntrance.withdraw shows `true`. Flag builder gap if `absent` (property never set). Check for name-dependence.
    Output: `.vrs/experiments/session-0/property-diagnostic.md` with expected vs actual table using three-state values and over-reporting/builder-gap flags.
  </action>
  <verify>
    <automated>python -c "import json, pathlib; b=json.loads(pathlib.Path('.vrs/experiments/plan-01/baseline-before.json').read_text()); assert 'meta' in b and 'query_params' in b['meta'] and b['meta']['query_params']['limit'] >= 200, 'Missing or invalid meta.query_params.limit'; assert 'per_contract' in b and 'totals' in b; fp=json.loads(pathlib.Path('.vrs/experiments/session-0/graph-fingerprints.json').read_text()); assert len(fp) >= 7; print(f'Baseline schema valid, limit={b[\"meta\"][\"query_params\"][\"limit\"]}, {len(fp)} fingerprints')"</automated>
    <manual>Verify baseline-before.json matches schema: {meta: {timestamp, query_params: {limit}, graph_fingerprint, corpus_size}, per_contract: {[name]: {findings: [...], tp_count, fp_count, total}}, totals: {tp, fp, total, contracts_tested}}. Verify graph fingerprints are 64-char hex strings. Verify rubrics have scoring bands. Verify property-diagnostic.md has expected vs actual comparison. Verify dry-run smoke tests (limit rejection, malformed CORPUS rejection) were executed and passed.</manual>
  </verify>
  <done>Baseline-before.json contains per-function firings for all 7 corpus contracts with is_tp classification, matches the canonical schema, and records meta.query_params.limit >= 200. Graph fingerprints recorded. Two rubrics exist with scoring bands for reasoning_depth and evidence_quality. Property diagnostic completed with has_access_gate verification for 3 functions. Dry-run smoke tests confirmed validate_corpus() and limit assertion are exercised. counting-policy.md exists with derivation section; branch table references it by path. Session 0 ceiling of 1 session respected.</done>
</task>

</tasks>

<verification>
1. DECISION-LOCK.md exists with sections for all 4 plan groups and no-modification clause
2. Plan 01 branch table uses `if observed_tp < (authoritative_tp - tolerance)` referencing counting-policy.md, not hardcoded absolute thresholds
3. pre-commitment.json exists with locked_at timestamp and decision rules for all plans
4. counting-policy.md exists with derivation section containing two baseline runs, decision rule, and authoritative TP threshold (or explicit blocker if undetermined)
5. All 7 pre-flight + session artifacts exist at their specified paths
6. Baseline-before.json uses per-function counting (not per-pattern deduplication) and contains `$.meta.query_params.limit >= 200`
7. Baseline-before.json matches schema: `{meta: {timestamp, query_params: {limit}, graph_fingerprint, corpus_size}, per_contract: {[name]: {findings, tp_count, fp_count, total}}, totals: {tp, fp, total, contracts_tested}}`
8. Graph fingerprints are 64-char hex strings from `graph_fingerprint()` (not legacy `fingerprint_graph()`)
9. Rubrics have security-specific scoring bands (not generic LLM evaluation text)
10. Property diagnostic has expected vs actual comparison (not just actual values)
11. CORPUS type+field validation passes on 7-contract corpus (validate_corpus() exercised in dry-run)
12. Dry-run with `--limit 49` exits code 1 with BLOCKING message (runtime limit invariant exercised)
13. Dry-run with malformed CORPUS element (plain dict) exits code 1 with diagnostic (validate_corpus() guard exercised)
14. Session completed within 1 session ceiling (~2 hours)
</verification>

<success_criteria>
Session 0 produces all measurement prerequisites and decision locks. Plans 01-05 can begin without re-running detection pipelines or re-discovering baseline numbers. Pre-flight policy documents prevent counting methodology confusion across plans. Decision rules are immutable and verifiable by Plan 05. counting-policy.md provides the authoritative baseline derivation that the branch table depends on.
</success_criteria>

<output>
After completion, create `.planning/phases/3.1e-evaluation-zero-empirical-sprint/3.1e-00-SUMMARY.md`
</output>
