---
phase: 3.1c-reasoning-evaluation-framework
plan: 05
type: execute
wave: 3
depends_on: ["3.1c-02", "3.1c-03"]
files_modified:
  - tests/workflow_harness/lib/debrief_protocol.py
  - tests/workflow_harness/hooks/debrief_gate.py
  - tests/workflow_harness/hooks/debrief_task_complete.py
  - tests/evaluation/test_debrief_protocol.py
autonomous: true
requirements:
  - EVAL-02
  - EVAL-10

must_haves:
  truths:
    - "Layer 1 SendMessage debrief reads serialized artifacts from disk"
    - "Layer 2 hook gates use exit code 2 with stop_hook_active guard"
    - "Compaction-aware branching provides short debrief when compacted"
    - "DebriefResponseValidator strips fence markers and detects dead agents"
    - "TeamManager has zero send_*/spawn_*/shutdown/wait_* methods"
    - "bridge_transformer.py exists at src/alphaswarm_sol/testing/evaluation/bridge_transformer.py, contains transform() method, and has at least one passing test against a real Plan 02 hook payload fixture (3.1e deferred Item 3)"
  artifacts:
    - path: "tests/workflow_harness/lib/debrief_protocol.py"
      provides: "Four-layer debrief cascade with live mode disk-read path"
    - path: "tests/workflow_harness/hooks/debrief_gate.py"
      provides: "Blocking debrief hook with timeout polling"
  key_links:
    - from: "tests/workflow_harness/lib/debrief_protocol.py"
      to: ".vrs/observations/{session_id}_debrief.json"
      via: "Disk-read path for live mode artifacts"
      pattern: "_debrief.json"
---

<objective>
Implement the four-layer debrief strategy: SendMessage (disk-read) -> hook gates (exit 2) -> LLM quality gate -> transcript fallback. Fix debrief_gate.py from exit-0 stub to real blocking. Add compaction-aware branching.

Purpose: Debrief data feeds Plan 07 evaluator and Plan 12 baseline. Without it, evaluation has no agent self-report signal.
Output: Working debrief cascade, blocking hooks, compaction-aware branching, validated debrief artifact schema.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/3.1c-reasoning-evaluation-framework/3.1c-CONTEXT.md (section: "Plan 05: Debrief Protocol")
@.planning/phases/3.1c-reasoning-evaluation-framework/3.1c-RESEARCH.md (section: "Debrief Strategy")
@.planning/phases/3.1c-reasoning-evaluation-framework/3.1c-03-SUMMARY.md
@tests/workflow_harness/lib/debrief_protocol.py
@tests/workflow_harness/hooks/debrief_gate.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement debrief cascade + blocking hooks + compaction branching</name>
  <files>
    tests/workflow_harness/lib/debrief_protocol.py
    tests/workflow_harness/hooks/debrief_gate.py
    tests/workflow_harness/hooks/debrief_task_complete.py
  </files>
  <action>
    **Layer 1 (SendMessage) — disk-read path:**
    - CC test orchestrator (Plans 09-11) calls SendMessage during teardown, serializes DebriefResponse to `.vrs/observations/{session_id}_debrief.json`
    - Python runner reads this artifact from disk in live mode
    - `attempt_send_message_layer()` in live mode loads from disk, NOT calls SendMessage

    **Layer 2 (hook gates) — implement real blocking:**
    Update `debrief_gate.py` from exit-0 stub to real exit-2 blocking:
    ```python
    # MANDATORY: Prevent infinite loops
    if input_data.get("stop_hook_active", False):
        sys.exit(0)
    teammate_name = input_data.get("teammate_name", "")
    marker_path = Path.cwd() / ".vrs" / "debrief" / f"{teammate_name}.done"
    # Timeout polling loop
    for _ in range(timeout_iterations):
        if marker_path.exists():
            sys.exit(0)
        time.sleep(poll_interval)
    sys.exit(2)  # Block
    ```
    BUG #20221: Use `type: "command"` ONLY. Never `type: "prompt"`.

    **DebriefResponseValidator (~20 LOC):**
    - Fence-stripping (```json markers)
    - `layer_used="send_message_malformed"` on parse failure
    - `layer_used="send_message_no_response"` for dead-agent detection (ADV-5-01)
    - delivery_status chain (P15-IMP-21): phantom debriefs scored as applicable=False

    **Compaction-aware branching (P11-IMP-04, ~15 LOC):**
    - If `{session_id}.compacted` marker present: 3 structural questions only
    - If absent: full 7-question deep debrief
    - DebriefResponse.compacted = True when marker detected

    **Confidence fix (debrief_protocol.py:44-48):**
    Check `len(answers) > 0` before MEDIUM_CONFIDENCE; assign LOW_CONFIDENCE when gate fired but no answers.

    **TeamManager scope (P15-IMP-20):**
    Verify: `grep -r 'def send' src/alphaswarm_sol/testing/` returns 0 matches on TeamManager.
  </action>
  <verify>
    <automated>cd . && uv run pytest tests/evaluation/test_debrief_protocol.py -x -q && grep -c "stop_hook_active" tests/workflow_harness/hooks/debrief_gate.py | xargs -I{} test {} -ge 1 && echo "stop_hook_active guard present"</automated>
  </verify>
  <done>Debrief cascade implements all 4 layers. debrief_gate.py blocks with exit 2 and timeout polling. Compaction-aware branching works. DebriefResponseValidator handles malformed and dead-agent cases. Confidence labeling fixed.</done>
</task>

<task type="auto">
  <name>Task 2: Debrief prompt template + integration tests</name>
  <files>
    tests/evaluation/test_debrief_protocol.py
  </files>
  <action>
    **Debrief prompt template:**
    Create structured DebriefResponse format with:
    - Per-answer confidence (high/medium/low)
    - Key-uncertainties list
    - Conditional section that fires only with sentinel env var (non-evaluation sessions skip debrief)

    **Full debrief questions (7, when not compacted):**
    1. What was your primary hypothesis?
    2. What BSKG queries informed your analysis?
    3. What surprised you in the results?
    4. What evidence supports your conclusion?
    5. What evidence contradicts your conclusion?
    6. What would you investigate further?
    7. Rate your confidence in the finding (1-5 with justification)

    **Short debrief (3, when compacted):**
    1. What was your primary conclusion?
    2. What key evidence supports it?
    3. Rate your confidence (1-5)

    **Tests:**
    - Layer cascade: SendMessage → hook gate → LLM → transcript fallback
    - Disk-read path: write artifact, verify runner reads it
    - Compaction branching: 3 vs 7 questions
    - Dead agent detection: SendMessage returns success but no artifact written → sentinel
    - Fence stripping: ```json wrapper removed correctly
    - Confidence fix: gate with no answers = LOW_CONFIDENCE
    - delivery_status: phantom vs not_attempted distinction
  </action>
  <verify>
    <automated>cd . && uv run pytest tests/evaluation/test_debrief_protocol.py -x -q -v 2>&1 | tail -5</automated>
  </verify>
  <done>Debrief prompt template defined. All debrief tests pass including cascade, disk-read, compaction branching, dead agent detection, and confidence labeling.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Bridge Transformer (3.1e deferred Item 3, ~150 LOC)</name>
  <files>
    src/alphaswarm_sol/testing/evaluation/bridge_transformer.py
    tests/evaluation/test_bridge_transformer.py
  </files>
  <action>
    **Gate check:** Only implement if Plan 02 Task 3 GO confirmation is in validity-matrix.json.
    If Item 1 is NO-GO, create a stub with `raise NotImplementedError("Bridge transformer
    requires Plan 02 GO gate — see validity-matrix.json")` and a clear docstring explaining the gate.

    **Bridge transformer implementation (~150 LOC, 3.1e deferred Item 3):**
    File: `src/alphaswarm_sol/testing/evaluation/bridge_transformer.py`

    Adapter class with single primary method:
    `transform(hook_payload: dict) -> EvaluationInput | None`

    Input schema (Plan 02 hook payload JSON):
    - `tool_use_id`: str
    - `timestamp`: ISO8601 str
    - `session_id`: str
    - `raw_observation`: str
    - `hook_type`: str (PreToolUse | PostToolUse | etc.)

    Output schema (`EvaluationInput`):
    - `dimension`: str
    - `move_type`: str
    - `evidence_excerpt`: str
    - `session_id`: str
    - `tool_use_id`: str

    Null-safety: if `hook_payload` lacks `tool_use_id`, log a WARNING and return `None`
    (callers filter None). Validate against at least one real Plan 02 hook payload fixture.

    **Tests:**
    Round-trip test: `transform(real_plan02_hook_payload)` produces `EvaluationInput` that validates
    against Plan 07 schema. None-return test: payload missing tool_use_id → returns None with warning.
  </action>
  <verify>
    <automated>cd . && uv run pytest tests/evaluation/test_bridge_transformer.py -x -q && test -f src/alphaswarm_sol/testing/evaluation/bridge_transformer.py && echo "bridge_transformer.py exists"</automated>
  </verify>
  <done>bridge_transformer.py exists in evaluation pipeline package. transform(ObservationSummary) -> EvaluatorInput passes a round-trip test with a real Plan 02 hook payload fixture. Null-safety path returns None on missing tool_use_id with log warning. Plan 07 Task 1 can verify bridge.py exists before proceeding.</done>
</task>

</tasks>

<verification>
1. All debrief protocol tests pass
2. debrief_gate.py has stop_hook_active guard
3. Compacted sessions get 3 questions, non-compacted get 7
4. `grep -r 'def send' src/alphaswarm_sol/testing/` returns 0 matches on TeamManager (P15-IMP-20)
5. DebriefResponseValidator strips fences and detects dead agents
</verification>

<success_criteria>
Four-layer debrief cascade operational. Blocking hooks work with timeout polling. Compaction-aware branching tested. Artifact schema validated. TeamManager boundary preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/3.1c-reasoning-evaluation-framework/3.1c-05-SUMMARY.md`
</output>
