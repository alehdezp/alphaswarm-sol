---
phase: 3.1c-reasoning-evaluation-framework
plan: 08
type: execute
wave: 5
depends_on: ["3.1c-07"]
files_modified:
  - tests/workflow_harness/lib/evaluation_runner.py
  - tests/workflow_harness/fixtures/evaluation_runner/
  - tests/workflow_harness/fixtures/scoring_quality_gate/
  - tests/evaluation/test_evaluation_runner.py
autonomous: true
requirements:
  - EVAL-01
  - EVAL-03
  - EVAL-04
  - EVAL-07

must_haves:
  truths:
    - "EvaluationRunner runs standalone on fixture without importing CC skill code"
    - "Scoring Quality Gate: good transcript scores higher than bad on GVS (>=3 dims) AND reasoning (>=2 dims)"
    - "update_baseline() gated on three-condition AND: valid session + completed status + non-null effective_score"
    - "3 negative fixture sessions produce valid=False with correct reasons"
    - "Session validity check rejects invalid sessions before scoring"
  artifacts:
    - path: "tests/workflow_harness/lib/evaluation_runner.py"
      provides: "Full pipeline orchestrator: contract -> observations -> scores -> result -> persist"
    - path: "tests/workflow_harness/fixtures/evaluation_runner/"
      provides: "Standard fixture session with JSONL + debrief + BSKG query result"
    - path: "tests/workflow_harness/fixtures/scoring_quality_gate/"
      provides: "Paired good/bad transcript fixtures from real agent runs"
  key_links:
    - from: "tests/workflow_harness/lib/evaluation_runner.py"
      to: "tests/workflow_harness/graders/reasoning_evaluator.py"
      via: "Evaluator invocation for reasoning assessment"
      pattern: "reasoning_evaluator"
    - from: "tests/workflow_harness/lib/evaluation_runner.py"
      to: "tests/workflow_harness/graders/graph_value_scorer.py"
      via: "GVS invocation for graph value scoring"
      pattern: "graph_value_scorer"
---

<objective>
Build the full evaluation pipeline orchestrator that chains contract loading -> observation parsing -> graph value scoring -> reasoning evaluation -> deterministic checks -> EvaluationResult -> persistence. Includes session validity checks, scoring quality gate, negative fixtures, and mode-aware dimension filtering.

Purpose: The runner is the backbone connecting all upstream components (Plans 01-07) into a single invocable pipeline. Plans 09-11 depend on it for test execution.
Output: Working EvaluationRunner with fixture-validated pipeline, scoring quality gate, and baseline update guards.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/3.1c-reasoning-evaluation-framework/3.1c-CONTEXT.md (section: "Plan 08: EvaluationRunner Pipeline")
@.planning/phases/3.1c-reasoning-evaluation-framework/3.1c-CONTEXT.md (section: "Plan 08: Fixture Spec & Parallel Config")
@.planning/phases/3.1c-reasoning-evaluation-framework/3.1c-07-SUMMARY.md
@tests/workflow_harness/lib/evaluation_runner.py
@src/alphaswarm_sol/testing/evaluation/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: EvaluationRunner pipeline + session validity + fixture sessions</name>
  <files>
    tests/workflow_harness/lib/evaluation_runner.py
    tests/workflow_harness/fixtures/evaluation_runner/
    tests/workflow_harness/fixtures/scoring_quality_gate/
  </files>
  <action>
    **EvaluationRunner pipeline (~200 LOC):**
    `run(session_id, contract) -> EvaluationResult`:
    1. `_check_session_validity(session_id, obs_dir, debrief_dir) -> SessionValidityManifest` (~25 LOC, SYN-02). Invalid sessions write `EvaluationResult(status="invalid_session")` — never enter baseline.
    2. Parse observations via ObservationParser(session_id=session_id)
    3. Check `obs_summary.data_quality.degraded` — if True, set `data_quality_warning` (SYN-01, ~10 LOC). Do NOT reject automatically.
    4. Run GVS if contract says `run_gvs: true`
    5. Run reasoning evaluator if contract says `run_reasoning: true`
    6. Apply mode-aware dimension filtering per Mode Capability Matrix (IMP-25): set `applicable=False` for unavailable dimensions per `run_mode`. Use `ScoreCard.effective_score()` for all computations.
    7. Fail-fast on unconsumed config fields (DC-4)
    8. Persist EvaluationResult

    **Three-condition baseline AND gate (P15-IMP-38):**
    `_baseline_manager.update_baseline()` requires ALL three:
    (a) `SessionValidityManifest.valid == True`
    (b) `status == 'completed'`
    (c) `effective_score() is not None`
    Negative test per condition required.

    **Per-scenario timeout:**
    Default 300s standard, 600s deep, 900s deep-with-debate.

    **Improvement queue threshold (IMP-01, ~30 LOC):**
    After each Core-tier workflow, if `improvement_hints` has >3 entries with `score < 30`, append `HIGH_PRIORITY` flag to `progress.json`.

    **Post-run anomaly detection (~30-50 LOC, IMP-26):**
    After 5th Core-tier run, detect ceiling/floor/zero-variance anomalies. Full healing stays in Plan 12.

    **Mode-specific execution profiles (P15-IMP-19):**
    Three profiles: `interactive_investigation`, `headless_tool`, `headless_standard`.

    **Parallel headless config (P15-IMP-17):**
    `max_parallel_headless: int = 1` (increment to 3 after isolation test passes). Session-scoped subdirectories. `fcntl.flock()` for BaselineManager writes.

    **Standard fixture session (CSC-04):**
    Create `tests/workflow_harness/fixtures/evaluation_runner/` with:
    (a) JSONL >= 5 records covering PreToolUse, PostToolUse, SubagentStop, Stop, including >= 1 paired PreToolUse+PostToolUse with matching tool_use_id
    (b) Debrief JSON matching DebriefResponse schema with >= 1 non-empty answer
    (c) >= 1 BSKG query result record
    Exit: EvaluationRunner produces EvaluationResult with non-zero tool_counts, non-null graph_value_score, non-null reasoning_assessment.

    **3 negative fixture sessions (P11-IMP-11):**
    `session_interrupted/`, `session_stale/`, `session_missing_debrief/` — each produces `valid=False` with correct reasons. Assert invalid sessions NOT stored to baseline. ~50 LOC test code + ~15 fixture files.

    **ground_truth_rubric context wiring (P15-IMP-33):**
    Pass `ground_truth_rubric` from contract to Plan 07 evaluator via `context['ground_truth_rubric']`. Evaluator fills `{expected}` template variable.
  </action>
  <verify>
    <automated>cd /Volumes/ex_ssd/home/projects/python/vkg-solidity/true-vkg && uv run pytest tests/evaluation/test_evaluation_runner.py -x -q</automated>
    <manual>Verify EvaluationRunner imports zero CC skill code (no alphaswarm_sol.shipping imports)</manual>
  </verify>
  <done>EvaluationRunner produces EvaluationResult from fixture session. Session validity rejects invalid sessions. Three-condition baseline AND gate works. Mode-aware filtering applied. No CC skill imports.</done>
</task>

<task type="auto">
  <name>Task 2: Scoring Quality Gate + runner tests</name>
  <files>
    tests/workflow_harness/fixtures/scoring_quality_gate/
    tests/evaluation/test_evaluation_runner.py
  </files>
  <action>
    **Scoring Quality Gate (P14-IMP-14/P14-CSC-01):**
    Create paired good/bad transcript fixtures at `tests/workflow_harness/fixtures/scoring_quality_gate/`. Source: run vrs-attacker on simple corpus contract twice — standard prompt (good) vs BSKG-skipping prompt (bad).
    Good transcript: >= 2 BSKG queries before reading .sol, uses node IDs in conclusion.
    Bad transcript: reads .sol directly, no BSKG queries.
    Exit: higher effective_score() for good vs bad on GVS (>= 3 dimensions) AND reasoning (>= 2 dimensions). Failure blocks Plan 09.
    Document fixture provenance in `README.md`.

    **Runner tests:**
    - Full pipeline on standard fixture → non-zero EvaluationResult
    - Session validity: invalid session → status="invalid_session"
    - Three-condition AND gate: 3 negative tests (invalid session, failed status, None score) → baseline NOT updated
    - 3 negative fixtures each produce valid=False
    - Mode-aware dimension filtering: headless run skips interactive-only dimensions
    - Data quality warning: degraded obs → data_quality_warning set, NOT auto-rejected
    - Fail-fast on unconsumed config: extra field → error
    - Improvement queue: 4 hints with score <30 → HIGH_PRIORITY flag
    - Scoring Quality Gate: good > bad on GVS (>=3) and reasoning (>=2)
    - Post-run anomaly detection: ceiling fixture (all 100) → warning
    - ground_truth_rubric: context dict has rubric when contract provides it
    - Parallel config: session-scoped subdirectories verified

    **Incomplete evaluation tracking (IMP-27):**
    If >50% of Core baseline entries are `evaluation_complete: False`, surface warning about debate timeout.
  </action>
  <verify>
    <automated>cd /Volumes/ex_ssd/home/projects/python/vkg-solidity/true-vkg && uv run pytest tests/evaluation/test_evaluation_runner.py -x -q -v 2>&1 | tail -10</automated>
  </verify>
  <done>Scoring Quality Gate passes — good transcript scores higher than bad. All runner tests pass including negative fixtures, baseline guards, mode-aware filtering, and anomaly detection. EvaluationRunner standalone criterion met.</done>
</task>

</tasks>

<verification>
1. EvaluationRunner produces EvaluationResult on standard fixture
2. Session validity rejects all 3 negative fixtures
3. Baseline AND gate: all 3 negative conditions tested
4. Scoring Quality Gate: good > bad on GVS (>=3 dims) AND reasoning (>=2 dims)
5. Mode-aware filtering applied per Mode Capability Matrix
6. Zero imports from alphaswarm_sol.shipping in evaluation_runner.py
7. ground_truth_rubric flows from contract through context to evaluator template
</verification>

<success_criteria>
Full pipeline orchestrator works on fixtures. Scoring Quality Gate discriminates good vs bad. Session validity prevents invalid sessions from entering baseline. Three-condition AND gate enforced. Mode-aware filtering operational.
</success_criteria>

<output>
After completion, create `.planning/phases/3.1c-reasoning-evaluation-framework/3.1c-08-SUMMARY.md`
</output>
