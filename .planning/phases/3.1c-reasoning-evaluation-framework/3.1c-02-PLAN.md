---
phase: 3.1c-reasoning-evaluation-framework
plan: 02
type: execute
wave: 2
depends_on: ["3.1c-01"]
files_modified:
  - tests/workflow_harness/hooks/obs_session_start.py
  - tests/workflow_harness/hooks/obs_precompact.py
  - tests/workflow_harness/hooks/obs_session_end.py
  - tests/workflow_harness/hooks/delegate_guard.py
  - tests/workflow_harness/lib/transcript_parser.py
  - tests/workflow_harness/lib/workspace.py
  - tests/workflow_harness/fixtures/real_sessions/
  - tests/workflow_harness/fixtures/hook_schema_snapshots/
autonomous: true
requirements:
  - EVAL-01
  - EVAL-02

must_haves:
  truths:
    - "3+ real Claude Code session transcripts captured from distinct workflow categories"
    - "TranscriptParser.to_observation_summary() produces complete data on real transcripts"
    - "6 retained hooks validated against real sessions with golden file snapshots"
    - "obs_precompact.py runs with stdlib imports only and completes within 5s"
    - "delegate_guard.py blocks prohibited tool calls via config"
    - "Plan 02 is NOT complete until Task 3 GO gate produces >= 3 valid transcripts and validity-matrix.json is updated. Plans 03+ MUST NOT begin until this gate passes."
  artifacts:
    - path: "tests/workflow_harness/fixtures/real_sessions/"
      provides: "3+ real CC transcripts"
    - path: "tests/workflow_harness/hooks/obs_precompact.py"
      provides: "Synchronous precompact hook with BSKG node extraction"
    - path: "tests/workflow_harness/hooks/delegate_guard.py"
      provides: "Config-driven delegation enforcement hook"
  key_links:
    - from: "tests/workflow_harness/lib/transcript_parser.py"
      to: "tests/workflow_harness/fixtures/real_sessions/"
      via: "to_observation_summary() parsing real JSONL"
      pattern: "to_observation_summary"
---

<objective>
Capture real CC transcripts (P0 hard gate), implement 6 essential hooks (eliminating passive hooks per P13-IMP-08), add TranscriptParser.to_observation_summary() as primary evaluation data source, and validate everything against live sessions.

Purpose: Prestep P0 (real transcript capture) is the hard gate blocking Plans 03-12. This plan also establishes the hook infrastructure for all downstream evaluation.
Output: 3+ real transcripts, 6 validated hooks, TranscriptParser extraction method, hook golden files.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/3.1c-reasoning-evaluation-framework/3.1c-CONTEXT.md (section: "Plan 02: Real Transcript Capture & Hook Infrastructure")
@.planning/phases/3.1c-reasoning-evaluation-framework/3.1c-CONTEXT.md (section: "Prestep P0: Real Transcript Gate")
@.planning/phases/3.1c-reasoning-evaluation-framework/3.1c-RESEARCH.md (section: "Hook Patterns")
@.planning/phases/3.1c-reasoning-evaluation-framework/3.1c-01-SUMMARY.md
@tests/workflow_harness/lib/transcript_parser.py
@tests/workflow_harness/lib/workspace.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture 3+ real transcripts + TranscriptParser.to_observation_summary()</name>
  <files>
    tests/workflow_harness/fixtures/real_sessions/
    tests/workflow_harness/lib/transcript_parser.py
  </files>
  <action>
    **P0 DELIVERABLE (must be first — P14-IMP-03):**
    Capture 3+ real Claude Code session transcripts from distinct workflow categories:
    - 1 investigation-type (run vrs-attacker on a simple corpus contract)
    - 1 tool-type (run Slither analysis on a test contract)
    - 1 orchestration-type (run a basic audit flow)

    Use `claude -p` or interactive sessions. Store at `tests/workflow_harness/fixtures/real_sessions/`.

    **TranscriptParser.to_observation_summary() (~60 LOC, P13-IMP-08 Track A):**
    Add method producing ObservationSummary with all fields previously from passive hooks:
    - tool_counts, tool_sequences with timestamps
    - BSKG query events with timestamps and node IDs (regex: `[FCE]-\w+-\w+`)
    - Tool failures (PostToolUseFailure with `is_error: true`)
    - Agent lifecycle events
    - Satisfy GVS Data Requirements manifest: `bskg_query_events[].timestamp`, `bskg_query_events[].node_ids`, `tool_sequence_with_timestamps[]`
    - Null-safety: all list-typed fields return `[]` when not used, KNOWN_ABSENT fields return None

    **TranscriptParser.get_subagent_spawns() (~80-100 LOC, P13-IMP-11):**
    Extract subagent lifecycle from transcript Task tool calls + filesystem glob of `subagents/agent-*.jsonl`.

    **Validate:** Parser produces >0 tool calls on all 3 real transcripts. Document any structural surprises as Known Code Issues.

    **Sequence constraint:** Do NOT merge IMP-08 Track A (passive hook elimination) until this P0 completes.
  </action>
  <verify>
    <automated>cd /Volumes/ex_ssd/home/projects/python/vkg-solidity/true-vkg && ls tests/workflow_harness/fixtures/real_sessions/*.jsonl 2>/dev/null | wc -l | xargs -I{} test {} -ge 3 && echo "3+ real transcripts exist"</automated>
    <manual>Verify TranscriptParser.to_observation_summary() returns non-empty tool_counts on real transcripts</manual>
  </verify>
  <done>3+ real transcripts from distinct categories stored. TranscriptParser.to_observation_summary() produces complete ObservationSummary on real data. get_subagent_spawns() extracts lifecycle. No list-typed field is None on any real transcript.</done>
</task>

<task type="auto">
  <name>Task 2: Implement 6 essential hooks + bifurcated installation + golden files</name>
  <files>
    tests/workflow_harness/hooks/obs_session_start.py
    tests/workflow_harness/hooks/obs_precompact.py
    tests/workflow_harness/hooks/obs_session_end.py
    tests/workflow_harness/hooks/delegate_guard.py
    tests/workflow_harness/hooks/debrief_gate.py
    tests/workflow_harness/hooks/debrief_task_complete.py
    tests/workflow_harness/lib/workspace.py
    tests/workflow_harness/fixtures/hook_schema_snapshots/
  </files>
  <action>
    **Update obs_session_start.py (P13-IMP-05):** Add `source` and `agent_type` fields (~5 LOC).

    **Create obs_precompact.py (~35 LOC, P11-IMP-01/IMP-08):**
    - On `trigger == "auto"`: read transcript_path to completion (P13-ADV-1-01), extract BSKG node IDs using INLINE stdlib JSONL parsing (NO TranscriptParser import — P11-ADV-1-02), write `precompact_snapshot` observation event and `{session_id}.compacted` marker
    - On `trigger == "manual"`: exit 0, no marker
    - MUST be synchronous (P11-CSC-02), return target < 2s

    **Create obs_session_end.py (~15 LOC, P11-IMP-01):** Write `{session_id}.session_ended` marker. Observation-only, cannot block.

    **Create delegate_guard.py (~25 LOC, P15-IMP-22):**
    - PreToolUse blocking hook
    - Read config from `delegate_guard_config.yaml`: blocked_tools, blocked_patterns, allowed_reads
    - Return exit 2 on match (block), exit 0 on allow

    **Bifurcated hook installation (P11-ADV-2-01):**
    - Extend `_ensure_hook()` in workspace.py to support `"async": True` in hook entry dict via HookConfig
    - In headless mode: EvaluationRunner installs hooks before ClaudeCodeRunner.run()
    - In interactive mode: pre-session setup mandatory
    - Both paths call same install_hooks()

    **Hook health check (~15 LOC, P15-IMP-27):** Post-installation check: fire one tool call, verify hook wrote expected output.

    **Golden file snapshots (P13-ADV-2-02):** Store raw stdin JSON dumps from real sessions at `tests/workflow_harness/fixtures/hook_schema_snapshots/` for CI regression.

    **Validate against real CC sessions:** Run the 6 hooks against a real session. Confirm correct field names (especially PostToolUse uses `tool_response` NOT `tool_output` — P13-IMP-01).
  </action>
  <verify>
    <automated>cd /Volumes/ex_ssd/home/projects/python/vkg-solidity/true-vkg && uv run pytest tests/evaluation/test_observation_hooks.py -x -q && test -f tests/workflow_harness/hooks/obs_precompact.py && test -f tests/workflow_harness/hooks/delegate_guard.py && grep -c "import json" tests/workflow_harness/hooks/obs_precompact.py | xargs -I{} test {} -ge 1 && echo "No TranscriptParser import in precompact"</automated>
  </verify>
  <done>6 retained hooks implemented and validated against real sessions. obs_precompact uses stdlib only and completes within 5s. delegate_guard blocks prohibited calls. Golden file snapshots stored. Bifurcated hook installation working for headless and interactive modes. Hook health check passes.</done>
</task>

<task type="auto">
  <name>Task 3: GO/NO-GO Gate: Track B Finalization (3.1e Items 1+2)</name>
  <files>
    .vrs/experiments/plan-02/transcript-validity-report.json
    .vrs/experiments/plan-03-04/validity-matrix.json
  </files>
  <action>
    **Validity re-scoring (3.1e deferred Item 2):**
    For each transcript captured in Task 1, run validity re-scoring:
    - Verify session has > N tool calls (N defined from 3.1e specification)
    - No PreCompact truncation detected in session log
    - No cache-replay signature (check for identical tool outputs across sessions)

    Produce `.vrs/experiments/plan-02/transcript-validity-report.json` with per-transcript fields:
    `{transcript_id, verdict: 'GO'|'NO-GO', tool_call_count, precompact_detected: bool, cache_replay_detected: bool}`

    **GO/NO-GO Gate (3.1e deferred Item 1):**
    If fewer than 3 transcripts receive GO verdict, STOP — capture additional real transcripts
    before proceeding. Plans 03+ MUST NOT begin until this gate passes.

    On success, update `.vrs/experiments/plan-03-04/validity-matrix.json` with Plan 02 entry:
    `{plan: '3.1c-02', gate_timestamp: ISO8601, go_transcripts: [transcript_id...], verdict: 'GO'}`
  </action>
  <verify>
    <automated>cd /Volumes/ex_ssd/home/projects/python/vkg-solidity/true-vkg && test -f .vrs/experiments/plan-02/transcript-validity-report.json && python -c "import json; r=json.load(open('.vrs/experiments/plan-02/transcript-validity-report.json')); go=[x for x in r if x['verdict']=='GO']; assert len(go)>=3, f'Only {len(go)} GO transcripts'; print(f'{len(go)} GO transcripts confirmed')"</automated>
    <manual>Verify validity-matrix.json contains Plan 02 GO entry before proceeding to Plan 03</manual>
  </verify>
  <done>transcript-validity-report.json exists with >= 3 GO verdicts. validity-matrix.json contains Plan 02 GO entry. Track B finalization gate passed — Plans 03+ may proceed.</done>
</task>

</tasks>

<verification>
1. 3+ real transcripts exist at `tests/workflow_harness/fixtures/real_sessions/`
2. TranscriptParser.to_observation_summary() returns non-empty tool_counts on all 3
3. obs_precompact.py has zero TranscriptParser imports
4. delegate_guard.py blocks .sol Read AND permits .vrs/ Read (positive + negative test)
5. Hook golden file snapshots stored
6. All observation hook tests pass
</verification>

<success_criteria>
P0 hard gate satisfied: 3+ real transcripts, TranscriptParser validates them. 6 hooks implemented, validated against real sessions, golden files stored. Infrastructure ready for Plans 03-12.
</success_criteria>

<output>
After completion, create `.planning/phases/3.1c-reasoning-evaluation-framework/3.1c-02-SUMMARY.md`
</output>
