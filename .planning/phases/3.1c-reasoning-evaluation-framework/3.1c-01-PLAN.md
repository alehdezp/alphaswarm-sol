---
phase: 3.1c-reasoning-evaluation-framework
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/alphaswarm_sol/testing/evaluation/models.py
  - src/alphaswarm_sol/testing/evaluation/intelligence/__init__.py
  - src/alphaswarm_sol/shipping/skills/test-suite/SKILL.md
  - tests/evaluation/test_models.py
autonomous: true
requirements:
  - EVAL-01
  - EVAL-03
  - EVAL-04
  - EVAL-05
  - EVAL-07
  - EVAL-08

must_haves:
  truths:
    - "All v1 Pydantic models instantiable with validated fields"
    - "EvaluationPlugin protocol includes context kwarg (DC-3 fix)"
    - "DC-2 import boundaries verified — zero alphaswarm.kg/vulndocs imports in models.py"
    - "EvaluatorConstants.current() provides single source of truth for evaluator config"
    - "/vrs-test-suite SKILL.md stub exists with forced-eval and delegation rules"
  artifacts:
    - path: "src/alphaswarm_sol/testing/evaluation/models.py"
      provides: "All v1 Pydantic evaluation types (~30+ models)"
      contains: "ReasoningAssessment"
    - path: "src/alphaswarm_sol/shipping/skills/test-suite/SKILL.md"
      provides: "Test suite skill stub with delegation rules"
      contains: "forced-eval: true"
  key_links:
    - from: "src/alphaswarm_sol/testing/evaluation/models.py"
      to: "tests/evaluation/test_models.py"
      via: "Import and instantiation tests"
      pattern: "from alphaswarm_sol.testing.evaluation.models import"
---

<objective>
Define all Pydantic data models for the evaluation framework — the shared vocabulary consumed by every downstream plan. This includes ~30+ types covering assessment, scoring, calibration, baseline, failure, and progress tracking. Also create the /vrs-test-suite SKILL.md stub and intelligence module structure.

Purpose: Every plan 02-12 depends on these models. Wave 1 must establish the type foundation.
Output: Complete models.py with all v1 types, intelligence/ directory stubs, SKILL.md stub.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/3.1c-reasoning-evaluation-framework/3.1c-CONTEXT.md (section: "Plan 01: Assessment Data Structures")
@.planning/phases/3.1c-reasoning-evaluation-framework/3.1c-RESEARCH.md (section: "Existing Test Stubs")
@.planning/phases/3.1e-evaluation-zero-empirical-sprint/3.1e-04-SUMMARY.md (35 schema fields, Track A/B/C)
@src/alphaswarm_sol/testing/evaluation/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete v1 Pydantic models + EvaluatorConstants + field stability tiers</name>
  <files>
    src/alphaswarm_sol/testing/evaluation/models.py
    src/alphaswarm_sol/testing/evaluation/intelligence/__init__.py
  </files>
  <action>
    Extend models.py with all v1 types from CONTEXT.md Plan 01 deliverables 1-14. Key models:

    **Core Assessment:**
    - `ReasoningAssessment` with 7-move decomposition (HYPOTHESIS_FORMATION through SELF_CRITIQUE)
    - `EvaluatorDisagreement` with `B_score_pre_exposure`, `debate_completed`, `resolved_by`
    - `ImprovementHint` with `calibration_status`, `dimension`, `hypothesis`, `kill_criterion`
    - `MetaEvaluationResult` for dual-evaluator protocol

    **Calibration:**
    - `CalibrationConfig` (inputs: evaluator_model, effort_level, debate_enabled, evaluator_prompt_hash, status, temperature_controllable)
    - `CalibrationResult` (outputs: spearman_rho, spearman_rho_ci, partial_rho_tp_only, anchor_diagnosis, consistency_class, anchor_phase)
    - `EvaluatorConstants` frozen dataclass with `.current(prompt_template_hash)` factory (P11-SYN-02). NO imports from reasoning_evaluator.py (P14-IMP-06)

    **Infrastructure:**
    - `InfrastructureFailureType` as Literal (not Enum, for Pydantic serialization)
    - `FailureSignal` with signal_type and detail
    - `SessionValidityManifest` with session_id, valid, reasons
    - `ObservationDataQuality` with serialize_errors, cross_session_records_dropped, stale_files_excluded, degraded
    - `INFRA_TO_SIGNAL` mapping dict

    **Detection + Ground Truth:**
    - `DetectionSummary` with tp/fp/fn counts and precision
    - `GroundTruthEntry` and `NormalizedFinding` (tier: Literal["A","B","C","uncatalogued"])
    - `GroundTruthComparison` for ground truth comparison

    **Progress + Reporting:**
    - `EvaluationProgress` with per-workflow status tracking
    - `SuiteExitReport` model
    - `FailureReport` with recommendation enum
    - `ExperimentLedgerEntry` with decision and abandon_reason fields

    **Scoring updates:**
    - `DimensionScore` gains `applicable: bool = True`
    - `PluginScore` gains `applicable: bool = True`
    - `ScoreCard.effective_score()` method excluding non-applicable dimensions (returns None when all N/A)
    - `EvaluationResult` gains all new fields: improvement_hints, run_mode (DC-1), evaluator_disagreements, detection_summary, failure_signals, infrastructure_failure_type, scoring_cost_usd, baseline_update_status, data_quality_warning

    **Baseline:**
    - `BaselineKey` with workflow_id, run_mode, debate_enabled, effort_level, scoring_denominator_version

    **Field Stability (3.1e Plan 04):**
    - Track A (17 fields): Implement as specified, VALIDITY_CLAIM
    - Track B (7 fields): Tag `[pending-real-transcript-validation]`, PROVISIONAL
    - Track C (11 fields): Tag `[structural-proxy]`, prefix with `heuristic_`
    - Apply layer-qualified naming: `heuristic_` vs `llm_` prefixes

    **DebriefResponse updates:**
    - Add `compacted: bool = False` (P11-IMP-04)
    - Add `delivery_status: Literal["delivered", "phantom", "not_attempted"]` (P15-IMP-21)

    **EvaluationPlugin protocol fix (DC-3):**
    - Add `context: dict[str, Any] | None = None` parameter to `score()` method
    - Verify GraphValueScorer compatibility

    **Intelligence stubs:**
    - Create `evaluation/intelligence/__init__.py` with `get_active_modules(store) -> list[str]`
    - Create 3 module stubs: `tier_manager.py`, `contract_healer.py`, `coverage_radar.py`
    - Each exports `is_active(store: EvaluationStoreProtocol) -> bool`

    **DC-2 enforcement:** Zero imports from alphaswarm_sol.kg or alphaswarm_sol.vulndocs in models.py.
    Check overlap with existing trajectory dataclasses in `trajectory/evaluator.py` — keep separate (Pydantic for new, dataclass for existing).

    **Construct disambiguation (3.1e deferred Item 5):**
    Implement `disambiguation_check()` as a classmethod on the base dimension Pydantic model.
    The method accepts a registry of all registered dimensions and raises `ConstructAmbiguityError`
    if this dimension's `behavioral_signature` overlaps with another registered dimension's signature
    by more than a defined threshold. This is a schema pre-condition: Plan 06 Task 1 must call
    `disambiguation_check()` on each registered dimension before the registry is finalized.

    **Self-evaluation guard (3.1e deferred Item 4):**
    Add `self_evaluation_guard: bool = True` field to EvaluationConfig. Add a validator that raises
    `ValueError` if the evaluator `session_id` matches the evaluated `session_id`. This resolves the
    "Plan 01 or 09" assignment ambiguity — Plan 01 owns this at the model layer (earliest enforcement).
  </action>
  <verify>
    <automated>cd /Volumes/ex_ssd/home/projects/python/vkg-solidity/true-vkg && uv run python -c "from alphaswarm_sol.testing.evaluation.models import ReasoningAssessment, CalibrationConfig, CalibrationResult, EvaluatorConstants, BaselineKey, SessionValidityManifest, EvaluationProgress, SuiteExitReport, FailureSignal, InfrastructureFailureType, DetectionSummary, GroundTruthEntry, NormalizedFinding, ExperimentLedgerEntry, ImprovementHint, EvaluatorDisagreement; print('All models importable')" && grep -c "from alphaswarm_sol.kg\|from alphaswarm_sol.vulndocs" src/alphaswarm_sol/testing/evaluation/models.py; echo "DC-2 check: expect 0"</automated>
    <manual>Verify effective_score() returns None when all dimensions are applicable=False</manual>
  </verify>
  <done>All ~30+ v1 Pydantic models instantiable. EvaluatorConstants.current() works without importing reasoning_evaluator. DC-2 import boundaries verified (zero matches). Field stability tiers applied with correct prefixes. disambiguation_check() callable on all dimension models with at least one pytest case raising ConstructAmbiguityError on a deliberately overlapping dimension pair. EvaluationConfig rejects self-evaluation (evaluator_session == evaluated_session raises ValueError) with a test case confirming rejection.</done>
</task>

<task type="auto">
  <name>Task 2: SKILL.md stub + SuiteExitReport schema + model tests</name>
  <files>
    src/alphaswarm_sol/shipping/skills/test-suite/SKILL.md
    .vrs/schemas/test_suite_exit_report.schema.json
    tests/evaluation/test_models.py
  </files>
  <action>
    **SKILL.md stub (P11-IMP-13, P11-IMP-12, P11-CSC-01):**
    Create `src/alphaswarm_sol/shipping/skills/test-suite/SKILL.md` with:
    - `forced-eval: true` in frontmatter
    - Skill purpose: orchestrate evaluation suite across tiers
    - Input parameters with types/defaults (tier, category, workflow_ids, dry_run, debate)
    - Exit report schema reference
    - Per-workflow loop pseudocode (6-8 steps)
    - `## Delegation Rules` section with prohibited categories:
      - PROHIBITED: `alphaswarm query`, `alphaswarm build-kg`, Read on `.sol` files, Write to contract paths
      - ALLOWED: `src/alphaswarm_sol/testing/evaluation/contracts/*.yaml`, `.vrs/evaluations/*`, `.planning/testing/scenarios/**`
    - TODO markers for Wave 2-5 decisions: `[TO-FILL: Plan 09]`

    **SuiteExitReport JSON Schema:**
    Create `.vrs/schemas/test_suite_exit_report.schema.json` matching SuiteExitReport Pydantic model.

    **Model tests:**
    Extend `tests/evaluation/test_models.py` with tests for all new models:
    - Instantiation tests for all ~30+ types
    - `effective_score()` tests: normal case, all-N/A case (returns None), mixed applicable
    - `EvaluationPlugin` PEP 544 trap test: compliant plugin succeeds, non-compliant raises TypeError with `context={}`
    - `ExperimentLedgerEntry` round-trip test: `.model_dump_json()` → reload → equal
    - `EvaluatorConstants.current()` test with SHA256 hash
    - `CalibrationConfig` / `CalibrationResult` split verification
    - DC-2 grep checks as test assertions
  </action>
  <verify>
    <automated>cd /Volumes/ex_ssd/home/projects/python/vkg-solidity/true-vkg && uv run pytest tests/evaluation/test_models.py -x -q && test -f src/alphaswarm_sol/shipping/skills/test-suite/SKILL.md && grep "forced-eval: true" src/alphaswarm_sol/shipping/skills/test-suite/SKILL.md</automated>
  </verify>
  <done>SKILL.md stub exists with forced-eval and delegation rules. SuiteExitReport schema validates synthetic exit report. All model tests pass including DC-2/DC-3 enforcement, effective_score edge cases, and EvaluationPlugin protocol compliance.</done>
</task>

</tasks>

<verification>
1. `uv run pytest tests/evaluation/test_models.py -x` — all pass
2. `grep -r 'from alphaswarm_sol.kg\|from alphaswarm_sol.vulndocs' src/alphaswarm_sol/testing/evaluation/models.py` — zero matches (DC-2)
3. `grep -r 'from alphaswarm_sol.kg\|from alphaswarm_sol.vulndocs' src/alphaswarm_sol/testing/evaluation/intelligence/` — zero matches (DC-2)
4. SKILL.md exists with `forced-eval: true` and Delegation Rules section
5. `EvaluatorConstants.current()` takes `prompt_template_hash` as parameter; zero imports from `reasoning_evaluator.py` in `models.py`
</verification>

<success_criteria>
All v1 Pydantic evaluation types instantiable. DC-1/DC-2/DC-3 satisfied. EvaluationPlugin has context kwarg with unit test. EvaluatorConstants provides single source of truth. SKILL.md stub created. Field stability tiers from 3.1e applied.
</success_criteria>

<output>
After completion, create `.planning/phases/3.1c-reasoning-evaluation-framework/3.1c-01-SUMMARY.md`
</output>
