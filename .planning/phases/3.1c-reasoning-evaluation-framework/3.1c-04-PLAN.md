---
phase: 3.1c-reasoning-evaluation-framework
plan: 04
type: execute
wave: 3
depends_on: ["3.1c-02", "3.1c-03"]
files_modified:
  - tests/workflow_harness/graders/graph_value_scorer.py
  - tests/evaluation/test_graph_value_scorer.py
autonomous: true
requirements:
  - EVAL-04

must_haves:
  truths:
    - "GVS uses TranscriptParser primary path for citation rate"
    - "_check_graph_first() uses observation timestamps, not tool name guessing"
    - "Keyword-soup fallback replaced with BSKG node ID structural matching"
    - "GVS returns applicable=False when contract says run_gvs: false"
    - "PreCompact snapshot merge preserves GVS accuracy on compacted sessions"
  artifacts:
    - path: "tests/workflow_harness/graders/graph_value_scorer.py"
      provides: "Calibrated graph value scoring with 4 dimensions"
  key_links:
    - from: "tests/workflow_harness/graders/graph_value_scorer.py"
      to: "tests/workflow_harness/lib/transcript_parser.py"
      via: "transcript.graph_citation_rate() primary path"
      pattern: "graph_citation_rate"
---

<objective>
Fix graph-first compliance check, replace keyword-soup fallback, wire TranscriptParser for citation rate, add precompact snapshot merge, and provision calibration on 3+ real transcripts.

Purpose: GVS is the mechanical scoring backbone. Without these fixes, it produces false positives (any Bash = BSKG query) and noise (keyword-soup fallback).
Output: Fixed and provisionally calibrated GraphValueScorer.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/3.1c-reasoning-evaluation-framework/3.1c-CONTEXT.md (section: "Plan 04: Graph Value Scorer")
@.planning/phases/3.1c-reasoning-evaluation-framework/3.1c-03-SUMMARY.md
@tests/workflow_harness/graders/graph_value_scorer.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix graph-first compliance + citation fallback + precompact merge</name>
  <files>
    tests/workflow_harness/graders/graph_value_scorer.py
  </files>
  <action>
    **Fix _check_graph_first (P13-IMP-19, ~15 LOC):**
    Current bug: `tool_sequence` is `list[str]` with tool names only — any Bash scores as graph-first compliant (30% GVS weight). Refactor:
    - Add optional `obs_summary` parameter to `_check_graph_first()`
    - When obs_summary has `bskg_query` events, use timestamps for precise ordering
    - When absent, conservative `False`
    - Three test cases: (a) BSKG before Read, (b) non-BSKG Bash before Read, (c) Read before BSKG

    **Graduated graph_first_score (P13-ADV-3-02, ~20 LOC):**
    Add `graph_first_score: float = 0.0` to GraphValueScore — proportion of Read/Bash calls preceded by at least one BSKG query. Binary `graph_first_compliant` preserved. Graduated score fed to Plan 07 evaluator as context.

    **Replace citation rate keyword-soup fallback (P13-ADV-3-01):**
    Current lines 88-96: regex `node:|edge:|graph:|BSKG|build-kg` matches English prose. Replace with:
    - PRIMARY: `transcript.graph_citation_rate()` — unchanged
    - FALLBACK: structural matching using BSKG node IDs (`[FCE]-\w+-\w+` pattern) from observation data. Cross-reference against conclusion text. ~30 LOC.

    **Graph-Reasoning Coherence crash fix:**
    `_check_citation_relevance()` must begin with:
    `if context is None or context.get('ground_truth_entry') is None: return DimensionScore(name='graph_reasoning_coherence', applicable=False)` (~3 LOC)

    **run_gvs: false guard (~5 LOC):**
    When contract says `run_gvs: false`, return GraphValueScore with all dimensions `applicable=False`. No transcript parsing.

    **PreCompact snapshot merge (P11-IMP-08, ~20 LOC):**
    Before computing citation_rate, check for `precompact_snapshot` in observations. If present, merge pre-compaction node inventory with post-compaction data in `_get_node_inventory()`.

    **PostToolUseFailure handling (CSC-01, ~10 LOC):**
    When PostToolUseFailure for `alphaswarm query`: count toward graph-first compliance (agent tried) but reduce result_utilization score (no result).

    **DEFAULT-30 fix (P15-IMP-32):**
    `exploit_path_construction`, `arbitration_quality`, `evidence_weighing`, `investigation_depth` currently score DEFAULT-30. Change to return `DimensionScore(applicable=False)`. BREAKING CHANGE: must be followed by baseline reset.

    **3.1e permission gate:** If Plan 03/04 shows only query_coverage has correlation >0.3 while others <0.1, reduce to single-proxy (per P15-ADV-2-01). Check `.vrs/experiments/plan-03-04/validity-matrix.json`.
  </action>
  <verify>
    <automated>cd . && uv run pytest tests/evaluation/test_graph_value_scorer.py -x -q</automated>
  </verify>
  <done>Graph-first uses timestamps not tool names. Keyword-soup replaced with node ID matching. PreCompact merge works. Non-corpus contracts get applicable=False. DEFAULT-30 dimensions return applicable=False.</done>
</task>

<task type="auto">
  <name>Task 2: Provisional calibration on real transcripts + updated tests</name>
  <files>
    tests/evaluation/test_graph_value_scorer.py
  </files>
  <action>
    **Provisional calibration (Wave 3):**
    Use 3+ real transcripts from Plan 02 to verify threshold separability:
    - Run GVS on each real transcript
    - Document whether the scoring produces meaningful spread
    - This is provisional — full GVS Weight Calibration (66 combos x 10 transcripts) is Wave 6

    **Updated unit tests:**
    - graph_first: (a) BSKG before Read=True, (b) non-BSKG Bash before Read=False, (c) Read before BSKG=False
    - Citation rate: primary path with TranscriptParser, fallback with node ID matching
    - run_gvs=false produces all applicable=False
    - PreCompact merge: compacted session uses merged inventory
    - Non-corpus contract: applicable=False for graph_reasoning_coherence
    - PostToolUseFailure: counted for compliance, reduced utilization
    - DEFAULT-30 dimensions: return applicable=False
    - graduated graph_first_score: proportion calculation correct

    **GVS integration test:**
    GVS passes with TranscriptParser-sourced data (P13-SYN-01 manifest satisfied).
  </action>
  <verify>
    <automated>cd . && uv run pytest tests/evaluation/test_graph_value_scorer.py -x -q -v 2>&1 | tail -5</automated>
  </verify>
  <done>GVS provisionally calibrated on 3+ real transcripts. All test cases pass including graph-first fix, citation fallback, precompact merge, and DEFAULT-30 fix.</done>
</task>

</tasks>

<verification>
1. All GVS tests pass
2. _check_graph_first correctly returns False for non-BSKG Bash before Read
3. Citation rate does not match on English prose ("graph:" in documentation text)
4. GVS integration test passes with TranscriptParser data
5. Compacted sessions use merged inventory
</verification>

<success_criteria>
GVS produces accurate mechanical scores. Graph-first compliance uses timestamps. Citation rate uses node IDs not keywords. Provisional calibration shows separability on real data.
</success_criteria>

<output>
After completion, create `.planning/phases/3.1c-reasoning-evaluation-framework/3.1c-04-SUMMARY.md`
</output>
