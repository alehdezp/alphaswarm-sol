---
phase: 3.1c-reasoning-evaluation-framework
plan: 03
type: execute
wave: 2b
# NOTE: Within Wave 2, Plan 03 is sequentially AFTER Plan 02. Do not start Plan 03 until
# Plan 02 is fully ALIGNED (including Task 3 GO/NO-GO gate). Plan 03 requires real transcripts
# with tool_use_id hook payloads produced by Plan 02's hooks — pre-Plan-02-hook transcripts
# do not qualify. The depends_on field enforces the ordering; "2b" clarifies intra-wave sequence.
depends_on: ["3.1c-01", "3.1c-02"]
files_modified:
  - tests/workflow_harness/lib/observation_parser.py
  - tests/evaluation/test_observation_parser.py
autonomous: true
requirements:
  - EVAL-01
  - EVAL-04

must_haves:
  truths:
    - "ObservationParser is a thin adapter over TranscriptParser (~40 LOC)"
    - "LIFO pairing replaced with tool_use_id-based pairing"
    - "Session ID filter provides O(1) lookup"
    - "Parse real transcripts with zero errors"
  artifacts:
    - path: "tests/workflow_harness/lib/observation_parser.py"
      provides: "Thin adapter over TranscriptParser with ObservationSummary interface"
  key_links:
    - from: "tests/workflow_harness/lib/observation_parser.py"
      to: "tests/workflow_harness/lib/transcript_parser.py"
      via: "Adapter delegation to to_observation_summary()"
      pattern: "TranscriptParser"
---

<objective>
Redefine ObservationParser as a thin adapter over TranscriptParser (~40 LOC) with preserved interface, fix LIFO pairing with tool_use_id, add session_id filtering, and validate on real transcripts.

Purpose: Plans 04, 07, 08 consume parsed observation data. This adapter ensures a single data source (TranscriptParser) while preserving the ObservationSummary interface.
Output: Refactored observation_parser.py, validated on real transcripts from Plan 02.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/3.1c-reasoning-evaluation-framework/3.1c-CONTEXT.md (section: "Plan 03: ObservationParser Adapter")
@.planning/phases/3.1c-reasoning-evaluation-framework/3.1c-02-SUMMARY.md
@tests/workflow_harness/lib/observation_parser.py
@tests/workflow_harness/lib/transcript_parser.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor ObservationParser as TranscriptParser adapter + session_id filter</name>
  <files>
    tests/workflow_harness/lib/observation_parser.py
  </files>
  <action>
    **Prerequisite check (first action — IMP-08):**
    Confirm `hooks/transcript_parser.py` exists and `TranscriptParser.to_observation_summary()` is
    implemented (Plan 02 Task 1 output). Confirm at least one real session exists in
    `fixtures/real_sessions/` AND that at least one session file contains `tool_use_id` fields
    in hook payload JSON (Plan 02 Task 3 GO gate must have passed). If any condition is missing, STOP.

    **Adapter refactor (P13-IMP-12):**
    Redefine ObservationParser as thin adapter over TranscriptParser (~40 LOC):
    - Constructor gains `session_id: str | None = None` parameter (P13-IMP-07)
    - When session_id provided, `_load()` reads only `{session_id}.jsonl` — O(1) lookup
    - Validate derived session_id against constructor parameter; mismatch adds to `parse_errors`
    - Add `warnings.warn()` to no-session-id constructor path
    - Delegate to `TranscriptParser.to_observation_summary()` for data extraction

    **LIFO replacement (Plan 02 deliverable):**
    Replace LIFO stack pairing at line 161 with `tool_use_id`-based pairing.
    Use typed `ToolUseData`/`ToolResultData` submodels when present.
    Fall back to LIFO only when tool_use_id absent (with warning, not crash).

    **Data quality (SYN-01):**
    `parse()` returns `ObservationSummary` with `data_quality: ObservationDataQuality` populated from writer counter, session filter, and staleness guard. ~10 LOC.

    **Tool failure handling (CSC-01):**
    Add `_handle_tool_failure(record)` branch for `PostToolUseFailure` event type.
    Populate `ObservationSummary.tool_failures: list[ToolFailureRecord]`. ~15 LOC.

    **PreCompact snapshot (P11-IMP-08):**
    Add `precompact_snapshot` event type to ObservationRecord.event_type enum and parser (~5 LOC).

    **Staleness guard:**
    Before evaluation, check file mtime against session start time. Files older than `max_staleness_seconds` (default 3600) produce WARNING and skip.

    **Atomic migration (P13-ADV-6-01):**
    ALL ObservationParser() call sites (~4) must be updated in same commit.
    Run `grep -r "ObservationParser(" tests/ src/` to enumerate call sites first.

    **Null-safety (P15-ADV-5-01):**
    `len(obs.bskg_query_events) == 0` uses `applicable=False`, not None check.
    Uncatalogued patterns: `normalize()` returns `tier='uncatalogued'`, not None or raise.
  </action>
  <verify>
    <automated>cd /Volumes/ex_ssd/home/projects/python/vkg-solidity/true-vkg && uv run pytest tests/evaluation/test_observation_parser.py -x -q</automated>
  </verify>
  <done>ObservationParser is ~40 LOC adapter over TranscriptParser. LIFO replaced with tool_use_id pairing. Session ID filter provides O(1) lookup. All call sites updated atomically. Data quality, tool failures, precompact snapshot, and staleness guard implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Validate adapter on real transcripts + GroundTruthAdapter</name>
  <files>
    tests/evaluation/test_observation_parser.py
  </files>
  <action>
    **Real transcript validation (IMP-02):**
    Before running validation, confirm that the transcript source directory contains at least one file
    with `tool_use_id` in its hook payload JSON (confirming Plan 02 hooks are installed, not pre-hook
    transcripts). Log the source directory and one example payload as evidence. If no tool_use_id-
    containing transcripts exist, STOP — Plan 02 Task 2 hooks are not yet installed.

    Test ObservationParser on validated real transcripts from Plan 02:
    - Parse each real transcript with zero parse_errors
    - Verify tool_use_id pairing works on sessions with parallel tool calls
    - Verify session_id filter returns only matching session (unit test with two .jsonl files)

    **GroundTruthAdapter validation (ADV-02):**
    Validate GroundTruthAdapter.normalize() produces correct matches on 3 known ground truth entries.
    Test that _is_match() works with the adapter's {contract, function, line_range} format.
    Verify uncatalogued patterns produce tier='uncatalogued' (P15-IMP-28).

    **Update existing tests:**
    The 17 existing test functions in test_observation_parser.py become adapter behavior tests.
    Add new tests for:
    - session_id filtering (two files, one matching)
    - data quality population
    - tool failure handling
    - precompact snapshot parsing
    - staleness guard
  </action>
  <verify>
    <automated>cd /Volumes/ex_ssd/home/projects/python/vkg-solidity/true-vkg && uv run pytest tests/evaluation/test_observation_parser.py -x -q -v 2>&1 | tail -5</automated>
  </verify>
  <done>Adapter validated on real transcripts produced by Plan 02's hook implementation, confirmed via log inspection to contain tool_use_id fields in hook payload JSON — synthetic fixtures and pre-Plan-02-hook transcripts do not qualify. GroundTruthAdapter matches correctly. All adapter tests pass including edge cases.</done>
</task>

</tasks>

<verification>
1. ObservationParser parses real transcripts with zero errors
2. tool_use_id pairing works on parallel tool call sessions
3. Session ID filter test passes
4. GroundTruthAdapter.normalize() produces correct results on 3 known entries
5. All observation parser tests pass
</verification>

<success_criteria>
ObservationParser is a clean adapter. Real transcripts parse without errors. Tool pairing uses tool_use_id. All downstream consumers (Plans 04, 07, 08) can use the same ObservationSummary interface.
</success_criteria>

<output>
After completion, create `.planning/phases/3.1c-reasoning-evaluation-framework/3.1c-03-SUMMARY.md`
</output>
