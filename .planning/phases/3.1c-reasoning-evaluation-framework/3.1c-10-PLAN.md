---
phase: 3.1c-reasoning-evaluation-framework
plan: 10
type: execute
wave: 6
depends_on: ["3.1c-08", "3.1c-09"]
# NOTE: Sequential within Wave 6. BaselineManager and progress.json are shared with Plans 09
# and 11 — concurrent writes will corrupt baseline data. Do not start Plan 10 until Plan 09
# is fully ALIGNED with REAL Agent Teams data (not simulated).
files_modified:
  - tests/workflow_harness/test_agent_evaluation.py
  - .vrs/evaluations/progress.json
autonomous: false
requirements:
  - EVAL-06
  - EVAL-12

must_haves:
  truths:
    - "CC skill writes conformant exit report for all 21 agents"
    - "Core tier ~10 agents pass behavioral + reasoning evaluation with REAL Agent Teams (not simulated)"
    - "Joint HITL gate (Plans 09a + 10a) must pass before Important sub-waves"
    - "Ground truth comparison using GroundTruthAdapter with per-tier accuracy"
    - "Core agents include debrief + meta-evaluation from REAL interactive sessions"
    - "RunMode.SIMULATED is PROHIBITED for Core and Important agent evaluations"
    - "Each Core agent evaluation produces non-empty JSONL transcript with real tool_use events"
  artifacts:
    - path: "tests/workflow_harness/test_agent_evaluation.py"
      provides: "Thin pytest wrappers asserting on agent exit reports with anti-simulation guards"
  key_links:
    - from: "tests/workflow_harness/test_agent_evaluation.py"
      to: "src/alphaswarm_sol/shipping/skills/test-suite/SKILL.md"
      via: "Shared CC skill for test orchestration"
      pattern: "vrs-test-suite"
---

<objective>
Test all 21 shipped agents with behavioral + reasoning evaluation using REAL Agent Teams.
Shares /vrs-test-suite CC skill with Plan 09. Staged rollout: Core ~10 with HITL gate -> Important.

**CRITICAL CONSTRAINT — TOP-LEVEL SESSION REQUIRED:**
This plan MUST execute from a top-level Claude Code session, NOT from a subagent executor.
Agent Teams spawning via Task tool cannot work from within subagent contexts.

**ANTI-SIMULATION REQUIREMENT:**
Core agents MUST be evaluated with RunMode.INTERACTIVE (real dual-Opus reasoning evaluation).
Important agents MUST use RunMode.HEADLESS (real single-evaluator).
RunMode.SIMULATED is PROHIBITED for any agent contributing to baseline.

Purpose: Agent evaluation provides behavioral scores for the baseline. Combined with Plan 09,
covers the full shipped workflow surface.
Output: All 21 agent evaluations with conformant exit reports, joint Core HITL gate passed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/3.1c-reasoning-evaluation-framework/3.1c-CONTEXT.md (section: "Plan 10: Agent Evaluation")
@.planning/phases/3.1c-reasoning-evaluation-framework/3.1c-CONTEXT.md (section: "Shared Wave 6 Execution Note")
@.planning/phases/3.1c-reasoning-evaluation-framework/3.1c-09-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute Core agent evaluations (~10 agents) with REAL Agent Teams + debrief</name>
  <files>
    .vrs/evaluations/progress.json
    .vrs/observations/plan10/
  </files>
  <action>
    **PREREQUISITE CHECK — Plan 09 REAL execution verified:**
    Before starting, verify Plan 09 was executed with REAL Agent Teams:
    1. Check `.vrs/evaluations/progress.json` has Plan 09 entries with run_mode != "SIMULATED"
    2. Check Plan 09 JSONL transcripts exist and are non-empty
    3. If Plan 09 used RunMode.SIMULATED, STOP — Plan 09 must be re-executed first

    **Core agent sub-wave (a) — REAL Agent Teams spawning:**
    Execute via `/vrs-test-suite --tier core --category agent` or equivalent.
    Core agents (~10): vrs-attacker, vrs-defender, vrs-verifier, vrs-pattern-* agents,
    vrs-integrator, vrs-finding-merger.

    **CONCRETE AGENT TEAMS SPAWNING FOR EACH CORE AGENT:**
    Each Core agent evaluation MUST spawn a REAL subagent via Task tool:

    ```
    # Example: Evaluate vrs-attacker agent
    Task(
      subagent_type="general-purpose",
      name="eval-vrs-attacker",
      prompt="You are the vrs-attacker agent under evaluation.
        Read .claude/agents/vrs-attacker.md for your role definition.
        Target: {evaluation_contract_target}
        Execute the investigation workflow:
        1. Build BSKG: uv run alphaswarm build-kg {contract_path}
        2. Query for vulnerability patterns
        3. Construct exploit paths with graph node ID evidence
        4. Write structured finding with behavioral signature
        Debrief: After investigation, answer evaluation debrief questions.",
      description="Agent eval: vrs-attacker"
    )
    ```

    Each Core agent evaluation MUST include:
    - Full capability contract check
    - Reasoning evaluation via dual-Opus evaluator (RunMode.INTERACTIVE)
    - Debrief collection: agent answers structured evaluation questions
    - Meta-evaluation: two independent evaluators score the transcript
    - Non-empty JSONL transcript with real tool_use events

    **ANTI-SIMULATION VERIFICATION per agent:**
    After each Core agent evaluation completes:
    1. Verify JSONL transcript exists and is non-empty
    2. Verify transcript contains real tool_use events (alphaswarm commands, Read calls)
    3. Verify evaluation result has run_mode = "INTERACTIVE" (not "SIMULATED")
    4. Verify reasoning_score is populated (not null)
    5. Verify debrief artifact exists and contains real agent responses

    **Ground truth comparison (ADV-02):**
    Run `GroundTruthComparison` using `GroundTruthAdapter` where ground truth available.
    Per-tier accuracy tracking.

    **Plan-scoped observation directories:**
    `.vrs/observations/plan10/` for all agent evaluation data.

    **Intra-stage failure policy:**
    Try/continue, .failed.json for failures, no silent gaps.
    Circuit breaker: 3 consecutive identical infrastructure_failure_type -> HALT.

    **Budget:**
    Per-workflow: 20-45 min (non-debate), 30-60 min (debate).
  </action>
  <verify>
    <automated>cd /Volumes/ex_ssd/home/projects/python/vkg-solidity/true-vkg && ls .vrs/observations/plan10/ 2>/dev/null | wc -l | xargs -I{} test {} -ge 1 && echo "Plan 10 observations exist"</automated>
    <manual>For each Core agent: verify (1) non-empty JSONL transcript, (2) run_mode=INTERACTIVE, (3) reasoning_score populated, (4) debrief artifact present</manual>
  </verify>
  <done>Core agents (~10) evaluated with REAL Agent Teams. Each has non-empty JSONL transcript, populated reasoning scores, debrief artifacts, and meta-evaluation agreement. RunMode.SIMULATED not used for any Core agent.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Joint Core HITL gate — Plans 09a (Core skills) and 10a (Core agents) results from REAL Agent Teams</what-built>
  <how-to-verify>
    1. Review progress.json — both Plan 09 Core and Plan 10 Core sections complete
    2. Verify >= 8 of 10 Core skills AND >= 8 of 10 Core agents pass capability check
    3. ANTI-SIMULATION CHECK: Verify run_mode != "SIMULATED" for ALL Core evaluations in both plans
    4. Verify JSONL transcripts are non-empty for Core evaluations (not fabricated)
    5. Review meta-evaluation agreement rates for Core agents (expect > 80%)
    6. Check debrief artifacts written for Core agents — non-empty with real content
    7. Review ground truth comparison accuracy by tier
    8. Gate passage required: BOTH 09a and 10a complete with REAL data before any Important sub-wave
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to Important sub-waves, or describe issues</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Important agent sub-wave + thin pytest wrappers with anti-simulation guards</name>
  <files>
    tests/workflow_harness/test_agent_evaluation.py
  </files>
  <action>
    **Sub-wave (b) Important tier (~11 agents):**
    Execute with spot-check gate. MUST use RunMode.HEADLESS (not SIMULATED).
    Each spawns a REAL Agent Teams subagent via Task tool.

    **Thin pytest wrappers with ANTI-SIMULATION GUARDS:**
    `tests/workflow_harness/test_agent_evaluation.py` must assert:
    - All 21 agents have exit report or .failed.json
    - Core agents pass capability check
    - Delegate mode contamination check
    - Meta-evaluation agreement > 80% for Core

    **NEW ANTI-SIMULATION TESTS (mandatory):**
    - `test_core_agents_not_simulated`: For each Core agent in progress.json,
      verify run_mode != "SIMULATED". Fail if any Core agent used simulated mode.
    - `test_core_agents_have_real_transcripts`: Verify non-empty JSONL transcript
      exists for each Core agent evaluation.
    - `test_core_agents_have_debrief`: Verify debrief artifact exists and is non-empty
      for each Core agent (not just capability check).
    - `test_core_reasoning_scores_populated`: Verify reasoning_score is not null for
      each Core agent evaluation.
    - `test_meta_evaluation_not_fabricated`: Verify meta-evaluation agreement is not
      100% identical for all agents (anti-fabrication: identical scores = suspicious).

    **Standard-tier baseline exclusion (P15-CSC-02):**
    Standard-tier stub agent contracts evaluated for coverage only — no baseline contribution.
  </action>
  <verify>
    <automated>cd /Volumes/ex_ssd/home/projects/python/vkg-solidity/true-vkg && uv run pytest tests/workflow_harness/test_agent_evaluation.py -x -q</automated>
  </verify>
  <done>All 21 agents have exit reports from REAL Agent Teams. Core agents have populated reasoning scores, debrief artifacts, and meta-evaluation. Anti-simulation guards pass. Joint Core HITL gate passed.</done>
</task>

</tasks>

<verification>
1. CC skill writes conformant exit report for all 21 agents
2. Core agents: >= 8 of 10 pass capability check with REAL reasoning evaluation
3. Joint HITL gate: both 09a and 10a pass with REAL data (not simulated)
4. Meta-evaluation agreement > 80% for Core agents
5. Ground truth comparison with per-tier accuracy tracking
6. No silent gaps — all agents have exit report or .failed.json
7. RunMode.SIMULATED NOT used for Core or Important evaluations
8. Anti-simulation pytest guards pass
9. Non-empty JSONL transcripts exist for all Core agent evaluations
</verification>

<success_criteria>
21 agent evaluations complete with conformant exit reports from REAL Agent Teams.
Core agents have populated reasoning scores, debrief artifacts, and meta-evaluation from
REAL interactive sessions (not simulated). Anti-simulation guards verify no fake data.
Joint Core HITL gate passed with REAL data from both Plans 09 and 10.
</success_criteria>

<output>
After completion, create `.planning/phases/3.1c-reasoning-evaluation-framework/3.1c-10-SUMMARY.md`
</output>
