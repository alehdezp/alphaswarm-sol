---
phase: 3.1c-reasoning-evaluation-framework
plan: 06
type: execute
wave: 1
depends_on: ["3.1c-01"]
# NOTE: Wave 1 execution is SEQUENTIAL not parallel. Plan 06 MUST start after Plan 01 Task 2
# is complete (models.py exists with ReasoningAssessment, CalibrationConfig, EvaluatorConstants).
# The CONTEXT.md Wave 1 "zero dependencies" label refers to upstream phase dependencies only,
# not intra-wave ordering. Plan 06 Task 1 imports and validates Plan 01's Pydantic models.
files_modified:
  - src/alphaswarm_sol/testing/evaluation/contracts/
  - .vrs/testing/schemas/evaluation_contract.schema.json
  - src/alphaswarm_sol/testing/evaluation/contracts/dimension_registry.yaml
  - src/alphaswarm_sol/testing/evaluation/contract_loader.py
  - scripts/migrate_contracts.py
autonomous: true
requirements:
  - EVAL-05

must_haves:
  truths:
    - "51 evaluation contract YAML files exist (10 existing + 41 new)"
    - "Core tier (~10) hand-authored with ground_truth_rubric"
    - "Schema hardening: evaluation_config required with 4 sub-fields"
    - "Dimension registry with 27+ domain dimensions"
    - "Process/Outcome boundary audit passes: zero detection-outcome violations"
  artifacts:
    - path: "src/alphaswarm_sol/testing/evaluation/contracts/"
      provides: "51 per-workflow evaluation contracts"
    - path: "src/alphaswarm_sol/testing/evaluation/contracts/dimension_registry.yaml"
      provides: "Canonical dimension registry for 27+ dimensions"
    - path: ".vrs/testing/schemas/evaluation_contract.schema.json"
      provides: "Hardened evaluation contract schema"
  key_links:
    - from: "src/alphaswarm_sol/testing/evaluation/contracts/"
      to: ".vrs/testing/schemas/evaluation_contract.schema.json"
      via: "JSON Schema validation"
      pattern: "validate_contract"
---

<objective>
Author 41 remaining evaluation contracts (51 total), harden the schema, create dimension registry, implement debrief-mode validator, and run Process/Outcome boundary audit.

Purpose: Every evaluation run requires a contract. Plans 07-12 depend on these contracts for smart selection, hook configuration, and dimension specification.
Output: 51 contracts, hardened schema, dimension registry, migration script.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/3.1c-reasoning-evaluation-framework/3.1c-CONTEXT.md (section: "Plan 06: Evaluation Contracts")
@.planning/phases/3.1c-reasoning-evaluation-framework/3.1c-CONTEXT.md (section: "Schema Hardening & Dimension Registry")
@.planning/phases/3.1c-reasoning-evaluation-framework/3.1c-01-SUMMARY.md
@src/alphaswarm_sol/testing/evaluation/contracts/
@.vrs/testing/schemas/evaluation_contract.schema.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema hardening + dimension registry + migration script</name>
  <files>
    .vrs/testing/schemas/evaluation_contract.schema.json
    src/alphaswarm_sol/testing/evaluation/contracts/dimension_registry.yaml
    scripts/migrate_contracts.py
    src/alphaswarm_sol/testing/evaluation/contract_loader.py
  </files>
  <action>
    **Prerequisite check (first action — IMP-07):**
    Verify `src/alphaswarm_sol/testing/evaluation/models.py` exists and contains at least
    `ReasoningAssessment`, `CalibrationConfig`, and `EvaluatorConstants` before proceeding.
    If missing, STOP — Plan 01 has not completed Task 1. Do not attempt schema hardening
    without the Pydantic model foundation from Plan 01.

    Also verify `disambiguation_check()` is callable on dimension models (Plan 01 Item 5).
    Call `disambiguation_check()` on each registered dimension before the registry is finalized.

    **Schema Hardening (atomic commit — IMP-01+ADV-01+ADV-02):**
    ALL changes in one commit:
    - Add `evaluation_config` to top-level `required` array
    - Add `required: ["run_gvs", "run_reasoning", "debrief", "depth"]` inside evaluation_config
    - Set `additionalProperties: false` at top level and inside `metadata`
    - Deprecate `metadata.evaluation_depth` in favor of `evaluation_config.depth`
    - Add ALL fields from Schema Hardening Field Manifest (CONTEXT.md lines 200-222)
    - Add `hooks` as array with enum of 14 CC hook lifecycle events
    - Add `status` enum (active, stub)
    - Add `coverage_axes`, `ground_truth_rubric`, `active_dimensions`, `debrief_layer_weight`, `debrief_wait_seconds`

    **Dimension Registry:**
    Create `contracts/dimension_registry.yaml`:
    - 7 canonical move types as enum
    - 27+ domain dimensions (graph_utilization, evidence_quality, tool_selection, etc.)
    - Each dimension: name, description, applicable move types
    - `has_handler: bool` field per dimension

    **Migration Script (~60 LOC):**
    `scripts/migrate_contracts.py` with three operations:
    (a) `insert_with_category_default()` for hooks and evaluation_config
    (b) `migrate_deprecated()` for evaluation_depth → evaluation_config.depth
    (c) `set_if_absent()` for status: active, coverage_axes: []
    One-time idempotent (run → diff → commit).

    **Debrief-mode validator (P11-ADV-4-01, ~15 LOC):**
    `_validate_debrief_mode_compatibility()`:
    (a) tier=="standard" + debrief: true → REJECT
    (b) run_mode=="headless" + debrief: true → REJECT

    **Stub contracts fix (P15-IMP-03):**
    If reasoning_dimensions: [] AND run_reasoning: true → validator rejects.
    Stub template: reasoning_dimensions: [], run_reasoning: false, status: stub.

    **validate_mapping_completeness() (~15 LOC):**
    Cross-check workflow_id against _WORKFLOW_TO_CONTRACT dict, warn on gaps.

    Retrofit all 10 existing contracts + 4 templates to new schema.
  </action>
  <verify>
    <automated>cd . && uv run python scripts/migrate_contracts.py --dry-run 2>/dev/null; uv run pytest tests/evaluation/test_contracts.py -x -q</automated>
  </verify>
  <done>Schema hardened with all required fields. Dimension registry created. Migration script works idempotently. All 14 existing files validate against updated schema. Debrief-mode validator rejects incompatible combinations.</done>
</task>

<task type="auto">
  <name>Task 2: Author 41 remaining contracts (tiered: Core → Important → Standard stubs)</name>
  <files>
    src/alphaswarm_sol/testing/evaluation/contracts/
  </files>
  <action>
    **Tiered authoring (CONTEXT.md constraint):**

    **Core tier (~10) — hand-authored with ground_truth_rubric:**
    Investigation agents (vrs-attacker, vrs-defender, vrs-verifier already exist), orchestrator-full-audit (exists), plus:
    - skill-vrs-tool-aderyn, skill-vrs-tool-mythril, skill-vrs-tool-coordinator
    - orchestrator-debate, orchestrator-verify
    Each Core contract includes:
    - All 4 evaluation_config sub-fields
    - ground_truth_rubric (reasoning process, NOT detection outcomes)
    - hooks appropriate to category
    - active_dimensions whitelist (8-12 dimensions)
    - graph_reasoning_coherence: true for investigation types

    **Important tier (~15) — stub + 1 workflow-specific check:**
    Create from templates with one workflow-specific capability check marked `TODO: validate against real run`:
    - skill-vrs-investigate, skill-vrs-bead-* (4 skills), skill-vrs-orch-spawn, skill-vrs-orch-resume
    - agent-vrs-pattern-*, agent-vrs-integrator, agent-vrs-finding-merger
    - skill-vrs-health-check
    Each has template defaults + one required check placeholder.

    **Standard tier (~26) — template-derived stubs:**
    Create from templates with status: stub, run_reasoning: false, reasoning_dimensions: []:
    - All remaining support skills (context-pack, evidence-audit, etc.)
    - All remaining agents (vulndocs agents, testing agents, etc.)
    Mark each `status: stub` with valid schema.

    **Authoring order:** Core first (as CONTEXT.md constrains).

    **Process/Outcome boundary audit (P14-IMP-09/P14-SYN-01):**
    Grep all 51 contracts for fields referencing detection outcomes used as capability gates or reasoning dimensions. Zero violations required.
    Check: no `findings_count`, `vulnerabilities_found` as capability criteria.
    Check: rubric text contains no detection-outcome phrases ('find', 'detect', 'identify the vulnerability').

    **Run New Scored Concept governance checklist** on all 27 existing dimensions before commit.
  </action>
  <verify>
    <automated>cd . && ls src/alphaswarm_sol/testing/evaluation/contracts/*.yaml | wc -l | xargs -I{} test {} -ge 51 && echo "51+ contract files exist" && uv run pytest tests/evaluation/test_contracts.py -x -q</automated>
  </verify>
  <done>51 contract files exist. Core (~10) fully tailored with rubric. Important (~15) have template + 1 check. Standard (~26) are template-derived stubs. Process/Outcome boundary audit: zero violations. All validate against hardened schema.</done>
</task>

</tasks>

<verification>
1. 51 contract YAML files exist and validate against hardened schema
2. Core-tier contracts include ground_truth_rubric with zero detection-outcome phrases
3. No contract has findings_count or vulnerabilities_found as capability criteria
4. dimension_registry.yaml has 27+ dimensions
5. Debrief-mode validator rejects standard+debrief:true combinations
6. All contract tests pass
</verification>

<success_criteria>
51 per-workflow evaluation contracts exist with tiered authoring quality. Schema hardened. Dimension registry canonical. Process/Outcome boundary enforced.
</success_criteria>

<output>
After completion, create `.planning/phases/3.1c-reasoning-evaluation-framework/3.1c-06-SUMMARY.md`
</output>
