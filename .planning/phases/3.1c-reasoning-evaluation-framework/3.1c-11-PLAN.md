---
phase: 3.1c-reasoning-evaluation-framework
plan: 11
type: execute
wave: 6
depends_on: ["3.1c-08", "3.1c-09", "3.1c-10"]
# NOTE: Sequential within Wave 6. Must start after Plan 10 is fully ALIGNED with REAL Agent Teams data.
# BaselineManager and progress.json are shared — concurrent writes corrupt baseline data.
# Requires @3.1c-10-SUMMARY.md to exist before starting.
files_modified:
  - tests/workflow_harness/test_orchestrator_evaluation.py
  - tests/workflow_harness/lib/output_collector.py
  - .vrs/evaluations/progress.json
autonomous: false
requirements:
  - EVAL-06
  - EVAL-12

must_haves:
  truths:
    - "Full 3-agent lifecycle observed: spawn, task assignment, DM exchange, shutdown"
    - "Cross-agent coherence scoring operational"
    - "Evidence fidelity metric produces non-None fidelity_score for attacker->verifier edge"
    - "At least 1 Core orchestrator test exercises full pipeline (IMP-18)"
    - "Prestep P1 multi-agent ground truth templates completed before execution"
    - "RunMode.SIMULATED is PROHIBITED for orchestrator evaluations"
    - "Each orchestrator evaluation spawns REAL Agent Teams (TeamCreate + Task) with non-empty JSONL transcripts"
    - "SendMessage DM exchange is from REAL inter-agent communication, not mock/simulated"
  artifacts:
    - path: "tests/workflow_harness/test_orchestrator_evaluation.py"
      provides: "Thin pytest wrappers asserting on orchestrator exit reports with anti-simulation guards"
  key_links:
    - from: "tests/workflow_harness/lib/output_collector.py"
      to: "tests/workflow_harness/test_orchestrator_evaluation.py"
      via: "EvidenceFlowEdge.fidelity_score for cross-agent evidence tracking"
      pattern: "fidelity_score"
---

<objective>
Test full team lifecycle with multi-agent coherence scoring using REAL Agent Teams.
Measure evidence fidelity across agent handoffs with REAL inter-agent communication.
Requires Prestep P1 (multi-agent ground truth templates).

**CRITICAL CONSTRAINT — TOP-LEVEL SESSION REQUIRED:**
This plan MUST execute from a top-level Claude Code session, NOT from a subagent executor.
Agent Teams spawning via TeamCreate and Task tool cannot work from within subagent contexts.

**ANTI-SIMULATION REQUIREMENT:**
Orchestrator evaluations MUST spawn REAL Agent Teams with actual inter-agent communication.
RunMode.SIMULATED is PROHIBITED. Each orchestrator evaluation MUST produce:
- Non-empty JSONL transcripts for each spawned agent
- Real SendMessage DM exchange records between agents
- Real tool_use events (alphaswarm commands, Read calls)

Purpose: Orchestrator evaluation is the most complex test surface — it validates that the
multi-agent system works as a coherent whole, not just individual parts.
Output: Orchestrator evaluations with cross-agent coherence scores and evidence fidelity metrics
from REAL Agent Teams interaction.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/3.1c-reasoning-evaluation-framework/3.1c-CONTEXT.md (section: "Plan 11: Orchestrator Evaluation")
@.planning/phases/3.1c-reasoning-evaluation-framework/3.1c-CONTEXT.md (section: "Shared Wave 6 Execution Note")
@.planning/phases/3.1c-reasoning-evaluation-framework/3.1c-10-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Evidence fidelity metric + Prestep P1 ground truth + orchestrator evaluations with REAL Agent Teams</name>
  <files>
    tests/workflow_harness/lib/output_collector.py
    .vrs/evaluations/progress.json
    .vrs/observations/plan11/
  </files>
  <action>
    **PREREQUISITE CHECK — Plans 09 and 10 REAL execution verified:**
    Before starting, verify Plans 09 and 10 were executed with REAL Agent Teams:
    1. Check `.vrs/evaluations/progress.json` has Plans 09/10 entries with run_mode != "SIMULATED"
    2. Check Plans 09/10 JSONL transcripts exist and are non-empty
    3. Verify joint Core HITL gate (09a + 10a) has passed
    4. If either plan used RunMode.SIMULATED, STOP — they must be re-executed first

    **Prestep P1 — Multi-agent ground truth templates:**
    P1-schema and P1-templates must be completed before orchestrator evaluation begins.
    The evidence fidelity metric specification must be drafted first (co-design requirement).

    **Evidence Fidelity Across Handoffs (IMP-31):**
    Add to `output_collector.py` (3.1b dataclass, NOT Plan 01 models.py):
    `EvidenceFlowEdge.fidelity_score: float | None = None`
    Metric: extract structured tuples `(function_name, vulnerability_class, severity)` from
    attacker's finding text and verifier's assessment text via regex on `EvidenceFlowEdge.content_preview`.
    Compute: `fidelity_score = |intersection| / max(|attacker_tuples|, 1)`.
    Threshold: >= 0.6 for Core tier WARN (not hard FAIL — evidence degrades in serialization).

    **Cross-agent coherence scoring:**
    Measure coherence across the full lifecycle using REAL Agent Teams interaction:
    1. Spawn: all agents started successfully via TeamCreate + Task
    2. Task assignment: each agent received correct investigation target
    3. DM exchange: SendMessage communications delivered between agents (REAL, not mock)
    4. Shutdown: all agents completed or timed out cleanly

    **CONCRETE AGENT TEAMS SPAWNING FOR ORCHESTRATOR EVALUATION:**
    Each orchestrator evaluation MUST spawn a REAL multi-agent team:

    ```
    # Step 1: Create a team for this orchestrator evaluation
    TeamCreate(
      team_name="orch-eval-{contract_id}",
      description="Orchestrator evaluation: full audit lifecycle"
    )

    # Step 2: Spawn attacker agent
    Task(
      subagent_type="general-purpose",
      name="eval-attacker",
      team_name="orch-eval-{contract_id}",
      prompt="You are the vrs-attacker agent in a multi-agent audit team.
        Read .claude/agents/vrs-attacker.md for your role definition.
        Target: {evaluation_contract_target}
        1. Build BSKG: uv run alphaswarm build-kg {contract_path}
        2. Query for vulnerability patterns
        3. Construct exploit paths with graph node ID evidence
        4. Send findings to defender via SendMessage for review
        5. Write structured finding with behavioral signature",
      description="Attacker agent eval"
    )

    # Step 3: Spawn defender agent
    Task(
      subagent_type="general-purpose",
      name="eval-defender",
      team_name="orch-eval-{contract_id}",
      prompt="You are the vrs-defender agent in a multi-agent audit team.
        Read .claude/agents/vrs-defender.md for your role definition.
        Target: {evaluation_contract_target}
        1. Receive attacker findings via team communication
        2. Search for guards, mitigations, and protective patterns
        3. Challenge or confirm each finding with evidence
        4. Send assessment to verifier via SendMessage",
      description="Defender agent eval"
    )

    # Step 4: Spawn verifier agent
    Task(
      subagent_type="general-purpose",
      name="eval-verifier",
      team_name="orch-eval-{contract_id}",
      prompt="You are the vrs-verifier agent in a multi-agent audit team.
        Read .claude/agents/vrs-verifier.md for your role definition.
        Target: {evaluation_contract_target}
        1. Receive attacker findings and defender assessments
        2. Cross-check evidence from both sides
        3. Render verdict with confidence score
        4. Write final verdict to observation directory",
      description="Verifier agent eval"
    )
    ```

    Each REAL Agent Teams orchestrator evaluation MUST produce:
    - Non-empty JSONL transcripts for EACH agent (attacker, defender, verifier)
    - Real SendMessage DM exchange records between agents
    - Real tool_use events (alphaswarm build-kg, alphaswarm query, Read calls)
    - Evidence flow edges traceable across agent boundaries

    **ANTI-SIMULATION VERIFICATION per orchestrator evaluation:**
    After each orchestrator evaluation completes:
    1. Verify JSONL transcripts exist for ALL agents (attacker, defender, verifier)
    2. Verify transcripts contain real tool_use events
    3. Verify SendMessage records exist showing inter-agent communication
    4. Verify evaluation result has run_mode != "SIMULATED"
    5. Verify fidelity_score is computed from REAL agent text (not placeholder tuples)

    **Orchestrator flow lifecycle tests:**
    At least 1 Core orchestrator test exercises full pipeline (IMP-18):
    - orchestrator-full-audit contract: spawn attacker+defender+verifier as REAL team
    - orchestrator-debate contract: structured adversarial debate flow
    - orchestrator-verify contract: cross-check evidence flow

    **Plan-scoped observation directories:**
    `.vrs/observations/plan11/` for orchestrator evaluation data.

    **Staged rollout:**
    Plan 11 runs as part of Stage 5 (Important tier) in the Wave 6 schedule.
    Requires joint Core HITL gate (Plans 09a + 10a) to have passed first.

    **Intra-stage failure policy:**
    Same as Plans 09-10: try/continue, .failed.json for failures, no silent gaps.
    Tier-aware circuit breaker: 3 for Core/Important, 5 for Standard.
  </action>
  <verify>
    <automated>cd . && uv run python -c "from tests.workflow_harness.lib.output_collector import EvidenceFlowEdge; e = EvidenceFlowEdge.__dataclass_fields__; assert 'fidelity_score' in e; print('fidelity_score field present')"</automated>
    <manual>Verify: (1) JSONL transcripts exist for ALL agents in at least 1 Core orchestrator run, (2) SendMessage DM records present, (3) non-None fidelity_score for attacker->verifier edge, (4) run_mode != SIMULATED</manual>
  </verify>
  <done>Evidence fidelity metric implemented. Prestep P1 completed. At least 1 Core orchestrator test exercises full pipeline with REAL Agent Teams. Cross-agent coherence scoring operational from REAL inter-agent SendMessage exchanges. RunMode.SIMULATED not used.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Orchestrator evaluation results with REAL multi-agent coherence and evidence fidelity from REAL Agent Teams</what-built>
  <how-to-verify>
    1. Review orchestrator exit reports — full lifecycle observed (spawn, assign, DM, shutdown) with REAL Agent Teams
    2. ANTI-SIMULATION CHECK: Verify run_mode != "SIMULATED" for ALL orchestrator evaluations
    3. Verify JSONL transcripts exist for EACH agent (attacker, defender, verifier) — not empty
    4. Verify SendMessage DM exchange records show REAL inter-agent communication
    5. Check evidence fidelity: at least 1 attacker->verifier edge has non-None fidelity_score
    6. Review cross-agent coherence scores — derived from lifecycle events, not per-agent score averages
    7. Verify full pipeline test (IMP-18) exercises BSKG build -> investigation -> merge -> report
    8. Confirm no silent gaps in orchestrator evaluations
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues with orchestrator evaluation results</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Thin pytest wrappers for orchestrator evaluation with anti-simulation guards</name>
  <files>
    tests/workflow_harness/test_orchestrator_evaluation.py
  </files>
  <action>
    **Thin pytest wrappers with ANTI-SIMULATION GUARDS:**
    `tests/workflow_harness/test_orchestrator_evaluation.py` asserts on structured exit reports:
    - At least 1 Core orchestrator has non-None fidelity_score
    - Full lifecycle observed: spawn + assign + DM + shutdown events present
    - Cross-agent coherence scores populated
    - Evidence fidelity >= 0.6 for Core tier (WARN, not FAIL)
    - No silent gaps in orchestrator evaluations

    **NEW ANTI-SIMULATION TESTS (mandatory):**
    - `test_orchestrator_not_simulated`: For each orchestrator evaluation in progress.json,
      verify run_mode != "SIMULATED". Fail if any used simulated mode.
    - `test_orchestrator_real_transcripts`: Verify non-empty JSONL transcripts exist for
      EACH agent (attacker, defender, verifier) in at least 1 Core orchestrator run.
    - `test_orchestrator_real_dm_exchange`: Verify SendMessage records exist showing
      inter-agent communication — not mock/placeholder DMs.
    - `test_fidelity_from_real_text`: Verify fidelity_score was computed from real agent
      text (non-placeholder tuples). Check that extracted tuples differ across evaluations
      (identical tuples across all runs = fabrication).
    - `test_coherence_not_agent_average`: Verify coherence_score is NOT computed as a
      simple average of per-agent reasoning scores. It must derive from lifecycle phase
      events (spawn, assign, DM, shutdown).

    **Sub-wave gate counts:**
    Standard-tier stub orchestrator contracts evaluated for coverage tracking only (P15-CSC-02).
  </action>
  <verify>
    <automated>cd . && uv run pytest tests/workflow_harness/test_orchestrator_evaluation.py -x -q</automated>
  </verify>
  <done>All orchestrator evaluations complete with REAL Agent Teams. Evidence fidelity metric produces non-None scores from real agent text. Cross-agent coherence operational from lifecycle events. Full lifecycle tested. Anti-simulation guards pass. SendMessage DM exchanges verified as real.</done>
</task>

</tasks>

<verification>
1. Full 3-agent lifecycle observed in at least 1 Core orchestrator test with REAL Agent Teams
2. Evidence fidelity: non-None fidelity_score for attacker->verifier edge from REAL text
3. Cross-agent coherence scoring produces meaningful scores from lifecycle events (not agent averages)
4. Full pipeline test (IMP-18) exercises complete audit flow with REAL agents
5. Prestep P1 ground truth templates completed
6. No silent gaps in orchestrator evaluations
7. RunMode.SIMULATED NOT used for any orchestrator evaluation
8. SendMessage DM exchange is REAL inter-agent communication
9. Non-empty JSONL transcripts exist for ALL agents in orchestrator evaluations
10. Anti-simulation pytest guards pass
</verification>

<success_criteria>
Orchestrator evaluation tests the multi-agent system as a whole using REAL Agent Teams
(TeamCreate + Task tool spawning). Evidence fidelity tracks information preservation
across REAL handoffs. Cross-agent coherence scoring operational from lifecycle events.
Full pipeline tested. SendMessage DM exchanges verified as real inter-agent communication.
Anti-simulation guards verify no fake data.
</success_criteria>

<output>
After completion, create `.planning/phases/3.1c-reasoning-evaluation-framework/3.1c-11-SUMMARY.md`
</output>
