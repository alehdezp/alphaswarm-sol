---
phase: 3.1c-reasoning-evaluation-framework
plan: 12
type: execute
wave: 7
depends_on: ["3.1c-09", "3.1c-10", "3.1c-11"]
files_modified:
  - src/alphaswarm_sol/evaluation/improvement.py
  - src/alphaswarm_sol/evaluation/regression.py
  - src/alphaswarm_sol/evaluation/reporter.py
  - src/alphaswarm_sol/evaluation/intelligence/tier_manager.py
  - src/alphaswarm_sol/evaluation/intelligence/contract_healer.py
  - src/alphaswarm_sol/evaluation/intelligence/coverage_radar.py
  - .vrs/evaluations/calibration_config.yaml
  - .vrs/evaluations/variance_summary.yaml
  - .vrs/evaluations/baseline_data/
autonomous: false
requirements:
  - EVAL-07
  - EVAL-08

must_haves:
  truths:
    - "Calibration anchor: Spearman rho > 0.6 on external ground truth before calibrated hints"
    - "Baseline established for all 51 workflows (Core 5x for variance)"
    - "Improvement loop never modifies production .claude/ — Jujutsu workspace isolation"
    - "Tier-weighted regression detection operational (Core >10pt=REJECT)"
    - "3.1c scope is Parts 0-1 only; Parts 2-5 are 3.1f scope"
    - "All calibration runs use REAL Agent Teams (Task tool spawning), not simulated pipeline data"
    - "Baseline entries derived from REAL evaluation data (Plans 09-11), not fabricated progress.json"
    - "Anti-fabrication checks detect 100% pass rate, identical outputs, scores at 100, duration < 5s"
  artifacts:
    - path: ".vrs/evaluations/calibration_config.yaml"
      provides: "Calibration configuration with evaluator fields from EvaluatorConstants"
    - path: ".vrs/evaluations/variance_summary.yaml"
      provides: "Per-dimension variance from 5x Core runs"
    - path: ".vrs/evaluations/baseline_data/"
      provides: "Regression baseline keyed by (workflow_id, run_mode, debate_enabled, effort_level)"
  key_links:
    - from: "src/alphaswarm_sol/evaluation/improvement.py"
      to: ".vrs/evaluations/calibration_config.yaml"
      via: "CalibrationConfig governs improvement loop entry"
      pattern: "calibration_config"
    - from: "src/alphaswarm_sol/evaluation/regression.py"
      to: ".vrs/evaluations/baseline_data/"
      via: "BaselineManager for regression detection"
      pattern: "BaselineManager"
---

<objective>
Establish regression baseline, calibration anchor, and variance analysis using REAL evaluation
data from Plans 09-11. Parts 0-1 are 3.1c scope (calibration + baseline). Parts 2-5
(improvement loop, metaprompting, failure reporter, intelligence modules) are 3.1f scope —
document handoff artifacts only.

**CRITICAL CONSTRAINT — TOP-LEVEL SESSION REQUIRED:**
This plan MUST execute from a top-level Claude Code session, NOT from a subagent executor.
Calibration anchor runs require spawning REAL Agent Teams via Task tool.

**ANTI-SIMULATION REQUIREMENT:**
All calibration runs MUST use REAL Agent Teams (Task tool spawning vrs-attacker.md agents).
Baseline entries MUST be derived from REAL evaluation data produced by Plans 09-11.
Fabricated progress.json entries are PROHIBITED. Anti-fabrication checks MUST be active.

**JUJUTSU WORKSPACE ISOLATION:**
The improvement loop in improvement.py MUST use Jujutsu workspace isolation. NEVER modify
production `.claude/` directly. WorkspaceManager API must be documented for 3.1f.

Purpose: The baseline is the ground truth for regression detection. Without calibration,
all hints are noise. Without variance data, convergence detection is unreliable.
Output: Calibrated baseline with variance data, handoff artifacts schema-stable for 3.1f.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/3.1c-reasoning-evaluation-framework/3.1c-CONTEXT.md (section: "Plan 12: Calibration & Baseline")
@.planning/phases/3.1c-reasoning-evaluation-framework/3.1c-CONTEXT.md (section: "Intelligence Layer")
@.planning/phases/3.1c-reasoning-evaluation-framework/3.1c-09-SUMMARY.md
@.planning/phases/3.1c-reasoning-evaluation-framework/3.1c-10-SUMMARY.md
@.planning/phases/3.1c-reasoning-evaluation-framework/3.1c-11-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Part 0 — Calibration Anchor with REAL Agent Teams + Infrastructure Diagnostic + CalibrationConfig</name>
  <files>
    .vrs/evaluations/calibration_config.yaml
    .vrs/evaluations/infrastructure_misses.yaml
    src/alphaswarm_sol/evaluation/improvement.py
  </files>
  <action>
    **PREREQUISITE CHECK — Plans 09-11 REAL execution verified:**
    Before starting, verify ALL upstream plans used REAL Agent Teams:
    1. Check `.vrs/evaluations/progress.json` has Plans 09/10/11 entries with run_mode != "SIMULATED"
    2. Check JSONL transcripts exist and are non-empty for Plans 09/10/11
    3. Verify Plan 11 orchestrator evaluations include real SendMessage DM exchanges
    4. If ANY upstream plan used RunMode.SIMULATED, STOP — upstream must be re-executed first

    **Calibration Anchor (ADV-03) — REAL Agent Teams REQUIRED:**
    4 Core investigation agents on 18 corpus contracts, compare against `ground_truth.yaml`.
    Compute Spearman rho(GVS, detection_accuracy). If rho < 0.3: HALT and diagnose GVS failure.
    If rho > 0.6: proceed. Between 0.3-0.6: compute partial_rho_tp_only and record diagnosis.
    External anchor runs BEFORE any calibrated hints or metaprompting changes.
    Each Experiment Ledger entry must include a counterexample scenario.
    **Cost: 4 agents x 18 contracts = 72 sessions. Requires explicit HITL approval.**

    **CONCRETE AGENT TEAMS SPAWNING FOR CALIBRATION RUNS:**
    Each calibration session MUST spawn a REAL Agent Teams subagent:

    ```
    # Example: Calibration run for vrs-attacker on contract X
    Task(
      subagent_type="general-purpose",
      name="calibration-attacker-{contract_id}",
      prompt="You are the vrs-attacker agent running a calibration evaluation.
        Read .claude/agents/vrs-attacker.md for your role definition.
        Target contract: {contract_path}
        Ground truth available: {ground_truth_path}

        Execute the full investigation workflow:
        1. Build BSKG: uv run alphaswarm build-kg {contract_path}
        2. Query for vulnerability patterns relevant to this contract
        3. Construct exploit paths with graph node ID evidence
        4. Write structured finding with behavioral signature
        5. Write debrief to .vrs/observations/plan12/{session_id}/debrief.json

        IMPORTANT: You MUST actually execute alphaswarm commands.
        Do NOT simulate or fabricate tool outputs.",
      description="Calibration: attacker on {contract_id}"
    )
    ```

    **ANTI-SIMULATION VERIFICATION per calibration session:**
    After each calibration session:
    1. Verify JSONL transcript exists and is non-empty
    2. Verify transcript contains real tool_use events (alphaswarm build-kg, alphaswarm query)
    3. Verify debrief artifact exists with real agent responses
    4. Flag sessions with duration < 5s as suspicious (anti-fabrication)

    **GVS-Detection Divergence (P13-IMP-16):**
    Compute and report the GVS-detection correlation. If divergent, document why.

    **Infrastructure Diagnostic (~30 LOC):**
    For each VulnDocs pattern where recall < 30% across all 4 Core agents:
    (a) `alphaswarm query "pattern:{pattern_id}"` — if 0 results where ground truth expects detection, flag BSKG infrastructure miss
    (b) `alphaswarm vulndocs list --pattern {pattern_id}` — if no entry or `status: draft`, flag VulnDocs miss
    Output: `.vrs/evaluations/infrastructure_misses.yaml` with per-pattern classification.

    **Goodhart Screen (P14-IMP-24):**
    If `goodhart_risk.yaml` exists: follow decision tree (low=proceed, medium=proceed+WARNING+surveillance, high=HALT for human approval).
    If absent: `goodhart_screen: deferred`, proceed with WARNING.

    **Part 0.5 — CalibrationConfig + EvaluatorConstants (P11-ADV-3-01, P11-SYN-02):**
    Write `CalibrationConfig` to `.vrs/evaluations/calibration_config.yaml` with all evaluator fields from `EvaluatorConstants.current()`.
    Startup guard: `assert CalibrationConfig.from_disk().evaluator_fields == EvaluatorConstants.current().to_dict()`.
    Any CalibrationConfig field change requires full anchor re-run.

    **Wave 6 re-validation (CSC-02):**
    If GVS thresholds shift >15pt, mark all ExperimentLedgerEntry records with `calibration_epoch: 'provisional'`. ~20 LOC.

    **CC LLM agent path (ADV-RG-02):**
    All calibration runs MUST use Task tool spawning vrs-attacker.md, NOT Python AttackerAgent class.

    **Plan-scoped observation directories:**
    `.vrs/observations/plan12/` for all calibration data.
  </action>
  <verify>
    <automated>cd . && test -f .vrs/evaluations/calibration_config.yaml && test -f .vrs/evaluations/infrastructure_misses.yaml && echo "Part 0 artifacts exist"</automated>
    <manual>Verify: (1) Spearman rho > 0.6 (or documented diagnosis), (2) 72 REAL calibration sessions completed (non-empty JSONL), (3) run_mode != SIMULATED for all sessions, (4) anti-fabrication: no sessions with duration < 5s</manual>
  </verify>
  <done>Calibration anchor computed from REAL Agent Teams (72 sessions) with Spearman rho reported. Infrastructure diagnostic classifies all low-recall patterns. CalibrationConfig written with EvaluatorConstants binding. Goodhart screen evaluated. All sessions have non-empty JSONL transcripts with real tool_use events.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: HITL Gate: Calibration Anchor Review</name>
  <what-built>Part 0 calibration anchor results from REAL Agent Teams and infrastructure diagnostic</what-built>
  <how-to-verify>
    1. Review calibration_config.yaml — verify evaluator fields match EvaluatorConstants
    2. Check Spearman rho — must be > 0.6 to proceed to Part 1
    3. ANTI-SIMULATION CHECK: Verify run_mode != "SIMULATED" for ALL 72 calibration sessions
    4. Verify JSONL transcripts are non-empty for calibration sessions (not fabricated)
    5. Anti-fabrication: verify no sessions have duration < 5s, no identical outputs across runs
    6. Review infrastructure_misses.yaml — verify miss classifications are accurate
    7. If rho < 0.3, verify GVS failure diagnosis is documented
    8. Check Goodhart screen result
    9. Produce disk artifact: `.vrs/experiments/plan-12/hitl-gate-01.json` with schema
       `{decision: 'GO'|'NO-GO', reviewer: string, timestamp: ISO8601, issues: string[], notes: string}`.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to Part 1 baseline, or describe calibration issues</resume-signal>
  <done>`.vrs/experiments/plan-12/hitl-gate-01.json` exists with `decision: 'GO'`. Human has reviewed calibration anchor from REAL Agent Teams data.</done>
</task>

<task type="auto">
  <name>Task 3: Part 1 — Baseline Establishment from REAL Data + Variance + Jujutsu Isolation + 3.1f Handoff</name>
  <files>
    .vrs/evaluations/baseline_data/
    .vrs/evaluations/variance_summary.yaml
    src/alphaswarm_sol/evaluation/regression.py
    src/alphaswarm_sol/evaluation/improvement.py
    src/alphaswarm_sol/evaluation/intelligence/tier_manager.py
    src/alphaswarm_sol/evaluation/intelligence/contract_healer.py
    src/alphaswarm_sol/evaluation/intelligence/coverage_radar.py
  </files>
  <action>
    **Task 2 HITL gate verification (first action):**
    Verify `.vrs/experiments/plan-12/hitl-gate-01.json` exists and contains `decision: 'GO'`.
    If absent or NO-GO, STOP.

    **Baseline Establishment from REAL evaluation data:**
    All 51 workflows 1x. Core 5x for variance.
    Keyed by `(workflow_id, run_mode, debate_enabled, effort_level)` per locked decision + P11-IMP-06.

    **BASELINE DATA SOURCE VERIFICATION:**
    Baseline entries MUST be derived from REAL evaluation data produced by Plans 09-11:
    1. For each workflow_id in baseline, verify corresponding progress.json entry has run_mode != "SIMULATED"
    2. Verify source JSONL transcripts are non-empty for baseline-contributing evaluations
    3. Cross-reference baseline entry scores against actual evaluation results (not fabricated)

    **Anti-fabrication checks (6 triggers — MANDATORY):**
    Before accepting any baseline entry, verify:
    1. NOT 100% pass rate across all capability checks (suspicious uniformity)
    2. NOT identical outputs across multiple runs of same workflow (copy-paste fabrication)
    3. NOT scores at exactly 100 for all dimensions (ceiling fabrication)
    4. NOT duration < 5s for any evaluation session (too fast to be real)
    5. NOT identical reasoning_score across all Core agents (anti-fabrication: identical = suspicious)
    6. NOT all fidelity_scores identical in orchestrator evaluations
    Exclude sessions with `data_quality_warning` from variance computation.

    **Anchoring Analysis (P13-ADV-5-01):**
    Compute delta(B_pre, B_post) distribution from `B_score_pre_exposure`.
    If median delta < 3pt across Core dimensions, flag for human review.

    **Variance Analysis:**
    From 5x Core runs, compute per-dimension standard deviation.
    Write `.vrs/evaluations/variance_summary.yaml`.
    Incomplete evaluation tracking (IMP-27): if >50% Core entries are evaluation_complete=False, surface debate timeout warning.

    **Tier-weighted regression detection:**
    Core >10pt = REJECT, Important >15pt = REJECT, Standard >25pt = REJECT.
    Operational for all baseline entries.

    **Baseline version guard (P15-CSC-01):**
    All baseline entries must have current `scoring_denominator_version`. Baseline empty or all entries current.

    **JUJUTSU WORKSPACE ISOLATION (MANDATORY):**
    `improvement.py` MUST enforce Jujutsu workspace isolation:
    - Before any prompt mutation, create a Jujutsu workspace: `jj workspace add variant-{id}`
    - All prompt changes happen ONLY in the workspace, NOT in production `.claude/`
    - WorkspaceManager API:
      ```python
      class WorkspaceManager:
          def create_workspace(self, variant_id: str) -> Path:
              """Create isolated Jujutsu workspace for variant testing."""
          def apply_variant(self, workspace: Path, changes: dict) -> None:
              """Apply prompt changes in workspace only."""
          def discard_workspace(self, workspace: Path) -> None:
              """Clean up workspace after evaluation."""
      ```
    - Startup guard: if any code path writes to `.claude/` without workspace isolation, HALT
    - Document WorkspaceManager API for 3.1f Variant Tester wrapper (~50 LOC)

    **3 Intelligence Module v1 Implementations:**
    - `tier_manager.py` (~50 LOC): wraps BaselineManager thresholds. Human approval for demotion via `tier_demotion_proposals.yaml`.
    - `contract_healer.py` (~30-50 LOC): detects ceiling/floor/zero-variance anomalies from BaselineManager's 20-score windows.
    - `coverage_radar.py` (~150-200 LOC): 4-axis coverage heat map. REQUIRES `coverage_axes` from Plan 06.
    Remaining 9 intelligence modules as stubs with `is_active(store) -> bool`.

    **3.1f Handoff Artifacts (P15-IMP-12, P15-IMP-14):**
    Schema-stable handoff files:
    - `calibration_config.yaml` (from Part 0)
    - `variance_summary.yaml` (from Part 1)
    - `baseline_data/` directory
    - `experiment_ledger.jsonl` with `decision: Literal['accept', 'reject', 'abandoned']` per P15-IMP-40
    Document WorkspaceManager API for 3.1f Variant Tester wrapper (~50 LOC).
    Parts 2-5 (improvement loop, metaprompting, failure reporter, remaining intelligence modules) are explicitly 3.1f scope.

    **CLI Subcommand:**
    `alphaswarm evaluation summary [--workflow ID] [--format json|table]` (~80 LOC). Reads `progress.json`.

    **TP-only filter (P15-IMP-02):**
    If rho < 0.3, compute partial_rho_tp_only and record diagnosis.

    **Process/Outcome boundary (P14-SYN-01):**
    Verify no ExperimentLedgerEntry uses detection_accuracy as kill_criterion for a reasoning dimension improvement.
  </action>
  <verify>
    <automated>cd . && test -f .vrs/evaluations/variance_summary.yaml && test -d .vrs/evaluations/baseline_data && echo "Part 1 artifacts exist" && uv run alphaswarm evaluation summary --format json 2>/dev/null | head -5</automated>
    <manual>Verify: (1) baseline has all 51 workflows from REAL Plans 09-11 data (not fabricated), (2) Core has 5x entries, (3) anti-fabrication checks pass, (4) Jujutsu workspace isolation enforced in improvement.py, (5) no writes to .claude/ without workspace</manual>
  </verify>
  <done>Baseline established for all 51 workflows from REAL evaluation data. Core 5x variance computed. Regression detection operational with tier-weighted thresholds. Anti-fabrication checks active (6 triggers). Jujutsu workspace isolation enforced — no production .claude/ modifications. 3 intelligence modules implemented. 3.1f handoff artifacts schema-stable. CLI summary command works.</done>
</task>

</tasks>

<verification>
1. Calibration anchor: Spearman rho reported from REAL Agent Teams (72 sessions with non-empty JSONL)
2. Infrastructure diagnostic classifies all low-recall patterns
3. Baseline has all 51 workflows from REAL evaluation data (Plans 09-11) — not fabricated
4. Variance summary has per-dimension std dev for Core dimensions
5. Tier-weighted regression detection: Core >10pt, Important >15pt, Standard >25pt
6. Anti-fabrication checks implemented and active (6 triggers)
7. Jujutsu workspace isolation enforced — improvement.py NEVER modifies production .claude/
8. 3 intelligence modules have v1 implementations
9. 3.1f handoff artifacts exist and are schema-stable
10. CLI `alphaswarm evaluation summary` returns formatted output
11. No ExperimentLedgerEntry uses detection_accuracy as kill_criterion for reasoning dimensions
12. RunMode.SIMULATED NOT used for any calibration session
13. Baseline entries cross-referenced against actual REAL evaluation results
</verification>

<success_criteria>
Calibrated baseline established from REAL evaluation data (not simulated/fabricated).
72 calibration sessions completed with REAL Agent Teams. Variance analysis complete.
Regression detection operational. Anti-fabrication checks active. Jujutsu workspace
isolation enforced for all prompt mutations. Intelligence modules v1 working. 3.1f
handoff clean — Parts 2-5 can start from schema-stable artifacts without revisiting 3.1c.
</success_criteria>

<output>
After completion, create `.planning/phases/3.1c-reasoning-evaluation-framework/3.1c-12-SUMMARY.md`
</output>
