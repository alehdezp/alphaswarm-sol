---
phase: 3.1c-reasoning-evaluation-framework
plan: 09
type: execute
wave: 6
depends_on: ["3.1c-08"]
files_modified:
  - src/alphaswarm_sol/shipping/skills/test-suite/SKILL.md
  - tests/workflow_harness/test_skill_evaluation.py
  - .vrs/evaluations/progress.json
autonomous: false
requirements:
  - EVAL-06
  - EVAL-11

must_haves:
  truths:
    - "CC skill /vrs-test-suite writes conformant exit report for all 30 skills"
    - "Gate 0 dry-run spawns REAL Agent Teams via Task tool — produces non-empty JSONL transcript with real tool_use events"
    - "Delegate mode: zero alphaswarm query/build-kg/Read .sol calls from orchestrator session"
    - "Sub-wave (a) Core ~10 skills pass capability check before Important sub-wave begins"
    - "Pre-flight activation test: 10/10 on matching + non-matching prompts"
    - "Important sub-wave (09b) does not begin until Plan 10 Task 1 (Core agent evaluations) is confirmed complete — joint 09a+10a HITL gate required"
    - "RunMode.SIMULATED is PROHIBITED for Core and Important tiers — only HEADLESS or INTERACTIVE permitted"
    - "Real JSONL transcripts exist with real tool_use events (not fabricated progress.json entries)"
  artifacts:
    - path: "src/alphaswarm_sol/shipping/skills/test-suite/SKILL.md"
      provides: "Complete CC skill orchestrating 30 skill evaluations"
    - path: "tests/workflow_harness/test_skill_evaluation.py"
      provides: "Thin pytest wrappers asserting on exit reports with anti-simulation guards"
  key_links:
    - from: "src/alphaswarm_sol/shipping/skills/test-suite/SKILL.md"
      to: "tests/workflow_harness/lib/evaluation_runner.py"
      via: "python evaluation_runner.py --session-id {id} --contract {path}"
      pattern: "evaluation_runner"
---

<objective>
Test all 30 shipped skills with capability + reasoning evaluation via the /vrs-test-suite CC skill.
Staged rollout: Gate 0 dry-run -> Standard headless -> Core with HITL gate -> Important with spot-check.

**CRITICAL CONSTRAINT — TOP-LEVEL SESSION REQUIRED:**
This plan MUST execute from a top-level Claude Code session, NOT from a subagent executor.
Agent Teams (Task tool spawning) cannot be invoked from within subagent contexts.
The executor running this plan MUST be the top-level CC session itself.

**ANTI-SIMULATION REQUIREMENT:**
All evaluations for Core and Important tiers MUST use RunMode.HEADLESS or RunMode.INTERACTIVE.
RunMode.SIMULATED is PROHIBITED for any tier that contributes to baseline or capability scoring.
Standard tier MAY use RunMode.HEADLESS for structural-only checks.

Purpose: Skill evaluation provides the scores that Plan 12 uses for baseline establishment.
Output: All 30 skill evaluations with conformant exit reports, staged rollout gates passed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/3.1c-reasoning-evaluation-framework/3.1c-CONTEXT.md (section: "Plan 09: Skill Evaluation")
@.planning/phases/3.1c-reasoning-evaluation-framework/3.1c-CONTEXT.md (section: "Shared Wave 6 Execution Note")
@.planning/phases/3.1c-reasoning-evaluation-framework/3.1c-08-SUMMARY.md
@src/alphaswarm_sol/shipping/skills/test-suite/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete /vrs-test-suite skill + Gate 0 dry-run with REAL Agent Teams</name>
  <files>
    src/alphaswarm_sol/shipping/skills/test-suite/SKILL.md
    .vrs/evaluations/progress.json
  </files>
  <action>
    **Complete SKILL.md (from Plan 01 stub):**
    Fill `[TO-FILL: Plan 09]` markers with:
    - Per-workflow loop: load contract -> spawn Agent Teams -> hooks observe -> debrief teardown -> call `python evaluation_runner.py --session-id {id} --contract {path}` -> read EvaluationResult -> write progress
    - Scope parameters: `--tier core|important|standard`, `--category investigation|tool|orchestration|support`, `--workflow-ids` list, `--dry-run`, `--debate`
    - Kill-file mechanism: polls `.vrs/evaluations/kill.signal` between workflows
    - Circuit breaker: 3 consecutive identical infrastructure_failure_type -> HALT stage
    - Progress tracking: atomic write to `.vrs/evaluations/progress.json` (~40 LOC)

    **CONCRETE AGENT TEAMS SPAWNING INSTRUCTIONS:**
    Each workflow evaluation spawns Agent Teams using Claude Code's Task tool from the
    TOP-LEVEL session. Example for a skill investigation workflow:

    ```
    # Spawn an investigation agent for this skill workflow
    Task(
      subagent_type="general-purpose",
      name="skill-eval-{workflow_id}",
      prompt="You are evaluating skill workflow {workflow_id}.
        Read .claude/agents/vrs-attacker.md for investigation methodology.
        Target contract: {contract_path}
        1. Build BSKG: uv run alphaswarm build-kg {contract_path}
        2. Query graph for vulnerability patterns
        3. Document findings with graph node IDs and behavioral signatures
        4. Write debrief to .vrs/observations/plan09/{session_id}/debrief.json",
      description="Evaluate: {workflow_id}"
    )
    ```

    The spawned subagent produces JSONL transcripts at:
    `~/.claude/projects/{encoded-path}/{session-uuid}/`

    TranscriptParser reads these JSONL files to extract:
    - tool_use events (Bash calls to alphaswarm, Read calls, etc.)
    - BSKG query results
    - Session lifecycle (start, compaction, end)

    **RunMode requirements per tier:**
    - Core: RunMode.INTERACTIVE (real dual-Opus reasoning evaluation)
    - Important: RunMode.HEADLESS (real single-evaluator reasoning)
    - Standard: RunMode.HEADLESS (structural validation only, run_reasoning: false)
    - RunMode.SIMULATED is PROHIBITED for Core and Important

    **Delegate mode enforcement (P11-IMP-12):**
    SKILL.md Delegation Rules section:
    - PROHIBITED: `alphaswarm query`, `alphaswarm build-kg`, Read on `.sol` files
    - Per-teammate file boundaries in spawn prompts
    - Observation JSONL exit check: zero prohibited calls from orchestrator session

    **Pre-flight activation test (P11-IMP-13):**
    5 matching + 5 non-matching prompts, 10/10 required. If < 10/10, HALT.

    **Gate 0 dry-run — REAL Agent Teams REQUIRED:**
    Run `/vrs-test-suite --dry-run` on 1 Core investigation workflow.
    This MUST spawn a REAL Agent Teams subagent via Task tool.
    Gate criteria:
    (a) Agent Teams subagent spawned via Task tool and completed
    (b) Non-empty JSONL transcript exists with real tool_use events
    (c) Debrief artifact written to `.vrs/observations/plan09/`
    (d) Exit report passes schema validation
    (e) Delegate mode contamination check passes

    **Gate 0 ANTI-SIMULATION CHECK:**
    Verify the spawned subagent's JSONL transcript contains:
    - At least 1 `alphaswarm build-kg` Bash command
    - At least 1 `alphaswarm query` Bash command
    - At least 1 Read tool call
    If any are missing, Gate 0 FAILS — the subagent did not perform real work.

    **Intra-stage failure policy (P15-ADV-4-02):**
    Try/continue: failed workflows write `{workflow_id}.failed.json` with error context.
    No silent gaps. If .failed.json > 20% of wave, escalate.
  </action>
  <verify>
    <automated>cd /Volumes/ex_ssd/home/projects/python/vkg-solidity/true-vkg && test -f src/alphaswarm_sol/shipping/skills/test-suite/SKILL.md && grep -c "PROHIBITED" src/alphaswarm_sol/shipping/skills/test-suite/SKILL.md | xargs -I{} test {} -ge 1 && echo "Delegation rules present"</automated>
    <manual>Verify Gate 0 dry-run produced: (1) non-empty JSONL transcript with real tool_use events, (2) debrief artifact, (3) conformant exit report. Check that RunMode is NOT SIMULATED.</manual>
  </verify>
  <done>SKILL.md complete with delegation rules and Agent Teams spawning instructions. Gate 0 dry-run passes with REAL Agent Teams (non-empty JSONL, real tool_use events). Pre-flight activation test 10/10.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Gate 0 dry-run with REAL Agent Teams + Core sub-wave (a) skill evaluations (~10 Core skills)</what-built>
  <how-to-verify>
    1. Verify Gate 0 JSONL transcript is non-empty and contains real tool_use events (alphaswarm build-kg, query, Read)
    2. Verify RunMode is NOT SIMULATED for Core evaluations — check evaluation result JSON for run_mode field
    3. Review Core sub-wave (a) results in progress.json — verify >= 8 of 10 Core skills pass capability check
    4. Check delegate mode: grep orchestrator JSONL for prohibited calls (expect 0)
    5. Verify debrief artifacts written for Core skills — non-empty JSON with real content
    6. Check that reasoning_score fields are populated (not null/None) for Core evaluations
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to Important sub-wave, or describe issues with Core results</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Important + Standard sub-waves + thin pytest wrappers with anti-simulation guards</name>
  <files>
    tests/workflow_harness/test_skill_evaluation.py
  </files>
  <action>
    **JOINT GATE (cross-plan, IMP-13 — execute before Important sub-wave):**
    Confirm Plan 10 Task 1 complete before proceeding to Important sub-wave.

    **Sub-wave (b) Important tier (~15 skills):**
    Execute with spot-check gate. MUST use RunMode.HEADLESS (not SIMULATED).
    Each spawns a REAL Agent Teams subagent via Task tool.

    **Sub-wave (c) Standard tier (~26 skills, headless):**
    Structural check only. `run_reasoning: false`. Standard-tier does NOT contribute to baseline.
    MAY use RunMode.HEADLESS.

    **Thin pytest wrappers with ANTI-SIMULATION GUARDS:**
    `tests/workflow_harness/test_skill_evaluation.py` must assert:
    - All 30 skills have exit report or .failed.json (EXPECTED_SKILL_COUNT = 30)
    - No silent gaps
    - Core skills pass capability check (>= 8 of 10)
    - Zero prohibited calls in orchestrator JSONL
    - 3.2-critical readiness: >= 3 of 8 critical workflows above threshold (CRITICAL_READINESS_THRESHOLD = 3)

    **NEW ANTI-SIMULATION TESTS (mandatory):**
    - `test_core_evaluations_not_simulated`: For each Core workflow in progress.json,
      verify `run_mode != "SIMULATED"`. Fail if any Core evaluation used SIMULATED mode.
    - `test_real_transcripts_exist`: For each Core workflow, verify a non-empty JSONL
      transcript file exists at the expected observation path. Fail if JSONL is empty
      or missing.
    - `test_reasoning_scores_populated`: For each Core workflow, verify `reasoning_score`
      is not null/None. Fail if reasoning evaluation was skipped.
    - `test_thresholds_not_gamed`: Assert EXPECTED_SKILL_COUNT == 30 and
      CRITICAL_READINESS_THRESHOLD == 3. These are compile-time constants that MUST NOT
      be modified to accommodate simulated data.

    **Wave 6 Exit Report Section 1:**
    3.2-critical workflows report CONCLUSION_SYNTHESIS and EVIDENCE_INTEGRATION scores.
    3.2 readiness signal: 3 of 8 critical workflows above tier threshold.
  </action>
  <verify>
    <automated>cd /Volumes/ex_ssd/home/projects/python/vkg-solidity/true-vkg && uv run pytest tests/workflow_harness/test_skill_evaluation.py -x -q</automated>
  </verify>
  <done>All 30 skills have exit reports. Core >= 8/10 pass capability check with REAL reasoning scores (not simulated). Important evaluations use RunMode.HEADLESS. Anti-simulation guards pass. 3.2-critical readiness signal computed with CRITICAL_READINESS_THRESHOLD = 3.</done>
</task>

</tasks>

<verification>
1. /vrs-test-suite writes conformant exit report for all 30 skills
2. Gate 0 dry-run spawns REAL Agent Teams — non-empty JSONL with tool_use events
3. Core sub-wave: >= 8 of 10 pass capability check with populated reasoning_score
4. Delegate mode: zero prohibited calls from orchestrator session
5. Pre-flight activation test: 10/10
6. No silent gaps — all workflows have exit report or .failed.json
7. RunMode.SIMULATED NOT used for Core or Important evaluations
8. Anti-simulation pytest guards pass (thresholds not gamed)
9. 3.2-critical workflows reported with CRITICAL_READINESS_THRESHOLD = 3
</verification>

<success_criteria>
30 skill evaluations complete with conformant exit reports from REAL Agent Teams execution.
Core evaluations use RunMode.INTERACTIVE with populated reasoning scores.
Anti-simulation guards verify no threshold gaming and no simulated-only runs.
Staged rollout gates all passed. Delegate mode clean.
</success_criteria>

<output>
After completion, create `.planning/phases/3.1c-reasoning-evaluation-framework/3.1c-09-SUMMARY.md`
</output>
