# HITL Checkpoint: 3.1-05 Evaluation Contract Foundation

**Scenario:** HITL-3.1-05-eval-contracts
**Phase:** 3.1-testing-audit-cleanup
**Date:** 2026-02-11

---

## Verification Steps

### 1. Schema Validation

```bash
uv run python -c "
import json, jsonschema
schema = json.load(open('.vrs/testing/schemas/evaluation_contract.schema.json'))
jsonschema.Draft7Validator.check_schema(schema)
print('Schema is valid JSON Schema Draft 7')
print(f'Required fields: {schema[\"required\"]}')
print(f'Category enum: {schema[\"properties\"][\"category\"][\"enum\"]}')
print(f'Grader type enum: {schema[\"properties\"][\"grader_type\"][\"enum\"]}')
"
```

**Expected:** Schema self-validates, required fields include workflow_id/category/rule_refs/capability_checks/grader_type.

### 2. Sample Contract Validation

```bash
uv run python -c "
import json, jsonschema
schema = json.load(open('.vrs/testing/schemas/evaluation_contract.schema.json'))
for f in ['skill-audit.json', 'agent-attacker.json', 'orchestrator-debate.json']:
    contract = json.load(open(f'.vrs/testing/contracts/samples/{f}'))
    jsonschema.validate(contract, schema)
    print(f'{f}: VALID ({len(contract[\"capability_checks\"])} checks, refs={contract[\"rule_refs\"]})')
"
```

**Expected:** All 3 validate, each has >= 3 checks, rule_refs are from the 3.1-04 stable ID set.

### 3. Assertion Helpers - Pass Case

```bash
uv run python -c "
from tests.workflow_harness.lib.assertions import assert_matches_contract, assert_reasoning_dimensions_covered
import json

contract = json.load(open('.vrs/testing/contracts/samples/skill-audit.json'))

# Pass case: result with evidence
result = {'transcript': 'build-kg output', 'evidence_nodes': ['n1'], 'code_locations': ['Vault.sol:42']}
assert_matches_contract(result, contract)
print('assert_matches_contract: PASS (no error)')

transcript = 'Ran build-kg for knowledge graph. Found evidence of reentrancy pattern detection.'
assert_reasoning_dimensions_covered(transcript, contract)
print('assert_reasoning_dimensions_covered: PASS (no error)')
"
```

### 4. Assertion Helpers - Fail Case

```bash
uv run python -c "
from tests.workflow_harness.lib.assertions import assert_matches_contract, assert_reasoning_dimensions_covered
import json

contract = json.load(open('.vrs/testing/contracts/samples/skill-audit.json'))

# Fail case: missing evidence
try:
    assert_matches_contract({}, contract)
    print('ERROR: should have raised')
except AssertionError as e:
    print(f'assert_matches_contract fail case: PASS (raised: {e})')

try:
    assert_reasoning_dimensions_covered('nothing relevant', contract)
    print('ERROR: should have raised')
except AssertionError as e:
    print(f'assert_reasoning_dimensions_covered fail case: PASS (raised: {e})')
"
```

### 5. Existing Assertions Non-Regression

```bash
uv run pytest tests/workflow_harness -q
```

**Expected:** 91 tests pass (same as before 3.1-05 changes).

### 6. Full Test Suite

```bash
uv run pytest tests/test_evaluation_contracts.py -v
```

**Expected:** 37 tests pass, 0 failures.

---

## Result

All 6 verification steps pass. Schema, samples, and assertions work correctly.
No standalone enforcement files created. Existing assertions unaffected.
