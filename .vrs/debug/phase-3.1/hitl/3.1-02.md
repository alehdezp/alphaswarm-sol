# HITL-3.1-02: Legacy Infrastructure Sweep Verification

**Scenario:** HITL-3.1-02-legacy-sweep
**Date:** 2026-02-11
**Plan:** 3.1-02 (Legacy Infrastructure Cleanup)

---

## Grep Sweep Output Summary

| Pattern | Scope | Hits | Assessment |
|---------|-------|------|------------|
| `tmux` (case-insensitive) | `src/alphaswarm_sol/**/*.py` | 0 | CLEAN |
| `tmux` (case-insensitive) | `src/alphaswarm_sol/**/*.yaml` | 0 | CLEAN |
| `tmux` (case-insensitive) | `configs/**/*.yaml` | 0 | CLEAN |
| `tmux` (case-insensitive) | `.claude/skills/**` | 0 | CLEAN |
| `tmux` (case-insensitive) | `.claude/agents/**` | 0 | CLEAN |
| `jujutsu\|jj-workspace` | `src/alphaswarm_sol/**/*.py` | 18 | ACTIVE infrastructure, not legacy |
| `MasterOrchestrator\|AgenticRunner\|self_improving_runner\|tmux_harness\|tmux_controller` | `src/alphaswarm_sol/**/*.py` | 0 in production exports | Files exist but not exported |
| Legacy runner classes | `.claude/skills/**` | 0 | CLEAN |
| Legacy runner classes | `.claude/agents/**` | 0 | CLEAN |

---

## Registry Diff (Before/After)

**Before:** 53 skills registered
**After:** 51 skills registered

### Removed entries:

1. **`strict-audit`** -- `shipped: null`, `dev: skills/vrs-strict-audit.md` (file missing from disk). No file exists anywhere. Orphaned entry removed.

2. **`today-date`** -- `shipped: null`, `dev: skills/today-date/SKILL.md` (file missing from disk). No file exists anywhere. Orphaned entry removed.

### Fixed dev paths (set to `null`):

3. **`audit`** -- `dev: skills/vrs-legacy/audit.md` -> `null` (vrs-legacy/ directory deleted)
4. **`investigate`** -- `dev: skills/vrs-legacy/investigate.md` -> `null`
5. **`verify`** -- `dev: skills/vrs-legacy/verify.md` -> `null`
6. **`debate`** -- `dev: skills/vrs-legacy/debate.md` -> `null`

### Metadata update:

7. **`skill_tool_policies.yaml` metadata** -- `phase: "07.3.1.5-full-testing-orchestrator"` -> `phase: "3.1-testing-audit-cleanup"` (v5.0 phase reference updated to v6.0)

---

## Spot-Checked Removed Legacy References

### 1. `strict-audit` skill (registry entry removed)

- **Location:** `src/alphaswarm_sol/skills/registry.yaml` line 28-38
- **Justification:** Both `shipped` (null) and `dev` (`skills/vrs-strict-audit.md`) paths resolve to no file on disk. The skill was never shipped and its dev source was deleted. Keeping the entry would cause skill lookup failures.

### 2. `skills/vrs-legacy/audit.md` dev path (set to null)

- **Location:** `src/alphaswarm_sol/skills/registry.yaml` line 26
- **Justification:** The `vrs-legacy/` directory under `.claude/skills/` does not exist. The shipped path (`src/alphaswarm_sol/shipping/skills/audit.md`) does exist and is the canonical location. The dev path was a remnant from a pre-shipping migration.

### 3. `skill_tool_policies.yaml` phase metadata (updated)

- **Location:** `configs/skill_tool_policies.yaml` line 444
- **Justification:** Referenced `07.3.1.5-full-testing-orchestrator` which is a v5.0 phase archived in `.planning/phases/v5.0-archive/`. Updated to current phase `3.1-testing-audit-cleanup` to reflect current milestone context.

---

## Pre-existing Issues Documented (Not Fixed -- Out of Scope)

- `tests/agents/test_attacker_agent.py` imports `alphaswarm_sol.testing.unit` (module deleted)
- `tests/test_checkpoint.py` imports `alphaswarm_sol.testing.flexible.checkpoint` (module deleted from installed package)
- `tests/test_session_state.py` imports `alphaswarm_sol.testing.flexible.session_state` (module deleted)
- `tests/e2e/test_workflow_infrastructure.py` imports `ImprovementLoop` from workflow (module deleted)

These are scope of plan 3.1-06 (Clean up test files for removed infrastructure).
