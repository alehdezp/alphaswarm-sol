workflow_id: skill-vrs-tool-mythril
category: skill
grader_type: hybrid
rule_refs:
- EXEC-INTEGRITY
- TOOL-FIDELITY
reasoning_dimensions:
- tool_selection
- result_interpretation
- error_handling
evaluation_config:
  run_gvs: false
  run_reasoning: true
  debrief: false
  depth: standard
capability_checks:
- id: mythril_invoked
  description: Mythril was invoked on target contracts
  expected_behavior: Bash tool call with mythril or myth command
  grader_type: code
- id: results_structured
  description: Mythril output was parsed into structured format
  expected_behavior: Response organizes symbolic execution results by severity
  grader_type: model
- id: error_handled
  description: Tool errors handled gracefully
  expected_behavior: If mythril fails, agent reports error and suggests fixes
  grader_type: model
evidence_requirements:
- type: tool_output
  description: Raw mythril output
hooks:
- Stop
- SessionEnd
status: active
coverage_axes: []
metadata:
  tier: core
ground_truth_rubric: Skill invokes mythril with appropriate analysis depth for the
  target. Skill interprets symbolic execution traces and constraint solving results.
  Skill distinguishes concrete exploit paths from theoretical feasibility. Skill handles
  timeout and resource errors by adjusting analysis parameters. Skill contextualizes
  mythril findings against protocol-specific constraints.
active_dimensions:
- tool_selection
- result_interpretation
- error_handling
- completeness
- rubric_coverage
- task_completion
- conclusion_support
- reasoning_depth
